{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "check_calendar",
  "description": "Pruefe ob an einem bestimmten Tag eine bestimmte Dauer frei ist. Liefert {available, conflicts[]} mit Liste der belegten Termine des Tages.",
  "type": "object",
  "additionalProperties": false,
  "required": ["date", "duration_minutes"],
  "properties": {
    "date": {
      "type": "string",
      "format": "date",
      "description": "Datum im ISO-Format YYYY-MM-DD, z.B. '2026-04-20'. Keine relativen Begriffe wie 'morgen' — konvertiere zuerst."
    },
    "duration_minutes": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1440,
      "description": "Benoetigte Dauer in Minuten, z.B. 60 fuer eine Stunde."
    }
  }
}
