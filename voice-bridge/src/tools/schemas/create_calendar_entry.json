{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "create_calendar_entry",
  "description": "Lege einen Termin in Carstens Google Kalender an. Idempotent: gleicher Titel+Datum+Uhrzeit+Dauer liefert dieselbe Event-ID zurueck.",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "date", "time", "duration"],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Kurzer Termin-Titel, z.B. 'Zahnarzt Dr. Mueller' oder 'Besprechung mit Tim'."
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "Datum im ISO-Format YYYY-MM-DD. Keine relativen Begriffe."
    },
    "time": {
      "type": "string",
      "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
      "description": "Uhrzeit im 24-Stunden-Format HH:mm, lokale Zeit (Europa/Berlin). Beispiele: '09:30', '14:00', '17:45'."
    },
    "duration": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1440,
      "description": "Termin-Dauer in Minuten."
    },
    "location": {
      "type": "string",
      "maxLength": 200,
      "description": "Optional: Ort oder Adresse des Termins."
    },
    "travel_buffer_before_min": {
      "type": "integer",
      "minimum": 0,
      "maximum": 180,
      "description": "Optional: Anfahrt-Puffer in Minuten vor dem Termin (wird als separater Blocker-Event eingetragen)."
    },
    "travel_buffer_after_min": {
      "type": "integer",
      "minimum": 0,
      "maximum": 180,
      "description": "Optional: Rueckfahrt-Puffer in Minuten nach dem Termin."
    }
  }
}
