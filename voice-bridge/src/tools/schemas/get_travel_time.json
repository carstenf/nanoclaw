{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "get_travel_time",
  "description": "Berechne Fahrzeit und Distanz zwischen zwei Orten via Google Maps. Liefert duration_seconds + distance_meters.",
  "type": "object",
  "additionalProperties": false,
  "required": ["origin", "destination"],
  "properties": {
    "origin": {
      "type": "string",
      "minLength": 1,
      "maxLength": 300,
      "description": "Startort. Moeglichst praezise: 'Eisenmannstraße 1, 80331 München' oder 'Münchner Hauptbahnhof'."
    },
    "destination": {
      "type": "string",
      "minLength": 1,
      "maxLength": 300,
      "description": "Zielort. WICHTIG bei Flughaefen: nutze 'Munich Airport' oder 'MUC Airport' oder 'Flughafen München MUC', NICHT nur 'Flughafen München' (Google verwechselt das sonst mit 'Munich Stadtzentrum'). Bei Bahnhoefen nutze 'Hauptbahnhof München' oder 'München Hbf'."
    },
    "mode": {
      "type": "string",
      "enum": ["driving", "walking", "bicycling", "transit"],
      "default": "driving",
      "description": "Fortbewegungsart. 'driving' fuer Auto, 'transit' fuer OEPNV."
    },
    "departure_time": {
      "type": "string",
      "description": "Optional: ISO-8601 Abfahrtszeitpunkt oder 'now'. Fuer Verkehrslage bei 'driving'."
    }
  }
}
