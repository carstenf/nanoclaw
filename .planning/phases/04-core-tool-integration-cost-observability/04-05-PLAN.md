---
phase: 4
plan: 05
type: execute
wave: 5
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - docs/phase-4-gate-evidence.md
  - .planning/phases/04-core-tool-integration-cost-observability/04-GATE-EVIDENCE.md
autonomous: false
requirements: []
tags:
  - phase-gate
  - verification
user_setup: []

must_haves:
  truths:
    - "Phase 4 Success Criterion 1: Vollständiges Toolset (check_calendar, create_calendar_entry+travel, get_contract, search_competitors, get_practice_profile, schedule_retry) ist aufrufbar aus Case-6-Live-Call UND aus Claude-Chat via StreamableHTTP; mutating tools carry idempotency keys"
    - "Phase 4 Success Criterion 2: Synthetic 30-min-Test-Call akkumuliert response.done.usage in state.db, soft-warn bei 80%, hard-stop bei 100%, daily+monthly caps via startup-SUM enforced"
    - "Phase 4 Success Criterion 3: Pricing-refresh-cron alertet bei >5% drift; monthly-recon cross-checkt accumulator vs invoice, alert bei >5% drift"
    - "Phase 4 Success Criterion 4: Monthly §201 audit läuft via cron auf beiden Hosts, postet JSON summary to Discord, fails laut bei seeded .wav; drift-monitor alertet bei P50>1200ms rolling-24h"
    - "Phase 4 Success Criterion 5: 3-way recon (calendar ↔ transcript ↔ Discord) läuft nightly, alertet bei 2-of-3 drift"
    - "ROADMAP Phase 4 Plans-Liste aktualisiert, Phase-Status auf 'Complete' gesetzt"
  artifacts:
    - path: "docs/phase-4-gate-evidence.md"
      provides: "Vollständiger Phase-4-Evidence-Sammlung: test logs, systemd-status, curl-outputs, screenshots"
    - path: ".planning/phases/04-core-tool-integration-cost-observability/04-GATE-EVIDENCE.md"
      provides: "GSD-formatiertes Phase-gate evidence dokument (in Summary referenziert)"
  key_links:
    - from: "Phase 4 Gate"
      to: "Phase 5 kick-off"
      via: "Phase 4 Success Criteria + Phase 0 legal gate (beide green) = Phase 5 ready"
      pattern: "phase.*5.*gated"
---

<objective>
Wave 5 ist der Phase-4-Gate: deploy + manuelle Verifikation der nicht-automatisierbaren Success Criteria. Autonomous=false — dieser Plan enthält Human-Verify Checkpoints.

Purpose: ROADMAP.md Phase-4 "Success Criteria (what must be TRUE)" Punkt 1-5 mit Evidence belegen. Automatisierte Tests aus Plans 01-04 sind notwendig, aber nicht hinreichend — Carsten muss live bestätigen:
- StreamableHTTP funktioniert von iPhone Claude-App
- Ein synthetisches 30-min Dauer-Call überschreitet den €1-Per-Call Cap und feuert korrekt Farewell + Discord
- §201 audit Fails loud wenn eine seed-.wav-Datei platziert ist
- Beide systemd-Host-Timer (Lenovo1 + Hetzner) laufen nach Install in systemctl --user list-timers sichtbar
- Monatliche Invoice-Reconciliation kann (wenn letzten Monat's CSV verfügbar) eine echte Zahl vergleichen

Output:
- Deploy-Commands auf beiden Hosts ausgeführt (systemctl --user enable --now auf Lenovo1 + Hetzner)
- Evidence-Dokument mit actual command outputs, screenshots, Discord-notification-timestamps
- ROADMAP.md Phase 4 auf "Complete" + Plan-Checkboxes abgehakt
- REQUIREMENTS.md Status-Updates: TOOLS-05, INFRA-06/07, COST-01..05, QUAL-03/04 → "Complete"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-core-tool-integration-cost-observability/04-01-PLAN.md
@.planning/phases/04-core-tool-integration-cost-observability/04-02-PLAN.md
@.planning/phases/04-core-tool-integration-cost-observability/04-03-PLAN.md
@.planning/phases/04-core-tool-integration-cost-observability/04-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy systemd-Units auf beiden Hosts + Boot verify</name>
  <read_first>
    - docs/runbook-phase-4-cron.md (aus Plan 04)
    - .planning/phases/04-core-tool-integration-cost-observability/04-04-PLAN.md (systemd-Unit-Listen)
    - MASTER.md §1, §2 (welcher User auf welchem Host)
    - .planning/phases/04-core-tool-integration-cost-observability/04-04-SUMMARY.md (Hetzner-User-Choice)
  </read_first>
  <action>
    **Subtask 1a: Lenovo1 (carsten_bot) Deploy:**
    ```bash
    mkdir -p ~/.config/systemd/user ~/.config/nanoclaw
    cp ~/nanoclaw/systemd/user/nanoclaw-*.{service,timer} ~/.config/systemd/user/

    # Env files via OneCLI (CLAUDE.md compliance — never bake tokens into units)
    cat > ~/.config/nanoclaw/audit.env <<EOF
    DISCORD_AUDIT_WEBHOOK_URL=$(onecli get DISCORD_AUDIT_WEBHOOK_URL)
    EOF
    chmod 600 ~/.config/nanoclaw/audit.env

    systemctl --user daemon-reload
    systemctl --user enable --now nanoclaw-audit-audio.timer nanoclaw-drift-monitor.timer nanoclaw-recon-3way.timer nanoclaw-recon-invoice.timer
    systemctl --user list-timers 'nanoclaw-*'   # verify 4 timers active
    ```

    **Subtask 1b: Hetzner Deploy** (per SSH von carsten_bot, User laut Plan-04-Summary: wahrscheinlich carsten):
    ```bash
    ssh -i ~/server/ssh-keys/lenovo1 carsten@10.0.0.2 'mkdir -p ~/.config/systemd/user ~/.config/nanoclaw'
    scp -i ~/server/ssh-keys/lenovo1 ~/nanoclaw/systemd/hetzner/voice-*.{service,timer} carsten@10.0.0.2:~/.config/systemd/user/
    # Env file:
    ssh -i ~/server/ssh-keys/lenovo1 carsten@10.0.0.2 'cat > ~/.config/nanoclaw/pricing.env' <<EOF
    DISCORD_AUDIT_WEBHOOK_URL=<from OneCLI via manual transfer>
    CORE_MCP_BASE_URL=http://10.0.0.2:3200
    CORE_MCP_TOKEN=<from OneCLI>
    EOF
    ssh -i ~/server/ssh-keys/lenovo1 carsten@10.0.0.2 '
      chmod 600 ~/.config/nanoclaw/pricing.env
      systemctl --user daemon-reload
      systemctl --user enable --now voice-audit-audio.timer voice-pricing-refresh.timer
      systemctl --user list-timers "voice-*"
    '
    ```

    **Subtask 1c: Evidence-Capture:**
    ```bash
    {
      echo "## Lenovo1 timers"
      systemctl --user list-timers 'nanoclaw-*'
      echo "## Hetzner timers"
      ssh -i ~/server/ssh-keys/lenovo1 carsten@10.0.0.2 "systemctl --user list-timers 'voice-*'"
      echo "## Lenovo1 ports (3200 + 3201)"
      ss -tlnp | grep -E ':320[01]' || echo "ERROR: expected ports not bound"
    } > docs/phase-4-gate-evidence.md
    ```
  </action>
  <verify>
    <automated>systemctl --user list-timers 'nanoclaw-*' 2>/dev/null | grep -cE 'nanoclaw-(audit-audio|drift-monitor|recon-3way|recon-invoice)' | grep -q '4' && ssh -i ~/server/ssh-keys/lenovo1 carsten@10.0.0.2 "systemctl --user list-timers 'voice-*'" 2>/dev/null | grep -cE 'voice-(audit-audio|pricing-refresh)' | grep -q '2' && ss -tlnp | grep -q ':3200' && ss -tlnp | grep -q ':3201'</automated>
  </verify>
  <acceptance_criteria>
    - 4 Lenovo1 Timers aktiv (audit-audio, drift-monitor, recon-3way, recon-invoice)
    - 2 Hetzner Timers aktiv (voice-audit-audio, voice-pricing-refresh)
    - Port 3200 + Port 3201 beide auf 10.0.0.2 gebunden (nicht 0.0.0.0 — Pitfall 6)
    - Evidence-Output in docs/phase-4-gate-evidence.md
    - OneCLI-env-Files chmod 600, nicht world-readable
  </acceptance_criteria>
  <done>Alle 6 Timer + beide MCP-Server deployed + sichtbar. No 0.0.0.0 bind. OneCLI secret-management respektiert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-Verify — Synthetischer Cost-Cap-Test (COST-01)</name>
  <what-built>
    Plan 01-04 Ergebnis: Bridge sideband.ts hat response.done-Hook, accumulator, 80%-soft-warn, 100%-hard-stop via instructions-only session.update + response.create + delayed ws.close(1000). Plan 04 hat Gate auf /accept.
  </what-built>
  <how-to-verify>
    Carsten führt folgenden synthetic test aus (er kann das via Claude-Chat instruieren — der Plan-Executor darf den Test nicht selbst machen, weil OpenAI-Kosten entstehen):

    1. **Option A — Spike-Replay-Harness** (kostenlos, preferred):
       Nutze `voice-bridge/tests/spike-replay/` Phase-2-Harness (wenn vorhanden). Injiziere 10 synthetische response.done Events mit je ~€0.12 kost (insg. €1.20). Erwarte:
       - Nach Event 7 (€0.84): Discord soft-warn "⚠️ Call <id> at 80%"
       - Nach Event 9 (€1.08): Discord hard-stop "🛑 Call <id> hard-stopped at €1.08", session.update mit `instructions` aber KEIN `tools`, response.create, ws.close nach 4s
       - state.db: `SELECT * FROM voice_call_costs WHERE call_id='<synthetic>'` zeigt terminated_by='cost_cap_call', cost_eur≈1.08, soft_warn_fired=1

    2. **Option B — Live €1 Test-Call** (wenn A nicht verfügbar; kostet real ~€1):
       - Carsten ruft seinen Sipgate DID und lässt die Konversation solange laufen bis die Akkumulation €1 überschreitet (~25-30 min dauernde Konversation). 
       - Beobachten: Discord soft-warn bei ~€0.80, Hard-stop TTS "Vielen Dank, ich melde mich später..." bei €1.00, Leitung schließt innerhalb 4s.
       - State.db row wie in Option A.

    **Erwartete Evidence:**
    - Screenshot Discord (beide Alerts)
    - `sqlite3 ~/nanoclaw/state.db 'SELECT * FROM voice_call_costs WHERE call_id=? ORDER BY started_at DESC LIMIT 1' <id>` Output
    - `sqlite3 ~/nanoclaw/state.db 'SELECT * FROM voice_turn_costs WHERE call_id=? ORDER BY ts' <id>` — >=7 rows
    - `cat ~/nanoclaw/voice-container/runs/turns-<id>.jsonl | grep 'cost_hard_stop\|cost_soft_warn'` — beide events
  </how-to-verify>
  <action>See &lt;how-to-verify&gt; above — Carsten performs the synthetic cost-cap test (Option A preferred, spike-replay-harness). Executor prepares and runs the replay harness script if available; otherwise documents Option B preparation steps.</action>
  <verify>
    <automated>test -s docs/phase-4-gate-evidence.md &amp;&amp; grep -q "cost_hard_stop\|hard-stopped" docs/phase-4-gate-evidence.md &amp;&amp; grep -q "cost_soft_warn\|at 80%" docs/phase-4-gate-evidence.md &amp;&amp; sqlite3 ~/nanoclaw/state.db "SELECT COUNT(*) FROM voice_call_costs WHERE terminated_by='cost_cap_call'" 2&gt;/dev/null | grep -qvE '^0$'</automated>
  </verify>
  <resume-signal>Carsten antwortet "approved" + packt screenshots + sqlite-outputs in docs/phase-4-gate-evidence.md; oder beschreibt Issues zur Fix-Iteration</resume-signal>
  <acceptance_criteria>
    - Discord soft-warn Screenshot vorhanden
    - Discord hard-stop Screenshot vorhanden
    - State.db voice_call_costs terminated_by='cost_cap_call', soft_warn_fired=1
    - voice_turn_costs rows >= 3
    - turns-<id>.jsonl enthält cost_soft_warn + cost_hard_stop event entries
    - Kein "tools" field in hard-stop session.update (AC-04/AC-05 Compliance — via grep im Bridge log)
  </acceptance_criteria>
  <done>COST-01 Behavior live bestätigt. Evidence in Gate-Dokument konsolidiert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-Verify — StreamableHTTP end-to-end von iPhone Chat-Claude (AC-07)</name>
  <what-built>
    Plan 03 Ergebnis: Core hat zwei MCP-Server: Port 3200 (REST, unverändert) + Port 3201 (StreamableHTTP, bearer+allowlist). Chat-Invocations bekommen synthetic call_id='chat-<uuid>'.
  </what-built>
  <how-to-verify>
    Carsten konfiguriert auf iPhone Claude-App:
    ```
    claude mcp add nanoclaw-voice http://10.0.0.2:3201/mcp/stream --header "Authorization: Bearer ${MCP_STREAM_BEARER}"
    ```

    Dann im Chat:
    - "Welche Tools hast du via nanoclaw-voice?" — erwartet: Liste inkl. voice.check_calendar, voice.search_competitors, voice.get_day_month_cost_sum
    - "Check my calendar for Monday 10 AM, duration 30 minutes" — erwartet: tool-call voice.check_calendar, response mit {available: true/false, conflicts: []}
    - "Show me today's voice spend" — tool-call voice.get_day_month_cost_sum → today_eur, month_eur

    **Auf Lenovo1 Monitoring:**
    ```bash
    journalctl --user -f -u nanoclaw  # or main nanoclaw process log
    # expected: log entries with call_id="chat-<uuid>", turn_id="chat-<uuid>"
    ```

    **Evidence:**
    - iPhone-Screenshot der Tool-Liste
    - Log-Snippet mit call_id='chat-…' (Pitfall 8 Proof)
    - Response von mindestens 2 tool invocations
  </how-to-verify>
  <action>See &lt;how-to-verify&gt; above — Carsten adds nanoclaw-voice MCP-server to iPhone Claude-App via `claude mcp add`, invokes 2+ tools, verifies chat-prefixed call_ids in journal logs.</action>
  <verify>
    <automated>curl -fsS --max-time 5 http://10.0.0.2:3201/mcp/stream/health 2&gt;&amp;1 | grep -q '"ok":true' &amp;&amp; ss -tlnp | grep -q "10.0.0.2:3201" &amp;&amp; journalctl --user -u nanoclaw --since "1 hour ago" 2&gt;/dev/null | grep -qE "call_id.*chat-"</automated>
  </verify>
  <resume-signal>Carsten antwortet "approved" + Screenshots in gate-evidence; oder listet Probleme (Bearer nicht akzeptiert, Tools nicht sichtbar, etc.)</resume-signal>
  <acceptance_criteria>
    - iPhone Claude kann nanoclaw-voice als MCP-Server hinzufügen
    - Tool-Liste in Chat sichtbar
    - Mindestens 2 tool invocations erfolgreich
    - Bridge/Core logs zeigen call_id='chat-<uuid>' (Pitfall 8 proof)
    - `ss -tlnp | grep 3201` zeigt 10.0.0.2:3201 (nicht 0.0.0.0 — Pitfall 6 proof)
  </acceptance_criteria>
  <done>AC-07 erfüllt: Chat-Claude kann dieselben Tools via StreamableHTTP aufrufen wie Bridge. Disjunktes Key-Space bestätigt.</done>
</task>

<task type="auto">
  <name>Task 4: Synthetic §201 audit seeded-file-Test auf beiden Hosts (QUAL-04)</name>
  <read_first>
    - scripts/audit-audio.sh
    - docs/runbook-phase-4-cron.md
  </read_first>
  <action>
    **Lenovo1:**
    ```bash
    SEED=/tmp/audit-test-$(date +%s).wav
    echo "seeded test file — safe to delete" > "$SEED"
    systemctl --user start nanoclaw-audit-audio.service
    # wait ~5s for completion
    sleep 5
    systemctl --user status nanoclaw-audit-audio.service --no-pager
    # expected: failed (exit 1 weil seed-file gefunden)
    # check Discord: 🚨 §201 AUDIT FAIL on lenovo1 — 1 files found
    rm "$SEED"
    systemctl --user start nanoclaw-audit-audio.service
    sleep 5
    systemctl --user status nanoclaw-audit-audio.service --no-pager
    # expected: active/success, Discord: ✅ §201 audit pass on lenovo1 (0 audio files)
    ```

    **Hetzner (via SSH):**
    ```bash
    ssh -i ~/server/ssh-keys/lenovo1 carsten@10.0.0.2 '
      SEED=/tmp/audit-test-$(date +%s).wav
      echo "seeded test file" > "$SEED"
      systemctl --user start voice-audit-audio.service
      sleep 5
      systemctl --user status voice-audit-audio.service --no-pager
      rm "$SEED"
      systemctl --user start voice-audit-audio.service
      sleep 5
      systemctl --user status voice-audit-audio.service --no-pager
    '
    ```

    Log Ergebnis in `docs/phase-4-gate-evidence.md` — 4 systemctl-status snapshots (2 per host × fail/pass).
  </action>
  <verify>
    <automated>test -s docs/phase-4-gate-evidence.md && grep -q "Failed\|failed\|exit-code.*=.*1" docs/phase-4-gate-evidence.md && grep -q "AUDIT PASS\|Active: active" docs/phase-4-gate-evidence.md</automated>
  </verify>
  <acceptance_criteria>
    - Lenovo1 audit-audio.service FAILS (exit 1) mit seeded .wav, status SUCCESS nach rm
    - Hetzner voice-audit-audio.service FAILS (exit 1) mit seeded .wav, status SUCCESS nach rm
    - Discord Audit-Channel zeigt 4 Posts (2 per host: FAIL + PASS)
    - Evidence-Dokument enthält alle 4 systemctl-status snapshots
  </acceptance_criteria>
  <done>QUAL-04 live verifiziert: Audit fails laut bei Audio-File-Seed. Cross-host Staffelung funktioniert.</done>
</task>

<task type="auto">
  <name>Task 5: Full repo test-suite + final phase state updates</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  </read_first>
  <action>
    **Subtask 5a: Full test suite beide Repos:**
    ```bash
    cd /home/carsten_bot/nanoclaw && npm run typecheck && npm run lint && npm run test
    cd /home/carsten_bot/nanoclaw/voice-bridge && npm run typecheck && npm run lint && npm run test
    ```
    Alle grün. Konsolidierte Output-Länge + pass/fail Count in Gate-Evidence.

    **Subtask 5b: REQUIREMENTS.md Status-Updates** (traceability table):
    Change `| INFRA-06 | Phase 4 | Pending |` → `| INFRA-06 | Phase 4 | Complete |`
    Dto für: INFRA-07, TOOLS-05, COST-01, COST-02, COST-03, COST-04, COST-05, QUAL-03, QUAL-04
    (TOOLS-01/02/04/06/07 sind schon "Complete" — nichts ändern)

    Im oberen Teil der Datei (§TOOLS, §COST, §QUAL, §INFRA) Checkbox-Status `- [ ]` → `- [x]` für jede abgeschlossene REQ-ID.

    **Subtask 5c: ROADMAP.md Update** — Phase 4:
    - `- [ ] **Phase 4: ...**` → `- [x] **Phase 4: ...** ` mit Datum
    - Status-Tabelle am Ende: Phase 4 row "Complete" + Plans 5/5, Completed date
    - Plans-Liste unter Phase 4: alle 5 Plans `- [ ]` → `- [x]`

    **Subtask 5d: STATE.md Update:**
    - stopped_at: "Phase 4 complete — cost enforcement live, StreamableHTTP deployed, audit + recon cron running"
    - completed_phases: increment
    - progress.completed_plans: +5
    - Add to Accumulated Context → Decisions:
      - "[Phase 04-core-tool-integration-cost-observability]: A12 Idempotency-Lücke geschlossen — dispatch.ts routet mutating tools durch invokeIdempotent (allowlist.mutating=true/false als switch)"
      - "[Phase 04-core-tool-integration-cost-observability]: state.db erweitert um voice_call_costs + voice_turn_costs + voice_price_snapshots; PRIMARY KEY (call_id,turn_id) als SQL-seitige dedup für voice.record_turn_cost"
      - "[Phase 04-core-tool-integration-cost-observability]: Hard-stop via updateInstructions (AC-04/AC-05 compliant — kein tools-mutate mid-call)"
      - "[Phase 04-core-tool-integration-cost-observability]: StreamableHTTP MCP auf Port 3201, bearer+allowlist, disjunktes Chat-vs-Voice key space (call_id='chat-<uuid>')"

    **Subtask 5e: Git-commit mit Evidence + Roadmap-Updates + REQUIREMENTS-Updates:**
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04): phase 4 gate evidence + state updates" \
      --files docs/phase-4-gate-evidence.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md \
      .planning/phases/04-core-tool-integration-cost-observability/04-GATE-EVIDENCE.md
    ```
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && npm run test 2>&1 | tail -3 | grep -qi "passed\|pass" && cd voice-bridge && npm run test 2>&1 | tail -3 | grep -qi "passed\|pass" && cd /home/carsten_bot/nanoclaw && grep -q "| INFRA-06 | Phase 4 | Complete |" .planning/REQUIREMENTS.md && grep -q "| COST-01 | Phase 4 | Complete |" .planning/REQUIREMENTS.md && grep -qE "\[x\] \*\*Phase 4" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Beide Repos full test suite grün (`npm run test` exit 0, pass count in gate-evidence)
    - REQUIREMENTS.md traceability table zeigt "Complete" für INFRA-06, INFRA-07, TOOLS-05, COST-01..05, QUAL-03, QUAL-04
    - REQUIREMENTS.md §COST, §QUAL, §INFRA, §TOOLS checkboxes `[x]` wo entsprechend
    - ROADMAP.md Phase 4 `[x]` mit completion-date
    - STATE.md reflects Phase-4 complete: stopped_at, completed_phases+1, completed_plans+5
    - Accumulated Context Decisions enthält 4 neue Phase-4-Einträge
    - Git commit angelegt mit allen Update-Files
  </acceptance_criteria>
  <done>Phase 4 formal abgeschlossen. Phase 5 (Case 2) ist jetzt gated nur noch auf Phase 0 (Legal Gate) — Phase 4-Dependency erfüllt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deployment shell (carsten_bot on Lenovo1 + carsten on Hetzner via SSH) → systemd-user + /etc/config | scoped per MASTER.md §2; no sudo |
| Evidence-capture commands → Discord webhook (test alerts) | authentic secrets, non-production traffic labelled |
| iPhone Claude-App → Port 3201 | bearer + WG only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | Evidence document contains screenshots of Discord with call_id/cost information | accept | docs/phase-4-gate-evidence.md is gitignored or kept local per Carsten's runbook preference; if committed, only under local git repo (not pushed to public remote). Flag in Plan 05 completion message. |
| T-04-05-02 | Tampering | Hetzner systemd deploy via SSH is done by non-root carsten_bot on Lenovo1 pushing to carsten user on Hetzner — could be attacked if SSH key is stolen | accept | Phase-1 established SSH key contract; Phase-4 reuses same key. Scope per MASTER.md §2.1 voice-bot section (updated for user: carsten). |
| T-04-05-03 | Denial of Service | Synthetic cost-cap test accidentally bills Carsten €1 on real call (Option B) | mitigate | Prefer Option A (spike-replay) — cost €0. Option B only if Option A unavailable. Documented in task as "preferred path". |
| T-04-05-04 | Repudiation | Evidence gathered but human-verify checkpoint approved without actual verification | accept | checkpoint:human-verify gate design — trust the user. If Carsten approves hastily and something's broken, next Phase 4 cost-incident reveals it. |
</threat_model>

<verification>
- Alle 6 systemd-user-timer aktiv auf beiden Hosts (4 Lenovo1 + 2 Hetzner)
- Port 3200 + 3201 beide auf 10.0.0.2 gebunden
- COST-01 soft-warn + hard-stop sequence live bestätigt (synthetic oder live call)
- AC-07 iPhone Chat-Claude StreamableHTTP E2E bestätigt
- QUAL-04 audit seed-file-test auf beiden Hosts bestätigt (fail loud, pass clean)
- Vollständige Test-Suite beide Repos grün
- STATE.md + ROADMAP.md + REQUIREMENTS.md konsistent aktualisiert
- Git-commit erstellt
</verification>

<success_criteria>
Phase 4 Success Criteria aus ROADMAP.md alle belegt:
- SC#1: Tool set aufrufbar aus Live-Call + Chat ✓ (Plan 03 + Task 3)
- SC#2: response.done.usage → state.db + 80% soft-warn + 100% hard-stop + daily/monthly caps ✓ (Plan 01+02 + Task 2)
- SC#3: Pricing-refresh drift >5% alert + monthly recon vs OpenAI invoice ✓ (Plan 04 + Timer läuft)
- SC#4: Monthly §201 FS audit auf beiden Hosts, fails laut bei seed + drift monitor P50>1200ms ✓ (Plan 04 + Task 4)
- SC#5: 3-way recon cal↔transcript↔Discord nightly mit Alert ✓ (Plan 04)

Phase-4-REQ-Coverage: alle 15 REQs abgeschlossen (INFRA-06/07, TOOLS-01/02/04/05/06/07, COST-01/02/03/04/05, QUAL-03/04)

Phase 5 unblocked (nur noch Phase-0-Dependency = Legal Gate — carsten_bot scope done)
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-tool-integration-cost-observability/04-SUMMARY.md` (phase-level summary aggregating alle 5 plan-summaries) with:
- Executive phase outcome (success criteria met)
- Hetzner user choice documented (Plan 04 decision)
- A12 resolution confirmation (idempotency-wrapper now active)
- Full list of new state.db columns + row counts after seed test
- Gate-evidence document cross-reference
- Open follow-ups (if any — e.g. OpenAI usage-API probe for Phase 5 automation of COST-05, SEARCH_COMPETITORS_PROVIDER wiring in Phase 7)
- ROADMAP Phase 4 status: Complete
</output>
