---
phase: 05.2
plan: 06
type: execute
wave: 3
depends_on:
  - 05.2-01
  - 05.2-02
  - 05.2-03
  - 05.2-04
  - 05.2-05
files_modified:
  - .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/
  - .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - C2-01
  - C2-02
  - C2-03
  - C2-04
  - C2-05
  - C2-06
  - C2-07
  - VOICE-01
  - VOICE-04
  - VOICE-05
  - VOICE-06
  - VOICE-07
  - VOICE-08
  - VOICE-09
  - VOICE-14
tags:
  - live-verification
  - pstn
  - combined-05.1-and-05.2
  - checkpoint
  - human-verify
user_setup: []
must_haves:
  truths:
    - "3 live PSTN calls completed: HAPPY (human answers, restaurant books), VOICEMAIL (answering machine detected), BUSY/NO-ANSWER (no speech detected within outbound nudge window). Matches the deferred Plan 05.1-05 matrix + adds 05.2-specific assertions."
    - "Phase 05.1 success criteria 1-5 from ROADMAP.md lines 163-169 all hold in live traces: defect #6 persona-swap works (session.update lands, bot opens with 'NanoClaw im Auftrag von Carsten'); defect #3 ASR renders German cleanly; defect #4 voicemail retry args zod-valid; defect #5 same-day retry attempt_no increments transactionally."
    - "Phase 05.2 success criteria 1-6 from ROADMAP.md lines 185-191 all hold: (1) baseline+Case-2 overlay drive the post-AMD handoff; (2) no role-hallucination (bot plays ONLY its own role in HAPPY trace); (3) 'Bist du noch da'-equivalent does NOT fire mid-bot-turn (silence-monitor rewire proven); (4) outbound bot waits for counterpart speech before first utterance (NO-ANSWER scenario exercises 3-nudge ladder + D-2 farewell); (5) inbound self-greet unchanged (separate inbound-smoke test confirms 1000ms post-/accept behavior); (6) combined Phase 05.1 + 05.2 verification pass with new traces in task5-traces-05.2/."
    - "§201 StGB zero-audio-leak invariant (Plan 05-03 T-05-03-01) re-verified: traces show zero audio_transcript.delta OR response.output_audio.delta events before amd_result=human in Case-2 calls. Carried forward unbroken."
    - "ROADMAP.md marks Phase 05.1 (all 5 plans) AND Phase 05.2 (all 6 plans) as complete; VERIFICATION.md written to the phase directory; traces committed."
    - "Carsten's resume-signal received: explicit 'approved' or issue description. Checkpoint gate blocks autonomous completion until Carsten confirms."
  artifacts:
    - path: ".planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/"
      provides: "3 JSONL trace files from live PSTN calls (happy-human.jsonl, voicemail.jsonl, busy-no-answer.jsonl) with full WS event streams"
      contains: "happy-human|voicemail|busy-no-answer"
    - path: ".planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md"
      provides: "Verification report: table of 11 success criteria (5 from 05.1 + 6 from 05.2) with PASS/FAIL + trace evidence for each"
      contains: "PASS|FAIL|VERIFICATION"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 05.1 plans 01-05 marked [x]; Phase 05.2 plans 01-06 marked [x]"
      contains: "[x]"
  key_links:
    - from: ".planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/happy-human.jsonl"
      to: "Phase 05.2 success criteria 1, 2 (baseline+overlay, role-lock)"
      via: "Trace contains session.update with TURN-DISCIPLIN + Bellavista; response text does NOT contain hallucinated counterpart speech"
      pattern: "TURN-DISCIPLIN|response.output_text.delta"
    - from: ".planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/busy-no-answer.jsonl"
      to: "Phase 05.2 success criterion 4 (wait-for-speech + 3-nudge ladder + D-2 farewell)"
      via: "Trace shows 3 response.create events with nudge-1/nudge-2/farewell text; final event is end_call with reason='silence'"
      pattern: "Hallo, ist da jemand|Hallo? Hoeren Sie mich|ich versuche es spaeter nochmal|end_call"
    - from: ".planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/voicemail.jsonl"
      to: "Phase 05.1 success criterion 3 (voicemail retry args zod-valid)"
      via: "Trace shows amd_result verdict=voicemail; voice_case_2_schedule_retry MCP call has all required fields"
      pattern: "amd_result|voicemail|voice_case_2_schedule_retry"
---

<objective>
Combined live PSTN verification for Phase 05.1 + Phase 05.2. Replaces the deferred Phase 05.1-05 live-verify (D-10: "Running 05.1-05 with the known-broken Case-1 persona would burn telephony minutes on predictably-bad traces").

Wave 3, autonomous: false (human-verify checkpoint gate).

Three scenarios matching Plan 05-03 Task 5 matrix:

1. **HAPPY** — human answers, restaurant books:
   - Carsten triggers Case-2 outbound via /tmp/trigger-case2.sh (MCP :3201, built in 05.1 session).
   - Expected: AMD detects human → session.update pushes baseline+Case-2 overlay → bot greets with "NanoClaw im Auftrag von Carsten" → counterpart offers time within tolerance → bot does Zwei-Form readback → bot calls create_calendar_entry → bot confirms + farewell.
   - Assertions: 05.1 defect #6 (persona swap), 05.1 defect #3 (ASR German clean), 05.2 criterion 1 (baseline+overlay), 05.2 criterion 2 (no role-hallucination), 05.2 criterion 3 (silence-monitor does NOT fire mid-bot-turn).

2. **VOICEMAIL** — answering machine detected:
   - Expected: AMD detects voicemail → voice_case_2_schedule_retry called with zod-valid args.
   - Assertions: 05.1 defect #4 (retry args), 05.1 defect #5 (attempt_no transactional — fire a same-day second attempt manually after first to verify attempt_no increments). §201 invariant: zero audio_transcript.delta before amd_result.

3. **BUSY / NO-ANSWER** — counterpart never speaks (dial into unattended number; let ring-tone continue):
   - Expected: AMD listen phase completes without human verdict; post-AMD or alternative path — the bot waits (create_response:false from 05.2-03), fires 3 persona-driven nudges ("Hallo, ist da jemand?" → "Hallo? Hoeren Sie mich?" → apologetic farewell), then end_call.
   - Assertions: 05.2 criterion 4 (wait-for-speech), D-1 (3 attempts), D-2 (farewell text verbatim), silence-monitor hard-safety floor does NOT misfire.

**Trace analysis:** For each call, capture the full WS event stream + MCP logs; analyze against the 11 success criteria (5 from 05.1, 6 from 05.2); write VERIFICATION.md with PASS/FAIL + quoted trace evidence per criterion.

**Carsten checkpoint gate:** after automated pre-flight + scenarios execute, pause for Carsten to inspect traces + VERIFICATION.md and either "approved" (Plan and Phase complete) or describe issues (triggers gap-closure plan).

**Service restart requirement:** voice-bridge + nanoclaw must be restarted on the merged Wave 1/Wave 2 code BEFORE Scenario 1 runs. Pre-flight Task 1 automates this.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-CONTEXT.md
@.planning/research/voice-persona-architecture.md
@.planning/phases/05-case-2-restaurant-reservation-outbound/05-03-TASK5-DEFECTS.md
@.planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-01-PLAN.md
@voice-bridge/src/persona/baseline.ts
@voice-bridge/src/persona/overlays/case-2.ts
@voice-bridge/src/persona.ts
@voice-bridge/src/silence-monitor.ts
@voice-bridge/src/config.ts
@voice-bridge/src/webhook.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — commits present, tests green, build clean, services restarted</name>
  <files>
    voice-bridge (build output)
    nanoclaw (service state)
  </files>
  <read_first>
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-01-PLAN.md through 05.2-05-PLAN.md (check all Wave 1 + Wave 2 plans are committed)
    /tmp/trigger-case2.sh (existing trigger script from 05.1 session — MCP :3201 direct; verify still present; if absent, re-locate via git-log search for 05.1 trigger scripts)
  </read_first>
  <action>
    1. Verify all Wave 1 + Wave 2 plan commits are present on main branch. Use: `git log --oneline --all | grep -E "(05.2-0[1-5])" | head -20`. Expect at least 2 commits per plan (RED + GREEN) = ~10 commits minimum.
    2. Run full voice-bridge test suite: `cd /home/carsten_bot/nanoclaw/voice-bridge && npm test`. All tests must pass (0 failures). Minimum test counts (baseline, adjust if Wave 1/2 overdelivered):
       - persona-baseline.test.ts: ≥8 tests (05.2-01)
       - silence-monitor.test.ts: ≥6 new tests (05.2-02)
       - sideband-wait-for-speech.test.ts: ≥4 tests (05.2-03)
       - persona-baseline-nudge.test.ts: ≥3 tests (05.2-03)
       - persona-overlay-case-2.test.ts: ≥4 tests (05.2-04)
       - persona-migration-case-2.test.ts: ≥10 tests (05.2-04)
       - persona-overlay-case-6b.test.ts: ≥3 tests (05.2-04)
       - webhook-amd-handoff.test.ts: ≥4 tests (05.2-05)
    3. Build check: `cd /home/carsten_bot/nanoclaw/voice-bridge && npm run build`. Exits 0.
    4. Build check (nanoclaw): `cd /home/carsten_bot/nanoclaw && npm run build`. Exits 0.
    5. Restart voice-bridge service (service name per project CLAUDE.md — Linux systemd user scope):
       ```bash
       systemctl --user restart voice-bridge  # or actual service name; verify via: systemctl --user list-units 'voice*'
       systemctl --user status voice-bridge --no-pager | head -20
       ```
       Expected: status=active (running), last log line shows successful /accept route mount + MCP :3201 listening.
    6. Restart nanoclaw service:
       ```bash
       systemctl --user restart nanoclaw
       systemctl --user status nanoclaw --no-pager | head -20
       ```
       Expected: status=active (running).
    7. Verify OpenAI SDK picks up new SESSION_CONFIG.audio.input.turn_detection.create_response=false: check voice-bridge startup logs for any config-dump; if absent, confirm via `grep create_response /home/carsten_bot/nanoclaw/voice-bridge/dist/config.js` shows `false`.
    8. Verify /tmp/trigger-case2.sh still exists and is executable: `test -x /tmp/trigger-case2.sh`. If absent, reconstruct from git history — look for commit messages in 05.1 plans mentioning "trigger-case2".
    9. Pre-flight complete. Log summary to /tmp/05.2-06-preflight.log. Commit: `chore(05.2-06): pre-flight verification — all tests green, services restarted on Wave 1/2 merged code`.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge &amp;&amp; npm test 2>&amp;1 | tail -20 | grep -qE "(passed|✓).*Test" &amp;&amp; npm run build 2>&amp;1 | tail -5 | (! grep -qE "error TS") &amp;&amp; grep -qE "create_response:\s*false" dist/config.js &amp;&amp; systemctl --user is-active voice-bridge &amp;&amp; systemctl --user is-active nanoclaw &amp;&amp; test -x /tmp/trigger-case2.sh</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline --all | grep -cE "05\\.2-0[1-5]"` returns ≥10 (at least 2 commits per Wave 1/2 plan)
    - `cd voice-bridge && npm test` exits 0, stdout includes new test-file names per Task 1 step 2
    - `cd voice-bridge && npm run build` exits 0
    - `cd nanoclaw && npm run build` exits 0
    - `systemctl --user is-active voice-bridge` prints 'active'
    - `systemctl --user is-active nanoclaw` prints 'active'
    - `grep -qE "create_response:\\s*false" voice-bridge/dist/config.js` succeeds (runtime config reflects D-8 flip)
    - `test -x /tmp/trigger-case2.sh` succeeds
    - Pre-flight log written to /tmp/05.2-06-preflight.log
    - `git log --oneline -1` shows `chore(05.2-06): pre-flight` commit
  </acceptance_criteria>
  <done>All code shipped, all tests green, services running on Wave 1/2 merged code, trigger script ready. Ready for live PSTN calls.</done>
</task>

<task type="auto">
  <name>Task 2: Execute 3 PSTN scenarios, capture traces</name>
  <files>
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/happy-human.jsonl
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/voicemail.jsonl
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/busy-no-answer.jsonl
  </files>
  <read_first>
    .planning/phases/05-case-2-restaurant-reservation-outbound/05-03-TASK5-DEFECTS.md (reference: how prior Task 5 traces were captured; format is JSONL, one WS event per line)
    /tmp/trigger-case2.sh (MCP :3201 invocation shape — restaurant_name, date, time, party_size, tolerance, notes args)
  </read_first>
  <action>
    1. Create target directory: `mkdir -p /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2`
    2. **Scenario 1 HAPPY:**
       - Target: a Carsten-controlled test number that answers live (mobile or landline) — use the same number used in 05-03 Task 5 HAPPY scenario if available. If unavailable, document in the VERIFICATION.md under "Scenario 1 target" field.
       - Invoke: `/tmp/trigger-case2.sh '{restaurant_name:"TestBellavista", requested_date:"2026-05-23", requested_time:"19:00", time_tolerance_min:30, party_size:4, target_phone:"+49<test-number>", idempotency_key:"05.2-06-happy-$(date +%s)"}'`.
       - Start voice-bridge log tail in parallel: `journalctl --user -u voice-bridge -f > /tmp/05.2-06-happy-bridge.log 2>&1 &`
       - Role-play: Carsten answers, says "Hallo, hier TestBellavista, was kann ich für Sie tun?" — let bot drive conversation. Follow standard script: offer "19 Uhr" → bot does Zwei-Form readback → Carsten says "Ja, korrekt" → bot calls create_calendar_entry → bot confirms + farewell → end.
       - After end: stop the log tail (`kill %1`). Extract WS events from journalctl into task5-traces-05.2/happy-human.jsonl.
       - Smoke assertions (quick eyeball; full analysis in Task 3):
         - bot-first-utterance fires AFTER Carsten's "Hallo" (not before)
         - session.update payload (visible in bridge log) includes 'TURN-DISCIPLIN' and 'TestBellavista'
         - No hallucinated counterpart dialogue in bot's response.output_text stream
         - No 'Bist du noch da'-equivalent fires mid-bot-turn
    3. **Scenario 2 VOICEMAIL:**
       - Target: a Carsten-controlled number that goes directly to voicemail (test mobile with /do-not-disturb + voicemail greeting, OR a test voicemail box). Same trigger-case2.sh invocation with different idempotency_key.
       - Do NOT answer. Let voicemail greeting play.
       - Expected: AMD detects voicemail → voice_case_2_schedule_retry MCP call made → call end_call'd.
       - Trace captured to voicemail.jsonl.
       - Smoke assertions: amd_result verdict='voicemail' present; voice_case_2_schedule_retry args include call_id, target_phone, calendar_date, prev_outcome (enum), idempotency_key (all fields — defect #4 zod-validity).
       - Fire a SECOND scenario 2 call right after with same target_phone+calendar_date → verify retry table attempt_no=2 in db (defect #5).
    4. **Scenario 3 BUSY / NO-ANSWER:**
       - Target: a number that rings without pickup (Carsten's test mobile on silent, or a test trunk that lets it ring). Same trigger-case2.sh invocation.
       - Do NOT answer. Do NOT let voicemail answer (use a number without voicemail or hang up before voicemail).
       - Expected: AMD silence timer expires (30s) OR (alternative path) AMD classifies 'silent'. Bot post-classifier behavior: apologetic farewell + end_call. If Case-2 AMD 'silent' verdict routes to the same path as voicemail (voice_case_2_schedule_retry), scenario verifies that. If instead AMD verdict triggers a bot-to-counterpart "are you there" nudge ladder, scenario exercises D-1 + D-2.
       - NOTE: the outbound 3-nudge ladder lives in BASELINE PERSONA CONVERSATION-FLOW section (05.2-03), but it is only active AFTER session.update pushes baseline+overlay post-AMD verdict. In pure no-answer case, AMD classifies before the baseline is ever pushed. Accept either outcome as valid; VERIFICATION.md documents which path fired.
       - Trace captured to busy-no-answer.jsonl.
    5. Commit traces: `git add .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/ && git commit -m "test(05.2-06): 3 PSTN scenario traces captured (happy / voicemail / busy-no-answer)"`.
  </action>
  <verify>
    <automated>test -f /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/happy-human.jsonl &amp;&amp; test -f /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/voicemail.jsonl &amp;&amp; test -f /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/busy-no-answer.jsonl &amp;&amp; test $(wc -l &lt; /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/happy-human.jsonl) -gt 20</automated>
  </verify>
  <acceptance_criteria>
    - 3 trace files exist in task5-traces-05.2/, each with ≥20 JSONL lines (realistic WS event count for a full call)
    - `grep -q "session.update" .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/happy-human.jsonl` succeeds
    - `grep -q "amd_result" .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/voicemail.jsonl` succeeds
    - Traces committed to git
    - `git log --oneline -1` shows `test(05.2-06)` traces commit
  </acceptance_criteria>
  <done>3 real PSTN traces captured and committed; ready for analysis in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Trace analysis + VERIFICATION.md</name>
  <files>
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/happy-human.jsonl
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/voicemail.jsonl
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/busy-no-answer.jsonl
    .planning/ROADMAP.md lines 163-169 (Phase 05.1 success criteria 1-5)
    .planning/ROADMAP.md lines 185-191 (Phase 05.2 success criteria 1-6)
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/q7-atomicity-finding.md (05.2-05 Q7 verdict — trace evidence should align)
  </read_first>
  <action>
    1. Parse the 3 JSONL traces. For each event, extract: timestamp, type, relevant payload fields (session.instructions, amd_result, response.output_text.delta, tool-call args, end_call reason).
    2. Build a verification matrix table with 11 rows (5 from Phase 05.1 success criteria + 6 from Phase 05.2). For each row:
       - Criterion text (verbatim from ROADMAP)
       - PASS / FAIL / N/A (if scenario doesn't exercise this criterion)
       - Evidence: quoted line from the relevant trace OR explanation of failure
    3. **Phase 05.1 criteria:**
       - 1 (defect #6 persona-swap): happy-human.jsonl must show `session.update` event with `session.type='realtime'` and instructions containing "NanoClaw im Auftrag von Carsten"; no `session_update_rejected` log. PASS/FAIL.
       - 2 (defect #3 ASR): happy-human.jsonl must show `conversation.item.input_audio_transcription.completed` events with clean German text (no English/Swedish garbage); `config.audio.input.transcription.model==='gpt-4o-mini-transcribe'` and `language='de'`. PASS/FAIL.
       - 3 (defect #4 voicemail retry args): voicemail.jsonl must show a MCP call to voice_case_2_schedule_retry with all 5 required zod fields (call_id, target_phone, calendar_date, prev_outcome, idempotency_key). PASS/FAIL.
       - 4 (defect #5 attempt_no): after running a second Scenario 2 call (same target_phone + calendar_date), the retry db row must show attempt_no=2. Inspect via `sqlite3 state.db "SELECT * FROM case2_retries ORDER BY created_at DESC LIMIT 2;"`. PASS/FAIL.
       - 5 (3-call matrix pass): all 3 scenarios completed without bridge-crash or unhandled exception. PASS/FAIL.
    4. **Phase 05.2 criteria:**
       - 1 (baseline+Case-2 overlay): happy-human.jsonl session.update payload contains 'TURN-DISCIPLIN' + 'Bellavista' + tolerance substring. PASS/FAIL.
       - 2 (no role-hallucination): happy-human.jsonl response.output_text.delta stream does NOT contain hallucinated counterpart dialogue. Analyze: bot's responses should only be its own lines; no fabricated "Restaurant: 'Willkommen'" + bot responding to itself. Human review may be needed — mark PASS if trace is clean, FAIL if hallucination observed, FLAG-FOR-REVIEW if ambiguous.
       - 3 (silence-monitor does NOT fire mid-bot-turn): happy-human.jsonl must NOT show 'silence_round_1' or 'silence_round_2' events while `output_audio_buffer.started/stopped` cycles are ongoing within <15s gaps. PASS/FAIL.
       - 4 (outbound wait-for-speech + 3 nudges): busy-no-answer.jsonl (or happy if first speech timing is measurable) shows first `response.create` fires AFTER first `input_audio_buffer.speech_stopped` (or after 3-nudge ladder + D-2 farewell if no speech ever arrived). PASS/FAIL. Expected nudge text substrings: 'Hallo, ist da jemand?', 'Hallo? Hoeren Sie mich?', 'ich versuche es spaeter nochmal'.
       - 5 (inbound self-greet unchanged): NOT exercised by the 3 outbound scenarios; mark N/A OR run a supplementary inbound-smoke scenario (call voice-bridge from Carsten's CLI-whitelisted number, verify self-greet after 1000ms per D-6). If N/A, note that inbound regression was covered by full voice-bridge test suite in Task 1.
       - 6 (combined 05.1+05.2 verify): aggregate of all above. PASS if all 5+6 are PASS (N/A accepted).
    5. **§201 invariant check:** all three traces must show ZERO `response.output_audio.delta` AND ZERO `audio_transcript.delta` events BEFORE `amd_result` for Case-2 calls. PASS/FAIL. Separate row in VERIFICATION.md.
    6. Write VERIFICATION.md with:
       - Header: Phase 05.1+05.2 Combined Live Verification — date, 3 scenario summaries, overall verdict
       - Pre-flight summary (from Task 1 log)
       - Verification matrix table (11 rows + §201 row)
       - Per-scenario summary (turns, total duration, cost, anomalies)
       - Any defects surfaced (if a criterion FAILs, describe the defect and propose gap-closure scope)
       - Overall verdict: ALL PASS → phase complete; ANY FAIL → recommend `/gsd-plan-phase 05.2 --gaps` for closure plan
    7. Commit: `docs(05.2-06): verification report — 3 PSTN traces analyzed, 11 criteria evaluated`.
  </action>
  <verify>
    <automated>test -f /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md &amp;&amp; grep -q "VERIFICATION" /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md &amp;&amp; grep -cE "(PASS|FAIL|N/A)" /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md | awk '{ if ($1 &gt;= 11) exit 0; else exit 1 }' &amp;&amp; grep -q "§201" /home/carsten_bot/nanoclaw/.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md exists, ≥2000 bytes
    - Contains verification matrix with ≥11 PASS/FAIL/N/A rows
    - Contains §201 invariant row (separate from 11)
    - Contains per-scenario summary for all 3 scenarios
    - Contains overall verdict (ALL PASS or specific FAILs)
    - `git log --oneline -1` shows `docs(05.2-06)` verification commit
  </acceptance_criteria>
  <done>VERIFICATION.md written and committed; Carsten can now review traces + report.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Carsten reviews traces + VERIFICATION.md, approves or describes issues</name>
  <files>
    (no files produced — human-review checkpoint)
  </files>
  <what-built>
    - 3 PSTN traces in .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/
    - 05.2-06-VERIFICATION.md with 11-criterion verification matrix
    - Q7-atomicity-finding.md with empirical session.update verdict
    - Pre-flight log at /tmp/05.2-06-preflight.log
    - Full Phase 05.2 code changes merged to main (all 5 wave-1/2 plans)
    - Phase 05.1 defects #3, #4, #5, #6 ALL closed (live-verified post-05.2 refactor)
    - Phase 05.2 architectural changes shipped: baseline+Case-2 overlay (05.2-01, 05.2-04), silence-monitor bot-awareness rewire (05.2-02), outbound wait-for-speech + 3-nudge ladder (05.2-03), AMD→baseline handoff integration (05.2-05)
  </what-built>
  <action>
    Checkpoint gate — this task has NO automated action. Carsten performs the manual review described in <how-to-verify> below. Executor pauses here and awaits resume-signal before proceeding to Task 5.
  </action>
  <how-to-verify>
    1. Read `.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md` — scan the verification matrix for any FAIL or FLAG-FOR-REVIEW rows. Confirm overall verdict.
    2. Eyeball the HAPPY trace (task5-traces-05.2/happy-human.jsonl):
       - Find the first `session.update` event post-AMD verdict. Confirm `session.instructions` contains the TURN-DISCIPLIN role-lock block verbatim and the Case-2 goal (restaurant name, date, tolerance).
       - Scan the `response.output_text.delta` stream chronologically — is the bot playing ONLY its own role? Any hallucinated counterpart dialog is a FAIL.
       - Look for `silence_round_1` / `silence_round_2` log events — should only fire during genuine post-bot-turn silence, never during `output_audio_buffer.started` windows.
    3. Eyeball the VOICEMAIL trace (task5-traces-05.2/voicemail.jsonl):
       - Confirm `amd_result` event with `verdict='voicemail'`.
       - Confirm the MCP tool-call `voice_case_2_schedule_retry` fires with all 5 zod-required fields.
    4. Eyeball the BUSY/NO-ANSWER trace (task5-traces-05.2/busy-no-answer.jsonl):
       - Confirm bot waits for counterpart speech before first utterance (no `response.create` before first `input_audio_buffer.speech_started` OR explicit AMD handoff).
       - If counterpart never spoke: confirm 3-nudge ladder OR AMD-silent path fired, with apologetic farewell + `end_call reason='silence'` (or equivalent).
    5. Open Q7 finding at `.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/q7-atomicity-finding.md` — confirm the verdict and implications.
    6. If everything looks good: reply with "approved". If issues found: describe them concisely — an executor will create a gap-closure plan.
  </how-to-verify>
  <verify>
    Human verification only — no automated command. Resume-signal from Carsten is the gate.
  </verify>
  <done>Carsten has either responded "approved" (Task 5 proceeds to ROADMAP update) OR described specific issues (executor returns gap-closure recommendation to orchestrator, Task 5 does NOT run).</done>
  <resume-signal>Type "approved" (both phases complete, ROADMAP marked) OR describe specific issues (triggers /gsd-plan-phase 05.2 --gaps).</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md — mark Phase 05.1 AND Phase 05.2 complete</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/ROADMAP.md (full — find Phase 05.1 entry lines 156-176, Phase 05.2 entry lines 178-194)
    .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md (must be ALL PASS before this task runs)
  </read_first>
  <action>
    **Gate:** only execute if Task 4 received "approved" signal. If Task 4 received issues, SKIP this task and return a gap-closure recommendation to orchestrator.
    1. Open .planning/ROADMAP.md. Find Phase 05.1 entry.
    2. Mark Plan 05.1-05 as [x]: `- [x] 05.1-05: Live PSTN verification (autonomous: false) — Wave 3 (deferred into Phase 05.2-06)`.
    3. Update Phase 05.1 status line if present (`**Plans:** 5/5 plans executed` or similar).
    4. Find Phase 05.2 entry. Replace `- [ ] TBD (run /gsd-plan-phase 05.2 to break down)` with the actual plan list:
       ```
       Plans:
       - [x] 05.2-01: Baseline persona + overlay framework — TDD — Wave 1
       - [x] 05.2-02: silence-monitor state-machine rewire (D-7) — TDD — Wave 1
       - [x] 05.2-03: Outbound wait-for-speech + nudge ladder (D-8) — TDD — Wave 2
       - [x] 05.2-04: Case-2 task-overlay migration + Case-6b overlay extract — TDD — Wave 2
       - [x] 05.2-05: AMD→baseline handoff mechanics (Q7 resolved) — TDD — Wave 2
       - [x] 05.2-06: Combined Phase 05.1 + 05.2 live verification (autonomous: false) — Wave 3
       ```
    5. Update Phase 05.2 status line: `**Plans:** 6/6 plans executed`.
    6. Commit: `docs(05.2-06): mark Phase 05.1 (05.1-05 deferred + completed) and Phase 05.2 (all 6 plans) complete in ROADMAP`.
    7. Push: `git push origin main` per project push-early rule.
  </action>
  <verify>
    <automated>grep -c "05.2-0[1-6].*\[x\]" /home/carsten_bot/nanoclaw/.planning/ROADMAP.md | awk '{ if ($1 == 6) exit 0; else exit 1 }' &amp;&amp; grep -q "05.1-05.*\[x\]" /home/carsten_bot/nanoclaw/.planning/ROADMAP.md</antomated>
  </verify>
  <acceptance_criteria>
    - 6 plan lines `05.2-0[1-6]` marked `[x]` in ROADMAP.md
    - Plan 05.1-05 marked `[x]` in Phase 05.1 section (deferred-and-completed)
    - Commit landed: `docs(05.2-06): mark Phase 05.1 ... Phase 05.2 ... complete`
    - `git push origin main` succeeded
  </acceptance_criteria>
  <done>Both phases marked complete; ROADMAP reflects final state; push landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| voice-bridge ↔ real PSTN via Sipgate | Live scenarios exercise real telephony; latency, jitter, NAT traversal all in play |
| Traces → committed git history | Traces may contain PII (phone numbers, conversation content). Scrub target phone to `+49<redacted>` before commit if private |
| Carsten checkpoint → ROADMAP update | Task 5 gated on Task 4 approval — no automatic phase-completion marking without human sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-06-01 | Information Disclosure | Traces commit captures PII (target_phone, conversation audio transcripts) | mitigate | Task 2 action includes "scrub target_phone to +49<redacted>" before commit. Transcripts of counterpart speech are necessary evidence — retained but in private state-repo context (nanoclaw repo may be public; verify before commit). If nanoclaw is public, consider writing traces to a private state-repo path instead. Human-verify checkpoint (Task 4) catches this before ROADMAP update. |
| T-05.2-06-02 | Information Disclosure | §201 StGB pre-verdict audio leak surfaces in live trace (regression from Wave 3 invariant) | mitigate | Task 3 step 5 explicitly checks for zero audio/transcript deltas before amd_result. If FAIL, Task 4 review surfaces it and gap-closure plan triggers. NOT a silent regression path. |
| T-05.2-06-03 | Denial of Service | Live scenario costs (Realtime minutes) exceed CAP_PER_CALL_EUR=€1.00 | accept (D-5) | D-5 explicitly defers cost-cap regression to separate phase. If a trace shows unusual cost spike, document in VERIFICATION.md for follow-up. |
| T-05.2-06-04 | Denial of Service | Service restart at pre-flight time kills an in-flight call (unlikely — assume no production calls during verification window) | accept | Pre-flight Task 1 restarts are scheduled by Carsten manually; Carsten controls call timing. |
| T-05.2-06-05 | Repudiation | Live-trace claim "ALL PASS" but one criterion failed and was overlooked | mitigate | VERIFICATION.md matrix REQUIRES explicit PASS/FAIL/N/A per row; Task 4 checkpoint surfaces any FAIL for explicit Carsten review before Task 5 ROADMAP update. |
</threat_model>

<verification>
```bash
cd /home/carsten_bot/nanoclaw && \
  # Traces captured
  test -f .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/happy-human.jsonl && \
  test -f .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/voicemail.jsonl && \
  test -f .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/task5-traces-05.2/busy-no-answer.jsonl && \
  # Verification report written
  test -f .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md && \
  grep -qE "(PASS|FAIL)" .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md && \
  grep -q "§201" .planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-VERIFICATION.md && \
  # ROADMAP updated (only if Task 4 approved)
  grep -q "05.2-06.*\[x\]" .planning/ROADMAP.md && \
  grep -q "05.1-05.*\[x\]" .planning/ROADMAP.md
```
</verification>

<success_criteria>
- [ ] Pre-flight: all Wave 1/2 commits present; full test suite green; services restarted on merged code; /tmp/trigger-case2.sh ready
- [ ] 3 live PSTN traces captured (happy-human.jsonl, voicemail.jsonl, busy-no-answer.jsonl) in task5-traces-05.2/
- [ ] 05.2-06-VERIFICATION.md with 11-criterion matrix + §201 invariant row; each row has explicit PASS/FAIL/N/A + trace evidence
- [ ] All 5 Phase 05.1 success criteria verified in live traces (defects #3, #4, #5, #6 + 3-call matrix pass)
- [ ] All 6 Phase 05.2 success criteria verified in live traces (baseline+overlay, role-lock, silence-monitor rewire, wait-for-speech + nudge ladder, inbound unchanged, combined verify)
- [ ] §201 StGB zero-audio-leak invariant re-verified in all 3 traces
- [ ] Q7-atomicity-finding.md referenced in VERIFICATION.md (trace evidence aligns with Task 1 verdict)
- [ ] Carsten checkpoint received "approved" signal
- [ ] ROADMAP.md: Phase 05.1 5/5 plans `[x]`; Phase 05.2 6/6 plans `[x]`
- [ ] All commits pushed to origin main (push-early rule)
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-persona-redesign-and-call-flow-state-machine/05.2-06-SUMMARY.md`:
- provides: combined Phase 05.1 + 05.2 live-verification report; 3 PSTN traces; 11-criterion matrix; §201 invariant re-verified; Q7 finding aligned with live traces
- decisions: Scenario 3 (BUSY/NO-ANSWER) accepted either AMD-silent path or 3-nudge persona path as valid — documented which fired; any FAIL criterion triggers gap-closure plan (not this plan's scope)
- affects: Phase 6 (Case 3 medical) — can now safely reuse baseline+overlay pattern; Phase 5 state-graph architecture — Q7 finding informs future multi-state design; retrospective at milestone v1.0 — documents refactor cost/quality delta
- defects-closed: 05.1 defects #3, #4, #5, #6; 05.2 architectural issues (a) role-hallucination, (b) premature silence re-prompt, (c) outbound bot-first
- risk-carryforward: D-5 cost-cap regression (if surfaced, separate phase); any FAILs from matrix (if surfaced, gap-closure plan)

Final step: `git push origin main`.
</output>
