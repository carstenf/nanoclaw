---
phase: 05.2
plan: 04
subsystem: voice-bridge/persona
tags: [case-2, case-6b, task-overlay, migration, persona, tdd, baseline-composition]
requires:
  - 05.2-01  # buildBasePersona factory + overlay dispatcher skeleton
provides:
  - voice-bridge/src/persona/overlays/case-2.ts buildCase2Overlay factory
  - voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts buildCase6bOverlay factory
  - voice-bridge/src/persona.ts buildCase2OutboundPersona (migrated — internal composition baseline+overlay, external signature unchanged)
  - voice-bridge/src/persona/overlays/index.ts dispatcher wired for case_2 + case_6b_inbound_carsten
  - voice-bridge/src/amd-classifier.ts D-10 scope-clarification comment
affects:
  - 05.2-05  # AMD→baseline handoff reuses this buildCase2OutboundPersona composition
  - 05.2-06  # live-verify Scenario HAPPY runs on new persona with TURN-DISCIPLIN role-lock
tech-stack:
  added: []
  patterns:
    - baseline + overlay composition (research §6.3)
    - verbatim research §6.3 overlay text (ASCII-umlaut convention)
    - sanitizeForPersona (curly-brace strip) at overlay boundary
    - internal migration preserving external signature (risk-minimization)
key-files:
  created:
    - voice-bridge/src/persona/overlays/case-2.ts
    - voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts
    - voice-bridge/tests/persona-overlay-case-2.test.ts
    - voice-bridge/tests/persona-migration-case-2.test.ts
    - voice-bridge/tests/persona-overlay-case-6b.test.ts
  modified:
    - voice-bridge/src/persona.ts  # imports baseline+overlay, deletes legacy blocks, rewires buildCase2OutboundPersona
    - voice-bridge/src/persona/overlays/index.ts  # dispatcher wires case_2 + case_6b
    - voice-bridge/src/amd-classifier.ts  # D-10 scope comment only; prompt text byte-identical
    - voice-bridge/tests/persona.test.ts  # tests 1+2 rewired to inspect overlay
    - voice-bridge/tests/persona-baseline.test.ts  # Tests H.3+H.4 updated (case_2/case_6b no longer throw)
    - voice-bridge/tests/accept.test.ts  # line 680 assertion split (NanoClaw + im Auftrag von Carsten now separate sentences)
decisions:
  - Case-2 migrates NOW — brings TURN-DISCIPLIN role-lock (D-9) into every Case-2 outbound call, addressing role-hallucination live-defect observed 2026-04-21
  - Case-6b ships overlay artifact but inbound webhook.ts path stays on legacy CASE6B_PERSONA (risk-minimization — no live-defect pressure on inbound, defer flip to future plan)
  - AMD classifier prompt (CASE2_AMD_CLASSIFIER_PROMPT) NOT consolidated into baseline in Phase 05.2 (D-10 scope clarification — listen-only detection mode is structurally distinct from conversation-mode)
  - Legacy OUTBOUND_PERSONA_TEMPLATE retained for now (still has callers — buildOutboundPersona in general outbound path) but no longer driving Case-2
  - 'im Auftrag von Carsten' semantic preserved via goal/context strings passed to baseline (not via template duplication)
metrics:
  duration_min: 12
  completed: 2026-04-21T19:58:00Z
  tasks_completed: 4
  files_created: 5
  files_modified: 6
---

# Phase 05.2 Plan 04: Case-2 + Case-6b Overlay Migration Summary

Migrated `buildCase2OutboundPersona` internals from monolithic legacy template (`OUTBOUND_PERSONA_TEMPLATE` + 3 concatenated blocks) to composition `buildBasePersona(outbound+Sie) + buildCase2Overlay(args)`, bringing the new TURN-DISCIPLIN role-lock into every Case-2 call without changing the external signature — and extracted a Case-6b overlay artifact for future inbound migration.

## Task Log

| Task | Commit | Status | Notes |
|------|--------|--------|-------|
| Task 1: RED — Case-2 overlay + migration golden tests (18 tests) | `cbacb4e` | done | 7 overlay unit tests + 11 migration regression tests. Test L asserts new TURN-DISCIPLIN role-lock. |
| Task 2: GREEN — buildCase2Overlay + migrate buildCase2OutboundPersona | `53b27e8` | done | Overlay 3752 bytes. Legacy blocks deleted. Helpers (sanitizeForPersona, toGermanNumber, timeToGerman) exported. |
| Task 3: Case-6b overlay extracted as artifact | `544dc4e` | done | Overlay 3299 bytes. Dispatcher wired. webhook.ts inbound path UNCHANGED (legacy CASE6B_PERSONA still live). |
| Task 4: AMD classifier D-10 scope-clarification comment | `c470082` | done | Prompt text byte-identical to HEAD (diff confirmed). Comment only. |

## Architecture Change

**Before** (`buildCase2OutboundPersona`):
```
OUTBOUND_PERSONA_TEMPLATE (with {{goal}}, {{context}} substituted)
+ CASE2_TOLERANCE_DECISION_BLOCK (with {time_tolerance_min} substituted)
+ CASE2_HOLD_MUSIC_CLARIFYING_BLOCK (static)
```
~586 tokens total. No role-lock clause.

**After**:
```
buildBasePersona({ anrede_form: 'Sie', counterpart_label: 'der Restaurant-Gegenueber',
                   goal, context, call_direction: 'outbound' })   # research §6.2 verbatim
+ '\n\n'
+ buildCase2Overlay(args)                                          # research §6.3 verbatim
```
~715 tokens total (baseline ~515 + overlay ~200). Includes TURN-DISCIPLIN role-lock, LANGUAGE-LOCK (Deutsch de-DE, NIEMALS andere Sprache), extended SAFETY & ESCALATION, and all other baseline-level rules.

External signature of `buildCase2OutboundPersona(args: Case2OutboundPersonaArgs): string` is unchanged — `webhook.ts:414` caller continues to work without modification.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] Worktree had no installed dependencies**
- **Found during:** Task 2 full-suite test
- **Issue:** `npm test` failed with `Cannot find package 'fastify'` across 14 test files — worktree was created without `node_modules/`.
- **Fix:** Ran `npm install` in voice-bridge directory.
- **Files modified:** none (`package-lock.json` already committed to repo; `node_modules/` is git-ignored)
- **Commit:** bundled into Task 2 commit `53b27e8` (no separate fix commit needed; install is infrastructure, not code).

**2. [Rule 2 - Preserve load-bearing semantic] 'im Auftrag von Carsten' phrase preservation**
- **Found during:** Task 2 full-suite (accept.test.ts:680)
- **Issue:** Legacy OUTBOUND_PERSONA_TEMPLATE opened with `"NanoClaw, ein freundlicher deutscher Sprach-Assistent im Auftrag von Carsten Freek"` — the phrase `"NanoClaw im Auftrag"` is both load-bearing for accept.test.ts and semantically important for the model's self-introduction on outbound calls. New baseline text says `"NanoClaw, der persoenliche Sprach-Assistent von Carsten Freek"` — no `"im Auftrag"`.
- **Fix:** Updated goal/context strings in `buildCase2OutboundPersona` to contain `"Im Auftrag von Carsten..."` (now appears in baseline's ROLE & OBJECTIVE `Deine Aufgabe: ...` line and `Kontext: ...` line). Updated accept.test.ts:680 assertion to check `NanoClaw` and `im Auftrag von Carsten` separately (documented as expected post-migration drift).
- **Commit:** bundled into Task 2 commit `53b27e8`.

### Updated Legacy Tests (by design — per plan's "legacy golden-string tests updated to new composition")

- `tests/persona.test.ts` tests 1 + 2: rewired to inspect the new `buildCase2Overlay()` output instead of the deleted `CASE2_TOLERANCE_DECISION_BLOCK` / `CASE2_HOLD_MUSIC_CLARIFYING_BLOCK` constants.
- `tests/persona-baseline.test.ts` Tests H.3 + H.4: updated — `buildTaskOverlay('case_2', {...})` and `buildTaskOverlay('case_6b_inbound_carsten', {})` now return overlay strings instead of throwing (05.2-04 Tasks 2 + 3 filled the bodies).
- `tests/accept.test.ts:680`: split single-substring assertion into two separate checks.

## Authentication Gates

None.

## Test + Build Results

| Metric | Before (HEAD@a994d72) | After (HEAD@c470082) |
|--------|-----------------------|----------------------|
| Tests passed | 402 | 426 |
| Tests failed | 0 | 0 |
| Tests skipped | 4 | 4 |
| Build (`tsc`) | clean | clean |
| New test files | — | 3 (persona-overlay-case-2, persona-migration-case-2, persona-overlay-case-6b) |
| New tests | — | 24 (7 + 11 + 6) |

## Byte Budgets

| File | Bytes | Budget |
|------|-------|--------|
| voice-bridge/src/persona/overlays/case-2.ts | 3752 | < 4000 ✓ |
| voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts | 3299 | < 4000 ✓ |
| Combined baseline + Case-2 overlay (at runtime) | ~4500 | < 5000 (≈ 1430 tokens, under 1500 ceiling) ✓ |

## Defect Addressed

**Role-hallucination on Case-1 outbound (observed 2026-04-21):** baseline `Rolle (KRITISCH)` clause `"Du SPRICHST NUR deine Rolle (NanoClaw). Du SPIELST NIEMALS den Gegenueber. Du ERFINDEST NIEMALS, was der Gegenueber sagt."` now flows into every Case-2 outbound persona via the baseline composition. This is the prompt-side fix.

## Risk Carry-Forward

- **T-05.2-04-05 combined token budget:** guarded by byte-budget tests in each overlay file + composition test M in persona-migration-case-2.test.ts (`result.length < 5000`). Final live verification in 05.2-06.
- **Case-6b inbound path skew (T-05.2-04-06):** Case-6b overlay is ARTIFACT-ONLY; `webhook.ts` inbound still uses legacy `CASE6B_PERSONA`. Future developers who edit `case-6b-inbound-carsten.ts` without also updating the legacy constant could cause drift. Mitigation: comment block at top of the overlay file explicitly says "NOT YET WIRED INTO webhook.ts INBOUND PATH". A follow-up plan should flip the inbound wiring.

## D-10 Note (AMD Classifier)

`CASE2_AMD_CLASSIFIER_PROMPT` in `voice-bridge/src/amd-classifier.ts` is a LISTEN-ONLY DETECTION prompt. Its only output is the `amd_result` function-call with verdict ∈ {human, voicemail, silent, noise}. It is structurally distinct from conversation-mode baseline+overlay personas and is NOT consolidated into the baseline in Phase 05.2. Consolidation is deferred to a future state-machine phase (Pipecat-Flows-style) where the LISTEN state would use baseline + listen-overlay.

Comment block added above the prompt declaration; prompt text byte-identical to HEAD.

## Self-Check: PASSED

- Files exist: voice-bridge/src/persona/overlays/case-2.ts (FOUND), voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts (FOUND)
- Commits exist:
  - `cbacb4e` test(05.2-04): RED — FOUND
  - `53b27e8` feat(05.2-04): GREEN Case-2 — FOUND
  - `544dc4e` feat(05.2-04): GREEN Case-6b — FOUND
  - `c470082` docs(05.2-04): D-10 comment — FOUND
- Plan verification block passed: VERIFY-ALL=OK
- Full voice-bridge test suite: 426 passed / 4 skipped
- TypeScript build: clean (tsc exit 0)
- Byte budgets: both overlays < 4000 bytes
- webhook.ts UNCHANGED (git diff --name-only a994d72..HEAD shows no webhook entry)
- STATE.md + ROADMAP.md UNCHANGED (orchestrator owns these)
