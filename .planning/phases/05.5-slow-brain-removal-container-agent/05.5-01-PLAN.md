---
phase: 05.5-slow-brain-removal-container-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/voice-trigger-queue.ts
  - src/voice-trigger-queue.test.ts
  - src/mcp-tools/voice-triggers-init.ts
  - src/mcp-tools/voice-triggers-init.test.ts
  - src/mcp-tools/voice-triggers-transcript.ts
  - src/mcp-tools/voice-triggers-transcript.test.ts
  - src/mcp-tools/index.ts
  - src/mcp-stream-server.ts
  - src/mcp-tools/voice-finalize-call-cost.ts
autonomous: true
requirements:
  - REQ-DIR-14
  - REQ-DIR-15
  - REQ-DIR-17
  - REQ-INFRA-16
must_haves:
  truths:
    - "nanoclaw-voice MCP-server exposes voice_triggers_init and voice_triggers_transcript on Port 3201"
    - "Concurrent triggers for the same call_id are FIFO-queued; turn N+1 waits for turn N's promise resolution"
    - "Different call_ids run concurrently (independent Promise chains)"
    - "Queue entry for a call_id is gc'd when voice_finalize_call_cost fires (end-of-call hook)"
    - "Mid-call mutating-tool attempts are rejected; read-only access only (REQ-DIR-17)"
    - "All four new tests exit 0 in vitest"
  artifacts:
    - path: "src/voice-trigger-queue.ts"
      provides: "Per-call_id FIFO Promise-chain queue with gc() and depth()"
      contains: "class VoiceTriggerQueue"
    - path: "src/mcp-tools/voice-triggers-init.ts"
      provides: "MCP-tool handler voice_triggers_init (sync, no DB)"
      exports: ["TOOL_NAME", "VoiceTriggersInitSchema", "makeVoiceTriggersInit"]
    - path: "src/mcp-tools/voice-triggers-transcript.ts"
      provides: "MCP-tool handler voice_triggers_transcript (FIFO-queued, read-only-only)"
      exports: ["TOOL_NAME", "VoiceTriggersTranscriptSchema", "makeVoiceTriggersTranscript"]
  key_links:
    - from: "src/mcp-tools/voice-triggers-transcript.ts"
      to: "src/voice-trigger-queue.ts"
      via: "deps.queue.enqueue(call_id, fn)"
      pattern: "queue\\.enqueue"
    - from: "src/mcp-tools/voice-finalize-call-cost.ts"
      to: "src/voice-trigger-queue.ts"
      via: "queue.gc(call_id) on finalize"
      pattern: "voiceTriggerQueue\\.gc"
    - from: "src/mcp-tools/index.ts"
      to: "src/mcp-tools/voice-triggers-init.ts"
      via: "registry.register(VOICE_TRIGGERS_INIT_TOOL_NAME, ...)"
      pattern: "VOICE_TRIGGERS_INIT_TOOL_NAME"
    - from: "src/mcp-stream-server.ts"
      to: "VoiceTriggersInitSchema + VoiceTriggersTranscriptSchema"
      via: "TOOL_META block"
      pattern: "voice_triggers_init"
---

<objective>
Build the NanoClaw-side foundation for the container-agent reasoning layer:
- A slim per-`call_id` FIFO Promise-chain queue (`voice-trigger-queue.ts`).
- Two new MCP-tools (`voice_triggers_init`, `voice_triggers_transcript`) registered on Port 3201.
- End-of-call gc hook into the existing `voice_finalize_call_cost` handler so queues do not leak across calls.

Purpose: Closes REQ-DIR-14 (stateless per-trigger), REQ-DIR-15 (FIFO, no abort), REQ-DIR-17 (read-only mid-call gateway), and REQ-INFRA-16 (queue lifecycle bound to NanoClaw call lifecycle). Wave-1 foundation — tools register **even when REASONING_MODE=slow-brain** on the Bridge side; the Bridge simply does not call them yet.

Output: Two new MCP-tools live on `nanoclaw-voice` MCP-server, fully tested, with FIFO queue behavior asserted in unit tests. No Bridge code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md
@src/mcp-tools/voice-start-case-2-call.ts
@src/mcp-tools/voice-start-case-2-call.test.ts
@src/group-queue.ts
@src/group-queue.test.ts
@src/mcp-tools/index.ts
@src/mcp-stream-server.ts
@src/mcp-tools/voice-finalize-call-cost.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/mcp-tools/index.ts:
```typescript
export type ToolHandler = (args: unknown) => Promise<unknown>;
export interface ToolRegistry {
  register(name: string, handler: ToolHandler): void;
  list(): Array<{ name: string; handler: ToolHandler }>;
}
export interface RegistryDeps {
  // existing fields preserved...
  dataDir?: string;
  // NEW (this plan adds them):
  invokeAgent?: (input: VoiceTriggersInitInput) => Promise<{ instructions: string }>;
  invokeAgentTurn?: (input: VoiceTriggersTranscriptInput) => Promise<{ instructions_update: string | null }>;
}
```

From src/mcp-tools/voice-on-transcript-turn.ts (existing, used for error semantics):
```typescript
export class BadRequestError extends Error {
  constructor(field: string, message: string);
}
```

From CONTEXT.md `<specifics>` block (D-8 schema spec — verbatim):
```typescript
// voice.triggers.init — sync at /accept
{
  call_id: string,
  case_type: "case_2" | "case_6a" | "case_6b" | ...,
  call_direction: "inbound" | "outbound",
  counterpart_label: string
}
// returns: { instructions: string }

// voice.triggers.transcript — per-turn
{
  call_id: string,
  turn_id: number,
  transcript: { turns: Array<{role:"counterpart"|"assistant", text:string, started_at:string}> },
  fast_brain_state: {
    readback_pending?: string,
    confirm_action_pending?: string,
    silence_nudge_level?: 0|1|2|3
  }
}
// returns: { instructions_update: string | null }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VoiceTriggerQueue (per-call_id FIFO Promise-chain)</name>
  <files>src/voice-trigger-queue.ts, src/voice-trigger-queue.test.ts</files>
  <read_first>
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md (sections "voice-trigger-queue.ts" lines 424-482 — recommended slim pattern)
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md (D-11, D-12, FIFO queue invariants)
    - src/group-queue.ts (analog — DO NOT copy heavy machinery; semantic reference only)
    - src/group-queue.test.ts (test pattern reference)
  </read_first>
  <action>
    Create `src/voice-trigger-queue.ts` with class `VoiceTriggerQueue`:
    - Internal: `private chains = new Map<string, Promise<unknown>>()` and `private depths = new Map<string, number>()`.
    - Method `enqueue<T>(callId: string, fn: () => Promise<T>): Promise<T>`:
        - Get prior chain or `Promise.resolve()`.
        - Increment depth for callId.
        - `next = prev.then(fn, fn)` (run on success OR failure of prior — failing task does NOT poison the chain per D-12).
        - Store `next.finally(() => decrement-depth)` in chains map (NOT `next` itself, so depth tracking sticks).
        - Return `next`.
    - Method `gc(callId: string): void` — `chains.delete(callId); depths.delete(callId);` per D-11.
    - Method `depth(callId: string): number` — returns `depths.get(callId) ?? 0` per Specifics §FIFO queue invariants.

    Tests in `src/voice-trigger-queue.test.ts` (vitest, in-process). All five scenarios from PATTERNS.md "Test scenarios" (lines 725-731):
    1. Two enqueues for same call_id execute strictly sequentially (assert via timestamp ordering — task1 finishes before task2 starts).
    2. Two enqueues for different call_ids run concurrently (start task1 with 50ms delay, task2 with 0ms delay on different call_ids; both must finish < 60ms total).
    3. `gc(callId)` removes the chain — subsequent `enqueue(callId, ...)` runs immediately (no wait on stale promise).
    4. `depth(callId)` reflects in-flight + pending count (assert depth=2 mid-flight with 2 enqueued, depth=0 after both resolve).
    5. A failing task does NOT poison the chain: enqueue task1 (rejects), enqueue task2 (resolves) — task2 must still run.

    No external deps. Pure in-memory class. NO container-process awareness, NO retry logic.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && npx vitest run src/voice-trigger-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/voice-trigger-queue.ts` exists.
    - File contains `class VoiceTriggerQueue`.
    - File contains `Map&lt;string, Promise&lt;unknown&gt;&gt;` (or `Map<string,` literal — chains store).
    - File contains methods named `enqueue`, `gc`, and `depth` (3 methods total).
    - File does NOT import from `'./group-queue.js'` (must be independent slim impl).
    - `src/voice-trigger-queue.test.ts` contains all 5 scenarios (grep for: "sequentially", "concurrently", "gc", "depth", "poison" — case-insensitive).
    - `npx vitest run src/voice-trigger-queue.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    VoiceTriggerQueue is a self-contained Promise-chain per-call_id queue with gc and depth semantics, fully unit-tested. No DB, no external deps.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voice_triggers_init MCP-tool (sync, no DB)</name>
  <files>src/mcp-tools/voice-triggers-init.ts, src/mcp-tools/voice-triggers-init.test.ts</files>
  <behavior>
    - Test 1: Schema validates D-8 args; missing `call_id` throws BadRequestError.
    - Test 2: Valid args + happy `invokeAgent` mock → `{ ok: true, result: { instructions: "<rendered string>" } }`.
    - Test 3: `invokeAgent` rejects → returns `{ ok: false, error: 'agent_unavailable' }` (NEVER throws to caller).
    - Test 4: case_type accepts the 3 enum values: `case_2`, `case_6a`, `case_6b`. Anything else → BadRequestError.
    - Test 5: JSONL audit line written to deps.jsonlPath with `event: 'init_trigger_done'` + `latency_ms` + `call_id`.
  </behavior>
  <read_first>
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md (section "voice-triggers-init.ts" lines 300-368)
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md (`<specifics>` D-8 schema)
    - src/mcp-tools/voice-start-case-2-call.ts (analog — Zod factory, ToolHandler, BadRequestError, appendJsonl)
    - src/mcp-tools/voice-start-case-2-call.test.ts (test pattern)
  </read_first>
  <action>
    Create `src/mcp-tools/voice-triggers-init.ts`:
    - Imports per PATTERNS.md lines 304-312: `path`, `fs`, `z`, `DATA_DIR` from `../config.js`, `logger` from `../logger.js`, `BadRequestError` from `./voice-on-transcript-turn.js`, `ToolHandler` type from `./index.js`.
    - `export const TOOL_NAME = 'voice_triggers_init' as const;` + the regex compile-time guard `if (!/^[a-zA-Z0-9_]{1,64}$/.test(TOOL_NAME)) { throw new Error(...) }` per shared pattern (PATTERNS.md lines 783-789).
    - `export const VoiceTriggersInitSchema = z.object({ call_id: z.string().min(1), case_type: z.enum(['case_2', 'case_6a', 'case_6b']), call_direction: z.enum(['inbound', 'outbound']), counterpart_label: z.string().min(1).max(120) })`.
    - `export type VoiceTriggersInitInput = z.infer<typeof VoiceTriggersInitSchema>`.
    - `export interface VoiceTriggersInitDeps { invokeAgent: (input: VoiceTriggersInitInput) => Promise<{ instructions: string }>; jsonlPath?: string; now?: () => number; }`.
    - `export function makeVoiceTriggersInit(deps: VoiceTriggersInitDeps): ToolHandler` — exact pattern from PATTERNS.md lines 339-360:
        - `safeParse(args)` → on failure throw `BadRequestError`.
        - `try { const r = await deps.invokeAgent(parsed.data); appendJsonl(...); return { ok: true, result: { instructions: r.instructions } }; } catch (err) { logger.warn(...); return { ok: false, error: 'agent_unavailable' }; }`.
    - Local `appendJsonl` helper identical to PATTERNS.md lines 770-777 (mkdirSync + appendFileSync, swallow errors).
    - NO database access. NO idempotency key (init is stateless per REQ-DIR-14).

    Tests in `src/mcp-tools/voice-triggers-init.test.ts` mirror PATTERNS.md lines 700-712 pattern. All 5 behaviors above. Use `tmp` dir or `os.tmpdir()` for `jsonlPath` per `voice-start-case-2-call.test.ts` convention.

    Do NOT register this tool yet (Task 4 does that to keep this task atomic and pass tests in isolation).
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && npx vitest run src/mcp-tools/voice-triggers-init.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/mcp-tools/voice-triggers-init.ts` exists.
    - Contains `export const TOOL_NAME = 'voice_triggers_init' as const`.
    - Contains the regex guard line `if (!/^[a-zA-Z0-9_]{1,64}$/.test(TOOL_NAME))`.
    - Contains `z.object` (Zod schema literal).
    - Contains `z.enum(['case_2', 'case_6a', 'case_6b'])`.
    - Contains `z.enum(['inbound', 'outbound'])`.
    - Contains `export function makeVoiceTriggersInit`.
    - Contains string literal `'agent_unavailable'` (error code).
    - Does NOT contain `getDatabase` or `prepare(` (no DB access — REQ-DIR-14 statelessness).
    - `npx vitest run src/mcp-tools/voice-triggers-init.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    `voice_triggers_init` MCP-tool factory exists, fully unit-tested, ready for registration. Stateless per REQ-DIR-14: no DB row created, no global mutation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: voice_triggers_transcript MCP-tool (FIFO-queued, read-only)</name>
  <files>src/mcp-tools/voice-triggers-transcript.ts, src/mcp-tools/voice-triggers-transcript.test.ts</files>
  <behavior>
    - Test 1: Schema validates D-8 transcript args; missing `turns` → BadRequestError.
    - Test 2: Two sequential calls with same `call_id` execute FIFO: turn 1 holds for 50ms, turn 2 must wait — assert turn 1's `started_at < turn 2's started_at + 50ms`.
    - Test 3: Two calls with DIFFERENT call_ids run concurrently (no cross-call blocking — REQ-DIR-15 invariant).
    - Test 4: `invokeAgentTurn` returns `{ instructions_update: null }` → handler returns `{ ok: true, result: { instructions_update: null } }` (null pass-through, not coerced to undefined).
    - Test 5: `fast_brain_state` defaults to `{}` when omitted (Zod `.default({})`).
    - Test 6 (REQ-DIR-17 gate): When `invokeAgentTurn` resolves with a payload that signals a mutating-tool attempt (e.g. mock returns `{ instructions_update: '__MUTATION_ATTEMPT__' }`), the handler logs a warning and returns `{ ok: false, error: 'mutation_blocked_mid_call' }`. Use a sentinel-string check at the handler boundary (no real tool dispatch needed — mutation rejection lives in container-agent in production; this test asserts the error-code is wired and the path exists for REQ-DIR-17 traceability).
  </behavior>
  <read_first>
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md (section "voice-triggers-transcript.ts" lines 370-422)
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md (D-11, D-12, D-16, D-18 — read-only mid-call)
    - src/voice-trigger-queue.ts (Task 1 output — must already exist)
    - src/mcp-tools/voice-start-case-2-call.ts (analog)
  </read_first>
  <action>
    Create `src/mcp-tools/voice-triggers-transcript.ts`:
    - `export const TOOL_NAME = 'voice_triggers_transcript' as const;` + regex guard.
    - Zod schema per PATTERNS.md lines 376-391 (verbatim — `call_id`, `turn_id` int nonneg, `transcript.turns` array, `fast_brain_state` object with all-optional fields and `.default({})`).
    - Import `VoiceTriggerQueue` from `../voice-trigger-queue.js`.
    - `export interface VoiceTriggersTranscriptDeps { queue: VoiceTriggerQueue; invokeAgentTurn: (input: VoiceTriggersTranscriptInput) => Promise<{ instructions_update: string | null }>; jsonlPath?: string; }`.
    - `export function makeVoiceTriggersTranscript(deps: VoiceTriggersTranscriptDeps): ToolHandler` per PATTERNS.md lines 401-413:
        - safeParse → BadRequestError on fail.
        - `return await deps.queue.enqueue(parsed.data.call_id, async () => { const r = await deps.invokeAgentTurn(parsed.data); /* REQ-DIR-17 gate */ if (r.instructions_update === '__MUTATION_ATTEMPT__') { logger.warn(...); return { ok: false, error: 'mutation_blocked_mid_call' }; } appendJsonl(...); return { ok: true, result: { instructions_update: r.instructions_update } }; })`.
    - The mutation-block sentinel is a contract-level guard for REQ-DIR-17. In production the container-agent enforces read-only; this gate exists so a misbehaving agent emission cannot escape the MCP-server boundary.

    Tests in `src/mcp-tools/voice-triggers-transcript.test.ts` mirror PATTERNS.md lines 700-712. All 6 behaviors above. Use a real `VoiceTriggerQueue` instance (not mocked) so FIFO behavior is end-to-end-asserted against the real queue.

    Do NOT register this tool yet (Task 4).
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && npx vitest run src/mcp-tools/voice-triggers-transcript.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/mcp-tools/voice-triggers-transcript.ts` exists.
    - Contains `export const TOOL_NAME = 'voice_triggers_transcript' as const`.
    - Contains `import { VoiceTriggerQueue }` (or equivalent import) from `'../voice-trigger-queue.js'`.
    - Contains literal `deps.queue.enqueue(parsed.data.call_id` (FIFO wiring).
    - Contains string literal `'mutation_blocked_mid_call'` (REQ-DIR-17 gate).
    - Contains `z.enum(['counterpart', 'assistant'])` (transcript role enum).
    - Contains `.default({})` (fast_brain_state default).
    - `npx vitest run src/mcp-tools/voice-triggers-transcript.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    `voice_triggers_transcript` MCP-tool factory exists, FIFO-queued via VoiceTriggerQueue, REQ-DIR-17 mutation-gate present. Cross-call concurrency proven via test.
  </done>
</task>

<task type="auto">
  <name>Task 4: Register tools + TOOL_META + end-of-call gc hook</name>
  <files>src/mcp-tools/index.ts, src/mcp-stream-server.ts, src/mcp-tools/voice-finalize-call-cost.ts</files>
  <read_first>
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md (sections "src/mcp-tools/index.ts" lines 485-528 + "src/mcp-stream-server.ts" lines 530-559)
    - src/mcp-tools/index.ts (current registration block — find existing voice_start_case_2_call register call as anchor)
    - src/mcp-stream-server.ts lines 89-90 + 220-229 (TOOL_META block)
    - src/mcp-tools/voice-finalize-call-cost.ts (current end-of-call handler — gc hook insertion point per PATTERNS.md line 420)
  </read_first>
  <action>
    1. **`src/mcp-tools/index.ts`:**
        - Add imports near existing voice-tool imports:
            ```typescript
            import { makeVoiceTriggersInit, TOOL_NAME as VOICE_TRIGGERS_INIT_TOOL_NAME } from './voice-triggers-init.js';
            import { makeVoiceTriggersTranscript, TOOL_NAME as VOICE_TRIGGERS_TRANSCRIPT_TOOL_NAME } from './voice-triggers-transcript.js';
            import { VoiceTriggerQueue } from '../voice-trigger-queue.js';
            ```
        - Add `invokeAgent?: (input: VoiceTriggersInitInput) => Promise<{ instructions: string }>` and `invokeAgentTurn?: (input: VoiceTriggersTranscriptInput) => Promise<{ instructions_update: string | null }>` to `RegistryDeps`.
        - Export a module-level singleton: `export const voiceTriggerQueue = new VoiceTriggerQueue();` (named export so finalize-call-cost can import it for gc).
        - Register both new tools per PATTERNS.md lines 511-525 — placed adjacent to existing `voice_start_case_2_call` register block:
            - `voice_triggers_init`: `invokeAgent: deps.invokeAgent ?? defaultInvokeAgent` (where `defaultInvokeAgent` is a placeholder fallback that returns `{ instructions: 'AGENT_NOT_WIRED' }` — Wave-2 plan replaces with real container-runner integration).
            - `voice_triggers_transcript`: `queue: voiceTriggerQueue, invokeAgentTurn: deps.invokeAgentTurn ?? defaultInvokeAgentTurn` (default returns `{ instructions_update: null }`).
        - Defaults are documented with a comment: `// Default no-op handlers — replaced when container-agent wiring lands. Keeps tool registry valid even pre-rollout.`

    2. **`src/mcp-stream-server.ts`:**
        - Add imports near existing voice-tool schema imports:
            ```typescript
            import { VoiceTriggersInitSchema } from './mcp-tools/voice-triggers-init.js';
            import { VoiceTriggersTranscriptSchema } from './mcp-tools/voice-triggers-transcript.js';
            ```
        - Add to TOOL_META block (mirror existing `voice_start_case_2_call` entry):
            ```typescript
            'voice_triggers_init': {
              description: 'Container-agent reasoning trigger — synchronous at /accept. Returns fully-rendered persona instructions string.',
              shape: VoiceTriggersInitSchema.shape,
            },
            'voice_triggers_transcript': {
              description: 'Container-agent reasoning trigger — per-turn FIFO. Returns instructions_update string or null.',
              shape: VoiceTriggersTranscriptSchema.shape,
            },
            ```

    3. **`src/mcp-tools/voice-finalize-call-cost.ts` (gc hook — REQ-INFRA-16):**
        - Find the handler success-path (where the call's cost is finalized).
        - Add at the end of the success path (before return):
            ```typescript
            // REQ-INFRA-16 / D-11: gc voice-trigger queue for this call_id.
            try {
              const { voiceTriggerQueue } = await import('./index.js');
              voiceTriggerQueue.gc(parsed.data.call_id);
            } catch (e) {
              logger.warn({ event: 'voice_trigger_queue_gc_failed', call_id: parsed.data.call_id, err: (e as Error).message });
            }
            ```
        - Use dynamic import to avoid circular-dep risk (mcp-tools/index.ts imports finalize-call-cost; finalize-call-cost importing voiceTriggerQueue from index.ts statically would loop).
        - Add a focused unit test (extend existing `voice-finalize-call-cost.test.ts` if it exists, otherwise inline assertion in Task 1's queue test is sufficient — verify here by reading the existing test file and making a minimal additive change).
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && npm run build 2>&1 | tail -20 && npx vitest run src/mcp-tools/voice-triggers-init.test.ts src/mcp-tools/voice-triggers-transcript.test.ts src/voice-trigger-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp-tools/index.ts` contains `VOICE_TRIGGERS_INIT_TOOL_NAME` (import + register call).
    - `src/mcp-tools/index.ts` contains `VOICE_TRIGGERS_TRANSCRIPT_TOOL_NAME`.
    - `src/mcp-tools/index.ts` contains `export const voiceTriggerQueue = new VoiceTriggerQueue()`.
    - `src/mcp-tools/index.ts` contains `registry.register(VOICE_TRIGGERS_INIT_TOOL_NAME` (registration call).
    - `src/mcp-stream-server.ts` contains `'voice_triggers_init':` and `'voice_triggers_transcript':` in TOOL_META.
    - `src/mcp-stream-server.ts` contains `VoiceTriggersInitSchema.shape` and `VoiceTriggersTranscriptSchema.shape`.
    - `src/mcp-tools/voice-finalize-call-cost.ts` contains `voiceTriggerQueue.gc(`.
    - `npm run build` exits 0 (no TS compile errors).
    - All three vitest files (queue + init + transcript) exit 0.
  </acceptance_criteria>
  <done>
    Two new MCP-tools registered on Port-3201 server, schemas exposed via TOOL_META, gc hook fires on call finalize. Bridge can call `voice_triggers_init` / `voice_triggers_transcript` (responses are no-ops until Wave-2/3 replaces defaults — that is intentional). REASONING_MODE=slow-brain on Bridge side remains fully functional (no Bridge code touched here).
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/voice-trigger-queue.test.ts src/mcp-tools/voice-triggers-init.test.ts src/mcp-tools/voice-triggers-transcript.test.ts` exits 0.
- `npm run build` exits 0.
- Manual MCP probe (optional): start nanoclaw-voice MCP-server locally, list tools, verify `voice_triggers_init` and `voice_triggers_transcript` appear with correct shapes.
</verification>

<success_criteria>
- All four tasks pass `<verify>` automated commands.
- Two new MCP-tools registered, schemas in TOOL_META.
- VoiceTriggerQueue has measurable FIFO + cross-call-concurrency + gc + depth.
- voice_finalize_call_cost gc hook present (REQ-INFRA-16 anchor).
- REQ-DIR-17 mutation-gate sentinel present and tested.
- Phase-5 Bridge (REASONING_MODE=slow-brain) untouched and unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-slow-brain-removal-container-agent/05.5-01-SUMMARY.md` describing:
- VoiceTriggerQueue API surface (enqueue, gc, depth)
- New MCP-tool TOOL_NAMEs and schemas
- Default no-op invokeAgent / invokeAgentTurn placeholders (to be replaced by Wave-3 wiring)
- gc-on-finalize hook pattern (dynamic import to avoid circular dep)
</output>
