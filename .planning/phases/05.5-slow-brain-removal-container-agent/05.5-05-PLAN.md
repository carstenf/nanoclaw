---
phase: 05.5-slow-brain-removal-container-agent
plan: 05
type: execute
wave: 3
depends_on:
  - 01
files_modified:
  - src/mcp-tools/voice-record-turn-cost.ts
  - src/mcp-tools/voice-record-turn-cost.test.ts
  - src/cost-ledger.ts
  - src/db.ts
  - src/db-migration.ts
autonomous: true
requirements:
  - REQ-COST-06
must_haves:
  truths:
    - "Container-agent invocations (init + transcript) record one cost-ledger row each per REQ-COST-06"
    - "trigger_type column distinguishes 'turn' (existing Realtime turns) from 'init_trigger' and 'transcript_trigger' (new container-agent invocations)"
    - "Existing voice_record_turn_cost calls (Phase-4 traffic) default trigger_type='turn' — backward-compatible schema change"
    - "Cost-ledger SUM aggregation (sumCostCurrentDay/Month) includes all trigger types in total voice cost"
    - "DB migration is forward-only and idempotent (ALTER TABLE if column missing)"
  artifacts:
    - path: "src/mcp-tools/voice-record-turn-cost.ts"
      provides: "Extended schema with trigger_type field, default 'turn'"
      contains: "trigger_type"
    - path: "src/db-migration.ts"
      provides: "Idempotent ALTER TABLE adding trigger_type column"
      contains: "ALTER TABLE voice_turn_costs ADD COLUMN trigger_type"
    - path: "src/cost-ledger.ts"
      provides: "Aggregation queries include all trigger types"
  key_links:
    - from: "src/mcp-tools/voice-record-turn-cost.ts"
      to: "voice_turn_costs DB table"
      via: "INSERT with trigger_type column populated"
      pattern: "trigger_type"
    - from: "src/mcp-tools/voice-triggers-init.ts (Plan 01)"
      to: "voice_record_turn_cost (this plan)"
      via: "JSONL or direct call recording init-trigger cost"
      pattern: "init_trigger"
---

<objective>
Extend the existing cost-ledger so each container-agent invocation (init + per-turn transcript trigger) shows up in the per-call cost ledger alongside existing OpenAI Realtime turn costs. Path-A from PATTERNS.md (extend existing schema, lower-churn — recommended for v1).

Purpose: Closes REQ-COST-06 — total voice-call cost remains observable when reasoning is offloaded to container-agent.

Output: A backward-compatible schema extension (`trigger_type` column with default `'turn'`), migrated DB, updated handler, regression-safe aggregation, unit-test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md
@src/mcp-tools/voice-record-turn-cost.ts
@src/mcp-tools/voice-record-turn-cost.test.ts
@src/cost-ledger.ts
@src/db.ts
@src/db-migration.ts

<interfaces>
From PATTERNS.md "voice-record-turn-cost.ts" Path A (recommended):
```typescript
// Extended schema:
RecordTurnCostSchema = z.object({
  call_id: z.string().min(1),
  turn_id: z.string().min(1),  // accepts 'init' or numeric-string for triggers
  audio_in_tokens: z.number().int().nonnegative().default(0),
  ...
  cost_eur: z.number().nonnegative(),
  // NEW:
  trigger_type: z.enum(['turn', 'init_trigger', 'transcript_trigger']).default('turn'),
});
```

DB schema (existing voice_turn_costs table, from STATE.md Phase-04 entry):
- PRIMARY KEY (call_id, turn_id)
- Existing columns: call_id, turn_id, audio_in_tokens, audio_out_tokens, text_in_tokens, text_out_tokens, cost_eur, recorded_at
- NEW column: trigger_type TEXT NOT NULL DEFAULT 'turn'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + voice-record-turn-cost.ts extension</name>
  <files>src/db-migration.ts, src/mcp-tools/voice-record-turn-cost.ts, src/mcp-tools/voice-record-turn-cost.test.ts</files>
  <read_first>
    - src/db-migration.ts (find existing migrations — pattern for idempotent ALTER TABLE)
    - src/db.ts (find voice_turn_costs CREATE TABLE statement — confirm column list)
    - src/mcp-tools/voice-record-turn-cost.ts FULL (current schema lines 18-27, INSERT statement lines ~50-86)
    - src/mcp-tools/voice-record-turn-cost.test.ts FULL (current test pattern)
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md (section "voice-record-turn-cost.ts" lines 622-651)
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md (D-19, D-20)
  </read_first>
  <action>
    1. **`src/db-migration.ts`:**

        - Find the existing migration pattern (likely a numbered list of `db.exec(...)` calls or a migrations array).
        - Add a new migration step:
            ```typescript
            // Phase 05.5 / REQ-COST-06: trigger_type column for per-trigger cost tracking
            const cols = db.prepare("PRAGMA table_info(voice_turn_costs)").all() as Array<{ name: string }>;
            if (!cols.some(c => c.name === 'trigger_type')) {
              db.exec("ALTER TABLE voice_turn_costs ADD COLUMN trigger_type TEXT NOT NULL DEFAULT 'turn'");
            }
            ```
        - Place it in execution order AFTER the original `voice_turn_costs` CREATE TABLE migration so the column adds cleanly to existing rows (backfilled with `'turn'`).

    2. **`src/mcp-tools/voice-record-turn-cost.ts`:**

        - Extend `RecordTurnCostSchema` per PATTERNS.md Path A (line 644):
            ```typescript
            trigger_type: z.enum(['turn', 'init_trigger', 'transcript_trigger']).default('turn'),
            ```
        - Update the INSERT statement to include `trigger_type` column. Existing `INSERT INTO voice_turn_costs (call_id, turn_id, ..., cost_eur)` becomes `INSERT INTO voice_turn_costs (call_id, turn_id, ..., cost_eur, trigger_type) VALUES (..., ?)`.
        - JSONL audit-trail line includes `trigger_type` field for separability.
        - PRIMARY KEY (call_id, turn_id) stays — but for triggers we use synthetic turn_id values like `'init'` or `'trigger-N'` (numeric prefix) so they never collide with existing numeric turn_ids from Realtime. Document this convention in a comment.
        - Backward compatibility: any caller that does NOT pass `trigger_type` gets the default `'turn'` (Zod `.default(...)`) — Phase-4 cost-recording call sites continue to work unchanged.

    3. **`src/mcp-tools/voice-record-turn-cost.test.ts`:**

        - Add 4 new test cases (mirror existing test structure):
            - Test A: existing happy-path (no trigger_type passed) → row stored with `trigger_type='turn'` (backward-compat).
            - Test B: explicit `trigger_type='init_trigger'` + `turn_id='init'` → row stored, queryable via SELECT WHERE trigger_type='init_trigger'.
            - Test C: explicit `trigger_type='transcript_trigger'` + `turn_id='trigger-1'` → row stored.
            - Test D: invalid `trigger_type='invalid_value'` → BadRequestError (Zod enum rejection).

    4. **Cost-ledger aggregation sanity check (`src/cost-ledger.ts`):**

        - Read existing `sumCostCurrentDay` and `sumCostCurrentMonth` (or equivalent) functions.
        - The aggregation uses `SELECT SUM(cost_eur) FROM voice_turn_costs WHERE ...` — adding a new column does NOT break this; all rows still sum.
        - Verify NO existing query uses `SELECT *` and depends on column count. If found, fix accordingly.
        - No additional code changes expected here; this is a verification step. If a query is fragile, fix it.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && npx vitest run src/mcp-tools/voice-record-turn-cost.test.ts src/db-migration.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/db-migration.ts` contains literal `ALTER TABLE voice_turn_costs ADD COLUMN trigger_type`.
    - `src/db-migration.ts` migration is idempotent: contains `PRAGMA table_info` check OR equivalent guard.
    - `src/mcp-tools/voice-record-turn-cost.ts` contains literal `z.enum(['turn', 'init_trigger', 'transcript_trigger'])`.
    - `src/mcp-tools/voice-record-turn-cost.ts` contains literal `trigger_type` in INSERT statement.
    - `src/mcp-tools/voice-record-turn-cost.ts` Zod schema uses `.default('turn')` (backward-compat).
    - `src/mcp-tools/voice-record-turn-cost.test.ts` contains literal `'init_trigger'` (Test B).
    - `src/mcp-tools/voice-record-turn-cost.test.ts` contains literal `'transcript_trigger'` (Test C).
    - `npx vitest run src/mcp-tools/voice-record-turn-cost.test.ts` exits 0 with at least 4 new tests added.
    - Existing `src/cost-ledger.test.ts` (if present) still passes (aggregation unchanged).
  </acceptance_criteria>
  <done>
    Schema extended with trigger_type column (idempotent migration). Handler accepts the new field with backward-compatible default. Aggregation queries unchanged and verified. Test coverage includes both old and new paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire per-trigger cost recording into voice-triggers handlers</name>
  <files>src/mcp-tools/voice-triggers-init.ts, src/mcp-tools/voice-triggers-transcript.ts</files>
  <read_first>
    - src/mcp-tools/voice-triggers-init.ts (Plan 01 output)
    - src/mcp-tools/voice-triggers-transcript.ts (Plan 01 output)
    - src/mcp-tools/voice-record-turn-cost.ts (Task 1 output — schema with trigger_type)
    - src/mcp-tools/voice-record-turn-cost.test.ts
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md (D-19)
  </read_first>
  <action>
    For both `voice-triggers-init.ts` and `voice-triggers-transcript.ts`:

    1. Add an optional dep `recordCost?: (entry: { call_id: string; turn_id: string; trigger_type: 'init_trigger' | 'transcript_trigger'; cost_eur: number }) => Promise<void>` to the handler factory's `Deps` interface.

    2. After the agent invocation succeeds (just before returning `{ ok: true, ... }`), call:
        ```typescript
        if (deps.recordCost) {
          await deps.recordCost({
            call_id: parsed.data.call_id,
            turn_id: <initOrTriggerId>,
            trigger_type: 'init_trigger', // or 'transcript_trigger'
            cost_eur: <calculated>, // model-cost from agent invocation
          }).catch(() => { /* non-fatal — JSONL audit still records */ });
        }
        ```

    3. **Cost-eur calculation:** The container-agent owns the model choice (D-20: NanoClaw decides). For v1, cost is computed at the container-agent level and surfaced via the agent's return shape. Extend the agent return:
        - For init: `{ instructions: string; cost_eur?: number }` (optional — if not provided, default 0 and log warning).
        - For transcript: `{ instructions_update: string | null; cost_eur?: number }`.
        - The `Plan 01 default no-op handler` (`AGENT_NOT_WIRED`) returns `cost_eur: 0`.
        - When real container-agent integration lands (separate concern, possibly post-Phase-05.5), it populates `cost_eur` from Claude API usage.

    4. **Wiring in `src/mcp-tools/index.ts`:** Pass `recordCost: makeRecordCostFromVoiceRecordTurnCost(deps)` (a thin wrapper that calls the existing voice_record_turn_cost handler programmatically) into both factories.

    5. **Test updates:** Add 1 new test in each of `voice-triggers-init.test.ts` and `voice-triggers-transcript.test.ts`:
        - Mock `recordCost = vi.fn(async () => {})`.
        - Run handler.
        - Assert `recordCost` was called once with `trigger_type === 'init_trigger'` (or `'transcript_trigger'`) and matching `call_id`.
        - Assert handler success path is unchanged when `recordCost` is omitted (backward-compat).
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && npx vitest run src/mcp-tools/voice-triggers-init.test.ts src/mcp-tools/voice-triggers-transcript.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp-tools/voice-triggers-init.ts` contains literal `recordCost`.
    - `src/mcp-tools/voice-triggers-init.ts` contains literal `'init_trigger'` (the trigger_type value).
    - `src/mcp-tools/voice-triggers-transcript.ts` contains literal `recordCost`.
    - `src/mcp-tools/voice-triggers-transcript.ts` contains literal `'transcript_trigger'`.
    - Both handlers invoke recordCost AFTER the agent invocation (cost = 0 acceptable for v1 default-no-op).
    - Failure of recordCost is non-fatal (caught + swallowed — does NOT affect handler return value).
    - `src/mcp-tools/voice-triggers-init.test.ts` contains a test that asserts `recordCost` was called.
    - `src/mcp-tools/voice-triggers-transcript.test.ts` contains a test that asserts `recordCost` was called.
    - Both vitest files exit 0.
  </acceptance_criteria>
  <done>
    Per-trigger cost recording wired into both new MCP-tools. REQ-COST-06 satisfied: every container-agent invocation produces one cost-ledger row distinguishable by trigger_type. Default-no-op invokeAgent (from Plan 01) records cost_eur=0 placeholder; real cost values flow through once Wave-3+ container-agent integration populates them.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` for all three test files (voice-record-turn-cost, voice-triggers-init, voice-triggers-transcript) exits 0.
- `npm run build` exits 0.
- DB migration is idempotent: running it twice on the same DB does NOT throw.
- Aggregation sanity: `sumCostCurrentDay` returns numeric value > 0 when test inserts mix of `'turn'` + `'init_trigger'` + `'transcript_trigger'` rows.
</verification>

<success_criteria>
- voice_turn_costs table schema includes trigger_type column with default 'turn'.
- Both new MCP-tools record per-trigger cost via the same voice_record_turn_cost pipeline.
- Existing Phase-4 cost-recording calls keep working (backward-compatible schema change).
- REQ-COST-06 fully satisfied with one row per container-agent invocation.
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-slow-brain-removal-container-agent/05.5-05-SUMMARY.md` describing:
- The schema change (one new TEXT column, default 'turn')
- The synthetic turn_id convention for triggers ('init', 'trigger-N')
- The recordCost DI seam in voice-triggers handlers
- Confirmation that aggregation (sumCostCurrentDay/Month) still includes all rows
- Backward-compat note: pre-Phase-05.5 callers do not pass trigger_type and get the default
</output>
