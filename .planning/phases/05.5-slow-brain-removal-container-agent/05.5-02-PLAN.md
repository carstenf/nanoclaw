---
phase: 05.5-slow-brain-removal-container-agent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - container/skills/voice-personas/SKILL.md
  - container/skills/voice-personas/baseline.md
  - container/skills/voice-personas/overlays/case-2-restaurant-outbound.md
  - container/skills/voice-personas/overlays/case-6b-inbound-carsten.md
autonomous: true
requirements:
  - REQ-DIR-18
must_haves:
  truths:
    - "container-agent finds voice-personas skill at container/skills/voice-personas/"
    - "Baseline content is migrated 1:1 from voice-bridge/src/persona/baseline.ts BASELINE_PERSONA_TEMPLATE (no semantic drift)"
    - "Case-2 overlay content is migrated 1:1 from voice-bridge/src/persona/overlays/case-2.ts buildCase2Overlay()"
    - "Case-6b overlay content is migrated 1:1 from voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts buildCase6bOverlay()"
    - "All placeholders ({{goal}}, {{anrede_form}}, {{counterpart_label}}, {{SCHWEIGEN_LADDER}}, etc.) preserved verbatim"
    - "ASCII-umlaut convention preserved (project rule per baseline.ts header lines 12-13)"
  artifacts:
    - path: "container/skills/voice-personas/SKILL.md"
      provides: "Skill contract — when/how container-agent assembles personas"
      contains: "name: voice-personas"
    - path: "container/skills/voice-personas/baseline.md"
      provides: "Baseline persona template (~515 tokens) shared across cases"
      contains: "{{goal}}"
    - path: "container/skills/voice-personas/overlays/case-2-restaurant-outbound.md"
      provides: "Case-2 overlay (restaurant reservation outbound)"
      contains: "{{restaurant_name}}"
    - path: "container/skills/voice-personas/overlays/case-6b-inbound-carsten.md"
      provides: "Case-6b overlay (Carsten inbound)"
      contains: "KALENDER-TERMIN-EINTRAG"
  key_links:
    - from: "container/skills/voice-personas/SKILL.md"
      to: "container/skills/voice-personas/baseline.md"
      via: "skill-loader merges baseline + overlay at runtime"
      pattern: "baseline\\.md"
    - from: "container/skills/voice-personas/SKILL.md"
      to: "overlays/case-{2,6b}-*.md"
      via: "case_type-keyed overlay selection"
      pattern: "overlays/"
---

<objective>
Ship the new `voice-personas` skill inside `container/skills/` so the NanoClaw container-agent owns persona content. Pure content migration — TS-strings to `.md` files. No code changes; no Bridge changes.

Purpose: Closes REQ-DIR-18 (persona ownership in NanoClaw, not Bridge). Required before Wave-3 wiring delivers real `instructions` from `voice_triggers_init`. MOS-4 anchor: "alle Brain-Funktionen bleiben im NanoClaw-Core".

Output: New skill directory with SKILL.md + baseline.md + 2 overlay .md files. Container-agent skill-loader picks them up automatically (existing convention).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md
@container/skills/voice-outbound/SKILL.md
@voice-bridge/src/persona/baseline.ts
@voice-bridge/src/persona/overlays/case-2.ts
@voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts

<interfaces>
Frontmatter convention (verbatim from sibling skill voice-outbound/SKILL.md:1-4):
```yaml
---
name: voice-personas
description: <one-line trigger description per D-13>
---
```

Placeholder set (verbatim from baseline.ts:60-72 placeholder list comment + D-14):
- {{goal}}
- {{anrede_form}}
- {{anrede_capitalized}}
- {{anrede_pronoun}}
- {{anrede_disclosure}}
- {{counterpart_label}}
- {{context}}
- {{call_direction}}
- {{SCHWEIGEN_LADDER}}

Plus case-2-specific placeholders (verbatim from case-2.ts:46-71):
- {{restaurant_name}}, {{requested_date}}, {{requested_date_wort}}, {{requested_time}}, {{requested_time_wort}}, {{party_size_wort}}, {{notes}}, {{time_tolerance_min}}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate baseline.ts to baseline.md + create SKILL.md</name>
  <files>container/skills/voice-personas/SKILL.md, container/skills/voice-personas/baseline.md</files>
  <read_first>
    - voice-bridge/src/persona/baseline.ts (FULL — content source. Read EVERY line of `BASELINE_PERSONA_TEMPLATE`).
    - container/skills/voice-outbound/SKILL.md (frontmatter + body convention reference).
    - container/skills/agent-browser/SKILL.md (frontmatter convention reference).
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md (sections "SKILL.md" lines 562-580 + "baseline.md" lines 583-595)
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md (D-13, D-14, D-18)
  </read_first>
  <action>
    1. Create `container/skills/voice-personas/SKILL.md`:
        - Frontmatter:
            ```
            ---
            name: voice-personas
            description: When voice_triggers_init or voice_triggers_transcript fires, load the matching case-overlay from overlays/{case_type}.md, merge with baseline.md, substitute placeholders, return the rendered string as instructions.
            ---
            ```
        - Body sections (mirror voice-outbound/SKILL.md structure):
            - `# voice-personas — persona assembly for the voice channel`
            - `## When to invoke` — describe the two triggers (voice_triggers_init at `/accept`, voice_triggers_transcript per turn).
            - `## Files` — table listing baseline.md + overlays/*.md and what they hold.
            - `## Assembly steps` — numbered list:
                1. Read `baseline.md`.
                2. Read `overlays/{case_type}.md` (e.g. `case-2-restaurant-outbound.md` for `case_type=case_2`; `case-6b-inbound-carsten.md` for `case_type=case_6b`). If overlay missing, baseline only.
                3. Concatenate baseline + overlay.
                4. Substitute placeholders.
                5. Return rendered string as `instructions`.
            - `## Placeholders` — list every `{{...}}` token (9 baseline + case-2 specifics) with one-line description.
            - `## case_type-to-overlay mapping` table.
            - `## ASCII-umlaut convention` — note: All persona content uses ASCII umlauts (ae/oe/ue/ss) per voice-bridge/src/persona/baseline.ts:12-13.
            - `## Out of scope` — case-3 / case-4 overlays added in later phases.

    2. Create `container/skills/voice-personas/baseline.md`:
        - Open `voice-bridge/src/persona/baseline.ts` and find `BASELINE_PERSONA_TEMPLATE` (TS array literal, joined with `'\n'`).
        - Convert each array element back to its plain-text line. Preserve EXACT line order, EXACT punctuation, EXACT ASCII-umlauts.
        - Result is a `.md` file that, when joined with an overlay and placeholder-substituted, produces the same string `BASELINE_PERSONA_TEMPLATE.join('\n')` produces today.
        - Preserve section headers (`### ROLE & OBJECTIVE`, `### PERSONALITY & TONE`, etc.) verbatim.
        - Keep `{{goal}}`, `{{anrede_form}}`, etc. as literal `{{...}}` tokens.
        - SCHWEIGEN ladder (baseline.ts:36-58): document both inbound and outbound variants. Recommend a per-direction conditional in baseline.md using a directive block like:
            ```
            <!-- BEGIN SCHWEIGEN_LADDER call_direction=inbound -->
            ... inbound ladder text ...
            <!-- END SCHWEIGEN_LADDER -->
            <!-- BEGIN SCHWEIGEN_LADDER call_direction=outbound -->
            ... outbound ladder text ...
            <!-- END SCHWEIGEN_LADDER -->
            ```
            Container-agent picks the matching block during placeholder substitution. Document this convention in SKILL.md `## Placeholders`.

    Do NOT delete `voice-bridge/src/persona/baseline.ts` yet — Plan 06 cleanup handles that after live-test PASS.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && test -f container/skills/voice-personas/SKILL.md && test -f container/skills/voice-personas/baseline.md && grep -q 'name: voice-personas' container/skills/voice-personas/SKILL.md && grep -q '{{goal}}' container/skills/voice-personas/baseline.md && grep -q 'SCHWEIGEN' container/skills/voice-personas/baseline.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `container/skills/voice-personas/SKILL.md` exists.
    - File `container/skills/voice-personas/baseline.md` exists.
    - SKILL.md contains `name: voice-personas` (frontmatter).
    - SKILL.md contains literal `voice_triggers_init` and `voice_triggers_transcript` (trigger names).
    - SKILL.md contains literal `case_2` and `case_6b` (case-type mapping section).
    - baseline.md contains literal `{{goal}}` (placeholder token preserved).
    - baseline.md contains literal `{{counterpart_label}}` (placeholder token preserved).
    - baseline.md contains literal `SCHWEIGEN` (ladder section present, both inbound and outbound).
    - baseline.md contains at least 3 markdown section headers starting with `###` (preserved structure from baseline.ts).
    - baseline.md does NOT contain literal `BASELINE_PERSONA_TEMPLATE` (TS array name not leaked into markdown).
    - baseline.md does NOT contain backtick template literals (no JS string interpolation syntax leaked).
  </acceptance_criteria>
  <done>
    SKILL.md describes how the container-agent assembles personas; baseline.md holds the migrated template content with all placeholders intact and the SCHWEIGEN ladder documented per direction.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate case-2 + case-6b overlays to .md</name>
  <files>container/skills/voice-personas/overlays/case-2-restaurant-outbound.md, container/skills/voice-personas/overlays/case-6b-inbound-carsten.md</files>
  <read_first>
    - voice-bridge/src/persona/overlays/case-2.ts (FULL — content source. Read `buildCase2Overlay()` body AND its 4 sections TASK / DECISION RULES / CLARIFYING-QUESTION ANSWERS / HOLD-MUSIC HANDLING per PATTERNS.md line 612).
    - voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts (FULL — content source. Read `buildCase6bOverlay()` body AND its 6 sections TASK / KALENDER-TERMIN-EINTRAG / KALENDER-TERMIN-LOESCHEN / KALENDER-TERMIN-AENDERN / FAHRZEIT-ANFRAGE / OFFENE FRAGEN per PATTERNS.md line 620).
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md (sections "case-2-restaurant-outbound.md" lines 597-613 + "case-6b-inbound-carsten.md" lines 614-620)
  </read_first>
  <action>
    1. Create `container/skills/voice-personas/overlays/case-2-restaurant-outbound.md`:
        - Open `voice-bridge/src/persona/overlays/case-2.ts` and find `buildCase2Overlay(args)` (typically lines 37-72).
        - Migrate the returned string to `.md`. Replace TS-template-literal interpolations with `{{...}}` placeholders:
            - `${args.restaurant_name}` → `{{restaurant_name}}`
            - `${args.requested_date}` → `{{requested_date}}`
            - `${args.requested_date_wort}` → `{{requested_date_wort}}` (if used)
            - `${args.requested_time}` → `{{requested_time}}`
            - `${args.requested_time_wort}` → `{{requested_time_wort}}`
            - `${args.party_size_wort}` → `{{party_size_wort}}`
            - `${args.notes}` → `{{notes}}`
            - `${args.time_tolerance_min}` → `{{time_tolerance_min}}`
        - Preserve all 4 sections from `case-2.ts:46-71` verbatim: TASK, DECISION RULES, CLARIFYING-QUESTION ANSWERS, HOLD-MUSIC HANDLING.
        - Preserve ASCII umlauts.
        - Keep section markers (e.g. `### TASK`).

    2. Create `container/skills/voice-personas/overlays/case-6b-inbound-carsten.md`:
        - Open `voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts` and find `buildCase6bOverlay()` (typically lines 14-44).
        - This function takes NO args — pure copy-paste of the section bodies into `.md`.
        - Preserve all 6 sections: TASK, KALENDER-TERMIN-EINTRAG, KALENDER-TERMIN-LOESCHEN, KALENDER-TERMIN-AENDERN, FAHRZEIT-ANFRAGE, OFFENE FRAGEN / RECHERCHE / WEB-ZUGRIFF.
        - Preserve ASCII umlauts.

    Do NOT delete the source `.ts` files yet — Plan 06 cleanup.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && test -f container/skills/voice-personas/overlays/case-2-restaurant-outbound.md && test -f container/skills/voice-personas/overlays/case-6b-inbound-carsten.md && grep -q '{{restaurant_name}}' container/skills/voice-personas/overlays/case-2-restaurant-outbound.md && grep -q 'KALENDER-TERMIN-EINTRAG' container/skills/voice-personas/overlays/case-6b-inbound-carsten.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `container/skills/voice-personas/overlays/case-2-restaurant-outbound.md` exists.
    - File `container/skills/voice-personas/overlays/case-6b-inbound-carsten.md` exists.
    - case-2 overlay contains literal `{{restaurant_name}}`.
    - case-2 overlay contains literal `{{requested_date}}` and `{{requested_time}}`.
    - case-2 overlay contains literal `{{party_size_wort}}` (per PATTERNS.md line 606).
    - case-2 overlay contains literal `TASK` and `DECISION RULES` and `CLARIFYING-QUESTION ANSWERS` and `HOLD-MUSIC HANDLING` (4 section markers).
    - case-6b overlay contains literal `KALENDER-TERMIN-EINTRAG`.
    - case-6b overlay contains literal `KALENDER-TERMIN-LOESCHEN`.
    - case-6b overlay contains literal `FAHRZEIT-ANFRAGE`.
    - case-6b overlay contains literal `OFFENE FRAGEN`.
    - Neither overlay contains backtick template literals (`${...}` syntax — no JS leaked).
  </acceptance_criteria>
  <done>
    Both case overlays migrated to `.md` form, placeholders intact for downstream substitution. case-3/case-4 explicitly NOT included (out of scope per CONTEXT.md `<deferred>`).
  </done>
</task>

</tasks>

<verification>
- All four files exist at the expected paths.
- All grep-based acceptance criteria pass (placeholders, section markers, frontmatter).
- Optional smoke check: `npm run build` exits 0 (no TS file changes; should be no-op for the build).
</verification>

<success_criteria>
- voice-personas skill directory exists with SKILL.md + baseline.md + 2 overlay .md files.
- Content is 1:1 migrated from voice-bridge persona TS files (no semantic drift).
- All `{{...}}` placeholders preserved verbatim.
- SKILL.md describes the assembly process so the container-agent can render `instructions` for any of the 2 supported case_types.
- REQ-DIR-18 satisfied: persona content lives in NanoClaw skill, not in Bridge code.
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-slow-brain-removal-container-agent/05.5-02-SUMMARY.md` describing:
- The skill directory structure
- The exact line-mapping from each TS source to its .md target (so reviewers can spot-check)
- The SCHWEIGEN-ladder convention chosen (inline conditional vs partials)
- Confirmation that NO source TS files were deleted (cleanup is Plan 06's job)
</output>
