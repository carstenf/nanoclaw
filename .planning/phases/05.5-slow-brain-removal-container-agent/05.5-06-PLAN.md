---
phase: 05.5-slow-brain-removal-container-agent
plan: 06
type: execute
wave: 4
depends_on:
  - 02
  - 04
  - 05
files_modified:
  - voice-bridge/src/slow-brain.ts
  - voice-bridge/tests/slow-brain.test.ts
  - voice-bridge/src/core-mcp-client.ts
  - voice-bridge/src/core-mcp-client.test.ts
  - voice-bridge/src/persona/baseline.ts
  - voice-bridge/src/persona/overlays/case-2.ts
  - voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts
  - voice-bridge/src/persona/overlays/index.ts
  - voice-bridge/src/config.ts
  - voice-bridge/src/webhook.ts
  - voice-bridge/src/call-router.ts
  - voice-bridge/src/sideband.ts
  - voice-bridge/.env.example
autonomous: false
requirements:
  - REQ-VOICE-13
must_haves:
  truths:
    - "Synthetic 2-turn test PASS with REASONING_MODE=container-agent (init returns valid persona, transcript returns instructions_update OR null, no exceptions)"
    - "1 live PSTN inbound test PASS (Carsten calls his own bot, receives Sie-form greeting from container-agent, conducts 1-2 turn dialog, BYE clean)"
    - "1 live PSTN outbound test PASS (Case-2 outbound to Carsten's own mobile or test-restaurant fixture, container-agent renders persona, conversation conducted, BYE clean)"
    - "After all three tests PASS + Carsten explicit approval, slow-brain code is deleted in single atomic commit"
    - "Post-cleanup: REASONING_MODE flag is REMOVED (no flag-check in code anymore — container-agent is the only path)"
    - "Post-cleanup: voice-bridge/src/persona/ directory is GONE (all persona content lives in container/skills/voice-personas/)"
    - "Post-cleanup: voice-bridge tests still pass (zero regressions)"
  artifacts:
    - path: "voice-bridge/src/slow-brain.ts (DELETED)"
      provides: "Confirmation file no longer exists"
    - path: "voice-bridge/src/core-mcp-client.ts (DELETED)"
      provides: "Confirmation file no longer exists"
    - path: "voice-bridge/src/persona/ (DELETED)"
      provides: "Entire directory removed"
  key_links:
    - from: "voice-bridge/src/webhook.ts (post-cleanup)"
      to: "voice-bridge/src/nanoclaw-mcp-client.ts"
      via: "direct call without REASONING_MODE branch"
      pattern: "nanoclawMcp\\.init"
---

<objective>
Wave-4 finalization. Two phases:
1. **Live verification** under REASONING_MODE=container-agent (synthetic + 1 inbound + 1 outbound PSTN call). Gated on Carsten explicit go-ahead.
2. **Hard cleanup commit** (D-22): atomic deletion of slow-brain.ts + core-mcp-client.ts + persona/ + REASONING_MODE flag + all CORE_MCP_*/SLOW_BRAIN_* config entries. Single commit, atomic revert possible.

Purpose: Closes REQ-VOICE-13 (Phase 6) live-verify, completes the architectural refactor by removing dead code paths. CONOPS MOS-4 strengthened — Bridge has zero reasoning code or persona content after this plan.

**autonomous: false** — multiple checkpoints requiring Carsten interaction (explicit go-ahead before live-tests, approval after PASS, approval before cleanup commit).

Output: A cleaner voice-bridge with NO dead code, NO REASONING_MODE flag, NO Slow-Brain. Container-agent is the only reasoning layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-PATTERNS.md
@voice-bridge/src/slow-brain.ts
@voice-bridge/src/core-mcp-client.ts
@voice-bridge/src/persona/baseline.ts
@voice-bridge/src/persona/overlays/case-2.ts
@voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts
@voice-bridge/src/config.ts
@voice-bridge/src/webhook.ts
@voice-bridge/src/call-router.ts

<interfaces>
Cleanup target list (CONTEXT.md "Files-to-modify summary" + PATTERNS.md line 296):

DELETE entirely:
- voice-bridge/src/slow-brain.ts
- voice-bridge/tests/slow-brain.test.ts
- voice-bridge/src/core-mcp-client.ts
- voice-bridge/src/core-mcp-client.test.ts
- voice-bridge/src/persona/baseline.ts
- voice-bridge/src/persona/overlays/case-2.ts
- voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts
- voice-bridge/src/persona/overlays/index.ts
- voice-bridge/src/persona/ (the directory itself, after files removed)

REMOVE FROM voice-bridge/src/config.ts:
- CORE_MCP_URL
- CORE_MCP_TOKEN
- CORE_MCP_TIMEOUT_MS
- SLOW_BRAIN_CADENCE_CAP
- SLOW_BRAIN_TIMEOUT_MS
- SLOW_BRAIN_QUEUE_MAX
- REASONING_MODE (entire flag — no longer needed once container-agent is the only path)
- PHASE2_PERSONA, PHASE1_PERSONA constants if they exist (deprecated by FALLBACK_PERSONA)

REMOVE FROM voice-bridge/src/webhook.ts:
- All `if (REASONING_MODE === 'container-agent')` branches → keep ONLY the container-agent body, delete the else-branches (legacy slow-brain code).
- Imports of removed persona files (baseline.ts, overlays/*).

REMOVE FROM voice-bridge/src/call-router.ts:
- All `if (REASONING_MODE === 'container-agent')` branches → keep ONLY the container-agent body.
- `makeNoopSlowBrain()` helper (not needed once slow-brain.ts is gone).
- Import of `startSlowBrain` from `./slow-brain.js`.
- `slowBrain` field on CallContext.
- `slowBrain.stop()` cleanup.
- `startSlowBrain` factory in `CallRouterFactories`.

REMOVE FROM voice-bridge/.env.example:
- CORE_MCP_URL, CORE_MCP_TOKEN, CORE_MCP_TIMEOUT_MS
- SLOW_BRAIN_*
- REASONING_MODE
ADD: NANOCLAW_VOICE_MCP_URL, NANOCLAW_VOICE_MCP_TOKEN, NANOCLAW_VOICE_MCP_TIMEOUT_MS
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Carsten explicit go-ahead — start live tests</name>
  <what-built>
    Wave 1-3 complete:
    - Plan 01: Two new MCP-tools registered on Port 3201 + FIFO queue + gc hook.
    - Plan 02: voice-personas skill (SKILL.md + baseline + 2 overlays).
    - Plan 03: NanoclawMcpClient + REASONING_MODE flag + FALLBACK_PERSONA in config.
    - Plan 04: Bridge wiring at /accept (Splices A + B) + onTranscriptTurn branch + 5 branch tests pass.
    - Plan 05: Cost-ledger trigger_type column + per-trigger recording wired.
    - All vitest suites exit 0; no regressions; REASONING_MODE=slow-brain default keeps Phase-5 live.
  </what-built>
  <how-to-verify>
    1. Confirm with Carsten explicitly: "Phase-5 ist im aktuellen Stand stabil und ich darf REASONING_MODE=container-agent fuer Live-Tests aktivieren?"
    2. Carsten checks his own state: any in-flight critical PSTN traffic? If yes, defer. If no, proceed.
    3. Plan 01-05 executor commits are pushed to main (`git log --oneline | head -10` shows them).
    4. Carsten gives verbal go: "Starte Live-Test 05.5".
  </how-to-verify>
  <resume-signal>Carsten responds with "Starte Live-Test 05.5" or equivalent explicit approval. If Carsten responds "warte" or "nein" — pause and report.</resume-signal>
  <files>(none — checkpoint awaits human signal)</files>
  <action>Wait for Carsten's explicit verbal go-ahead before proceeding to Task 2. Do NOT auto-proceed.</action>
  <verify><automated>echo "checkpoint:human-action — manual gate, no automated verify"</automated></verify>
  <done>Carsten responded with explicit go-ahead string.</done>
</task>

<task type="auto">
  <name>Task 2: Synthetic 2-turn test (REASONING_MODE=container-agent, no PSTN)</name>
  <files>(read-only verification — no source files modified)</files>
  <read_first>
    - voice-bridge/tests/webhook-accept-branch.test.ts (Plan 04 output — already verifies happy + fallback paths in unit tests)
    - voice-bridge/scripts/synthetic-call-replay.* (if a synthetic harness exists; otherwise build minimal one)
  </read_first>
  <action>
    Run a synthetic 2-turn call through the live nanoclaw-voice MCP-server (Port 3201) with REASONING_MODE=container-agent set ONLY for this run. Steps:

    1. Boot the nanoclaw-voice MCP-server normally (no override) — `voice_triggers_init` and `voice_triggers_transcript` tools registered with their default no-op handlers (or whatever real container-agent integration exists by Wave-4; if no real integration, no-op `'AGENT_NOT_WIRED'` is acceptable for this synthetic test as the goal is wire-correctness, not persona quality).

    2. Boot voice-bridge with `REASONING_MODE=container-agent NANOCLAW_VOICE_MCP_URL=http://127.0.0.1:3201/ NANOCLAW_VOICE_MCP_TOKEN=<token> npm run start`.

    3. Drive a synthetic /accept request (curl or scripted) simulating Case-2 outbound: assert log shows:
        - `event: 'voice_triggers_init'` (handler invoked on NanoClaw side).
        - Bridge receives instructions string (either real persona or `'AGENT_NOT_WIRED'`).
        - No `container_agent_init_failed` warning.

    4. Drive 2 synthetic transcript events (simulating turns): assert log shows:
        - `event: 'voice_triggers_transcript'` x 2 on NanoClaw side, FIFO ordered.
        - Bridge processes responses (instructions_update OR null acceptable).
        - No `transcript_trigger_failed` warning.

    5. Trigger end-of-call (synthetic finalize): assert `voice_finalize_call_cost` invokes `voiceTriggerQueue.gc(call_id)` and queue map empties.

    6. Tail logs to a file `synthetic-test-05.5-{timestamp}.log` and post to Discord summary.

    If ANY step fails: STOP, do NOT proceed to Task 3. Report root-cause to Carsten.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && grep -q 'voice_triggers_init' synthetic-test-05.5-*.log && grep -q 'voice_triggers_transcript' synthetic-test-05.5-*.log && ! grep -q 'container_agent_init_failed' synthetic-test-05.5-*.log && ! grep -q 'transcript_trigger_failed' synthetic-test-05.5-*.log && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - At least one log line matches `voice_triggers_init` (init-trigger fired).
    - At least 2 log lines match `voice_triggers_transcript` (FIFO turns).
    - Zero `container_agent_init_failed` events.
    - Zero `transcript_trigger_failed` events.
    - At least one log line matches `voiceTriggerQueue.gc` or `event: 'voice_trigger_queue_gc'` (queue cleanup on call-end).
    - Synthetic-test log file committed under `.planning/phases/05.5-slow-brain-removal-container-agent/test-evidence/` for traceability.
  </acceptance_criteria>
  <done>
    Synthetic test demonstrates wire-correctness end-to-end. PASS or DEFER (DEFER = "no real container-agent integration yet, retry after that lands"). On PASS or DEFER-with-acceptable-rationale, proceed to live PSTN gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live PSTN inbound test (Carsten calls his own bot)</name>
  <what-built>
    Synthetic test PASS. NanoClaw + voice-bridge running with REASONING_MODE=container-agent on Lenovo1.
  </what-built>
  <how-to-verify>
    1. Carsten dials his Sipgate inbound CLI (the number that routes to the FreeSWITCH inbound dialplan).
    2. Bot picks up; greeting MUST be in Sie-form German (per case-6b overlay or FALLBACK_PERSONA — both acceptable).
    3. Carsten conducts 1-2 turn dialog (e.g. "Wie spaet ist es?" or "Hast du heute einen Termin im Kalender?").
    4. Carsten says BYE; bot hangs up cleanly.
    5. Carsten reports back:
        - Did the greeting feel natural? (PASS / FAIL)
        - Did the 1-2 turns produce sensible responses? (PASS / FAIL)
        - Any audio glitches, long silences (>3s), or 500-errors? (yes/no)
    6. Check Discord for call summary; check `~/nanoclaw-state/voice-channel-spec/traces/` for trace artifacts.
    7. PASS criteria:
        - At least basic greeting in Sie-form German.
        - 1-2 turns produced ANY non-error response (perfect persona quality NOT required for v1; minimum bar = no exception, no 500, no 5+ second silence).
        - BYE clean, post-call summary posted.
  </how-to-verify>
  <resume-signal>Carsten reports "PSTN inbound PASS — bot reagiert sinnvoll" or describes specific failures. On FAIL: report and stop.</resume-signal>
  <files>(none — live PSTN test, no source files modified)</files>
  <action>Wait for Carsten to dial inbound and report PASS/FAIL with details. Capture the trace under .planning/phases/05.5-slow-brain-removal-container-agent/test-evidence/inbound-{timestamp}.log.</action>
  <verify><automated>echo "checkpoint:human-verify — Carsten subjective verdict required"</automated></verify>
  <done>Carsten reports PASS for inbound greeting + 1-2 turn dialog + clean BYE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live PSTN outbound test (Case-2 to Carsten's own mobile or fixture)</name>
  <what-built>
    Inbound PSTN test PASS. Now testing outbound.
  </what-built>
  <how-to-verify>
    1. Carsten triggers Case-2 outbound via WhatsApp/Discord ("Rufe meinen Anschluss XX an und buche ein Meeting...") OR uses a controlled fixture restaurant.
    2. Voice-MCP-tool `voice_start_case_2_call` registers; container-agent spawns; outbound call originates.
    3. Carsten (or fixture) picks up; bot greets in Sie-form German per case-2 overlay (restaurant assistant tone).
    4. Carsten conducts 2-3 turn dialog matching the Case-2 flow (begruessung -> reservation request -> bestaetigung/ablehnung).
    5. Carsten reports back:
        - Did the bot identify itself as NanoClaw / Carsten's assistant correctly? (PASS / FAIL)
        - Did Case-2 specifics appear (date/time/party_size)? (PASS / FAIL)
        - Audio quality, latency? (subjective)
        - BYE clean, calendar entry created or politely declined?
    6. PASS criteria:
        - Outbound dial succeeds.
        - Greeting reflects Case-2 overlay (or, if container-agent not yet wired, FALLBACK_PERSONA — acceptable for wire-test only).
        - At least 2 turns produced ANY non-error response.
        - End-of-call summary in Discord/calendar.
  </how-to-verify>
  <resume-signal>Carsten reports "PSTN outbound PASS" or specific failures. On FAIL: report and stop.</resume-signal>
  <files>(none — live PSTN test, no source files modified)</files>
  <action>Wait for Carsten to trigger outbound Case-2 and report PASS/FAIL. Capture trace under .planning/phases/05.5-slow-brain-removal-container-agent/test-evidence/outbound-{timestamp}.log.</action>
  <verify><automated>echo "checkpoint:human-verify — Carsten subjective verdict required"</automated></verify>
  <done>Carsten reports PASS for outbound dial + Case-2 persona + 2-3 turn dialog + clean BYE.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: Carsten approval — proceed to hard cleanup commit?</name>
  <decision>
    All three tests PASS (synthetic + inbound + outbound). The atomic cleanup commit removes ~7 files + REASONING_MODE flag. Once committed, atomic revert is possible by reverting the single commit, but slow-brain code is gone from main.
  </decision>
  <context>
    Per D-22: "Hard cleanup commit after 1-2 live-calls PASS." We now have 1 inbound + 1 outbound PASS. Cleanup removes:
    - slow-brain.ts (~200 LOC)
    - core-mcp-client.ts (~190 LOC)
    - persona/baseline.ts (~200 LOC)
    - persona/overlays/case-2.ts (~80 LOC)
    - persona/overlays/case-6b-inbound-carsten.ts (~70 LOC)
    - persona/overlays/index.ts
    - 2 test files (slow-brain.test.ts, core-mcp-client.test.ts)
    - REASONING_MODE flag + 6 ENV constants from config.ts
    - All `if (REASONING_MODE === 'container-agent')` branches in webhook.ts and call-router.ts (else-branches deleted, container-agent body retained as the only path)
  </context>
  <options>
    <option id="cleanup-now">
      <name>Cleanup now (atomic single commit)</name>
      <pros>D-22 honored; Bridge becomes clean; MOS-4 fully realized; tech debt eliminated.</pros>
      <cons>If a Phase-6 regression surfaces in next 24h, revert is one commit but state-repo coordination needed.</cons>
    </option>
    <option id="cleanup-defer">
      <name>Defer cleanup (keep both paths for one more week)</name>
      <pros>Extra observation window in production with both paths still selectable.</pros>
      <cons>Tech debt persists; D-22 violated; harder to remove later as new code may reference legacy paths.</cons>
    </option>
    <option id="cleanup-partial">
      <name>Partial cleanup (delete persona/ but keep slow-brain.ts and flag for one week)</name>
      <pros>Most observable risk reduction; persona migration is fully validated.</pros>
      <cons>Still has the flag; non-atomic; complicates the eventual second cleanup commit.</cons>
    </option>
  </options>
  <resume-signal>Carsten responds: cleanup-now / cleanup-defer / cleanup-partial. If `cleanup-defer` or `cleanup-partial`, executor stops here and creates a follow-up task in `~/nanoclaw-state/open_points.md`.</resume-signal>
  <files>(none — checkpoint awaits decision)</files>
  <action>Wait for Carsten's decision (cleanup-now / cleanup-defer / cleanup-partial). On cleanup-now: proceed to Task 6. On defer/partial: create follow-up task in nanoclaw-state and stop.</action>
  <verify><automated>echo "checkpoint:decision — Carsten input required"</automated></verify>
  <done>Carsten selected one of the three options.</done>
</task>

<task type="auto">
  <name>Task 6: Hard cleanup commit (atomic, only runs if Task 5 = cleanup-now)</name>
  <files>see frontmatter files_modified — ALL of them</files>
  <read_first>
    - voice-bridge/src/slow-brain.ts (full — to know what's referenced for grep cleanup)
    - voice-bridge/src/core-mcp-client.ts (full)
    - voice-bridge/src/persona/baseline.ts + overlays/* (full)
    - voice-bridge/src/config.ts (find every CORE_MCP_*/SLOW_BRAIN_*/REASONING_MODE/PHASE1_PERSONA/PHASE2_PERSONA reference)
    - voice-bridge/src/webhook.ts (every REASONING_MODE branch — to be flattened)
    - voice-bridge/src/call-router.ts (every REASONING_MODE branch + makeNoopSlowBrain helper)
    - voice-bridge/.env.example
    - .planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md (D-22)
  </read_first>
  <action>
    **STAGE 0 — pre-flight grep audit:** Before deleting files, grep for ALL references:
        ```bash
        cd voice-bridge && grep -rn "CORE_MCP_\|SLOW_BRAIN_\|REASONING_MODE\|persona/baseline\|persona/overlays\|slow-brain.js\|core-mcp-client.js\|PHASE1_PERSONA\|PHASE2_PERSONA" src tests
        ```
    Produce list of every reference. Confirm each can be safely deleted (no production-only path depends on it).

    **STAGE 1 — flatten branches in webhook.ts and call-router.ts:**

    1. `voice-bridge/src/webhook.ts`:
        - For every `if (REASONING_MODE === 'container-agent') { A } else { B }`: keep `A` (the `nanoclawMcp.init` body), delete `B`. The config-error guard (`if (!nanoclawMcp)` falling back to FALLBACK_PERSONA) STAYS — that's the REQ-DIR-18 fallback path.
        - Delete imports of `baseline`, `overlays/*`, `PHASE2_PERSONA`, `PHASE1_PERSONA`.
        - The /accept handler now ALWAYS calls `nanoclawMcp.init`. If `nanoclawMcp` undefined, log error + FALLBACK_PERSONA.

    2. `voice-bridge/src/call-router.ts`:
        - For every REASONING_MODE branch: keep container-agent body, delete legacy.
        - Delete `slowBrain` field from CallContext.
        - Delete `slowBrain.stop()` cleanup.
        - Delete `startSlowBrain` import + factory entry.
        - Delete local `makeNoopSlowBrain()` helper.

    3. `voice-bridge/src/sideband.ts`:
        - Remove any references to slow-brain (search "slow-brain" / "slowBrain"). Keep `onAssistantTurn` callback (Plan 04 added).

    **STAGE 2 — delete files (use `git rm`):**

    ```bash
    cd voice-bridge
    git rm src/slow-brain.ts tests/slow-brain.test.ts
    git rm src/core-mcp-client.ts src/core-mcp-client.test.ts
    git rm -r src/persona/   # baseline.ts + overlays/*.ts + overlays/index.ts + the directory
    ```

    **STAGE 3 — clean config.ts:**

    Delete from `voice-bridge/src/config.ts`:
        - `CORE_MCP_URL`, `CORE_MCP_TOKEN`, `CORE_MCP_TIMEOUT_MS` exports.
        - `SLOW_BRAIN_CADENCE_CAP`, `SLOW_BRAIN_TIMEOUT_MS`, `SLOW_BRAIN_QUEUE_MAX` exports.
        - `REASONING_MODE` export (no longer needed — container-agent is the only path).
        - `PHASE1_PERSONA`, `PHASE2_PERSONA` constants (deprecated by FALLBACK_PERSONA).

    **STAGE 4 — clean .env.example:**

    Remove `CORE_MCP_URL`, `CORE_MCP_TOKEN`, `CORE_MCP_TIMEOUT_MS`, `SLOW_BRAIN_*`, `REASONING_MODE` lines. Add (if missing):
        ```
        NANOCLAW_VOICE_MCP_URL=http://127.0.0.1:3201/
        NANOCLAW_VOICE_MCP_TOKEN=<token-from-vault>
        NANOCLAW_VOICE_MCP_TIMEOUT_MS=5000
        ```

    **STAGE 5 — final regression sweep:**

    1. `cd voice-bridge && npm run build` → must exit 0.
    2. `cd voice-bridge && npx vitest run` → must exit 0 (zero regressions; deleted-file tests are gone).
    3. `cd /home/carsten_bot/nanoclaw && npx vitest run` → must exit 0.
    4. Restart NanoClaw + voice-bridge services on Lenovo1; tail logs for 60s; assert no startup errors.

    **STAGE 6 — single atomic commit:**

    ```bash
    cd /home/carsten_bot/nanoclaw && git add -A voice-bridge/ && git commit -m "$(cat <<'EOF'
    feat(05.5): hard cleanup — remove slow-brain + persona files + REASONING_MODE flag (D-22)

    After Phase 05.5 live-test PASS (synthetic + inbound PSTN + outbound PSTN per Plan 06 Tasks 2-4):
    - Delete voice-bridge/src/slow-brain.ts + tests/slow-brain.test.ts
    - Delete voice-bridge/src/core-mcp-client.ts + tests/core-mcp-client.test.ts
    - Delete voice-bridge/src/persona/ (baseline.ts + overlays/*) — content lives in container/skills/voice-personas/
    - Remove CORE_MCP_*, SLOW_BRAIN_*, REASONING_MODE flag, PHASE{1,2}_PERSONA from config.ts
    - Flatten REASONING_MODE branches in webhook.ts + call-router.ts (container-agent path retained, legacy deleted)
    - Update .env.example to NANOCLAW_VOICE_MCP_*

    Bridge now zero-reasoning per CONOPS MOS-4. All tests pass.
    EOF
    )"
    ```

    **STAGE 7 — push:**

    ```bash
    git push origin main
    ```
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge && test ! -f src/slow-brain.ts && test ! -f src/core-mcp-client.ts && test ! -d src/persona && ! grep -rn 'REASONING_MODE\|CORE_MCP_\|SLOW_BRAIN_' src tests && npm run build && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `voice-bridge/src/slow-brain.ts` does NOT exist.
    - File `voice-bridge/src/core-mcp-client.ts` does NOT exist.
    - Directory `voice-bridge/src/persona/` does NOT exist.
    - File `voice-bridge/tests/slow-brain.test.ts` does NOT exist.
    - File `voice-bridge/tests/core-mcp-client.test.ts` does NOT exist.
    - `voice-bridge/src/config.ts` does NOT contain `REASONING_MODE`.
    - `voice-bridge/src/config.ts` does NOT contain `CORE_MCP_URL`.
    - `voice-bridge/src/config.ts` does NOT contain `SLOW_BRAIN_`.
    - `voice-bridge/src/webhook.ts` does NOT contain `REASONING_MODE`.
    - `voice-bridge/src/call-router.ts` does NOT contain `REASONING_MODE`.
    - `voice-bridge/src/call-router.ts` does NOT contain `slowBrain` (no leftover references).
    - `voice-bridge/.env.example` contains `NANOCLAW_VOICE_MCP_URL`.
    - `voice-bridge/.env.example` does NOT contain `CORE_MCP_URL` or `SLOW_BRAIN_` or `REASONING_MODE`.
    - `npm run build` exits 0 in voice-bridge.
    - `npx vitest run` exits 0 in voice-bridge (zero regressions).
    - Single atomic commit on main with subject starting `feat(05.5): hard cleanup`.
  </acceptance_criteria>
  <done>
    Bridge is zero-reasoning per MOS-4. ~700 LOC + 8 files removed. REASONING_MODE flag gone. Hard cleanup commit pushed to main.
  </done>
</task>

</tasks>

<verification>
- Bridge boots cleanly with no `REASONING_MODE` ENV set.
- /accept always calls nanoclawMcp.init (no flag check).
- onTranscriptTurn always calls nanoclawMcp.transcript (no flag check).
- All voice-bridge tests pass.
- All NanoClaw tests pass.
- 1 inbound + 1 outbound live PSTN call PASS criteria met (subjective Carsten approval, NOT just unit tests).
- `git log -1` shows the single atomic cleanup commit on main.
</verification>

<success_criteria>
- Live tests PASS (synthetic + 1 inbound + 1 outbound).
- D-22 honored: hard cleanup atomic commit.
- Voice-bridge zero-reasoning per MOS-4 (no slow-brain, no persona content, no flag).
- Phase-5 retire: legacy code paths gone from main (only available via git revert).
- REQ-VOICE-13 (Phase 6) live-verified.
- All 13 phase REQ-IDs satisfied across plans 01-06.
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-slow-brain-removal-container-agent/05.5-06-SUMMARY.md` describing:
- Live-test outcomes (synthetic / inbound / outbound — 3 distinct verdicts)
- The single atomic cleanup commit SHA
- LOC deleted (file count + line count)
- Confirmation that Bridge has zero reasoning code post-cleanup
- Update to STATE.md noting Phase 05.5 complete and Phase-5 architecture officially retired
</output>
