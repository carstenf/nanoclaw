---
phase: 05.3-refactor-cleanup-timer-removal
plan: 04
subsystem: research
tags: [openai-realtime, turn_detection, idle_timeout_ms, voice-bridge, research]

# Dependency graph
requires:
  - phase: 05.2-persona-redesign-and-call-flow-state-machine
    provides: "SESSION_CONFIG.audio.input.turn_detection.create_response=false (D-8); baseline.ts OUTBOUND_SCHWEIGEN/INBOUND_SCHWEIGEN nudge ladders (D-2); session.update atomicity (Q7 probe)"
provides:
  - idle-timeout-finding.md — authoritative D-4 research deliverable
  - SESSION_CONFIG recommended value (idle_timeout_ms=8000, clamped 5000..30000 per API)
  - silence-monitor.ts disposition verdict (SHRINK-TO-HARD-SAFETY-STUB, with full-delete path contingent on gate.ts audit)
  - sideband.ts event-handler wiring shape (input_audio_buffer.timeout_triggered)
  - §201 StGB zero-audio-leak invariant re-verified under native-idle-timeout path
affects: [05.3-05a-timer-removal-config, 05.3-05b-silence-monitor-retirement, 05.3-06-final-verification, phase-06-case-3-medical, phase-07-case-4-inbound-negotiation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Research deliverable before code plan — mirrors Plan 05.2-05 Q7 atomicity-probe pattern for risky refactors"
    - "Docs-verbatim evidence citation — schema URL + exact quote per question"
    - "HIGH/MED/LOW confidence rating per research question — empirical probe only triggered on MED/LOW"

key-files:
  created:
    - ".planning/phases/05.3-refactor-cleanup-timer-removal/idle-timeout-finding.md"
  modified: []

key-decisions:
  - "idle_timeout_ms fires server-side model response + input_audio_buffer.timeout_triggered event (NOT just state-change) — Q1 HIGH"
  - "Firing response receives full session instructions + conversation history — OUTBOUND_SCHWEIGEN/INBOUND_SCHWEIGEN persona ladders steer nudge text with zero bridge-side state (Q2 HIGH)"
  - "idle_timeout_ms is session-scoped only; per-turn changes via session.update (atomic per 05.2-05 Q7) if needed (Q3 HIGH)"
  - "idle_timeout_ms and create_response:false are SEPARATE trigger paths — both coexist; idle_timeout fires regardless of create_response setting (Q4 HIGH)"
  - "§201 StGB zero-audio-leak invariant preserved: idle_timeout chains off response.done → inactive before first bot turn → cannot circumvent AMD gate"
  - "Recommended SESSION_CONFIG value: idle_timeout_ms=8000 (env-override, clamped to API bounds 5000..30000)"
  - "silence-monitor.ts verdict: SHRINK-TO-HARD-SAFETY-STUB (~30 LOC residual catatonic-call guard); full-delete only if Plan 05.3-05b audit confirms cost/gate.ts already covers catatonic case"
  - "sideband.ts adds single handler for input_audio_buffer.timeout_triggered (observability log, ~10 LOC) — drop speculative response.created-metadata handler from PATTERNS.md §7 (not documented in spec)"
  - "Empirical probe skipped — docs (developers.openai.com server-events reference) answered all 4 questions verbatim at HIGH confidence"

patterns-established:
  - "Pattern: research deliverable-before-refactor — when refactor hinges on unverified 3rd-party API behavior, block the refactor on a research plan that produces a single authoritative finding file; downstream plans read finding as their first read_first item"
  - "Pattern: docs-verbatim-or-empirical-probe gate — if any question scores LOW/MED on docs-only confidence, STOP and run an empirical probe (analog: 05.2-05 session-update-atomicity-probe) before locking the finding"
  - "Pattern: §201-safety explicit re-verification in research — any research that touches audio-trigger semantics (idle_timeout, response.create, VAD) must explicitly re-verify zero-audio-leak invariant under the new path"

requirements-completed: [CODE-HEALTH-D4-RESEARCH]

# Metrics
duration: ~25min
completed: 2026-04-22
---

# Phase 05.3 Plan 04: idle_timeout_ms research finding Summary

**Docs-only HIGH-confidence D-4 research deliverable — idle_timeout_ms fires server-side response with full persona scope, coexists safely with create_response:false, and preserves the §201 StGB AMD gate invariant. Recommended SESSION_CONFIG value 8000ms; silence-monitor.ts to SHRINK-TO-HARD-SAFETY-STUB pending Plan 05.3-05b cost/gate.ts audit.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-22T21:25:00Z (approx, worktree base reset)
- **Completed:** 2026-04-22T21:51:00Z
- **Tasks:** 1 executed (Task 1 — docs scan + finding write) + 1 checkpoint auto-approved (auto_advance=true)
- **Files modified:** 1 created (idle-timeout-finding.md, 368 lines)

## Accomplishments

- 368-line authoritative D-4 finding file written at `.planning/phases/05.3-refactor-cleanup-timer-removal/idle-timeout-finding.md`
- All 4 D-4 questions answered with verbatim OpenAI docs quotes + schema path citations
- All 4 answers HIGH-confidence from docs-only research — empirical probe path available if later reviewers disagree but NOT required
- `idle_timeout_ms=8000` recommendation locked for Plan 05.3-05a config.ts write
- `silence-monitor.ts` verdict SHRINK-TO-HARD-SAFETY-STUB locked for Plan 05.3-05b (with FULLY-DELETE as conditional upgrade path contingent on cost/gate.ts audit)
- `sideband.ts` new-event-handler wiring shape documented: single `input_audio_buffer.timeout_triggered` handler (~10 LOC) — not the speculative `response.created`-metadata shape from PATTERNS.md §7 (no spec support for that shape)
- §201 StGB zero-audio-leak invariant explicitly re-verified under the native-idle-timeout path (idle_timeout chains off `response.done`, dormant before first bot turn, AMD gate preserved)

## Task Commits

1. **Task 1: Docs scan + finding write** — `1b27bf3` (research)
   - Fetched `developers.openai.com/api/reference/resources/realtime/server-events` (3.9MB HTML)
   - Fetched `developers.openai.com/api/docs/guides/realtime-vad` (245KB HTML) — no idle_timeout content (outside guide scope)
   - Fetched `developers.openai.com/cookbook/examples/realtime_prompting_guide` (2.4MB) — found Safety-&-Escalation no-input guidance
   - Extracted verbatim schema descriptions for `realtime_audio_input_turn_detection.idle_timeout_ms` + `input_audio_buffer.timeout_triggered`
   - Confidence check: all 4 Q's HIGH → empirical probe skipped
   - Wrote 368-line finding file

2. **Checkpoint (task 2): Carsten review human-verify** — `⚡ Auto-approved` per workflow.auto_advance=true
   - Auto-approval rationale: research deliverable with HIGH-confidence docs-verbatim evidence on all 4 questions, §201 invariant explicitly re-verified, recommended values within API bounds (5000..30000), silence-monitor disposition aligned with Carsten's feedback_no_timer_based_silence architectural steer (UX in persona, NOT server-side timers)
   - This SUMMARY file serves as the audit trail

**Plan metadata commit:** pending (this SUMMARY file)

## Files Created/Modified

- `.planning/phases/05.3-refactor-cleanup-timer-removal/idle-timeout-finding.md` (CREATED) — 368 lines, authoritative D-4 research deliverable; read_first for Plans 05.3-05a and 05.3-05b
- `.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-04-SUMMARY.md` (CREATED, this file)

## Decisions Made

Documented in frontmatter `key-decisions`. Key highlights:

| # | Decision | Rationale |
|---|---------|-----------|
| 1 | Q1 answer: YES idle_timeout fires server-side response | Docs verbatim: "a model response will be triggered automatically" + "a model response will be generated" |
| 2 | Q2 answer: YES full history + instructions in scope | Docs verbatim: empty audio committed to conversation as input_audio item, then model response generated |
| 3 | Q3 answer: session-level only | Schema location: `realtime_audio_input_turn_detection.idle_timeout_ms` (variant 0, server_vad); no response.create counterpart |
| 4 | Q4 answer: idle_timeout and create_response:false are independent paths | Docs verbatim: create_response governs "automatically generate a response when a VAD stop event occurs" ONLY; idle_timeout description has no create_response dependency clause |
| 5 | §201 StGB preserved | idle_timeout chains off response.done (post-bot-audio-played) → inactive before first bot turn → AMD gate cannot be circumvented |
| 6 | SESSION_CONFIG value = 8000ms | Research §4.5 recommended 8s for "post-bot-turn silence window round 1"; baseline.ts OUTBOUND_SCHWEIGEN uses "etwa 6 Sekunden" → 8s gives 2s German-etiquette slack; within API bounds 5000..30000 |
| 7 | silence-monitor.ts verdict: SHRINK-TO-HARD-SAFETY-STUB | UX layer (ladder, round counter, bot-state machine) fully replaced by native idle_timeout + persona steering; BUT residual ~30-LOC stub needed for catatonic-bot case (model fails to respond to timeout_triggered); FULL-DELETE if Plan 05.3-05b audit confirms cost/gate.ts covers catatonic case |
| 8 | sideband.ts new handler: ONLY `input_audio_buffer.timeout_triggered` | Spec documents this event; does NOT document `response.created` metadata.reason marker (dropping PATTERNS.md §7 speculation) |
| 9 | Empirical probe skipped | All 4 Q's HIGH-confidence on docs alone; probe pattern (~50 LOC) documented in finding §"Open caveats #6" as fallback if later reviewers disagree |

## Deviations from Plan

None — plan executed exactly as written.

Task 1 STEP 4 ("Empirical-probe decision") was reached and evaluated: docs were sufficient → probe not needed → proceeded to STEP 5 (Finalize finding file) per plan spec. Checkpoint reached and auto-approved per workflow.auto_advance=true (standard behavior documented in checkpoint_protocol).

**One minor file-location incident (not a deviation, caught-and-fixed-in-scope):**
- Initial Write of idle-timeout-finding.md landed at `/home/carsten_bot/nanoclaw/.planning/phases/.../` (main repo) instead of `/home/carsten_bot/nanoclaw/.claude/worktrees/agent-a635b824/.planning/phases/.../` (this worktree). Detected by `git add` failure ("pathspec did not match"). Fixed by `mv` to correct worktree path before commit. Root cause: Write tool with absolute path resolved to main-repo hierarchy even though cwd was the worktree. Final commit landed correctly in worktree. No duplicate files left behind.

## Issues Encountered

1. **OpenAI platform.openai.com anti-bot block (HTTP fetch):** `curl` of `platform.openai.com/docs/guides/realtime-vad` returned 9.6KB HTML with `<meta http-equiv="refresh">` loader (Cloudflare challenge). Worked around by using `developers.openai.com` (the stainless docs mirror) which serves raw schema HTML and was accessible directly via `curl -A "Mozilla/5.0"`. The developers.openai.com schema is the authoritative source anyway — the platform.openai.com guide is derived from it.

2. **Context7 MCP docs thin on Realtime API:** `ctx7 docs /openai/openai-node` and `/openai/openai-realtime-api-beta` contained SDK examples but not the `idle_timeout_ms` or `turn_detection.create_response` schema descriptions. Fell back to direct HTTP fetch of the stainless docs mirror. Finding file cites developers.openai.com as primary source accordingly.

3. **Bash cwd reset between calls (environment note, not an execution issue):** Every Bash call resets cwd; absolute paths or `git -C <path>` required. First `git add` failed because of the file-location issue above, not cwd reset. Subsequent operations used absolute `git -C <worktree-path>` pattern.

## User Setup Required

None — pure research deliverable, no runtime or infra changes.

## Next Phase Readiness

**Plan 05.3-05a (Wave 4) is unblocked** and has a fully-specified contract to read:
- Apply `SESSION_CONFIG.audio.input.turn_detection.idle_timeout_ms = 8000` with env-override clamped 5000..30000
- Add sideband.ts `input_audio_buffer.timeout_triggered` log handler (~10 LOC)
- Delete `GREET_TRIGGER_DELAY_OUTBOUND_MS` + setTimeout at webhook.ts:493-495 → replace with synchronous `requestResponse` (Pattern B)
- Delete `GREET_TRIGGER_DELAY_MS` + setTimeout at webhook.ts:737-753 → replace with synchronous `requestResponse` (NOT idle_timeout — API min is 5000ms, 1000ms self-greet delay is a different semantic, use Pattern B)

**Plan 05.3-05b (Wave 4) is unblocked** and has a branching contract:
- Audit `voice-bridge/src/cost/gate.ts` for catatonic-call coverage
  - If covered → FULLY-DELETE silence-monitor.ts + remove wiring from call-router.ts
  - If NOT covered → SHRINK silence-monitor.ts to ~30-LOC hard-safety stub (armHardSilenceHangup pattern documented in finding)
- Drop bridge-side nudge-ladder, round counter, bot-state machine in either branch
- Remove silence forwards from call-router.ts:132-149 (keep AMD-classifier forwards)

**Open low-risk items for Plan 05.3-05 live verification:**
- Live spot-check that model escalates Nudge-1 → Nudge-2 → Nudge-3+end_call across 3 consecutive idle_timeouts (baseline.ts ladder counting discipline — gpt-realtime full, not mini, should cope)
- Live spot-check that idle_timeout does NOT fire during AMD classifier phase (should be inactive before any bot response per Q4 crucial-safety-check analysis)

**Blockers:** None. Phase 05.3 Wave 4 (Plans 05.3-05a + 05.3-05b) can proceed in parallel.

## Self-Check

Verifying claims before finalizing:

**File existence:**
- `.planning/phases/05.3-refactor-cleanup-timer-removal/idle-timeout-finding.md` — FOUND (368 lines)

**Commit existence:**
- `1b27bf3` research(05.3-04): D-4 finding — FOUND (git log verified)

**Structural checks (automated verify from PLAN):**
- `wc -l` ≥ 80 — PASS (368 lines)
- `## Q1:` through `## Q4:` — PASS (all 4)
- `## Recommended SESSION_CONFIG value` — PASS
- `## silence-monitor.ts disposition verdict` — PASS
- `## Impact on sideband.ts event-handler wiring` — PASS

## Self-Check: PASSED

---
*Phase: 05.3-refactor-cleanup-timer-removal*
*Plan: 04 (Wave 3, research pre-plan for D-4)*
*Completed: 2026-04-22*
