---
phase: 05.3
plan: 02
type: execute
wave: 6
depends_on: ["05.3-01", "05.3-03", "05.3-05b"]
files_modified:
  - voice-bridge/src/sideband.ts
  - voice-bridge/src/webhook.ts
  - voice-bridge/src/call-router.ts
  - voice-bridge/src/tools/dispatch.ts
  - voice-bridge/src/outbound-router.ts
  - voice-bridge/src/config.ts
  - voice-bridge/src/persona.ts
  - voice-bridge/src/amd-classifier.ts
  - voice-bridge/src/silence-monitor.ts
  - voice-bridge/src/pre-greet.ts
  - voice-bridge/src/outbound-webhook.ts
  - voice-bridge/src/freeswitch-esl-client.ts
  - voice-bridge/src/index.ts
  - voice-bridge/src/sipgate-rest-client.ts
  - voice-bridge/src/slow-brain.ts
  - voice-bridge/src/tools/filler-inject.ts
  - voice-bridge/src/tools/tool-output-emitter.ts
autonomous: true
requirements:
  - CODE-HEALTH-D5
must_haves:
  truths:
    - "Plan-XX inline comment count across voice-bridge/src/*.ts drops from 79 baseline to ≤30"
    - "Each modified source file has ONE header comment block at top stating current intent + major decision refs"
    - "Invariant-anchor Plan-XX comments retained ONLY where documenting a subtle load-bearing invariant (§201 zero-audio-leak, event-driver trigger, wait-for-speech D-8, amd_result guard, idempotency key contract)"
    - "Zero test failures after the sweep (comment-only edits do not change runtime behavior)"
  artifacts:
    - path: "voice-bridge/src/sideband.ts"
      provides: "sideband WS client with header-normalized comments"
      max_inline_plan_refs: 4
    - path: "voice-bridge/src/webhook.ts"
      provides: "/accept handler with header-normalized comments"
      max_inline_plan_refs: 4
    - path: "voice-bridge/src/call-router.ts"
      provides: "CallRouter with header-normalized comments"
      max_inline_plan_refs: 2
  key_links:
    - from: "voice-bridge/src/persona/baseline.ts (style anchor)"
      to: "all voice-bridge/src/*.ts (D-5 sweep target)"
      via: "one-header + invariant-anchor comment style"
      pattern: "file top block matches PATTERNS.md Pattern D shape"
---

<objective>
Comment-archaeology consolidation (D-5). Sweep all 18 `voice-bridge/src/*.ts` files
to reduce 79 inline `// Plan XX-YY:` breadcrumbs to ≤30 invariant-anchor refs.
Replace per-file comment noise with a single header block stating current intent
+ major decision refs, mirroring the clean reference files created in Phase 05.2
(`persona/baseline.ts`, `persona/overlays/case-6b-inbound-carsten.ts`).

Purpose: Phase 2-5.2 accretion left breadcrumbs everywhere (Carsten 2026-04-22:
"habe das gefühl das der code zugemüllt ist"). Commit history preserves the
archaeology; source code should show current intent. This is text-only cleanup —
no runtime behavior changes.

Wave placement rationale: Deferred from CONTEXT D-8 preview Wave 1 to post-mutation Wave 5 — comment archaeology on webhook/call-router/silence-monitor is cleaner after Plans 01/03/05 have already mutated those files (avoids double-touch merge risk on the same lines).

Output:
- 17 source files with normalized headers
- Plan-XX ref count: 79 → ≤30 (grep-verifiable)
- Summary file `.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-02-SUMMARY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-CONTEXT.md
@.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-PATTERNS.md

<interfaces>
<!-- Style reference: PATTERNS.md §10 "Comment archaeology — D-5 cross-file sweep pattern" -->
<!-- Clean anchor files (created 05.2, DO NOT TOUCH — read for style only): -->
<!--   voice-bridge/src/persona/baseline.ts:1-14 -->
<!--   voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts:1-10 -->
<!--   voice-bridge/src/persona/overlays/case-2.ts:1-15 -->

Scan baseline (2026-04-22): 79 occurrences across 18 source files.

Per-file target budgets:

| File                                           | Current | Target ≤ |
|------------------------------------------------|---------|----------|
| voice-bridge/src/sideband.ts                   | 17      | 4        |
| voice-bridge/src/webhook.ts                    | 16      | 4        |
| voice-bridge/src/call-router.ts                | 7       | 2        |
| voice-bridge/src/tools/dispatch.ts             | 6       | 1        |
| voice-bridge/src/outbound-router.ts            | 6       | 2        |
| voice-bridge/src/config.ts                     | 5       | 1 (header only) |
| voice-bridge/src/persona.ts                    | 4       | 1 (header only; file will shrink more in 05.3-03) |
| voice-bridge/src/silence-monitor.ts            | 3       | 1 (pending retirement by 05.3-05b) |
| voice-bridge/src/outbound-webhook.ts           | 3       | 1        |
| voice-bridge/src/amd-classifier.ts             | 2       | 1        |
| voice-bridge/src/index.ts                      | 2       | 1        |
| voice-bridge/src/pre-greet.ts                  | 2       | 1        |
| voice-bridge/src/tools/filler-inject.ts        | 1       | 1        |
| voice-bridge/src/tools/tool-output-emitter.ts  | 1       | 1        |
| voice-bridge/src/freeswitch-esl-client.ts      | 1       | 1        |
| voice-bridge/src/slow-brain.ts                 | 1       | 1        |
| voice-bridge/src/sipgate-rest-client.ts        | 1       | 1        |

Target sum: ≤30.

**Retention criterion (verbatim from CONTEXT D-5):**
Inline `// Plan XX-YY:` markers retained ONLY when they document a load-bearing invariant:
  a) §201 StGB zero-audio-leak guard (e.g. "no audio_transcript.delta before amd_result")
  b) Wait-for-speech D-8 gate (sideband.ts armedForFirstSpeech trigger contract)
  c) Event-driver contract invariants (e.g. output_audio_buffer.stopped → silence re-arm)
  d) AMD-classifier VAD-fallback guard (2026-04-22 onTranscript settle verdict)
  e) Idempotency key contract (sha256(call_id+turn_id+tool_name+arg_hash))
  f) Atomic session.update contract (Plan 05.2-05 Q7 probe finding)

All OTHER inline `// Plan XX-YY:` markers are removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep sideband.ts + webhook.ts + call-router.ts (top-3 offenders, 40 of 79 total refs)</name>
  <files>voice-bridge/src/sideband.ts, voice-bridge/src/webhook.ts, voice-bridge/src/call-router.ts</files>
  <read_first>
    - voice-bridge/src/sideband.ts (full file — identify invariant-anchors; sideband.ts:346-360 Plan 05.2-03 D-8 wait-for-speech marker MUST be retained verbatim)
    - voice-bridge/src/webhook.ts (full file — DO NOT touch the setTimeout-adjacent "Plan 03-15" comment blocks at ~line 493 and ~line 745; those blocks are rewritten by Plan 05.3-05a D-3 and touching them here creates merge risk)
    - voice-bridge/src/call-router.ts (full file — AMD-classifier VAD-fallback markers are invariants; silence-monitor wiring markers evaluate per retention criteria)
    - voice-bridge/src/persona/baseline.ts lines 1-14 (verbatim style anchor — copy this shape)
    - voice-bridge/src/persona/overlays/case-6b-inbound-carsten.ts lines 1-10 (secondary style reference)
    - .planning/phases/05.3-refactor-cleanup-timer-removal/05.3-PATTERNS.md §10 + Pattern D (retention criteria + verbatim examples)
    - .planning/phases/05.3-refactor-cleanup-timer-removal/05.3-CONTEXT.md D-5 (scope) + D-6 (NO behavior change)
  </read_first>
  <action>
    STEP 1. For each of the 3 files, add (or replace) the top-of-file header block following the
    verbatim PATTERNS.md Pattern D shape (from persona/baseline.ts:1-14):
    ```typescript
    // voice-bridge/src/<filename>.ts
    // Phase 05.X — <one-sentence current purpose>.
    // Owning plans: <comma-list of load-bearing plan refs>.
    //
    // Load-bearing invariants:
    //   - <invariant 1 with plan ref>
    //   - <invariant 2 with plan ref>
    //
    // ASCII-umlaut convention enforced project-wide (see persona/baseline.ts header).
    ```

    STEP 2. Walk file body top-to-bottom. For each `// Plan XX-YY:` inline comment:
      - KEEP if it documents an invariant per criteria a-f in <interfaces>. Tighten wording if verbose — invariant anchors should be ≤3 lines and start with the Plan-XX ref.
      - DELETE if it's implementation history (e.g. "Plan 02-10: added retry path", "Plan 03-15 fix 22:18 PSTN"). Also delete any blank comment lines that become orphaned.

    STEP 3. File-specific retention guidance:
      - **sideband.ts (17 → ≤4):** PRESERVE Plan 05.2-03 D-8 wait-for-speech marker at the `input_audio_buffer.speech_stopped` handler (PATTERNS.md §7 textbook invariant-anchor example). PRESERVE any `§201` or `amd_result`-adjacent marker. PRESERVE idempotency-contract marker if present. Delete everything else.
      - **webhook.ts (16 → ≤4):** PRESERVE markers documenting (a) AMD-handoff synthetic user directive contract, (b) Carsten-whitelist caller-number → persona selection, (c) §201 no-audio-before-amd_result guard if present, (d) atomic session.update contract (Plan 05.2-05 Q7). DO NOT rewrite the setTimeout-adjacent comments at ~line 493 and ~line 745 — those are Plan 05.3-05a D-3 scope. Leave them untouched.
      - **call-router.ts (7 → ≤2):** PRESERVE marker documenting the AMD-classifier VAD-fallback onSpeechStart/Stop/Transcript wiring (2026-04-22 invariant). Silence-monitor wiring markers: delete if pure history, keep if documenting event-driven state-machine contract.

    STEP 4. After edits to the 3 files:
      ```bash
      cd voice-bridge
      grep -cE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/sideband.ts
      grep -cE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/webhook.ts
      grep -cE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/call-router.ts
      npx tsc --noEmit
      npm test -- --run --reporter=dot
      ```

    STEP 5. Atomic commit:
      `git add voice-bridge/src/sideband.ts voice-bridge/src/webhook.ts voice-bridge/src/call-router.ts`
      `git commit -m "refactor(05.3-02): comment archaeology sweep part 1/2 — sideband/webhook/call-router (D-5)"`
  </action>
  <verify>
    <automated>cd voice-bridge &amp;&amp; [ $(grep -cE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/sideband.ts) -le 4 ] &amp;&amp; [ $(grep -cE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/webhook.ts) -le 4 ] &amp;&amp; [ $(grep -cE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/call-router.ts) -le 2 ] &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - sideband.ts inline Plan-XX ref count ≤4 (was 17)
    - webhook.ts inline Plan-XX ref count ≤4 (was 16)
    - call-router.ts inline Plan-XX ref count ≤2 (was 7)
    - Every retained ref documents one of retention criteria a-f (human eyeball check)
    - Header block on all 3 files matches PATTERNS.md Pattern D shape
    - `npx tsc --noEmit` exit 0
    - `npm test -- --run` all green, zero assertion-value changes
    - Webhook.ts setTimeout-adjacent Plan 03-15 comment blocks are UNTOUCHED (Plan 05.3-05a scope)
  </acceptance_criteria>
  <done>Commit landed; per-file grep counts verified; tsc + tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep remaining 14 files + final total verification (79 → ≤30)</name>
  <files>voice-bridge/src/tools/dispatch.ts, voice-bridge/src/outbound-router.ts, voice-bridge/src/config.ts, voice-bridge/src/persona.ts, voice-bridge/src/amd-classifier.ts, voice-bridge/src/silence-monitor.ts, voice-bridge/src/pre-greet.ts, voice-bridge/src/outbound-webhook.ts, voice-bridge/src/index.ts, voice-bridge/src/freeswitch-esl-client.ts, voice-bridge/src/slow-brain.ts, voice-bridge/src/sipgate-rest-client.ts, voice-bridge/src/tools/filler-inject.ts, voice-bridge/src/tools/tool-output-emitter.ts</files>
  <read_first>
    - All 14 files in &lt;files&gt; (must read to identify invariant-anchor markers before deletion)
    - voice-bridge/src/persona/baseline.ts lines 1-14 (style anchor)
    - .planning/phases/05.3-refactor-cleanup-timer-removal/05.3-PATTERNS.md §10 + Pattern D
    - .planning/phases/05.3-refactor-cleanup-timer-removal/05.3-CONTEXT.md D-5 + D-6
    - The 3 files completed by Task 1 (visual reference for header style after Task 1 ships)
  </read_first>
  <action>
    STEP 1. For each of the 14 files, apply the same treatment as Task 1:
      (a) Add/normalize top-of-file header block (PATTERNS.md Pattern D shape).
      (b) Walk body; retain invariant-anchor Plan-XX markers per criteria a-f; delete implementation-history breadcrumbs.

    STEP 2. File-specific retention notes:
      - **amd-classifier.ts (2 → ≤1):** The 2026-04-22 VAD-fallback onTranscript settle-verdict marker IS a load-bearing invariant — retain. Other marker: delete if pure history.
      - **silence-monitor.ts (3 → ≤1):** File MAY be deleted entirely by Plan 05.3-05b. For THIS plan, reduce to 0-1 refs AND add a header note "Pending retirement by Plan 05.3-05b D-3; replacement is OpenAI Realtime `turn_detection.idle_timeout_ms` — see idle-timeout-finding.md."
      - **config.ts (5 → ≤1):** All 5 are implementation history. SESSION_CONFIG shape is the canonical reference — one file header only. Target: 0 inline refs.
      - **persona.ts (4 → ≤1):** File shrinks more in Plan 05.3-03 (CASE6B_PERSONA removal). For THIS plan: header only, 0-1 inline refs.
      - **outbound-router.ts (6 → ≤2):** PRESERVE any marker documenting idempotency-key contract OR DI-opts pattern (both invariants per STATE.md decisions). Delete pure history.
      - **tools/dispatch.ts (6 → ≤1):** PRESERVE the A12 invokeIdempotent wrapper marker (load-bearing for non-double-booking). Delete pure history.
      - **pre-greet.ts (2 → ≤1):** PRESERVE any marker documenting pre-greet injection timing contract (AMD handoff invariant). Delete history.
      - **outbound-webhook.ts (3 → ≤1):** PRESERVE any marker on /outbound trigger contract if present.
      - **index.ts (2 → ≤1):** Header block only.
      - Single-ref files (slow-brain.ts, sipgate-rest-client.ts, freeswitch-esl-client.ts, tools/filler-inject.ts, tools/tool-output-emitter.ts): each currently has 1 ref. KEEP the ref IF it's an invariant-anchor (most are); otherwise replace with header-block-only.

    STEP 3. After all 14 files complete, run GLOBAL verification:
      ```bash
      cd voice-bridge
      grep -rcE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/ | awk -F: '{s+=$2} END {print "total:",s}'
      # Goal: total ≤ 30 (ROADMAP success criterion #6)
      npx tsc --noEmit
      npm test -- --run
      ```

    STEP 4. Atomic commit:
      `git add voice-bridge/src/`
      `git commit -m "refactor(05.3-02): comment archaeology sweep part 2/2 — remaining 14 files (D-5); total Plan-XX refs 79 → N"`
      Replace N with the actual measured total count.

    STEP 5. Record per-file before/after counts in the summary file.
  </action>
  <verify>
    <automated>cd voice-bridge &amp;&amp; [ $(grep -rcE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/ | awk -F: '{s+=$2} END {print s+0}') -le 30 ] &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Global `voice-bridge/src/**` inline Plan-XX ref count ≤30 (was 79)
    - Every retained ref documents one of retention criteria a-f
    - Each modified file has header block matching PATTERNS.md Pattern D shape
    - silence-monitor.ts header includes retirement note pointing to Plan 05.3-05
    - `npx tsc --noEmit` exit 0
    - `npm test -- --run` all green, zero assertion-value changes
    - Two commits landed (Task 1 part 1/2 + Task 2 part 2/2)
    - Webhook.ts setTimeout-adjacent comments at ~line 493 and ~line 745 UNTOUCHED (out of scope for 05.3-02; owned by 05.3-05a)
  </acceptance_criteria>
  <done>Global grep ≤30; tsc + tests green; both commits landed; summary lists per-file deltas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→source | Developer edits 17 .ts comment blocks — trusted internal edits |
| build→runtime | Comment-only edits do not reach runtime; tsc ignores comments |

## STRIDE Threat Register

Comment-only sweep — zero runtime attack surface change.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0503-02-01 | Tampering | Accidental code edit while editing comments | mitigate | Run `npm test -- --run` after each file; any red test aborts plan. `git diff` reviewed per-file before commit to confirm ONLY comment lines changed. |
| T-0503-02-02 | Information Disclosure | Deleting an invariant-anchor comment that was load-bearing documentation | mitigate | Retention criteria a-f are explicit; retained refs listed in commit message; reviewer (carsten_bot + human spot-check) can quickly audit via `git show` |
| T-0503-02-03 | Regression (§201 invariant) | Accidentally deleting the §201 zero-audio-leak comment on audio-path code | mitigate | `grep -rn "§201\\|amd_result" voice-bridge/src/` pre-edit AND post-edit: counts must match (or post > pre if new invariant refs added during header rewrite) |
| T-0503-02-04 | Denial of Service | tsc breaks because header block contains malformed syntax (e.g. unclosed /* */ ) | accept | tsc runs in both Task 1 and Task 2 verify blocks; failure aborts before commit |
</threat_model>

<verification>
Post-plan:
1. `cd voice-bridge && grep -rcE '^\s*// Plan [0-9]+(\.[0-9]+)?-[0-9]+' src/ | awk -F: '{s+=$2} END {print s}'` → ≤30
2. `cd voice-bridge && npm test -- --run` green
3. `cd voice-bridge && npx tsc --noEmit` exit 0
4. `git log --oneline voice-bridge/src/ | head -5` shows 2 new 05.3-02 commits
5. `cd voice-bridge && grep -rn "§201\|amd_result" src/ | wc -l` — count of load-bearing §201/amd_result refs preserved or increased (never decreased)
</verification>

<success_criteria>
- D-5 closed: inline Plan-XX refs 79 → ≤30 (ROADMAP success criterion #6)
- Each file has header block per PATTERNS.md Pattern D
- Zero assertion-value test changes (D-6 preserved)
- Webhook.ts setTimeout blocks remain untouched (Plan 05.3-05a scope boundary respected)
- Total commits for this plan: 2 (one per task)
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-02-SUMMARY.md`
documenting:
- Per-file before/after Plan-XX ref counts (17-row table)
- Grand total before/after (79 → measured N)
- List of retained invariant-anchor refs (file:line + retention category a-f)
- tsc + npm test exit status
- 2 commit hashes
</output>
