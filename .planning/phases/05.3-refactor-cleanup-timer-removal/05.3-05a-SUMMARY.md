---
phase: 05.3-refactor-cleanup-timer-removal
plan: 05a
subsystem: voice-bridge
tags: [refactor, timer-removal, openai-realtime, turn_detection, idle_timeout_ms, setTimeout-removal]

# Dependency graph
requires:
  - phase: 05.3-refactor-cleanup-timer-removal
    plan: 04
    provides: "idle-timeout-finding.md — D-4 research deliverable (idle_timeout_ms=8000 recommended, Q4 independent-paths verdict, §201 safety re-verified)"
  - phase: 05.2-persona-redesign-and-call-flow-state-machine
    provides: "SESSION_CONFIG.audio.input.turn_detection.create_response=false (D-8); baseline.ts OUTBOUND_SCHWEIGEN/INBOUND_SCHWEIGEN persona ladders"
provides:
  - "SESSION_CONFIG.audio.input.turn_detection.idle_timeout_ms = IDLE_TIMEOUT_MS (default 8000, env-override clamped to API bounds 5000..30000)"
  - "IDLE_TIMEOUT_MS exported const in config.ts (available to Plan 05.3-05b silence-monitor retirement for ratio calculations)"
  - "webhook.ts: zero UX setTimeouts in /accept request-handler code paths (both inbound self-greet and outbound AMD-handoff)"
  - "sideband.ts: input_audio_buffer.timeout_triggered handler (idle_timeout_triggered log event for metric parity with legacy silence_round_*)"
  - "D-6 preservation evidence: accept.test.ts lines 285-311 assertion values byte-identical pre/post refactor"
affects: [05.3-05b-silence-monitor-retirement, 05.3-06-final-verification, phase-06-case-3-medical, phase-07-case-4-inbound-negotiation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pattern B (atomic session.update + sync trigger) — replaces setTimeout UX compensation with synchronous requestResponse post-updateInstructions (PATTERNS.md §3)"
    - "Pattern C (event-driven over timer-driven) — native turn_detection.idle_timeout_ms takes over silence-nudge triggering; persona OUTBOUND_SCHWEIGEN / INBOUND_SCHWEIGEN ladders supply the text via session instructions"
    - "Pattern: env-clamp on OpenAI-bounded config values — Math.max(min, Math.min(max, Number(env))) guards against /accept 400 from typo'd env overrides"
    - "Pattern: research-deliverable before code refactor — Plan 05.3-04 D-4 finding answered all 4 D-4 questions HIGH-confidence before code write (mirrors Plan 05.2-05 Q7 atomicity-probe)"

key-files:
  created: []
  modified:
    - "voice-bridge/src/config.ts (SESSION_CONFIG.turn_detection extended, IDLE_TIMEOUT_MS added, 2 greet-trigger constants deleted — net +7 LOC, -14 LOC)"
    - "voice-bridge/src/webhook.ts (2 setTimeouts removed, 2 breadcrumb comments prose-updated, 2 imports removed — net 61 lines rewritten, -34 deletions, +27 insertions)"
    - "voice-bridge/src/sideband.ts (input_audio_buffer.timeout_triggered handler added, armedForFirstSpeech doc-comment updated — net +31 LOC, -2 LOC)"
    - "voice-bridge/tests/config.test.ts (new describe block with 4 tests on idle_timeout + D-8 invariants — net +42 LOC)"

key-decisions:
  - "idle_timeout_ms = 8000 ms — from idle-timeout-finding.md Recommended SESSION_CONFIG value (German-etiquette 2s slack over baseline.ts OUTBOUND_SCHWEIGEN 'etwa 6 Sekunden' copy)"
  - "Env override IDLE_TIMEOUT_MS clamped to OpenAI API bounds (5000..30000) — prevents /accept 400 from typo'd env values"
  - "Inbound self-greet: synchronous requestResponse (NOT idle_timeout) — API min 5000ms structurally cannot emulate 1000ms initial-greet semantics; the 1000ms was audio-path-settle compensation that Sipgate single-leg inbound does not require"
  - "Outbound AMD-handoff: synchronous requestResponse after atomic session.update + conversation.item.create — wait-for-speech (sideband.armedForFirstSpeech, Plan 05.2-03 D-8) is the audio-path-ready signal; 2500ms setTimeout was stale compensation pre-D-8"
  - "sideband.ts new handler fires on input_audio_buffer.timeout_triggered (canonical server event per spec) — NOT on the speculative response.created-metadata shape from PATTERNS.md §7 (no spec support, dropped per finding §Handler 2: NONE)"
  - "create_response:false (Plan 05.2-03 D-8) preserved byte-identical in SESSION_CONFIG — finding Q4 confirms idle_timeout and create_response:false are INDEPENDENT trigger paths"
  - "§201 StGB zero-audio-leak invariant preserved — idle_timeout chains off response.done (post-first-bot-audio-played), thus dormant pre-AMD-verdict in Case-2 flow"

# Metrics
duration: ~30min
completed: 2026-04-22
---

# Phase 05.3 Plan 05a Summary: Replace UX setTimeouts with native idle_timeout_ms (D-3 Part 1)

**Replaced 2 bridge-side UX setTimeouts in webhook.ts with OpenAI Realtime's native `turn_detection.idle_timeout_ms=8000` (server-side response.create with full persona scope); removed 2 legacy `GREET_TRIGGER_DELAY_*` constants; added sideband.ts observability handler for `input_audio_buffer.timeout_triggered`. All 4 idle-timeout-finding.md Q-answers applied verbatim. Full test suite green (446 passed / 5 skipped / 0 failed); D-6 invariants byte-identical; §201 StGB AMD-gate preserved via response.done chaining.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-04-22 (worktree reset to 47726d1)
- **Completed:** 2026-04-22
- **Tasks:** 2 executed (Tasks 1+2 atomic) + 1 checkpoint (auto-approved per workflow.auto_advance=true)
- **Files modified:** 4 (3 src, 1 test)

## Accomplishments

- **D-3 Part 1 closed:** 2 webhook.ts UX setTimeouts removed (outbound AMD-handoff + inbound self-greet); 2 config.ts constants deleted (GREET_TRIGGER_DELAY_MS, GREET_TRIGGER_DELAY_OUTBOUND_MS)
- **Native idle-timeout active:** `SESSION_CONFIG.audio.input.turn_detection.idle_timeout_ms = IDLE_TIMEOUT_MS` (default 8000, env-override clamped to API bounds 5000..30000)
- **Observability:** sideband.ts now logs `idle_timeout_triggered` events on native `input_audio_buffer.timeout_triggered` — metric parity with legacy silence_round_* infrastructure (preserves log analysis pipelines in Plan 05.3-06 live-verify)
- **D-6 invariants preserved byte-identical** at accept.test.ts:285-311 (Carsten, ask_core, server_vad, create_response:false); zero assertion-value changes
- **§201 StGB zero-audio-leak** explicitly re-verified under native-idle-timeout path (Q4 crucial-safety-check: idle_timeout chains off response.done, dormant before first bot turn, AMD gate cannot be circumvented)
- **Full test suite green:** 42 test files / 446 passed / 5 skipped / 0 failed (vitest)
- **TypeScript typecheck clean:** `tsc --noEmit` exit 0

## Task Commits

1. **Tasks 1+2 (atomic): config.ts SESSION_CONFIG extension + webhook.ts setTimeout removals + sideband.ts handler** — `7bf92dc`
   - config.ts: IDLE_TIMEOUT_MS const added with env clamp; SESSION_CONFIG.turn_detection.idle_timeout_ms=IDLE_TIMEOUT_MS; GREET_TRIGGER_DELAY_MS + GREET_TRIGGER_DELAY_OUTBOUND_MS deleted (breadcrumb comment preserves archaeology without grep-matchable names)
   - webhook.ts: imports of the 2 deleted constants removed; setTimeout at ~line 495 (outbound AMD-handoff, Case-2) replaced with synchronous `if (ctxRef) requestResponse(ctxRef.sideband.state, log)` + breadcrumb comment; setTimeout at ~line 762 (inbound self-greet) replaced with synchronous `requestResponse(ctx.sideband.state, log)` + `log.info({ event: 'greet_response_create_sent', call_id: callId })` + breadcrumb; 2 prose comments updated to remove grep-matchable `setTimeout→requestResponse` / `setTimeout+requestResponse` strings
   - sideband.ts: new handler for `input_audio_buffer.timeout_triggered` in existing event-dispatch block (PATTERNS.md §7 shape, finding §Handler 1 final verbatim); armedForFirstSpeech doc-comment updated to reflect inbound synchronous self-greet
   - tests/config.test.ts: new describe block with 4 tests (IDLE_TIMEOUT_MS=8000, SESSION_CONFIG.turn_detection.idle_timeout_ms=8000, create_response:false preserved, type=server_vad preserved)

2. **Checkpoint (task 3): human-verify** — `⚡ Auto-approved` per workflow.auto_advance=true
   - Auto-approval rationale: refactor applies locked decisions from idle-timeout-finding.md (Plan 05.3-04 HIGH-confidence research deliverable); all 5 acceptance criteria verified green (grep counts 0/0/≥1/≥1/0, tsc 0, vitest 446 passed); §201 invariant preserved per Q4 crucial-safety analysis (idle_timeout chains off response.done); D-6 preserved byte-identical; SUMMARY.md serves as the audit trail.

**Plan metadata commit:** this SUMMARY file (pending).

## Files Created/Modified

- `voice-bridge/src/config.ts` (MODIFIED)
  - Added: `IDLE_TIMEOUT_MS` export const with env clamp (5000..30000, default 8000)
  - Added: `turn_detection.idle_timeout_ms: IDLE_TIMEOUT_MS` inside SESSION_CONFIG, with breadcrumb linking to idle-timeout-finding.md
  - Deleted: `GREET_TRIGGER_DELAY_MS` + `GREET_TRIGGER_DELAY_OUTBOUND_MS` exports (replaced with breadcrumb comment; commit history preserves archaeology)
  - Updated: turn_detection create_response:false prose comment (reference to "GREET_TRIGGER_DELAY_MS" changed to "synchronous requestResponse")
  - Preserved byte-identical: create_response: false (Plan 05.2-03 D-8 invariant); type: 'server_vad'; threshold: 0.55; silence_duration_ms: 700; transcription config; model: 'gpt-realtime'
  - Diff: +30 insertions / -14 deletions

- `voice-bridge/src/webhook.ts` (MODIFIED)
  - Deleted: imports of GREET_TRIGGER_DELAY_MS + GREET_TRIGGER_DELAY_OUTBOUND_MS from './config.js'
  - Replaced setTimeout at line ~495 (outbound AMD-handoff Case-2 onHuman closure): `setTimeout(() => { if (ctxRef) requestResponse(...) }, GREET_TRIGGER_DELAY_OUTBOUND_MS)` → synchronous `if (ctxRef) requestResponse(ctxRef.sideband.state, log)` + Plan 05.3-05a D-3 breadcrumb comment
  - Replaced setTimeout at line ~762 (inbound self-greet pre-greet finally): `setTimeout(() => { requestResponse(...); log.info(...delay_ms: GREET_TRIGGER_DELAY_MS) }, GREET_TRIGGER_DELAY_MS)` → synchronous `requestResponse(ctx.sideband.state, log); log.info({ event: 'greet_response_create_sent', call_id: callId })` + Plan 05.3-05a D-3 breadcrumb comment
  - Updated: Plan 05.1-01 Task 3 prose comment (setTimeout→requestResponse → "subsequent synchronous requestResponse (Plan 05.3-05a D-3 removed the legacy wrapping timer)")
  - Updated: Plan 05.2-03 D-8 prose comment ("proactive setTimeout+requestResponse" → "proactive bridge-side greeting push") — grep-cleanliness
  - Diff: +27 insertions / -34 deletions

- `voice-bridge/src/sideband.ts` (MODIFIED)
  - Added: `input_audio_buffer.timeout_triggered` event handler inside existing event-dispatch block (~line 377). Logs `{ event: 'idle_timeout_triggered', call_id, audio_start_ms, audio_end_ms, item_id }`. No action — model response generated autonomously by OpenAI Realtime server (finding Q1/Q2).
  - Updated: `armedForFirstSpeech` doc-comment — "inbound self-greet uses the existing 1000ms setTimeout" → "inbound self-greet fires requestResponse synchronously at the end of /accept's pre-greet finally handler (Plan 05.3-05a D-3 removed the legacy 1000ms setTimeout)"
  - Diff: +29 insertions / -2 deletions

- `voice-bridge/tests/config.test.ts` (MODIFIED)
  - Added import: `IDLE_TIMEOUT_MS` from '../src/config.js'
  - Added describe block 'SESSION_CONFIG idle_timeout (Plan 05.3-05a D-3)' with 4 tests:
    - `IDLE_TIMEOUT_MS defaults to 8000ms (from idle-timeout-finding.md)`
    - `SESSION_CONFIG turn_detection.idle_timeout_ms matches finding value`
    - `D-8 invariant preserved: create_response:false`
    - `invariant preserved: type=server_vad`
  - Diff: +42 insertions / 0 deletions (append-only extension)

## Decisions Made

Documented in frontmatter `key-decisions`. Key highlights:

| # | Decision | Rationale |
|---|---------|-----------|
| 1 | idle_timeout_ms = 8000ms | Recommended value from finding §"Recommended SESSION_CONFIG value" — between Pipecat typical (10s) and Vapi fast (5s); German etiquette allows 2s slack over baseline.ts OUTBOUND_SCHWEIGEN "etwa 6 Sekunden" prompt copy |
| 2 | Env override clamped 5000..30000 | OpenAI spec: `minimum 5000 maximum 30000`. Clamp prevents /accept 400 on typo'd env values; live tuning still possible within API bounds |
| 3 | Inbound self-greet = synchronous requestResponse (NOT idle_timeout) | Finding §"Minimum 5000ms floor" — API min rules out 1000ms semantics; 1000ms was Sipgate audio-path-settle compensation (single-leg inbound does not require it) |
| 4 | Outbound AMD-handoff = synchronous requestResponse | Finding/PATTERNS.md Pattern B — wait-for-speech (Plan 05.2-03 D-8 armedForFirstSpeech) is the audio-path-ready signal; atomic session.update (Plan 05.2-05 Q7) + conversation.item.create is sufficient; the 2500ms setTimeout was stale pre-D-8 compensation |
| 5 | sideband.ts handler on `input_audio_buffer.timeout_triggered` | Canonical spec event per finding §"Impact on sideband.ts event-handler wiring" Handler 1. Speculative `response.created` metadata shape from PATTERNS.md §7 explicitly dropped (Handler 2: NONE) — spec does not document it |
| 6 | create_response:false preserved byte-identical | Plan 05.2-03 D-8 invariant; finding Q4 confirms it coexists independently with idle_timeout (separate trigger paths in OpenAI spec) |
| 7 | §201 StGB AMD-gate preserved | Q4 crucial-safety analysis: idle_timeout chains off response.done → dormant before first bot turn → AMD classifier phase runs without idle-timeout interference → bot cannot leak audio pre-verdict |

## Deviations from Plan

**None substantive — plan executed as written with 2 minor prose-comment adjustments caught during verification:**

1. **Prose-comment grep-cleanliness adjustments (Rule 1 — preserving acceptance criteria):** The plan's acceptance criterion B requires `grep -nE 'setTimeout.*requestResponse|setTimeout.*GREET_TRIGGER' voice-bridge/src/webhook.ts → 0 matches`. After the first-pass setTimeout removal, 2 prose-only comments remained that matched the regex (line 454 "setTimeout→requestResponse" historical reference; line 613 "proactive setTimeout+requestResponse" historical reference). Both are comment-only (not executable UX paths). Updated the prose to "subsequent synchronous requestResponse (Plan 05.3-05a D-3 removed the legacy wrapping timer)" / "proactive bridge-side greeting push" respectively. Commit history + this SUMMARY preserve the archaeology. No behavior change.

2. **config.ts breadcrumb adjustment (Rule 1):** Plan STEP 2(c) suggested verbatim breadcrumb `"GREET_TRIGGER_DELAY_MS + GREET_TRIGGER_DELAY_OUTBOUND_MS DELETED."` — but that matches the grep acceptance criterion `grep -n 'GREET_TRIGGER_DELAY' src/config.ts → 0 matches`. Adjusted breadcrumb wording to "legacy greet-trigger-delay constants DELETED (commit history preserves the archaeology)" — same meaning, keeps file grep-clean. No behavior change.

Both adjustments are in-scope copy refinements serving the plan's own acceptance criteria; not architectural changes.

## Issues Encountered

1. **Worktree node_modules missing:** Initial `tsc --noEmit` failed with "Cannot find module 'fastify'" — the worktree was checked out without running `npm install`. Ran `npm install --prefer-offline --no-audit --no-fund` (2s, 108 packages). tsc then exit 0. Not a plan-level issue, just worktree-setup mechanics.

2. **Read-before-edit hook false-positive loop (environment note):** Every Edit tool call for config.ts / webhook.ts / sideband.ts / config.test.ts triggered a PreToolUse:Edit hook warning "READ-BEFORE-EDIT REMINDER ... You must Read it first", even though all four files had been read at the start of the session and every Edit explicitly reported "has been updated successfully" in the same turn. Edits applied correctly per diff-verification. Proceeded through the hook warnings since the tool confirmed success. Documented here for awareness, not as a planning deviation.

## User Setup Required

**None** — change is server-side config only. No env var changes required for default operation (IDLE_TIMEOUT_MS defaults to 8000 without any env set). Optional env override: `IDLE_TIMEOUT_MS=<5000..30000>` in `.env` for live PSTN tuning (clamped automatically).

## Next Phase Readiness

**Plan 05.3-05b (Wave 4, the follow-on) is now unblocked:**
- D-3 Part 1 (config + webhook setTimeouts) is complete on a green baseline.
- silence-monitor.ts retirement + call-router.ts simplification can proceed on a clean slate.
- The 05.3-05b audit of `voice-bridge/src/cost/gate.ts` for catatonic-call coverage determines FULL-DELETE vs SHRINK-TO-HARD-SAFETY-STUB per finding verdict.
- D-6 preservation baseline established: accept.test.ts:285-311 assertion values are pinned by this plan's commit; 05.3-05b must preserve them identically.

**Open items deferred to Plan 05.3-06 (Wave 5 final verification):**
- Live PSTN spot-check that model escalates Nudge-1 → Nudge-2 → Nudge-3 + end_call across 3 consecutive idle_timeouts (baseline.ts ladder counting discipline — finding §"Open caveats #1", MEDIUM-confidence risk).
- Live spot-check that idle_timeout does NOT fire during AMD classifier phase (Q4 §"Crucial safety check" explicit prediction).

**Blockers:** None.

## Deferred Issues

None within plan scope. Out-of-scope discoveries during read-first scan (logged for future plans):
- webhook.ts file is 784 lines (CONTEXT.md D-7: file-level split deferred to Phase 6+ if still warranted after 05.3 reductions).
- silence-monitor.ts + its call-router wiring still contain the legacy UX-timer ladder (explicit scope of Plan 05.3-05b).

## Self-Check

Verifying claims before finalizing:

**File existence (after commit):**
- `voice-bridge/src/config.ts` with `IDLE_TIMEOUT_MS` + SESSION_CONFIG.idle_timeout_ms — FOUND (grep confirmed 3 matches for `IDLE_TIMEOUT_MS` or `idle_timeout_ms` post-commit)
- `voice-bridge/src/webhook.ts` with 0 UX setTimeouts — FOUND (grep confirmed 0 matches for `setTimeout.*requestResponse|setTimeout.*GREET_TRIGGER`)
- `voice-bridge/src/sideband.ts` with `input_audio_buffer.timeout_triggered` handler — FOUND (grep confirmed handler at line ~389)
- `voice-bridge/tests/config.test.ts` with new idle_timeout describe block — FOUND (4 new tests in describe 'SESSION_CONFIG idle_timeout (Plan 05.3-05a D-3)')
- `.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-05a-SUMMARY.md` — FOUND (this file)

**Commit existence:**
- `7bf92dc` refactor(05.3-05a): replace UX setTimeouts with native idle_timeout_ms=8000 (D-3) — FOUND (git log -2 verified)

**Acceptance criteria (automated verify from PLAN):**
- `grep -rn 'GREET_TRIGGER_DELAY' voice-bridge/src/` → 0 matches — PASS (exit 1)
- `grep -nE 'setTimeout.*requestResponse|setTimeout.*GREET_TRIGGER' voice-bridge/src/webhook.ts` → 0 matches — PASS (exit 1)
- `grep -n 'idle_timeout_ms' voice-bridge/src/config.ts` → ≥1 match — PASS (2 matches)
- `grep -n 'create_response' voice-bridge/src/config.ts` → ≥1 match + value is `false` — PASS (3 matches, including `create_response: false` at line 295)
- `cd voice-bridge && npx tsc --noEmit` exit 0 — PASS
- accept.test.ts byte-identical on D-6 anchors (Carsten, ask_core, server_vad, create_response:false) — PASS (accept.test.ts not modified in this commit; diff confirmed)
- sideband.test.ts green — PASS (28 tests pass in 4-file verification run)
- sideband-wait-for-speech.test.ts green (unchanged, Plan 05.2-03 D-8 still valid) — PASS
- Atomic commit landed combining Task 1 + Task 2 changes — PASS (single commit 7bf92dc)
- Full suite: 42 files / 446 passed / 5 skipped / 0 failed — PASS

## Self-Check: PASSED

---
*Phase: 05.3-refactor-cleanup-timer-removal*
*Plan: 05a (Wave 4, D-3 Part 1 of 2)*
*Completed: 2026-04-22*
