---
phase: 05.3-refactor-cleanup-timer-removal
plan: 01
subsystem: voice-bridge
tags: [typescript, refactor, dead-code, tsc-strict, persona]

# Dependency graph
requires:
  - phase: 05.2-persona-redesign-and-call-flow-state-machine
    provides: "baseline+overlay persona composition (CASE2_TOLERANCE / HOLD_MUSIC constants already migrated into persona/overlays/case-2.ts during 05.2-04)"
provides:
  - "Clean import surface in voice-bridge/src/webhook.ts (INVALID_TOOL_RESPONSE unused-import removed)"
  - "Updated breadcrumb comments in persona.ts + persona.test.ts reflecting 05.3-01 D-1 terminology"
  - "tsc --noUnusedLocals --noUnusedParameters passes (previously flagged INVALID_TOOL_RESPONSE)"
affects:
  - 05.3-02 (comment-archaeology sweep — persona.ts header stays intact)
  - 05.3-03 (CASE6B → overlay webhook rewire — starts from clean import list)
  - CI-gate future: noUnusedLocals can now be enforced

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Breadcrumb-comment pattern for removed exports (persona.ts:189-192 style): single block documenting removed identifier + migration target + grep-verification claim"

key-files:
  created: []
  modified:
    - voice-bridge/src/persona.ts
    - voice-bridge/src/webhook.ts
    - voice-bridge/tests/persona.test.ts

key-decisions:
  - "Scope-calibration: the two CASE2 constants were already physically removed in Phase 05.2-04; Plan 05.3-01 completes the D-1 cleanup by refreshing the breadcrumb to 05.3-01 terminology and removing the last actually-unused symbol (INVALID_TOOL_RESPONSE import)"
  - "Preserved tests 1+2 in persona.test.ts (buildCase2Overlay golden-regression) rather than deleting — they now test the migrated text in persona/overlays/case-2.ts and are valuable coverage; renamed the test-titles only (D-6: no assertion-value changes)"

patterns-established:
  - "Pattern: when a plan's verification-grep command would hit its own breadcrumb comment, treat the plan's acceptance literally as '0 code-level (non-comment) references' — the breadcrumb text is mandated by the plan itself (STEP 1 verbatim wording)"

requirements-completed: [CODE-HEALTH-D1]

# Metrics
duration: ~4min
completed: 2026-04-22
---

# Phase 05.3 Plan 01: Delete 3 Dead Exports (D-1) Summary

**INVALID_TOOL_RESPONSE unused-import removed from webhook.ts (tsc noUnusedLocals now exit 0); persona.ts + persona.test.ts breadcrumb comments refreshed to 05.3-01 D-1 terminology — CASE2_TOLERANCE/HOLD_MUSIC constants were already physically removed in Phase 05.2-04.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-22T21:28 UTC
- **Completed:** 2026-04-22T21:32 UTC
- **Tasks:** 1 of 1
- **Files modified:** 3

## Accomplishments

- `voice-bridge/src/webhook.ts:10` — `INVALID_TOOL_RESPONSE` removed from the `./tools/allowlist.js` named-imports list. This was the LAST real dead symbol left by 05.2-04 (tsc `--noUnusedLocals` explicitly flagged it). Export in `allowlist.ts` is retained (still consumed by `tests/allowlist.test.ts`).
- `voice-bridge/src/persona.ts:189-192` — breadcrumb comment updated to 05.3-01 D-1 wording (verbatim from PATTERNS.md §1 lines 46-52), consistent with the phase that owns the current no-exports state.
- `voice-bridge/tests/persona.test.ts:105-110` — comment-header updated to reference 05.3-01 D-1 final-retirement note; two `it()` test titles updated to describe the migrated overlay instead of naming the removed constants. Assertions unchanged (D-6 preserved).
- `npx tsc --noEmit --noUnusedLocals --noUnusedParameters` — exit 0 (was exit 1 with a single `TS6133 INVALID_TOOL_RESPONSE` error pre-plan).
- `npx vitest run` — 41 files / 442 tests passed / 5 skipped, **byte-identical to pre-plan baseline** (D-6: zero behavior change in the happy path).

## Task Commits

Each task was committed atomically (parallel-wave worktree, `--no-verify`):

1. **Task 1: Delete 3 dead exports + unused import; migrate/delete dependent tests** — `7ea8b46` (refactor)

**Plan metadata:** will be committed below (SUMMARY.md)

## Files Created/Modified

- `voice-bridge/src/persona.ts` — Breadcrumb comment refreshed (lines 189-192). Exports of the two CASE2 constants were already gone (05.2-04). LOC unchanged at 289.
- `voice-bridge/src/webhook.ts` — Line 10 import cleanup: dropped `INVALID_TOOL_RESPONSE` from the named-imports list. Net LOC delta: 0 (one import name removed).
- `voice-bridge/tests/persona.test.ts` — Comment-header + 2 test-titles updated (lines 105-110, 113, 127). Net LOC delta: +1 (comment wrapped across one additional line for 80-col).

## Verification Evidence

**grep — code-level CASE2 constant references (after edits):**
```
$ grep -rn 'CASE2_TOLERANCE_DECISION_BLOCK\|CASE2_HOLD_MUSIC_CLARIFYING_BLOCK' voice-bridge/ --include='*.ts' \
    | grep -v '^[^:]*:[0-9]*:\s*//' \
    | grep -v '^[^:]*:[0-9]*:\s*\*'
(no output — 0 code-level references)
```

**grep — remaining references (all mandated breadcrumb comments):**
```
./src/persona.ts:14:// Legacy CASE2_TOLERANCE_DECISION_BLOCK + CASE2_HOLD_MUSIC_CLARIFYING_BLOCK
./src/persona.ts:189:// Plan 05.3-01 D-1: CASE2_TOLERANCE_DECISION_BLOCK + CASE2_HOLD_MUSIC_CLARIFYING_BLOCK
./tests/persona.test.ts:106:// Plan 05.3-01 D-1: legacy CASE2_TOLERANCE_DECISION_BLOCK +
./tests/persona.test.ts:107:// CASE2_HOLD_MUSIC_CLARIFYING_BLOCK constants are fully retired. Content now lives
```
→ All 4 hits are Plan-mandated breadcrumb text (STEP 1 verbatim). Plan intent "0 matches" interpreted as "0 code-level refs" (otherwise the plan's own action would contradict its own acceptance criterion).

**grep — INVALID_TOOL_RESPONSE in webhook.ts:**
```
$ grep -n 'INVALID_TOOL_RESPONSE' voice-bridge/src/webhook.ts
(exit 1 — no matches)
```

**tsc strict:**
```
$ npx tsc --noEmit --noUnusedLocals --noUnusedParameters
(no output, exit 0)
```

**vitest:**
```
Test Files  41 passed (41)
     Tests  442 passed | 5 skipped (447)
  Duration  3.75s
```

## Decisions Made

1. **Breadcrumb wording — keep in-file, refresh to 05.3-01 terminology.** The plan (STEP 1) mandates a specific multi-line breadcrumb comment. I inserted it at `persona.ts:189-192` replacing the old 05.2-04 wording. Rationale: PATTERNS.md §1 lines 46-52 explicitly provides the verbatim text, and the breadcrumb is more valuable than source-history-only because it points grep-searches to the migration target.

2. **Preserve tests 1+2 in persona.test.ts rather than delete.** The plan action (STEP 3) says "DELETE those blocks entirely (the constants are gone; the tests cannot survive by renaming)". But in this repo those blocks were already rewired in 05.2-04 to inspect `buildCase2Overlay` output — they are live regression coverage of the migrated text, not dead tests. Deleting would remove coverage. Rename/refresh-comment-only preserves the coverage and still hits D-6 ("no assertion-value changes — only renames/path-updates allowed"). Documented as a deviation (see below).

3. **Worktree node_modules via symlink to main repo.** The parallel-worktree came empty; I symlinked `voice-bridge/node_modules` → `/home/carsten_bot/nanoclaw/voice-bridge/node_modules` to run tsc + vitest without re-installing. Symlink is untracked (won't be committed); does not affect the commit.

## Deviations from Plan

### Scope-calibration — the plan's 3 D-1 targets had already been partially done

**1. [Rule 1 — Scope accuracy] CASE2_TOLERANCE_DECISION_BLOCK and CASE2_HOLD_MUSIC_CLARIFYING_BLOCK exports did not exist pre-plan**
- **Found during:** Task 1, initial-state scan (grep + file-read before edits)
- **Issue:** The Plan 05.3-01 action (STEP 1) assumes `export const CASE2_TOLERANCE_DECISION_BLOCK = …` + `export const CASE2_HOLD_MUSIC_CLARIFYING_BLOCK = …` are still present in `persona.ts` and need deletion. They are NOT — Phase 05.2-04 already removed those exports and replaced them with a breadcrumb comment (persona.ts:189-192). The phase CONTEXT.md D-1 scan reflects a pre-05.2-04 state.
- **Fix:** Interpreted the plan's actual goal (the truths in the frontmatter: "`CASE2_TOLERANCE_DECISION_BLOCK export no longer exists in voice-bridge/src/persona.ts`") as already satisfied, and refreshed the breadcrumb-comment wording to the Plan-05.3-01-D-1 verbatim form from PATTERNS.md §1. No code deletion needed; the comment-refresh makes the plan's phase-ownership explicit.
- **Files modified:** `voice-bridge/src/persona.ts` (comment block at lines 189-192), `voice-bridge/tests/persona.test.ts` (comment block at lines 105-110 + two it-titles at 113, 127)
- **Verification:** Acceptance criterion `CASE2_TOLERANCE_DECISION_BLOCK export no longer exists in voice-bridge/src/persona.ts` — TRUE (grep on code level: 0 matches). Acceptance criterion `tsc --noUnusedLocals --noUnusedParameters exits 0` — TRUE. Acceptance criterion `npm test exits green, zero assertion-value changes` — TRUE (baseline-identical 442 passed / 5 skipped).
- **Committed in:** `7ea8b46`

**2. [Rule 2 — Preserve coverage] Tests 1+2 in persona.test.ts renamed/retargeted instead of deleted**
- **Found during:** Task 1, step-3 planning
- **Issue:** Plan STEP 3 says "DELETE those blocks entirely". In the actual repo those blocks already call `buildCase2Overlay(...)` (not the deleted constants) — they are live regression coverage for the migrated overlay text introduced in 05.2-04. Deleting would erase a golden-regression test without replacement.
- **Fix:** Updated the two it-titles to describe the current reality ("migrated to persona/overlays/case-2.ts") instead of naming the removed constants. Assertions unchanged. Header comment updated from 05.2-04 to 05.3-01 D-1 terminology.
- **Files modified:** `voice-bridge/tests/persona.test.ts`
- **Verification:** Both tests still pass (included in the 442). D-6 honored: zero assertion-value changes.
- **Committed in:** `7ea8b46`

### Plan-acceptance LOC-delta interpretation

The plan expected `LOC delta on voice-bridge/src/persona.ts: -10 to -40 lines`. Actual delta: 0 lines (content already deleted in 05.2-04; this plan only refreshed a comment). **This is a plan-expectation vs. repo-reality miss, not a failure** — the *logical* LOC-reduction goal (removing ~30 LOC of dead exports) was achieved in the earlier phase. The total `voice-bridge/src` line count (7529) is unchanged by this plan because the heavy lifting already happened.

---

**Total deviations:** 2 auto-applied (1 scope-calibration from stale scan-baseline, 1 coverage-preservation test rename)
**Impact on plan:** Both deviations necessary for correctness. All 5 must-haves truths pass. No scope creep.

## Issues Encountered

- Fresh worktree had no `voice-bridge/node_modules` — symlinked from `/home/carsten_bot/nanoclaw/voice-bridge/node_modules`. Enabled `tsc` + `vitest` to run. Symlink is untracked and does not affect the commit.

## User Setup Required

None — pure source-code refactor.

## Next Phase Readiness

- **05.3-02 (comment-archaeology sweep):** starts from a clean `persona.ts` surface with the correct 05.3-01 D-1 breadcrumb already present; sweep should focus on `sideband.ts` / `webhook.ts` / `call-router.ts` bulk (per PATTERNS.md §10 table).
- **05.3-03 (CASE6B overlay webhook rewire):** `webhook.ts` imports list is now one symbol shorter; import-diff for Plan 05.3-03 will be cleaner (it removes CASE6B_PERSONA and adds buildBasePersona + buildTaskOverlay).
- **CI gate:** `tsc --noUnusedLocals --noUnusedParameters` passes — a future CI tightening can now enforce this without a failing baseline.

## Self-Check: PASSED

- FOUND: commit `7ea8b46` in `git log --oneline -3`
- FOUND: edited file `voice-bridge/src/persona.ts` (breadcrumb at lines 189-192)
- FOUND: edited file `voice-bridge/src/webhook.ts` (line 10 import cleaned)
- FOUND: edited file `voice-bridge/tests/persona.test.ts` (comment + 2 test titles refreshed)
- VERIFIED: `grep -n 'INVALID_TOOL_RESPONSE' voice-bridge/src/webhook.ts` → 0 matches
- VERIFIED: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters` → exit 0
- VERIFIED: `npx vitest run` → 41 files / 442 passed / 5 skipped (baseline-identical)

---
*Phase: 05.3-refactor-cleanup-timer-removal*
*Plan: 01*
*Completed: 2026-04-22*
