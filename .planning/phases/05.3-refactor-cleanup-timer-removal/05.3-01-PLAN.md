---
phase: 05.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - voice-bridge/src/persona.ts
  - voice-bridge/src/webhook.ts
  - voice-bridge/tests/persona.test.ts
autonomous: true
requirements:
  - CODE-HEALTH-D1
must_haves:
  truths:
    - "CASE2_TOLERANCE_DECISION_BLOCK export no longer exists in voice-bridge/src/persona.ts"
    - "CASE2_HOLD_MUSIC_CLARIFYING_BLOCK export no longer exists in voice-bridge/src/persona.ts"
    - "INVALID_TOOL_RESPONSE import no longer present in voice-bridge/src/webhook.ts"
    - "tsc --noUnusedLocals --noUnusedParameters exits 0 (previously flagged INVALID_TOOL_RESPONSE)"
    - "Full test suite (voice-bridge) exits green, zero assertion-value changes"
  artifacts:
    - path: "voice-bridge/src/persona.ts"
      provides: "Persona module, shrunk by dead-export removal"
      contains_not: "CASE2_TOLERANCE_DECISION_BLOCK"
    - path: "voice-bridge/tests/persona.test.ts"
      provides: "Persona test suite, dead-export refs deleted"
      contains_not: "CASE2_TOLERANCE_DECISION_BLOCK"
  key_links:
    - from: "voice-bridge/src/persona.ts"
      to: "voice-bridge/tests/persona.test.ts"
      via: "export-import dependency"
      pattern: "no dangling import of deleted constants"
---

<objective>
Delete 3 dead exports/imports surfaced by the 05.2-04 scan (D-1): two unused persona
constants (`CASE2_TOLERANCE_DECISION_BLOCK`, `CASE2_HOLD_MUSIC_CLARIFYING_BLOCK` — 0
src refs each, only test refs) and one unused import in webhook.ts
(`INVALID_TOOL_RESPONSE` — flagged by `tsc --noUnusedLocals`). Migrate or delete the
tests that reference the deleted exports. Produces the first LOC drop of Phase 05.3
with zero behavior change.

Purpose: Close the trivial part of the "code zugemüllt" backlog in isolation, so
Wave 2 (CASE6B rewire) starts from a clean persona.ts surface and the tsc strict
flags pass before we add overlay imports.

Output:
- `voice-bridge/src/persona.ts` shrunk by 2 dead exports (~30 LOC est.)
- `voice-bridge/src/webhook.ts` import cleaned
- `voice-bridge/tests/persona.test.ts` dead-export tests deleted/migrated
- Summary file `.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-01-SUMMARY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-CONTEXT.md
@.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-PATTERNS.md

<interfaces>
<!-- Exports whose removal is the deliverable of this plan. -->
<!-- Current location: voice-bridge/src/persona.ts — both constants currently exported as `export const …`. -->
<!-- Scan 2026-04-22: 0 src refs, 2 test refs each. -->
<!-- Scan 2026-04-22: INVALID_TOOL_RESPONSE imported at voice-bridge/src/webhook.ts:10 — unused in file body. -->

Deleted exports (after this plan):
- CASE2_TOLERANCE_DECISION_BLOCK  (was in voice-bridge/src/persona.ts)
- CASE2_HOLD_MUSIC_CLARIFYING_BLOCK (was in voice-bridge/src/persona.ts)

Deleted import (after this plan):
- INVALID_TOOL_RESPONSE (was imported in voice-bridge/src/webhook.ts:10 from ./tools/allowlist.js)

Preserved exports (still used — DO NOT TOUCH):
- sanitizeForPersona, toGermanNumber, timeToGerman (case-2 overlay dependencies)
- Case2OutboundPersonaArgs, buildCase2OutboundPersona (webhook.ts outbound callsite)
- PHASE2_PERSONA (webhook.ts inbound non-Carsten path)
- CASE6B_PERSONA (deleted in Plan 05.3-03 NOT in this plan — leave intact here)
- OUTBOUND_PERSONA_TEMPLATE, buildOutboundPersona (evaluation deferred — leave intact)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete 3 dead exports + unused import; migrate/delete dependent tests</name>
  <files>voice-bridge/src/persona.ts, voice-bridge/src/webhook.ts, voice-bridge/tests/persona.test.ts</files>
  <read_first>
    - voice-bridge/src/persona.ts (full file — verify current state before edit; confirm CASE6B_PERSONA still present)
    - voice-bridge/src/webhook.ts (lines 1-20 for import block)
    - voice-bridge/tests/persona.test.ts (full file — locate tests referencing each deleted constant)
    - .planning/phases/05.3-refactor-cleanup-timer-removal/05.3-PATTERNS.md §1 "dead-export deletion" (contains verbatim pattern for the deletion breadcrumb comment)
    - .planning/phases/05.3-refactor-cleanup-timer-removal/05.3-CONTEXT.md D-1 (scope) + D-6 (NO behavior change in happy path)
  </read_first>
  <behavior>
    - After this task: `grep -rn 'CASE2_TOLERANCE_DECISION_BLOCK' voice-bridge/ ` returns 0 matches.
    - After this task: `grep -rn 'CASE2_HOLD_MUSIC_CLARIFYING_BLOCK' voice-bridge/` returns 0 matches.
    - After this task: `grep -n 'INVALID_TOOL_RESPONSE' voice-bridge/src/webhook.ts` returns 0 matches. `INVALID_TOOL_RESPONSE` may still be exported from `voice-bridge/src/tools/allowlist.ts` (other consumers may exist — only the webhook.ts import is the D-1 target).
    - After this task: `cd voice-bridge && npx tsc --noEmit --noUnusedLocals --noUnusedParameters` exits 0.
    - After this task: `cd voice-bridge && npm test` exits green. No test assertion VALUES change (only test-file deletions / import-line deletions permitted).
  </behavior>
  <action>
    STEP 1. Delete from `voice-bridge/src/persona.ts`:
      - The `export const CASE2_TOLERANCE_DECISION_BLOCK = …` block (and any associated inline comment that exclusively documents this constant).
      - The `export const CASE2_HOLD_MUSIC_CLARIFYING_BLOCK = …` block (same rule).
      - Replace the removed block(s) with a single breadcrumb comment (verbatim from PATTERNS.md §1 lines 46-52 — the same style persona.ts already uses for the earlier CASE2_TOLERANCE deletion note):
      ```typescript
      // Plan 05.3-01 D-1: CASE2_TOLERANCE_DECISION_BLOCK + CASE2_HOLD_MUSIC_CLARIFYING_BLOCK
      // constants DELETED (0 src refs; migrated into voice-bridge/src/persona/overlays/case-2.ts
      // during Phase 05.2). Grep-verified: no external references remained in voice-bridge/src/
      // outside tests/persona.test.ts (updated as part of this migration).
      ```
      - DO NOT touch `CASE6B_PERSONA`, `PHASE2_PERSONA`, `OUTBOUND_PERSONA_TEMPLATE`, `buildOutboundPersona`, `buildCase2OutboundPersona`, `sanitizeForPersona`, `toGermanNumber`, `timeToGerman`, or `Case2OutboundPersonaArgs` — they are out of scope for this plan.

    STEP 2. Delete from `voice-bridge/src/webhook.ts` import at line 10:
      - Change from: `import { getAllowlist, type ToolEntry, INVALID_TOOL_RESPONSE } from './tools/allowlist.js'`
      - Change to:   `import { getAllowlist, type ToolEntry } from './tools/allowlist.js'`
      - Verify by grep that `INVALID_TOOL_RESPONSE` is not used anywhere in the webhook.ts body (it isn't — that's why tsc flagged it). If grep finds a reference in the body, STOP and report — do not proceed without investigation.

    STEP 3. Update `voice-bridge/tests/persona.test.ts`:
      - Locate any `it(…)` or `describe(…)` block that imports or asserts on `CASE2_TOLERANCE_DECISION_BLOCK` or `CASE2_HOLD_MUSIC_CLARIFYING_BLOCK`.
      - DELETE those blocks entirely (the constants are gone; the tests cannot survive by renaming).
      - Remove the import lines `import { CASE2_TOLERANCE_DECISION_BLOCK, CASE2_HOLD_MUSIC_CLARIFYING_BLOCK } from '../src/persona.js'` (or whichever combined import form exists).
      - DO NOT touch tests referencing `CASE6B_PERSONA`, `PHASE2_PERSONA`, `OUTBOUND_PERSONA_TEMPLATE`, or `buildCase2OutboundPersona` — those are Plan 05.3-03 scope or stay live.

    STEP 4. Run verification:
      ```bash
      cd voice-bridge
      grep -rn 'CASE2_TOLERANCE_DECISION_BLOCK\|CASE2_HOLD_MUSIC_CLARIFYING_BLOCK' .   # expect 0
      grep -n 'INVALID_TOOL_RESPONSE' src/webhook.ts                                   # expect 0
      npx tsc --noEmit --noUnusedLocals --noUnusedParameters                           # expect exit 0
      npm test -- --run                                                                # expect all green
      ```
      All four must pass before marking done.

    STEP 5. Atomic commit (per CLAUDE.md global rule — no Quick-Fixes, atomic commits):
      `git add voice-bridge/src/persona.ts voice-bridge/src/webhook.ts voice-bridge/tests/persona.test.ts`
      `git commit -m "refactor(05.3-01): delete 3 dead exports (D-1) — CASE2_TOLERANCE/HOLD_MUSIC blocks + INVALID_TOOL_RESPONSE unused import"`
  </action>
  <verify>
    <automated>cd voice-bridge &amp;&amp; grep -rn 'CASE2_TOLERANCE_DECISION_BLOCK\|CASE2_HOLD_MUSIC_CLARIFYING_BLOCK' . | wc -l | grep -q '^0$' &amp;&amp; ! grep -n 'INVALID_TOOL_RESPONSE' src/webhook.ts &amp;&amp; npx tsc --noEmit --noUnusedLocals --noUnusedParameters &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'CASE2_TOLERANCE_DECISION_BLOCK' voice-bridge/` → 0 matches
    - `grep -rn 'CASE2_HOLD_MUSIC_CLARIFYING_BLOCK' voice-bridge/` → 0 matches
    - `grep -n 'INVALID_TOOL_RESPONSE' voice-bridge/src/webhook.ts` → 0 matches
    - `cd voice-bridge && npx tsc --noEmit --noUnusedLocals --noUnusedParameters` → exit 0
    - `cd voice-bridge && npm test -- --run` → all tests pass, no assertion-value changes
    - LOC delta on `voice-bridge/src/persona.ts`: -10 to -40 lines expected (depending on length of deleted constants)
    - No new commits other than the D-1 cleanup commit
  </acceptance_criteria>
  <done>All 6 acceptance criteria pass; commit landed on branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→source | Developer edits persona.ts / webhook.ts / test file — all trusted internal edits |
| build→runtime | tsc strict-flags compile → production bundle (unchanged observable behavior) |

## STRIDE Threat Register

Refactor phase — no new attack surface. Regression-risk profile only.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0503-01-01 | Tampering | voice-bridge/src/persona.ts exports surface | mitigate | Grep-verified 0 src refs to deleted constants pre-edit; test migration deletes dependent tests; full suite green post-edit |
| T-0503-01-02 | Information Disclosure | Accidentally removing a constant still referenced in production | mitigate | STEP 1 grep check `grep -rn 'CASE6B_PERSONA\|PHASE2_PERSONA' voice-bridge/src/` (preserved) pre-edit; STEP 4 tsc + test re-run |
| T-0503-01-03 | Denial of Service | tsc strict flags break build post-edit | accept | Acceptance criterion requires `tsc --noUnusedLocals --noUnusedParameters` exit 0 before commit; failure aborts plan |
| T-0503-01-04 | Regression (§201 invariant) | n/a — no audio path or amd-classifier code touched | accept | Deleted constants are persona text only; §201 zero-audio-leak invariant is enforced by ghost-scan.ts + amd-classifier.ts which are out of scope for this plan |
</threat_model>

<verification>
Post-plan:
1. `git log --oneline voice-bridge/ | head -3` shows one new 05.3-01 commit
2. `find voice-bridge/src -name '*.ts' | xargs wc -l | tail -1` shows line count lower than pre-plan baseline (5695)
3. `cd voice-bridge && npm test -- --run` green
4. `cd voice-bridge && npx tsc --noEmit --noUnusedLocals --noUnusedParameters` exits 0
</verification>

<success_criteria>
- D-1 closed: 3 dead exports/imports deleted, 0 grep matches for the 3 names
- tsc strict flags pass (unblocks future `noUnusedLocals` enforcement in CI)
- Full test suite green — D-6 "no behavior change in happy path" preserved
- Net LOC reduction: ≥10 lines in voice-bridge/src (persona.ts shrinks)
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-refactor-cleanup-timer-removal/05.3-01-SUMMARY.md`
documenting:
- Exact LOC deltas (persona.ts before/after, tests before/after)
- Grep output confirming 0 refs to 3 deleted names
- tsc + npm test exit status
- Commit hash
</output>
