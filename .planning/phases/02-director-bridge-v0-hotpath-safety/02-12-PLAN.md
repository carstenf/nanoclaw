---
phase: 02-director-bridge-v0-hotpath-safety
plan: 12
type: execute
wave: 12
depends_on:
  - "02-11"  # dispatch async MCP-forward live
  - "03-05"  # voice.get_travel_time core-side
  - "03-08"  # voice.ask_core core-side
files_modified:
  - /home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/ask_core.json
  - /home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/get_travel_time.json
  - /home/carsten_bot/nanoclaw/voice-bridge/src/tools/allowlist.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/src/tools/allowlist.test.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/src/tools/dispatch.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/src/tools/dispatch.test.ts
autonomous: true

requirements:
  - TOOLS-08   # get_travel_time
  - TOOLS-10   # ask_core
  - DIR-11
  - C6-02

must_haves:
  truths:
    - "Zwei bridge-side-schemas + allowlist-Eintraege + dispatch-mappings nachziehen: `ask_core` und `get_travel_time`. Beide sind core-seitig live seit 03-05/03-08, aber in 02-11 uebersehen worden."
    - "`ask_core` schema (JSON-Schema draft-07 analog zu bestehenden): required [topic, request], topic pattern `^[a-z0-9_-]+$` maxLength 64, request minLength 1 maxLength 2000. mutating:false."
    - "`get_travel_time` schema: required [origin, destination], origin/destination minLength 1 maxLength 300, mode enum ['driving','walking','bicycling','transit'] default 'driving', departure_time optional string. mutating:false."
    - "allowlist.ts: Entries + ajv-compile wie bestehende 9. Jetzt 11 Entries — check REQ-TOOLS-09 ceiling 15 noch ok (4 spare)."
    - "dispatch.ts TOOL_TO_CORE_MCP Mapping: `'ask_core': 'voice.ask_core'`, `'get_travel_time': 'voice.get_travel_time'`. Beide core-existing, kein null-stub."
    - "Tests: allowlist.test prueft Count-Change (11 statt 9) + beide neuen Tools in getEntry; dispatch.test prueft forward-mapping + happy-path (mock callCoreTool)."
    - "Deploy: Bridge auf Lenovo1 (nicht Hetzner wie urspruenglich angenommen — siehe 02-11 summary). `npm run build` in voice-bridge, `systemctl --user restart voice-bridge`, /health zeigt tools_count=11."
  artifacts:
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/ask_core.json"
      provides: "JSON-Schema fuer ask_core (topic/request)"
      contains: "^[a-z0-9_-]+$"
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/get_travel_time.json"
      provides: "JSON-Schema fuer get_travel_time (origin/destination/mode/departure_time)"
      contains: "enum"
  key_links:
    - from: "allowlist.ts ENTRIES"
      to: "schemas/ask_core.json + get_travel_time.json"
      via: "import + ajv.compile"
      pattern: "askCoreSchema|getTravelTimeSchema"
    - from: "dispatch.ts TOOL_TO_CORE_MCP"
      to: "voice.ask_core + voice.get_travel_time"
      via: "prefix-mapping"
      pattern: "voice\\.ask_core|voice\\.get_travel_time"

---

<objective>
Nachtrag zu 02-11: In 02-11 wurde bridge-side dispatch implementiert, aber `voice.ask_core` (neu aus 03-08) und `voice.get_travel_time` (03-05, uebersehen) fehlen in bridge-allowlist + dispatch-mapping. Dieser Plan schliesst die Luecke.

Purpose: Nach 02-12 ist die bridge-Allowlist konsistent mit den 8 core-existing Tools (plus 2 bridge-internal Stubs transfer_call+confirm_action, plus 2 null-stubs search_hotels+competitors die bei Bot-Aufruf `not_implemented` zurueckliefern). Im PSTN-Test kann der Bot alle 8 Core-Tools vertont werden.

Output: 2 JSON-Schemas, 2 allowlist-Eintraege, 2 dispatch-mappings, Tests. Kein Deploy-Cross-Host (Bridge auf Lenovo1).
</objective>

<execution_context>
@/home/carsten_bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/carsten_bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/carsten_bot/nanoclaw-state/.planning/phases/02-director-bridge-v0-hotpath-safety/02-11-PLAN.md
@/home/carsten_bot/nanoclaw-state/.planning/phases/02-director-bridge-v0-hotpath-safety/02-11-SUMMARY.md
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/allowlist.ts
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/dispatch.ts
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/get_contract.json  # schema-shape reference
</context>

<tasks>

<task type="auto">
  <name>Task 02-12-01: JSON-Schemas anlegen</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/ask_core.json
    /home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/get_travel_time.json
  </files>
  <action>
    - `ask_core.json` — draft-07, required [topic, request], topic pattern+maxLength, request min/maxLength.
    - `get_travel_time.json` — draft-07, required [origin, destination], origin/destination maxLength 300, mode enum (driving/walking/bicycling/transit), departure_time optional string.
  </action>
  <acceptance_criteria>
    - `test -f voice-bridge/src/tools/schemas/ask_core.json && jq . voice-bridge/src/tools/schemas/ask_core.json >/dev/null`
    - `test -f voice-bridge/src/tools/schemas/get_travel_time.json && jq . voice-bridge/src/tools/schemas/get_travel_time.json >/dev/null`
    - `grep -c "^[a-z0-9_-]+$\|a-z0-9" voice-bridge/src/tools/schemas/ask_core.json` >= 1
    - `grep -c "driving\|transit" voice-bridge/src/tools/schemas/get_travel_time.json` >= 2
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 02-12-02: allowlist.ts + dispatch.ts updates + Tests</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/src/tools/allowlist.ts
    /home/carsten_bot/nanoclaw/voice-bridge/src/tools/allowlist.test.ts
    /home/carsten_bot/nanoclaw/voice-bridge/src/tools/dispatch.ts
    /home/carsten_bot/nanoclaw/voice-bridge/src/tools/dispatch.test.ts
  </files>
  <action>
    - **allowlist.ts**: import + 2 neue ENTRIES (ask_core mutating:false, get_travel_time mutating:false). Jetzt 11 total. REQ-TOOLS-09 ceiling 15 check bleibt.
    - **dispatch.ts TOOL_TO_CORE_MCP**: `'ask_core': 'voice.ask_core'`, `'get_travel_time': 'voice.get_travel_time'`.
    - **allowlist.test.ts**: bestehende Count-Assertion updaten von 9→11. 2 neue `it()` fuer die getEntry-Checks + mutating-Flag.
    - **dispatch.test.ts**: 2 neue happy-path-Tests mit mock callCoreTool, verify `voice.ask_core` + `voice.get_travel_time` als forward-name.
    - Full-Suite: `cd voice-bridge && npx tsc --noEmit && npx vitest run` — 0 neu failing.
  </action>
  <acceptance_criteria>
    - `grep -c "ask_core\|get_travel_time" voice-bridge/src/tools/allowlist.ts` >= 4  # 2 imports + 2 entries
    - `grep -c "voice\\.ask_core\|voice\\.get_travel_time" voice-bridge/src/tools/dispatch.ts` >= 2
    - `grep -c "it\(" voice-bridge/src/tools/allowlist.test.ts` >= 2  # mindestens 2 neue
    - `cd voice-bridge && npx tsc --noEmit` exits 0
    - `cd voice-bridge && npx vitest run` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 02-12-03: Deploy + Health</name>
  <files>(nur Ops)</files>
  <action>
    Step 1: `cd voice-bridge && npm run build`
    Step 2: `systemctl --user restart voice-bridge` (oder entsprechender Service-Name — check status first).
    Step 3: Status active + `curl http://10.0.0.2:4402/health` (Bridge auf Lenovo1 10.0.0.2:4402 per 02-11-summary) → 200 mit tools_count=11.
    Step 4: Log-tail zeigt `allowlist_compiled` mit tool_count=11 + mutating_count unveraendert (nur mutating:false added).
    Step 5: Optional smoke: cross-host curl von Hetzner an voice-bridge dispatch-endpoint (falls es einen direkten test-endpoint gibt) — sonst skippen, dispatcher wird erst bei echtem function_call live.
  </action>
  <acceptance_criteria>
    - systemctl status voice-bridge = active
    - /health zeigt tools_count=11
    - Log: allowlist_compiled tool_count=11
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

Minimal — reiner config/schema-Nachtrag, keine neue Logik:
- T-02-12-01: Schema-Sync-Drift zwischen bridge-schemas und core-zod — mitigated durch Shape-Vergleich im Test (bridge-schema-Keys match core-zod-Keys). Akzeptabel da manual-keep-in-sync, alternativ generate-from-core.

</threat_model>

<success_criteria>
- ask_core + get_travel_time sind via bridge aufrufbar (dispatch.TOOL_TO_CORE_MCP mapped, allowlist kompiliert).
- 11 Tools gesamt, unter REQ-TOOLS-09 ceiling.
- Tests gruen.
- Bridge deployed, /health zeigt 11 tools.
</success_criteria>

<output>
SUMMARY `_planning/phases/02-director-bridge-v0-hotpath-safety/02-12-SUMMARY.md`:
- Commits
- Bridge-health nach Deploy (tools_count=11)
- Caveats (keine — pure delta)
- Next: STOP fuer PSTN-Combined-Test (Carsten-Task per Briefing §7)
</output>
