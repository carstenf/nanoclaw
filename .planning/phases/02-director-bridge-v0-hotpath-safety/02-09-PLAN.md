---
phase: 02-director-bridge-v0-hotpath-safety
plan: 09
type: execute
wave: 6
depends_on:
  - "02-01"
  - "02-05"
  - "02-06"
  - "02-07"
files_modified:
  - /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/src/core-mcp-client.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/src/webhook.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/package.json
  - /home/carsten_bot/nanoclaw/voice-bridge/systemd/voice-bridge.service
  - /home/carsten_bot/nanoclaw/voice-bridge/tests/slow-brain.test.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/tests/core-mcp-client.test.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/tests/call-router.test.ts
  - /home/carsten_bot/nanoclaw-state/voice-channel-spec/REQUIREMENTS.md  # nur Traceability-Anker, KEIN REQ-Edit
autonomous: true  # Chat-GO commit cc6dbd3 + Authority-Briefing 1788873
requirements:
  - DIR-01
  - DIR-02
  - DIR-06
  - DIR-11
  - DIR-12
  - C6-02
  - INFRA-12
  - AC-09
tags:
  - slow-brain
  - retrofit
  - mcp-call
  - architecture-drift

must_haves:
  truths:
    - "voice-bridge hat NACH Migration keinen `@anthropic-ai/sdk` Import mehr (nicht in src/, nicht in tests/ ausser in Decision-Doc-Verweisen)."
    - "`ANTHROPIC_API_KEY` ist NACH Migration nicht mehr Teil des voice-bridge env-path (.env + systemd unit + dotenv-sample)."
    - "Slow-Brain-Ersatz ruft pro Turn `voice.on_transcript_turn(call_id, turn_id, transcript)` via MCP-Client `core-mcp-client.ts` am NanoClaw-Core-Endpoint (WireGuard 10.0.0.2, Port/URL aus CORE_MCP_URL env)."
    - "Core-Response-Schema: `{ok: true, instructions_update: string|null, warnings?: string[]}`. Bei `instructions_update` non-null ruft die Bridge `updateInstructions(state, instructions_update, log)` via bestehender sideband-Logik."
    - "Cadence-Cap (D-25, default 2 Turns) bleibt in der Bridge VOR dem MCP-Call als Rate-Limiter — verhindert Core-Overload (regressionsfreier Weiternutzung)."
    - "MCP-Timeout: `CORE_MCP_TIMEOUT_MS` (default 8000, wie frueher SLOW_BRAIN_TIMEOUT_MS). Bei Timeout/5xx/thrown error: log `slow_brain_degraded` WARN + no-op, Hot-Path unberuehrt (D-27, REQ-DIR-12)."
    - "`slow_brain_disabled` Fallback bleibt: wenn `CORE_MCP_URL` nicht gesetzt -> Worker im no-op-Modus startet (wie heute getAnthropicKey-Fallback), Phase-2-Tests gruen ohne Core-MCP."
    - "Back-pressure (D-28, queueMax=5) bleibt erhalten — oldest-shift vor dem MCP-Call."
    - "Kein neuer Core-Agent-Code in diesem Plan; Core-seitig wird NUR das MCP-Tool-Stub dokumentiert (Contract), Implementierung ist separater Core-Plan im nanoclaw-state-Repo."
    - "Integration-Test (neu): mock MCP-Server (fastify/msw) liefert fixe `instructions_update`, Harness fuehrt transcript-turn durch call-router, asserted dass updateInstructions mit genau diesem String aufgerufen wird."
    - "Graceful-degrade-Test (neu): MCP-Server antwortet 500 -> no-op, Hot-Path liefert Antwort ohne instructions_update, `slow_brain_degraded` WARN ist geloggt (REQ-DIR-12)."
    - "Threat-Model (Abschnitt <threat_model>): STRIDE-Scan fuer Bridge→Core MCP-Kommunikation — Transport WireGuard, Tool-Allowlist Core-seitig, Injection-Mitigation via Schema-Validation."
  artifacts:
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/src/core-mcp-client.ts"
      provides: "callCoreTool(toolName, args, opts): AbortController-wrapped fetch to CORE_MCP_URL with CORE_MCP_TIMEOUT_MS; returns parsed JSON or throws domain-specific error"
      contains: "CORE_MCP_URL"
      min_lines: 60
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts"
      provides: "startSlowBrain refactored — no Anthropic client, no process-level API key; dispatches to core-mcp-client; cadence cap + back-pressure + timeout SAME as Plan 02-05"
      contains: "voice.on_transcript_turn"
      min_lines: 100
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/src/config.ts"
      provides: "New: CORE_MCP_URL (no default — unset disables slow-brain), CORE_MCP_TIMEOUT_MS (default 8000). Removed: getAnthropicKey(), SLOW_BRAIN_MODEL."
      contains: "CORE_MCP_URL"
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/tests/core-mcp-client.test.ts"
      provides: "Happy-path + timeout + 5xx + 4xx schema-reject cases via in-process fastify mock"
      contains: "CORE_MCP_URL"
      min_lines: 80
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/tests/slow-brain.test.ts"
      provides: "Updated test suite — injected fake core-client replaces fake Anthropic client; cadence/back-pressure/graceful-degrade cases unchanged in shape, updated in dependency"
      contains: "on_transcript_turn"
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/systemd/voice-bridge.service"
      provides: "EnvironmentFile unchanged; no inline env needed (all from .env)"
      contains: "EnvironmentFile"
  key_links:
    - from: "slow-brain worker"
      to: "core-mcp-client.callCoreTool"
      via: "voice.on_transcript_turn({call_id, turn_id, transcript})"
      pattern: "voice\\.on_transcript_turn"
    - from: "core-mcp-client"
      to: "NanoClaw-Core MCP endpoint"
      via: "HTTPS POST CORE_MCP_URL with tool-name + args body"
      pattern: "CORE_MCP_URL"
    - from: "call-router on turn-end"
      to: "slow-brain.push(delta)"
      via: "Sideband WS response.done -> push -> worker drains"
      pattern: "push\\("

---

<gating>

**BLOCKS EXECUTION: 3 offene Fragen an Chat (siehe open_points.md Eintrag 2026-04-17 FOR CHAT nach Plan-Commit).**

1. **Core-MCP-Endpoint:** Gibt es schon einen erreichbaren MCP-Endpoint bei NanoClaw-Core, den voice-bridge via WireGuard rufen kann? Oder ist das der erste Bridge→Core-Call ueberhaupt?
   - Wenn ja: URL/Port + Auth-Mechanismus (Session-Token? mTLS? naked HTTP via WG-only?)
   - Wenn nein: braucht es einen Vor-Plan, der den Endpoint in NanoClaw-Core eroeffnet (separates Core-Plan-Repo)

2. **Tool-Namenskonvention:** `voice.on_transcript_turn` oder anderes Schema (z.B. `director.transcript.turn`, `nanoclaw.voice.turn`)?
   - Muss mit Core-MCP-Registry konsistent sein

3. **Claude-Chat-Zugriff:** Soll Chat denselben Tool-Endpoint zum Debuggen rufen duerfen?
   - REQ-AC-07 ("Developer Access") suggeriert ja — aber die MCP-Auth-Policy muss das explizit erlauben.

Plan-Execution nicht starten bevor diese 3 Fragen beantwortet sind.

</gating>

<objective>
Retrofit auf Original-Architektur (Decision 2026-04-13): Slow-Brain-Inference wandert aus voice-bridge raus in den NanoClaw-Core-Agenten. voice-bridge bleibt reiner Transport/Proxy (AC-09-konform) und spricht Core via MCP ueber WireGuard.

Purpose: Phase 2 Plan 02-05 hat Slow-Brain implizit in voice-bridge gebaut und damit Business-Logik in die Voice-Stack-Codebase getragen — Verletzung von AC-09 und REQ-INFRA-12 (auch wenn REQ-DIR-06 semantisch neutral zur Runtime-Lokation ist). Live-Test am 2026-04-17 hat das per `ANTHROPIC_API_KEY`-Mismatch offengelegt. Siehe `decisions/2026-04-17-slow-brain-architecture-drift.md`.

Output: voice-bridge schickt pro Turn-End einen MCP-Call `voice.on_transcript_turn` an Core, empfaengt optionales `instructions_update`, pusht das via bestehender Sideband-Logik. Kein Anthropic-SDK in voice-bridge mehr; `ANTHROPIC_API_KEY` aus voice-bridge-env entfernt. Tests gruen (tsc + vitest + audio-guard + replay-harness).
</objective>

<execution_context>
@/home/carsten_bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/carsten_bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/carsten_bot/nanoclaw-state/_planning/phases/02-director-bridge-v0-hotpath-safety/02-CONTEXT.md
@/home/carsten_bot/nanoclaw-state/_planning/phases/02-director-bridge-v0-hotpath-safety/02-PATTERNS.md
@/home/carsten_bot/nanoclaw-state/decisions/2026-04-17-slow-brain-architecture-drift.md
@/home/carsten_bot/nanoclaw-state/decisions/2026-04-13-voice-v6-findings-director-pattern.md
@/home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts
@/home/carsten_bot/nanoclaw/voice-bridge/src/sideband.ts
@/home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts
@/home/carsten_bot/nanoclaw/voice-bridge/src/config.ts

<interfaces>

<!-- Original-Decision (2026-04-13) Rollen-Definition, zitiert als Leitbild -->
Fast Brain = OpenAI Realtime (hot-path)
Director Bridge = Transport/Proxy (kein Brain)
Slow Brain = Claude via NanoClaw (async)

<!-- Plan 02-05 aktueller Slow-Brain-Flow (vor Retrofit) -->
call-router.onTurnDone -> slowBrain.push(delta)
slowBrain runLoop -> anthropic.messages.create(...) -> updateInstructions(sideband, text, log)
config.getAnthropicKey() liest ANTHROPIC_API_KEY oder throwed (no-op-Fallback seit Patch 042ab2c)

<!-- Nach Retrofit (dieser Plan) -->
call-router.onTurnDone -> slowBrain.push(delta)
slowBrain runLoop -> coreMcp.callTool('voice.on_transcript_turn', {call_id, turn_id, transcript})
  response = {ok:true, instructions_update: string|null}
  if instructions_update: updateInstructions(sideband, instructions_update, log)

<!-- AbortController pattern (copy aus heartbeat.ts — existierendes Vorbild) -->
```typescript
const ctrl = new AbortController()
const timer = setTimeout(() => ctrl.abort(), CORE_MCP_TIMEOUT_MS)
try {
  const r = await fetch(CORE_MCP_URL, { signal: ctrl.signal, ... })
} finally {
  clearTimeout(timer)
}
```

<!-- Graceful-degrade-Vorbild aus Plan 02-05 (unveraendert retention) -->
try { ... } catch (e) {
  log.warn({event:'slow_brain_degraded', call_id: sideband.callId, reason: e.message})
  // NO THROW — hot-path unaffected
}

<!-- config.ts Env-Pattern (existierend, erweitern) -->
export const CORE_MCP_URL = process.env.CORE_MCP_URL // unset = disabled
export const CORE_MCP_TIMEOUT_MS = Number(process.env.CORE_MCP_TIMEOUT_MS ?? 8000)
// REMOVE: getAnthropicKey(), SLOW_BRAIN_MODEL
// KEEP: SLOW_BRAIN_CADENCE_CAP, SLOW_BRAIN_QUEUE_MAX (still useful as rate-limiter for MCP)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 09-01: Neues core-mcp-client.ts + Tests (happy-path, timeout, 5xx, 4xx schema-reject)</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/src/core-mcp-client.ts
    /home/carsten_bot/nanoclaw/voice-bridge/tests/core-mcp-client.test.ts
  </files>
  <read_first>
    - /home/carsten_bot/nanoclaw/voice-bridge/src/heartbeat.ts (AbortController pattern)
    - /home/carsten_bot/nanoclaw/voice-bridge/src/alerts.ts (log-warn graceful pattern)
    - /home/carsten_bot/nanoclaw-state/_planning/phases/02-director-bridge-v0-hotpath-safety/02-PATTERNS.md (fastify test patterns)
  </read_first>
  <behavior>
    - `callCoreTool(name: string, args: unknown, opts?: {timeoutMs?, url?, signal?}): Promise<unknown>`
    - HTTP POST zu `CORE_MCP_URL` (oder `opts.url` in Tests) mit Body `{tool: name, arguments: args}` (MCP-kompatibel).
    - Auth: `Authorization: Bearer ${CORE_MCP_TOKEN}` wenn `CORE_MCP_TOKEN` gesetzt (Frage an Chat offen — Plan laesst beides zu, Default ohne Token fuer WG-only).
    - AbortController mit `CORE_MCP_TIMEOUT_MS` (default 8000) — ueberschreibbar per `opts.timeoutMs`.
    - 2xx + valides JSON -> return body.
    - Non-2xx -> throw `CoreMcpError` mit `{status, body}`.
    - Timeout/Abort -> throw `CoreMcpTimeoutError`.
    - Keine Retries (Cadence-Cap ist upstream, und Graceful-degrade uebernimmt).
    - Tests nutzen `fastify` in-proc-Mock (vorhanden im repo — gleiches Muster wie webhook-tests).
  </behavior>
  <action>
    Implementierung des core-mcp-client + Vitest-Suite mit 4 cases:
    1. Happy path: mock antwortet 200 + `{ok:true, instructions_update:"neue instr"}` -> client liefert das Body-Object.
    2. Timeout: mock delayt 10s, client timeoutMs=50 -> `CoreMcpTimeoutError` wird geworfen.
    3. 5xx: mock antwortet 500 -> `CoreMcpError` mit `status=500`.
    4. 4xx: mock antwortet 400 -> `CoreMcpError` mit `status=400`.

    Konkrete Error-Klassen:
    ```typescript
    export class CoreMcpError extends Error {
      constructor(public status: number, public body: unknown) {
        super(`core-mcp: HTTP ${status}`)
      }
    }
    export class CoreMcpTimeoutError extends Error {
      constructor() { super('core-mcp: timeout') }
    }
    ```
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge && npx tsc --noEmit && npx vitest run tests/core-mcp-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CoreMcpError" /home/carsten_bot/nanoclaw/voice-bridge/src/core-mcp-client.ts` returns at least 1
    - `grep -c "CoreMcpTimeoutError" /home/carsten_bot/nanoclaw/voice-bridge/src/core-mcp-client.ts` returns at least 1
    - `grep -c "AbortController" /home/carsten_bot/nanoclaw/voice-bridge/src/core-mcp-client.ts` returns 1
    - `grep -c "CORE_MCP_URL" /home/carsten_bot/nanoclaw/voice-bridge/src/core-mcp-client.ts` returns at least 1
    - `grep -c "it\(" /home/carsten_bot/nanoclaw/voice-bridge/tests/core-mcp-client.test.ts` returns at least 4
    - `cd /home/carsten_bot/nanoclaw/voice-bridge && npx vitest run tests/core-mcp-client.test.ts` exits 0
  </acceptance_criteria>
  <done>core-mcp-client.ts + tests gruen; 4 Cases abgedeckt; keine echten Netz-Calls in Tests.</done>
</task>

<task type="auto">
  <name>Task 09-02: config.ts Migration — CORE_MCP_URL/TIMEOUT in, getAnthropicKey/SLOW_BRAIN_MODEL out</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts
  </files>
  <read_first>
    - /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts (all lines — jede noch referenzierte Phase-1/2 Konstante bleibt erhalten)
    - Rueckwaerts-Greps fuer Namen die entfernt werden (siehe action step 1)
  </read_first>
  <action>
    Step 1 — Audit welche Symbole entfernt werden muessen:
    ```bash
    cd /home/carsten_bot/nanoclaw/voice-bridge
    grep -rn "getAnthropicKey\|SLOW_BRAIN_MODEL" src/ tests/
    ```
    Erwartung: Treffer in `slow-brain.ts` (wird in Task 09-03 refactored). Wenn andere Treffer existieren -> Plan updaten.

    Step 2 — In `config.ts`:
    - ENTFERNE: `export function getAnthropicKey()` + `SLOW_BRAIN_MODEL` + zugehoerige env-Kommentare
    - FUEGE HINZU (am Ende, nach bestehenden Phase-2-Exports):
      ```typescript
      // ----- Plan 02-09: NanoClaw-Core MCP endpoint (Slow-Brain Retrofit) -----

      // Unset = slow-brain no-op mode (backward-compat with Plan 02-05 slow_brain_disabled fallback).
      export const CORE_MCP_URL = process.env.CORE_MCP_URL

      // D-27 retention: Claude via Core has same timeout envelope as direct Anthropic had.
      export const CORE_MCP_TIMEOUT_MS = Number(
        process.env.CORE_MCP_TIMEOUT_MS ?? 8000,
      )

      // Optional Bearer token for Core-MCP auth. Unset = WG-only auth (Frage an Chat offen).
      export const CORE_MCP_TOKEN = process.env.CORE_MCP_TOKEN
      ```
    - BEHALTE: `SLOW_BRAIN_CADENCE_CAP`, `SLOW_BRAIN_QUEUE_MAX`, `SIDEBAND_CONNECT_TIMEOUT_MS`, `SIDEBAND_WS_URL_TEMPLATE`, `getApiKey()` (das ist OpenAI-SIP-Key, unveraendert), `PHASE1_PERSONA`, `PHASE2_PERSONA`.

    Step 3 — tsc check: `npx tsc --noEmit`. Erwartet: rote Zeilen in `slow-brain.ts` (behoben in Task 09-03).
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge && grep -c "getAnthropicKey\|SLOW_BRAIN_MODEL" src/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getAnthropicKey" /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts` returns 0
    - `grep -c "SLOW_BRAIN_MODEL" /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts` returns 0
    - `grep -c "CORE_MCP_URL" /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts` returns at least 1
    - `grep -c "CORE_MCP_TIMEOUT_MS" /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts` returns at least 1
    - `grep -c "SLOW_BRAIN_CADENCE_CAP" /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts` returns 1 (preserved)
    - `grep -c "SLOW_BRAIN_QUEUE_MAX" /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts` returns 1 (preserved)
    - `grep -c "PHASE2_PERSONA" /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts` returns 1 (preserved)
    - `grep -c "getApiKey" /home/carsten_bot/nanoclaw/voice-bridge/src/config.ts` returns at least 1 (OpenAI-SIP-Key preserved)
  </acceptance_criteria>
  <done>config.ts frei von Anthropic-API-Key-Exports; Core-MCP-Exports vorhanden; Phase-1/2-Exports unveraendert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09-03: slow-brain.ts Refactor — Core-MCP-Call statt Anthropic-SDK, Tests migriert</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts
    /home/carsten_bot/nanoclaw/voice-bridge/tests/slow-brain.test.ts
  </files>
  <read_first>
    - /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts (alle Zeilen — Cadence/Queue/Timeout-Logik bleibt strukturell gleich)
    - /home/carsten_bot/nanoclaw/voice-bridge/tests/slow-brain.test.ts (alle cases — Dependency-Injection aendert sich, Struktur bleibt)
    - /home/carsten_bot/nanoclaw/voice-bridge/src/core-mcp-client.ts (aus Task 09-01)
  </read_first>
  <behavior>
    - `startSlowBrain(log, sidebandState, opts?)` SIGNATURE UNVERAENDERT zum Outside-Caller (call-router.ts).
    - Intern entfaellt `anthropicClient`-DI + `createDefaultClient()` + `require('@anthropic-ai/sdk')`.
    - Ersatz: `coreClient: {callTool(name, args, opts): Promise<CoreResponse>}` (DI-faehig fuer Tests).
    - Default-coreClient = Wrapper um `core-mcp-client.callCoreTool`.
    - Wenn `CORE_MCP_URL` unset: Worker startet im no-op-Modus (analog altem `slow_brain_disabled`), log.info event:`slow_brain_disabled`, reason:`core_mcp_url_unset`.
    - Pro Turn: `callTool('voice.on_transcript_turn', {call_id: sideband.callId, turn_id: delta.turnId, transcript: delta.transcript})`.
    - Response-Schema-Check: `{ok: true, instructions_update: string|null}`. Fehlgeschlagener Schema-Check -> log.warn `slow_brain_bad_response` + no-op.
    - `instructions_update` non-null -> `updateInstructions(sideband, instructions_update, log)`. Cadence-Counter nur bei tatsaechlichem Update zuruecksetzen.
    - Cadence-Cap, Back-pressure, Timeout-Wrap-Semantik **unveraendert** zu Plan 02-05.
  </behavior>
  <action>
    Write new `slow-brain.ts` (core-mcp-based). Referenz-Signatur:

    ```typescript
    import type pino from 'pino'
    import {
      SLOW_BRAIN_CADENCE_CAP,
      SLOW_BRAIN_QUEUE_MAX,
      CORE_MCP_URL,
      CORE_MCP_TIMEOUT_MS,
    } from './config.js'
    import type { SidebandState } from './sideband.js'
    import { updateInstructions } from './sideband.js'
    import { callCoreTool } from './core-mcp-client.js'

    export interface TranscriptDelta {
      turnId: string
      transcript: string
      toolResults?: unknown
    }

    export interface CoreTurnResponse {
      ok: boolean
      instructions_update: string | null
      warnings?: string[]
    }

    export interface CoreClientLike {
      callTool: (
        name: string,
        args: unknown,
        opts?: { timeoutMs?: number; signal?: AbortSignal },
      ) => Promise<CoreTurnResponse>
    }

    export interface StartSlowBrainOpts {
      coreClient?: CoreClientLike
      cadenceCap?: number
      timeoutMs?: number
      queueMax?: number
    }

    export interface SlowBrainWorker {
      push: (delta: TranscriptDelta) => void
      stop: () => Promise<void>
    }

    export function startSlowBrain(
      log: pino.Logger,
      sideband: SidebandState,
      opts: StartSlowBrainOpts = {},
    ): SlowBrainWorker {
      const cadenceCap = opts.cadenceCap ?? SLOW_BRAIN_CADENCE_CAP
      const timeoutMs = opts.timeoutMs ?? CORE_MCP_TIMEOUT_MS
      const queueMax = opts.queueMax ?? SLOW_BRAIN_QUEUE_MAX

      // no-op mode if neither DI-client nor CORE_MCP_URL exists
      const disabled = !opts.coreClient && !CORE_MCP_URL
      if (disabled) {
        log.info({event: 'slow_brain_disabled', reason: 'core_mcp_url_unset', call_id: sideband.callId})
      }

      const coreClient: CoreClientLike =
        opts.coreClient ??
        {
          callTool: async (name, args, o) =>
            (await callCoreTool(name, args, {timeoutMs: o?.timeoutMs, signal: o?.signal})) as CoreTurnResponse,
        }

      const queue: TranscriptDelta[] = []
      let turnsSinceUpdate = 0
      let running = true
      let currentAbort: AbortController | null = null

      async function runLoop(): Promise<void> {
        while (running) {
          const delta = queue.shift()
          if (!delta) { await sleep(50); continue }
          if (disabled) { continue }
          turnsSinceUpdate++
          if (cadenceCap > 0 && turnsSinceUpdate < cadenceCap) { continue }
          try {
            const ctrl = new AbortController()
            currentAbort = ctrl
            const res = await coreClient.callTool(
              'voice.on_transcript_turn',
              {call_id: sideband.callId, turn_id: delta.turnId, transcript: delta.transcript},
              {timeoutMs, signal: ctrl.signal},
            )
            currentAbort = null
            if (!res || res.ok !== true || !('instructions_update' in res)) {
              log.warn({event: 'slow_brain_bad_response', call_id: sideband.callId})
              continue
            }
            if (typeof res.instructions_update === 'string' && res.instructions_update.length > 0) {
              updateInstructions(sideband, res.instructions_update, log)
              turnsSinceUpdate = 0
            }
          } catch (e) {
            currentAbort = null
            const err = e as Error
            log.warn({event: 'slow_brain_degraded', call_id: sideband.callId, reason: err?.message ?? 'unknown'})
          }
        }
      }

      void runLoop()

      return {
        push(delta) {
          if (queue.length >= queueMax) {
            const dropped = queue.shift()
            log.warn({event: 'slow_brain_backpressure', call_id: sideband.callId, queue_depth: queue.length + 1, dropped_turn_id: dropped?.turnId})
          }
          queue.push(delta)
        },
        async stop() {
          running = false
          currentAbort?.abort()
        },
      }
    }

    function sleep(ms: number): Promise<void> {
      return new Promise((r) => setTimeout(r, ms))
    }
    ```

    Update `tests/slow-brain.test.ts`:
    - Ersetze `anthropicClient: {messages: {create: vi.fn()}}` durch `coreClient: {callTool: vi.fn()}`.
    - `callTool` mock-returns `{ok:true, instructions_update:'neue instr'}`.
    - Cadence-Cap-Case: 4 pushes, cap=2 -> `callTool.mock.calls.length` <= 2.
    - Back-pressure-Case: queueMax=2, 5 pushes, `slow_brain_backpressure` WARN geloggt.
    - Graceful-degrade-Case: `callTool` rejects `new Error('timeout')` -> `slow_brain_degraded` WARN geloggt, kein throw.
    - NEUER Case: `slow_brain_disabled` — beide `opts.coreClient` und `process.env.CORE_MCP_URL` unset -> Worker startet, pushes kommen rein, `callTool` wird nie aufgerufen (weil es keinen Default-Client gibt) und `slow_brain_disabled` INFO wurde geloggt.
    - NEUER Case: bad-response — `callTool` liefert `{ok:false}` -> `slow_brain_bad_response` WARN, kein updateInstructions-Aufruf.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge && npx tsc --noEmit && npx vitest run tests/slow-brain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@anthropic-ai/sdk" /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts` returns 0
    - `grep -c "anthropicClient" /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts` returns 0
    - `grep -c "voice.on_transcript_turn" /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts` returns at least 1
    - `grep -c "slow_brain_degraded" /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts` returns 1
    - `grep -c "slow_brain_disabled" /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts` returns 1
    - `grep -c "slow_brain_backpressure" /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts` returns 1
    - `grep -c "updateInstructions" /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts` returns at least 1
    - `grep -c "it\(" /home/carsten_bot/nanoclaw/voice-bridge/tests/slow-brain.test.ts` returns at least 5
    - `cd /home/carsten_bot/nanoclaw/voice-bridge && npx tsc --noEmit` exits 0
    - `cd /home/carsten_bot/nanoclaw/voice-bridge && npx vitest run tests/slow-brain.test.ts` exits 0
  </acceptance_criteria>
  <done>slow-brain.ts frei von Anthropic-SDK; Core-MCP-Wiring; alle cases incl. zwei neuer abgedeckt.</done>
</task>

<task type="auto">
  <name>Task 09-04: package.json Dep-Cleanup — @anthropic-ai/sdk entfernen wenn nicht anderweitig genutzt</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/package.json
    /home/carsten_bot/nanoclaw/voice-bridge/package-lock.json
  </files>
  <read_first>
    - /home/carsten_bot/nanoclaw/voice-bridge/package.json
    - Rueckwaerts-Grep: `cd /home/carsten_bot/nanoclaw/voice-bridge && grep -rn "@anthropic-ai/sdk" src/ tests/` — erwartet: 0 Treffer nach Task 09-03.
  </read_first>
  <action>
    Step 1 — Verifikation dass keine Imports mehr existieren:
    ```bash
    cd /home/carsten_bot/nanoclaw/voice-bridge
    grep -rn "@anthropic-ai/sdk" src/ tests/
    # erwartet: leer
    ```
    Step 2 — Wenn Step 1 leer: `npm uninstall @anthropic-ai/sdk` + `npm install` fuer lockfile-update.
    Step 3 — tsc + vitest full: `npx tsc --noEmit && npx vitest run`.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge && grep -c '"@anthropic-ai/sdk"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@anthropic-ai/sdk"' /home/carsten_bot/nanoclaw/voice-bridge/package.json` returns 0
    - `cd /home/carsten_bot/nanoclaw/voice-bridge && grep -rn "@anthropic-ai/sdk" src/ tests/` returns empty
    - `cd /home/carsten_bot/nanoclaw/voice-bridge && npx tsc --noEmit` exits 0
    - `cd /home/carsten_bot/nanoclaw/voice-bridge && npx vitest run` exits 0 (alle bisherigen Tests bleiben gruen)
  </acceptance_criteria>
  <done>voice-bridge/package.json frei von Anthropic-SDK-Dependency; lockfile synchron; full-suite gruen.</done>
</task>

<task type="auto">
  <name>Task 09-05: Env-Cleanup — ANTHROPIC_API_KEY aus voice-bridge .env / dotenv-sample / docs entfernen</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/.env  # wenn existent
    /home/carsten_bot/nanoclaw/voice-bridge/.env.sample  # wenn existent
    /home/carsten_bot/nanoclaw/voice-bridge/README.md  # wenn existent
    /home/carsten_bot/nanoclaw/voice-bridge/systemd/voice-bridge.service  # cross-check (sollte nicht enthalten)
  </files>
  <read_first>
    - Rueckwaerts-Grep: `cd /home/carsten_bot/nanoclaw/voice-bridge && grep -rn "ANTHROPIC_API_KEY"` — alle Treffer listen.
  </read_first>
  <action>
    Fuer jeden Treffer:
    1. In `.env` (wenn existent): Zeile `ANTHROPIC_API_KEY=...` loeschen. NICHT das ganze .env ueberschreiben — nur die Zeile.
    2. In `.env.sample` / `.env.example` / `dotenv-sample`: Zeile entfernen + Kommentar-Zeile.
    3. In README/docs: Referenzen durch Hinweis auf `CORE_MCP_URL` (Plan 02-09) ersetzen oder entfernen.
    4. systemd-unit: cross-check dass `ANTHROPIC_API_KEY` nicht inline gesetzt ist (nur via EnvironmentFile).

    Wenn Plan-Autor (beim Execute) feststellt dass `.env` IM `/home/carsten_bot/nanoclaw/` root liegt (shared mit anderen services wie NanoClaw-Core): **stopp, Frage an Carsten ueber open_points** — soll die Zeile fuer die Core-Seite erhalten bleiben? Dann nur voice-bridge-spezifische Dokumentation bereinigen, nicht die Zeile selbst.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge && grep -rn "ANTHROPIC_API_KEY" .env .env.sample README.md docs/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/carsten_bot/nanoclaw/voice-bridge && grep -rn "ANTHROPIC_API_KEY" src/ tests/` returns empty (schon von 09-04 abgedeckt, Double-check)
    - voice-bridge-lokales .env (wenn existent) enthaelt ANTHROPIC_API_KEY nicht mehr
    - voice-bridge-lokales .env.sample (wenn existent) enthaelt ANTHROPIC_API_KEY nicht mehr
    - falls `.env` im NanoClaw-Root shared: nur Dokumentation in voice-bridge-Sub-Ordner bereinigt, keine zentrale .env-Aenderung (Frage an Carsten im open_points geloggt)
  </acceptance_criteria>
  <done>Kein ANTHROPIC_API_KEY mehr im voice-bridge-Scope. Shared .env-Entscheidung dokumentiert wenn applicable.</done>
</task>

<task type="auto">
  <name>Task 09-06: call-router.ts Integration-Check + Regression-Test fuer transcript-turn Pfad</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts
    /home/carsten_bot/nanoclaw/voice-bridge/tests/call-router.test.ts
  </files>
  <read_first>
    - /home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts (all lines — startSlowBrain-Aufruf bleibt unveraendert, aber Wiring an neuen Signatur-DI muss stimmen)
    - /home/carsten_bot/nanoclaw/voice-bridge/tests/call-router.test.ts
  </read_first>
  <action>
    Step 1 — call-router.ts: pruefen dass `startSlowBrain`-Aufruf keine Anthropic-spezifischen Opts mehr durchreicht. Falls doch (etwa ein historisch uebergebener `anthropicClient`-Hook) entfernen.

    Step 2 — Neuer Test-Case in `call-router.test.ts`:
    ```typescript
    it('retrofit 02-09: turn-end triggers core-mcp-call (not Anthropic)', async () => {
      const coreCall = vi.fn().mockResolvedValue({ok: true, instructions_update: 'neue instr'})
      const router = createCallRouter({
        // ... existing deps ...
        slowBrainFactory: (log, sideband) => startSlowBrain(log, sideband, {
          coreClient: {callTool: coreCall},
          cadenceCap: 1, // fire on first turn for test speed
        }),
      })
      await router.onTurnDone('rtc-9', 'turn-0', 'hallo claude')
      await new Promise((r) => setTimeout(r, 200))
      expect(coreCall).toHaveBeenCalledWith(
        'voice.on_transcript_turn',
        expect.objectContaining({call_id: 'rtc-9', turn_id: 'turn-0', transcript: 'hallo claude'}),
        expect.any(Object),
      )
    })
    ```
    (Detailwiring — `slowBrainFactory` oder analoges DI-Hook — der Executor muss beim Lesen von call-router.ts den exakten Hook-Punkt finden und adaptieren.)
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge && npx tsc --noEmit && npx vitest run tests/call-router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "anthropic" /home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts` returns 0 (case-insensitive greift auch auf Kommentare, die muessen auch weg)
    - `grep -c "on_transcript_turn\|coreClient" /home/carsten_bot/nanoclaw/voice-bridge/tests/call-router.test.ts` returns at least 1
    - `cd /home/carsten_bot/nanoclaw/voice-bridge && npx vitest run tests/call-router.test.ts` exits 0
  </acceptance_criteria>
  <done>call-router ruft neuen Slow-Brain-Pfad ueber MCP; Regression-Test dokumentiert das Wiring.</done>
</task>

<task type="auto">
  <name>Task 09-07: Full-suite Regression + Replay-Harness + audio-guard</name>
  <files>
    (keine — nur Verifikation)
  </files>
  <action>
    Step 1: `cd /home/carsten_bot/nanoclaw/voice-bridge && npx tsc --noEmit`
    Step 2: `cd /home/carsten_bot/nanoclaw/voice-bridge && npx vitest run`  # volle Suite, inkl. replay-harness-Tests aus Plan 02-08
    Step 3: `cd /home/carsten_bot/nanoclaw/voice-bridge && ./scripts/audio-guard.sh` (falls existent — Plan 02-03 hat audio-hygiene-guard gebracht)
    Step 4: CI-Simulation: `cd /home/carsten_bot/nanoclaw && git diff --name-only | xargs grep -l 'anthropic' || echo 'clean'`
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw/voice-bridge && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - tsc clean (exit 0)
    - vitest full-suite exit 0 (inkl. replay-harness aus 02-08)
    - audio-guard clean (wenn vorhanden)
    - git diff in voice-bridge enthaelt keine 'anthropic' oder 'ANTHROPIC' Token
  </acceptance_criteria>
  <done>Full-suite gruen nach Migration; Retrofit vollstaendig.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries (neu ggue. Plan 02-05)

| Boundary | Description | Transport | Auth |
|----------|-------------|-----------|------|
| voice-bridge → NanoClaw-Core (MCP) | Outbound HTTP POST ueber WireGuard 10.0.0.0/24 (Lenovo1). Ersetzt die vorherige Anthropic-Public-API-Boundary. | WG-only; AC-08-konform (nur WG-Peer 10.0.0.2) | TBD — Chat-Frage #1. Default: WG-only (Network-level). Optional: Bearer-Token `CORE_MCP_TOKEN`. |
| voice-bridge → OpenAI Realtime (sideband) | unveraendert zu Plan 02-05 | Public TLS | Bearer `OPENAI_SIP_API_KEY` |

## STRIDE-Scan (neu, Boundary voice-bridge → Core-MCP)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Spoofing | Fremder Host gibt sich als Core aus | mitigate | WireGuard-Peer-Authentication (AC-08). `CORE_MCP_URL` enthaelt nur `10.0.0.2:...` — niemand sonst im WG-Netz darf lauschen (WG-Konfig = Peering-gated). |
| T-02-09-02 | Tampering | MitM zwischen Bridge und Core | mitigate | Transport durch WG verschluesselt. Zusaetzlich optional HTTPS innerhalb WG — Entscheidung durch Chat-Frage #1. |
| T-02-09-03 | Repudiation | Core-Logs fuer Bridge-Calls fehlen | accept | JSONL-Turn-Timing (Plan 02-06) in Bridge protokolliert Request; Core-Seite ist separater Plan-Scope. |
| T-02-09-04 | Information Disclosure | Transcript verlaesst Bridge → Core ueber Netz | mitigate | WireGuard-Transport; kein Persist in Bridge. Transcript bleibt in NanoClaw-Agent-Scope (dort Hindsight-Speicher ohnehin vorgesehen, REQ-C6-02). |
| T-02-09-05 | DoS | Core antwortet langsam → Bridge-Worker haengt → Backlog | mitigate | `CORE_MCP_TIMEOUT_MS=8000` via AbortController (D-27 retention). Cadence-Cap (D-25) + Queue-Max (D-28) upstream. REQ-DIR-12 erfuellt: Hot-Path unberuehrt. |
| T-02-09-06 | DoS | Core ueberlastet durch zu viele Bridge-Calls | mitigate | Cadence-Cap (D-25) in Bridge = max 1 Call / 2 Turns. Bei geplanter Hochlast zusaetzliche Rate-Limits Core-seitig (separater Core-Plan). |
| T-02-09-07 | Elevation of Privilege | Transcript-Inhalt wird auf Core als MCP-Control-Channel interpretiert | mitigate | Core-seitige Tool-Allowlist: nur `voice.on_transcript_turn` (und Debug-Tool aus Frage #3) ist vom WG-Peer Bridge aufrufbar. Transcript-Text ist strikt `arguments.transcript` (String), kein Tool-Name. Schema-Validation Core-seitig pflicht (separater Core-Plan-Scope). |
| T-02-09-08 | Information Disclosure | `CORE_MCP_TOKEN` leakt via Logs | mitigate | Token wird in core-mcp-client NIE in Requests geloggt; AbortController-Errors enthalten URL aber nicht Header. |

## Bestehende Trust Boundaries (aus Plan 02-05, bleiben gueltig)

| Boundary | Status nach 09 |
|----------|----------------|
| Bridge → OpenAI Realtime sideband | unveraendert (Plan 02-05) |
| Bridge → Anthropic direct | **ENTFAELLT** — war alte Boundary, nach Retrofit entfernt |
| Slow-Brain output → sideband (tools-strip) | unveraendert (Plan 02-05 D-26) |

</threat_model>

<verification>
- `cd /home/carsten_bot/nanoclaw/voice-bridge && npx tsc --noEmit` exits 0
- `cd /home/carsten_bot/nanoclaw/voice-bridge && npx vitest run` exits 0 (voll-suite incl. 02-08 replay-harness)
- `grep -rn "@anthropic-ai/sdk" /home/carsten_bot/nanoclaw/voice-bridge/src /home/carsten_bot/nanoclaw/voice-bridge/tests` returns empty
- `grep -rn "ANTHROPIC_API_KEY" /home/carsten_bot/nanoclaw/voice-bridge` returns empty (falls shared-.env: dokumentierte Ausnahme)
- `grep -c "voice.on_transcript_turn" /home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts` returns at least 1
- `grep -c "voice.on_transcript_turn" /home/carsten_bot/nanoclaw/voice-bridge/tests/` returns at least 3 (slow-brain, core-mcp-client, call-router)
- Live-Smoke: bei gesetztem `CORE_MCP_URL` (Mock-Server auf localhost) -> ein PSTN-Test-Call laeuft ohne `slow_brain_disabled`, ein mid-call instructions-update wird via session.update gepusht (manuelle Verifikation durch Carsten).
</verification>

<success_criteria>
- voice-bridge Source-Tree ist FREI von `@anthropic-ai/sdk` und `ANTHROPIC_API_KEY` (einzige Ausnahme: shared .env im NanoClaw-Root, wenn vom User bestaetigt).
- Pro Turn-End wird `voice.on_transcript_turn` per MCP an NanoClaw-Core gerufen; `instructions_update` wird via bestehender Sideband-Logik gepusht.
- Cadence-Cap (D-25), Back-pressure (D-28), Timeout (D-27), Graceful-degrade (REQ-DIR-12) bleiben verhaltensidentisch zu Plan 02-05 — Ort der LLM-Inference aendert sich, Safety-Envelope bleibt.
- AC-09 und REQ-INFRA-12 wiederhergestellt: Bridge ist MCP-Proxy, Business-Logik in Core.
- Alle bisherigen Tests (sideband, slow-brain, webhook, call-router, replay-harness) gruen.
- STRIDE-Scan des neuen Boundaries dokumentiert; keine Accept-Dispositions ohne Begruendung.
</success_criteria>

<output>
After completion, create `_planning/phases/02-director-bridge-v0-hotpath-safety/02-09-SUMMARY.md` covering:
- migrierte Dateien + Dependency-Deltas
- Test-Matrix (mindestens 9 cases: 4 core-mcp-client, 5+ slow-brain, 1 call-router regression)
- Entscheidungen, die Chat im Pre-Gating beantwortet hat (Core-MCP-URL, Tool-Name, Auth-Mechanismus)
- Offene Themen fuer Phase 3 (wann wird Core-Seite implementiert, wer baut den NanoClaw-Voice-Channel)
- Commit-Hashes
</output>
