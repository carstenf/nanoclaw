---
phase: 02-director-bridge-v0-hotpath-safety
plan: 10
type: execute
wave: 7
depends_on:
  - "02-05"  # fixes gap in 02-05 slow-brain that was never wired to WS events
  - "02-09"  # retrofit on top, keeps MCP-call path
files_modified:
  - /home/carsten_bot/nanoclaw/voice-bridge/src/sideband.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/tests/sideband.test.ts
  - /home/carsten_bot/nanoclaw/voice-bridge/tests/call-router.test.ts
autonomous: true  # Chat-GO 4698d58, Briefing Auto-GO §4 Retrofit/Cleanup
requirements:
  - DIR-02
  - DIR-06
  - DIR-11
tags:
  - slow-brain
  - sideband-ws
  - turn-ingestion
  - gap-fix

must_haves:
  truths:
    - "sideband.ts hat `ws.on('message', ...)`-Handler, der JSON parst und auf `conversation.item.input_audio_transcription.completed`-Events filtert (Chat-Entscheidung 4698d58 Kandidat A)."
    - "Per completed-Event: `onTranscriptTurn(turnId, transcript)`-Callback (falls in SidebandOpenOpts gesetzt). turnId = `event.item_id` oder gleichwertiger stable key; transcript = `event.transcript` (letzter User-Satz, kein delta-akkumuliert)."
    - "`transcription.delta`-Events und alle anderen Event-Typen werden ignoriert — kein Handler-Aufruf."
    - "JSON-parse-Fehler und fehlende Felder werden gefangen + WARN-logged (event:`sideband_message_parse_failed`), niemals re-thrown — WS-Message-Loop darf nicht crashen."
    - "call-router.ts wired `onTranscriptTurn: (turnId, transcript) => ctx.slowBrain.push({turnId, transcript})` im sideband-factory-Opts bei startCall."
    - "Graceful: wenn slowBrain-Worker no-op ist (Disabled-Mode von 02-09), push ist harmlos (Worker-Interface garantiert's)."
    - "Test sideband.test.ts: neuer Case — MockWS emits 'message' mit realistischem completed-event-payload → onTranscriptTurn wird mit (item_id, transcript) aufgerufen."
    - "Test sideband.test.ts: neuer Case — delta-event und andere Event-Typen triggern KEIN Callback."
    - "Test sideband.test.ts: neuer Case — kaputtes JSON in message triggert WARN + kein Crash."
    - "Test call-router.test.ts: neuer Case — sideband-factory bekommt einen `onTranscriptTurn`-Callback, der `ctx.slowBrain.push` aufruft (End-to-end verdrahten)."
  artifacts:
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/src/sideband.ts"
      provides: "SidebandOpenOpts.onTranscriptTurn + ws.on('message') handler + completed-event filter"
      contains: "input_audio_transcription.completed"
    - path: "/home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts"
      provides: "sideband-factory-opts enthaelt onTranscriptTurn der zu slowBrain.push routed"
      contains: "onTranscriptTurn"
  key_links:
    - from: "OpenAI Realtime Sideband WS 'message'"
      to: "sideband.ts message-handler"
      via: "JSON.parse + filter type=='conversation.item.input_audio_transcription.completed'"
      pattern: "input_audio_transcription\\.completed"
    - from: "sideband.onTranscriptTurn callback"
      to: "call-router -> slowBrain.push"
      via: "DI in call-router.startCall sideband-factory-opts"
      pattern: "onTranscriptTurn"

---

<objective>
Schliesse die in Plan 02-05 nicht implementierte Luecke: Sideband-WS consumiert OpenAI-Realtime-Events. Ohne diese Wiring bleibt der Slow-Brain-Worker ungefuettert — die ganze Retrofit-Kette (02-09 + 03-02) ist wirkungslos auf echten Calls.

Scope: Minimaler Event-Handler im sideband.ts + Callback-Wiring im call-router.ts. Kein Inference-Change. Chat-Entscheidung: Kandidat A (user-transcription-completed), transcript-delta = einzelner User-Satz.
</objective>

<execution_context>
@/home/carsten_bot/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/carsten_bot/nanoclaw/voice-bridge/src/sideband.ts
@/home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts
@/home/carsten_bot/nanoclaw/voice-bridge/src/slow-brain.ts

<interfaces>

OpenAI Realtime event shape (aus docs, verifiziert in prior live-calls):
```json
{
  "type": "conversation.item.input_audio_transcription.completed",
  "item_id": "item_xyz",
  "content_index": 0,
  "transcript": "hallo ich haette gerne einen termin"
}
```

delta-shape (zu ignorieren):
```json
{
  "type": "conversation.item.input_audio_transcription.delta",
  "item_id": "item_xyz",
  "delta": "halt"
}
```

Neue SidebandOpenOpts-Signatur:
```typescript
export interface SidebandOpenOpts {
  wsFactory?: (url, headers) => WSType
  urlTemplate?: string
  apiKey?: string
  onClose?: (callId: string) => void
  onTranscriptTurn?: (turnId: string, transcript: string) => void  // NEU
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-10-01: sideband.ts message-handler + tests</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/src/sideband.ts
    /home/carsten_bot/nanoclaw/voice-bridge/tests/sideband.test.ts
  </files>
  <action>
    1. SidebandOpenOpts um `onTranscriptTurn?: (turnId: string, transcript: string) => void` erweitern.
    2. In openSidebandSession: `ws.on('message', (raw) => { ... })` Handler, der:
       - `raw.toString()` JSON.parse (try/catch mit WARN-log event:`sideband_message_parse_failed` + `call_id` + `err`)
       - Nur wenn parsed.type === 'conversation.item.input_audio_transcription.completed' UND typeof parsed.transcript === 'string': ruft `opts.onTranscriptTurn?.(parsed.item_id ?? 'unknown', parsed.transcript)`
       - Alles andere: silent ignore (kein Log pro non-trigger-event — wird sonst Log-Flood)
    3. Tests: 4 neue Cases — (a) completed-event triggert callback, (b) delta-event triggert NICHT, (c) anderes event-type ignoriert, (d) kaputtes JSON → WARN + kein throw.
  </action>
  <acceptance_criteria>
    - `grep -c "onTranscriptTurn" /home/carsten_bot/nanoclaw/voice-bridge/src/sideband.ts` >= 2
    - `grep -c "input_audio_transcription.completed" /home/carsten_bot/nanoclaw/voice-bridge/src/sideband.ts` >= 1
    - `grep -c "sideband_message_parse_failed" /home/carsten_bot/nanoclaw/voice-bridge/src/sideband.ts` >= 1
    - `grep -c "it\(" /home/carsten_bot/nanoclaw/voice-bridge/tests/sideband.test.ts` >= 9 (war 5, +4 neu)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run tests/sideband.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 02-10-02: call-router.ts wiring + test</name>
  <files>
    /home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts
    /home/carsten_bot/nanoclaw/voice-bridge/tests/call-router.test.ts
  </files>
  <action>
    1. In call-router.startCall: `fSide(callId, log, { onClose, onTranscriptTurn })` wo `onTranscriptTurn = (turnId, transcript) => ctx.slowBrain.push({turnId, transcript})`. Achtung: ctx wird NACH fSide gebaut — also entweder deferred-ref via Closure oder Setter-Pattern. Bevorzugt: push durch Map-Lookup `router.getCall(callId)?.slowBrain.push(...)`.
    2. Test-Case: mock sideband-factory speichert die opts; dann opts.onTranscriptTurn('t0', 'hallo') aufrufen und asserten dass slowBrainPush mit {turnId:'t0', transcript:'hallo'} aufgerufen wurde.
  </action>
  <acceptance_criteria>
    - `grep -c "onTranscriptTurn" /home/carsten_bot/nanoclaw/voice-bridge/src/call-router.ts` >= 1
    - `grep -c "onTranscriptTurn" /home/carsten_bot/nanoclaw/voice-bridge/tests/call-router.test.ts` >= 1
    - `npx tsc --noEmit` exits 0
    - `npx vitest run tests/call-router.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 02-10-03: full suite + live-smoke simuliert</name>
  <action>
    1. `npx tsc --noEmit && npx vitest run` voll gruen
    2. `npm run build && systemctl --user restart voice-bridge`
    3. `journalctl --user -u voice-bridge --since '1min ago' | grep bridge_listening` vorhanden
    4. Synthetischer Smoke-Test: kleines Test-Script das eine mock-Sideband-WS-message sendet → ist durch Tests schon covered. Live-PSTN-Call ist naechster echter Check.
    5. `curl -sS http://10.0.0.2:3200/health` via Hetzner weiterhin 200.
  </action>
  <acceptance_criteria>
    - tsc + vitest exit 0
    - voice-bridge systemd active
    - nanoclaw health endpoint 200 (unveraendert)
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- sideband.ts reagiert auf message-events und triggert onTranscriptTurn bei user-transcript-completed.
- call-router wired das zu slowBrain.push → Worker bekommt pro User-Turn einen delta.
- Tests: voice-bridge-Suite waechst um mindestens 5 neue Cases, alle gruen.
- Naechster PSTN-Call: JSONL-Eintraege `transcript_turn_received` in nanoclaw data/voice-slow-brain.jsonl erscheinen pro User-Turn.
- 03-02 kann danach den Stub durch echte Inference ersetzen und die Kette ist voll funktional.
</success_criteria>

<output>
`.planning/phases/02-director-bridge-v0-hotpath-safety/02-10-SUMMARY.md` nach Execution: commit-Hashes, Test-Evidence, live-smoke-Output, Hinweis dass Live-PSTN-Test pending ist.
</output>
