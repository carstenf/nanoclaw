---
phase: 03-voice-mcp-endpoint
plan: 09
type: execute
wave: 9
priority: critical  # blockt PSTN-Combined-Test
depends_on:
  - "03-03"  # Calendar-Tools
  - "03-04"  # Discord-Tool
  - "03-06"  # Contract + Practice-Profile
  - "03-07"  # Schedule-Retry
files_modified:
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.test.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.test.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-send-discord-message.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-send-discord-message.test.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-get-contract.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-get-contract.test.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-get-practice-profile.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-get-practice-profile.test.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-schedule-retry.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-schedule-retry.test.ts
  - /home/carsten_bot/nanoclaw/src/config.ts  # ggf. TZ-Default-Handling zentralisieren
autonomous: true  # Retrofit/Cleanup per Briefing §4 — Core conformiert zu REQ-TOOLS-Spec
requirements:
  - TOOLS-01
  - TOOLS-02
  - TOOLS-03
  - TOOLS-04
  - TOOLS-06
  - TOOLS-07
  - DIR-11

must_haves:
  truths:
    - "Root-cause der 400-Fehler im PSTN-Test 16:54 (call_id `rtc_u0_DW3DxfuDEr7U4FUjghq6X`): systematischer Schema-Drift. Bridge-Schemas (REQ-TOOLS-konform, conversational) wurden in 02-07 korrekt nach REQUIREMENTS.md §TOOLS-01..07 gebaut. Core-zod-Schemas wurden in 03-03..03-07 von mir API-nah (Google/Discord) gebaut ohne die Spec gegenzupruefen. Bridge sendet `{date, duration_minutes}`, Core erwartet `{timeMin, timeMax}` → 400."
    - "Fix: **Core-Handler refactoren damit sie REQ-TOOLS-konforme Args akzeptieren und die REQ-konformen Return-Shapes liefern**. Die API-nahen Schemas (Google Calendar `timeMin`/`timeMax`) werden interne Implementation-Details, NICHT Schema-Contract."
    - "Bridge bleibt UNTOUCHED — ihre Schemas sind spec-compliant."
    - "get_travel_time + ask_core sind aus REQ-TOOLS NICHT drin (Zusaetzliche Tools) — bleiben wie sind. Drift betrifft nur 6 Tools."
    - "Tool 1 `check_calendar`: args `{date: string (YYYY-MM-DD), duration_minutes: int 1..1440}`, return `{available: boolean, conflicts: Array<{start, end, summary?}>}`. Logic: convert date → local-TZ day-window (00:00..23:59 Europe/Berlin), fetch events on this date, return `available=true` wenn es eine Luecke >= duration_minutes gibt, sonst `available=false`. `conflicts` ist die events-Liste des Tages. Simpel fuer v0 — kein Slot-Puzzle, nur 'wenn Tag mehr als duration Min frei hat → available'."
    - "Tool 2 `create_calendar_entry`: args `{title, date, time, duration, location?, travel_buffer_before_min?, travel_buffer_after_min?}`, return `{id: string}`. Idempotent: wenn Event mit gleicher title+date+time+duration schon existiert → return existing id (hash-based lookup, v0 via list-events-at-start + title-match). `date+time` → ISO8601-start. `start+duration (min)` → end. Travel buffers: wenn > 0 → 2 zusaetzliche Blocker-Events mit title='Anfahrt' + 'Rueckfahrt' (reverts to Caveat wenn v0 zu komplex)."
    - "Tool 3 `send_discord_message`: args `{channel: string, content: string}`, return `{ok: boolean}`. Idempotent per content-hash: in-memory Map<(channel+sha256(content)), ts> mit 5min TTL. Gleicher Content in 5min → skip+return ok:true (satisfies idempotency). `channel` ist Snowflake-String (gleiche allowlist)."
    - "Tool 4 `get_contract`: args `{provider_name: string}`, return `{current_conditions, expiry_date, last_review}`. Lookup: flat-db-Lookup by provider (case-insensitive). Wenn kein Match → alle drei Felder null, aber `ok:true`. Alt-Felder (`id`-lookup) werden entfernt."
    - "Tool 5 `get_practice_profile`: args `{name: string}`, return `{phone, patient_id, insurance_type, last_visit, authorized_data_fields: string[]}`. Lookup: flat-db-Lookup by name. `authorized_data_fields` ist ein Array der Feld-Namen die der Bot freigeben darf (Case-3-Arzt-Privacy). Wenn kein Match → alle Felder null + `authorized_data_fields:[]`."
    - "Tool 6 `schedule_retry`: args `{case_type: string, target_phone: string (E.164), not_before_ts: string (ISO)}`, return `{scheduled: boolean}`. Idempotent: wenn schon ein offener Task mit gleichen (case_type, target_phone, not_before_ts) existiert → return scheduled:true ohne neuen Insert. Internal: synthetisieren einen Task-prompt aus case_type+target_phone (`Retry call for case <case_type> to <phone>`)."
    - "Timezone: alle Date-Ops nutzen `Europe/Berlin` (aus existierender TIMEZONE env-const). ISO-Strings zu Google-API werden mit Offset gesendet."
    - "JSONL-Schemas bleiben PII-clean (keine neuen Felder, bestehende behalten aber an neue return-shapes angepasst wo noetig — z.B. `available` flag statt `result_count`)."
    - "Bestehende Tests werden komplett refactored — alter {timeMin,timeMax}-Pfad verschwindet, neuer {date, duration_minutes}-Pfad kommt rein. Test-Count bleibt ungefähr gleich."
    - "MCP-Registry (mcp-tools/index.ts) bleibt unveraendert — nur handler-internal Aenderung."
    - "Smoke (Task 06 letzte): Cross-host oder loopback curl gegen alle 6 refactored Tools mit REQ-konformen args. Erwarte 200 + REQ-konforme returns. Danach: STOP fuer erneuten PSTN-Combined-Test."
  artifacts:
    - path: "/home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.ts"
      provides: "Handler akzeptiert {date, duration_minutes}, return {available, conflicts[]}"
      contains: "duration_minutes"
    - path: "/home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.ts"
      provides: "Handler akzeptiert {title, date, time, duration, location?, travel_buffer_*?}, return {id}"
      contains: "travel_buffer"
    - path: "/home/carsten_bot/nanoclaw/src/mcp-tools/voice-send-discord-message.ts"
      provides: "Handler akzeptiert {channel, content}, return {ok}, content-hash-idempotent"
      contains: "content_hash\\|dedupe"
  key_links:
    - from: "check_calendar handler"
      to: "REQ-TOOLS-01 shape"
      via: "{date, duration_minutes} → {available, conflicts}"
      pattern: "available"
    - from: "create_calendar_entry handler"
      to: "REQ-TOOLS-02 shape"
      via: "{title, date, time, duration, travel_buffers} → {id}, idempotent"
      pattern: "travel_buffer"
    - from: "send_discord_message handler"
      to: "REQ-TOOLS-03 shape"
      via: "{channel, content} → {ok}, content-hash dedup"
      pattern: "content_hash"

---

<objective>
Core-Handler refactoren damit sie exakt die REQ-TOOLS-01..07-Schema-Contracts erfuellen. Heilt den systematischen Drift der zwischen Bridge (spec-compliant seit 02-07) und Core (API-nah seit 03-03..03-07) entstanden ist. Single source of truth = REQUIREMENTS.md §TOOLS.

Purpose: Der erste PSTN-Test (call_id `rtc_u0_DW3DxfuDEr7U4FUjghq6X`, 16:54) hat zwei 400-Errors produziert (`check_calendar`, `send_discord_message`) weil Bridge-Args != Core-Zod-Args. Nach diesem Plan passen die Shapes auf beiden Seiten, PSTN-Test geht durch.

Output: 6 Handler refactored + Tests angepasst + Smokes bewiesen. Keine Aenderung an Bridge-Side. Phase 03 bleibt bei 9 Tools aber mit klaren Contracts.
</objective>

<execution_context>
@/home/carsten_bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/carsten_bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/carsten_bot/nanoclaw-state/.planning/REQUIREMENTS.md  # Zeilen 80-87 REQ-TOOLS-*
@/home/carsten_bot/nanoclaw-state/open_points.md   # 16:54 PSTN + schema-diff-table
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/check_calendar.json  # spec reference
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/create_calendar_entry.json
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/send_discord_message.json
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/get_contract.json
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/get_practice_profile.json
@/home/carsten_bot/nanoclaw/voice-bridge/src/tools/schemas/schedule_retry.json
@/home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.ts    # aktuelle API-nah Impl
@/home/carsten_bot/nanoclaw/src/mcp-tools/calendar-client.ts
@/home/carsten_bot/nanoclaw/src/config.ts  # TIMEZONE='Europe/Berlin'

<interfaces>

<!-- REQ-TOOLS-01 -->
```typescript
// check_calendar
args: {date: string (YYYY-MM-DD), duration_minutes: number}
return: {ok: true, result: {available: boolean, conflicts: Array<{start, end, summary}>}}
         | {ok: false, error: string}
```

<!-- REQ-TOOLS-02 -->
```typescript
// create_calendar_entry
args: {
  title: string (1..200)
  date: string (YYYY-MM-DD)
  time: string (HH:mm, 24h)
  duration: number (minutes, 1..1440)
  location?: string (max 200)
  travel_buffer_before_min?: number (0..180)
  travel_buffer_after_min?: number (0..180)
}
return: {ok: true, result: {id: string}} | {ok: false, error: string}
// Idempotency: title+date+time+duration match → return existing id
```

<!-- REQ-TOOLS-03 -->
```typescript
// send_discord_message
args: {channel: string (snowflake), content: string (1..4000)}
return: {ok: true, result: {ok: boolean}} | {ok: false, error: string}
// Idempotency via in-memory Map<channel+sha256(content), ts>, 5min TTL
```

<!-- REQ-TOOLS-04 -->
```typescript
// get_contract
args: {provider_name: string (1..200)}
return: {ok: true, result: {current_conditions: string|null, expiry_date: string|null, last_review: string|null}}
```

<!-- REQ-TOOLS-06 -->
```typescript
// get_practice_profile
args: {name: string (1..200)}
return: {ok: true, result: {phone: string|null, patient_id: string|null, insurance_type: string|null, last_visit: string|null, authorized_data_fields: string[]}}
```

<!-- REQ-TOOLS-07 -->
```typescript
// schedule_retry
args: {case_type: string, target_phone: string (E.164), not_before_ts: string (ISO)}
return: {ok: true, result: {scheduled: boolean}}
// Idempotency: (case_type, target_phone, not_before_ts) trio match existing active task → scheduled:true without insert
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-09-01: voice-check-calendar.ts — refactor zu REQ-TOOLS-01 shape</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.test.ts
  </files>
  <behavior>
    - Zod: `{call_id?, date: /^\d{4}-\d{2}-\d{2}$/, duration_minutes: 1..1440}`.
    - Intern: date → `start: <date>T00:00:00+02:00`, `end: <date>T23:59:59+02:00` (Europe/Berlin default, verwende TIMEZONE config).
    - events.list(calendarId='primary', timeMin=start, timeMax=end, singleEvents:true, orderBy:'startTime', maxResults=50).
    - Berechne `available`: totale-Tages-Minuten (24h = 1440) minus Summe-aller-event-Dauern. Wenn remaining >= duration_minutes → available=true. Note: v0 simpel — kein Puzzle ob kontinuierliche Luecke existiert; nur "Tag hat mehr freie Zeit als verlangt".
    - `conflicts`: `events.map(e => ({start, end, summary}))`.
    - JSONL Event bleibt `calendar_check_done`, nur Felder angepasst (`result_count` → `available:boolean`, `conflicts_count`).
    - Tests (mind 6): happy path available, happy path NOT available (Tag voll), invalid date format, duration zu gross/0, events empty → available=true, zod reject empty args.
  </behavior>
  <acceptance_criteria>
    - `grep -c "duration_minutes" src/mcp-tools/voice-check-calendar.ts` >= 3
    - `grep -c "\"available\"" src/mcp-tools/voice-check-calendar.ts` >= 1
    - `grep -c "conflicts" src/mcp-tools/voice-check-calendar.ts` >= 1
    - `grep -c "it\(" src/mcp-tools/voice-check-calendar.test.ts` >= 6
    - `npx vitest run src/mcp-tools/voice-check-calendar.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 03-09-02: voice-create-calendar-entry.ts — refactor zu REQ-TOOLS-02 shape + idempotency</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.test.ts
  </files>
  <behavior>
    - Zod: `{call_id?, title: 1..200, date: YYYY-MM-DD, time: /^\d{2}:\d{2}$/, duration: 1..1440, location?: max 200, travel_buffer_before_min?: 0..180, travel_buffer_after_min?: 0..180}`.
    - Baue `start = <date>T<time>:00+02:00`, `end = start + duration_minutes`.
    - Idempotenz: VOR dem insert, `events.list` im start..end Fenster; wenn ein Event mit summary==title + gleicher start-Zeit existiert → return existing.id.
    - Insert-Event body: `{summary: title, start: {dateTime: start, timeZone: 'Europe/Berlin'}, end: {dateTime: end, timeZone: 'Europe/Berlin'}, location?}`.
    - Travel-Buffers (v0 caveat): wenn before_min > 0, zusaetzliches Event im Fenster (start-before..start) title='Anfahrt' — EINFACHE Umsetzung, kein Rollback wenn 2. insert failed. Gleiches fuer after_min. **Caveat dokumentieren: travel_buffer-Events sind non-atomic mit main-event — wenn 2. insert crash, main bleibt.**
    - Return: `{id: primaryEvent.id}`.
    - JSONL: `calendar_create_done` wie bisher, neues Feld `was_duplicate: boolean`.
    - Tests (mind 6): happy (new event), idempotent (zweiter call same-time-title → same id, kein zweiter Google-insert), invalid date/time format, duration zu gross, travel_buffer_before > 0 creates 2 events, zod rejects.
  </behavior>
  <acceptance_criteria>
    - `grep -c "travel_buffer_before_min\|travel_buffer_after_min" src/mcp-tools/voice-create-calendar-entry.ts` >= 2
    - `grep -c "duplicate\|was_duplicate" src/mcp-tools/voice-create-calendar-entry.ts` >= 1
    - `grep -c "it\(" src/mcp-tools/voice-create-calendar-entry.test.ts` >= 6
    - `npx vitest run src/mcp-tools/voice-create-calendar-entry.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 03-09-03: voice-send-discord-message.ts — refactor zu REQ-TOOLS-03 shape + content-hash-dedup</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-send-discord-message.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-send-discord-message.test.ts
  </files>
  <behavior>
    - Zod: `{call_id?, channel: string (/^\d{17,20}$/ snowflake), content: 1..4000}`.
    - Feld-Rename: `channel_id` → `channel`, `text` → `content`. Allowlist-Check auf `channel` (wie vorher).
    - Idempotency: in-memory Map<channel+sha256(content), ts>, TTL 5min (`SEND_DISCORD_DEDUP_TTL_MS` env, default 300000). Wenn key existiert + nicht abgelaufen → return `{ok:true}` ohne an Discord zu senden. Logge `discord_message_deduplicated` event.
    - Return bei nicht-dedup: `{ok:true, result:{ok: deliveryResult}}`.
    - JSONL-Events: `discord_message_sent` + `discord_message_failed` + NEU `discord_message_deduplicated`. Fields: channel, content_hash (first 8 chars hex), latency_ms. NO content-text.
    - Tests (min 6): happy (allowlisted + send), dedup-second (gleicher content < 5min → kein Discord-send, return ok=true, dedup-event), dedup-expired (> 5min → neu send), channel_not_allowed, invalid snowflake, content empty.
  </behavior>
  <acceptance_criteria>
    - `grep -c "content_hash\|createHash" src/mcp-tools/voice-send-discord-message.ts` >= 1
    - `grep -c "discord_message_deduplicated" src/mcp-tools/voice-send-discord-message.ts` >= 1
    - `grep -Ec "\"channel\"|\"content\"" src/mcp-tools/voice-send-discord-message.ts` >= 2
    - `grep -c "it\(" src/mcp-tools/voice-send-discord-message.test.ts` >= 6
    - `npx vitest run src/mcp-tools/voice-send-discord-message.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 03-09-04: voice-get-contract.ts — refactor zu REQ-TOOLS-04 shape</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-get-contract.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-get-contract.test.ts
    /home/carsten_bot/nanoclaw/data/contracts.example.json   # update schema to match REQ-04 fields
  </files>
  <behavior>
    - Zod: `{call_id?, provider_name: 1..200}`.
    - Lookup: contracts.json `{contracts:[{provider, ...}]}` — case-insensitive match `provider.toLowerCase().includes(query.toLowerCase())`.
    - Return-Felder: `{current_conditions, expiry_date, last_review}` — alle optional strings, null wenn nicht-gefunden.
    - contracts.example.json schema update: fuege Felder `current_conditions, expiry_date, last_review` hinzu zum Beispiel-Eintrag.
    - Tests (mind 5): happy (provider match, alle Felder zurueck), no-match (alle null), empty provider_name (400), case-insensitive match, file-missing.
  </behavior>
  <acceptance_criteria>
    - `grep -c "provider_name" src/mcp-tools/voice-get-contract.ts` >= 2
    - `grep -c "current_conditions\|expiry_date\|last_review" src/mcp-tools/voice-get-contract.ts` >= 3
    - `grep -c "it\(" src/mcp-tools/voice-get-contract.test.ts` >= 5
    - `npx vitest run src/mcp-tools/voice-get-contract.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 03-09-05: voice-get-practice-profile.ts — refactor zu REQ-TOOLS-06 shape</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-get-practice-profile.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-get-practice-profile.test.ts
    /home/carsten_bot/nanoclaw/data/practice-profile.example.json
  </files>
  <behavior>
    - Zod: `{call_id?, name: 1..200}`.
    - Lookup: practice-profile.json `{profiles:{<key>:{name, ...}}}` — case-insensitive name-match.
    - Return-Felder: `{phone, patient_id, insurance_type, last_visit, authorized_data_fields}`. Alle optional, `authorized_data_fields` default `[]`.
    - Schema-update in example.json analog.
    - Tests (mind 5): happy, no-match (alle null + [] authorized_fields), empty name (400), case-insensitive match, file-missing.
  </behavior>
  <acceptance_criteria>
    - `grep -Ec "\"name\":" src/mcp-tools/voice-get-practice-profile.ts` >= 1  # zod
    - `grep -c "phone\|patient_id\|insurance_type\|last_visit\|authorized_data_fields" src/mcp-tools/voice-get-practice-profile.ts` >= 5
    - `grep -c "it\(" src/mcp-tools/voice-get-practice-profile.test.ts` >= 5
    - `npx vitest run src/mcp-tools/voice-get-practice-profile.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 03-09-06: voice-schedule-retry.ts — refactor zu REQ-TOOLS-07 shape + idempotency</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-schedule-retry.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-schedule-retry.test.ts
  </files>
  <behavior>
    - Zod: `{call_id?, case_type: string (1..64), target_phone: /^\+\d{8,15}$/, not_before_ts: ISO}`.
    - Bounds-Check: not_before_ts > now, < now+30d.
    - Synthesize prompt internal: `Retry for case '<case_type>', target: <target_phone>. Scheduled by voice at <call_id or 'voice'>.`
    - Idempotency: deps.getAllTasks() → find active task wo prompt contains `case '<case_type>'` AND `<target_phone>` AND same not_before_ts → return `{scheduled: true}` ohne neuen insert, JSONL event `retry_scheduled_deduplicated`.
    - Return: `{ok: true, result: {scheduled: true}}`.
    - Remove: old `prompt`-Arg (internal-only jetzt).
    - JSONL events: `retry_scheduled` + `retry_scheduled_deduplicated` + `retry_schedule_failed`.
    - Tests (mind 7): happy (new task insert), dedup (gleiches triple → scheduled:true ohne 2. insert), retry_at_in_past, retry_at_too_far, invalid phone E164, empty case_type, no_main_group.
  </behavior>
  <acceptance_criteria>
    - `grep -c "case_type\|target_phone\|not_before_ts" src/mcp-tools/voice-schedule-retry.ts` >= 6
    - `grep -c "retry_scheduled_deduplicated" src/mcp-tools/voice-schedule-retry.ts` >= 1
    - `grep -Ec "^\\+\\\\d\\{8,15\\}\\$" src/mcp-tools/voice-schedule-retry.ts` >= 1  # E164 regex
    - `grep -c "it\(" src/mcp-tools/voice-schedule-retry.test.ts` >= 7
    - `npx vitest run src/mcp-tools/voice-schedule-retry.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 03-09-07: Full-Suite + Deploy + Cross-Host Smoke</name>
  <files>(nur Ops)</files>
  <action>
    Step 1: `npx tsc --noEmit` + `npx vitest run` — erwartet 0 neu failing.
    Step 2: `npm run build`, `systemctl --user restart nanoclaw`, sleep 3, status active.
    Step 3: Cross-host curls von Hetzner (oder loopback wenn SSH unavail, analog 03-07) mit REQ-konformen args:
      - `check_calendar {date:"2026-04-20", duration_minutes:60}` → 200 + `{available:true|false, conflicts:[]}`
      - `create_calendar_entry {title:"03-09 Smoke", date:"2026-04-20", time:"15:00", duration:30}` → 200 + `{id}`
      - Zweiter call gleicher args → same id (idempotent)
      - `send_discord_message {channel:"1490365616518070407", content:"03-09 smoke dedup-test"}` → 200 ok:true
      - Zweiter call gleich content → dedup (JSONL `discord_message_deduplicated`)
      - `get_contract {provider_name:"Example AG"}` → 200 (example data matched case-insensitive)
      - `get_practice_profile {name:"Example Praxis"}` → 200
      - `schedule_retry {case_type:"test", target_phone:"+491708036426", not_before_ts:"<now+10min>"}` → 200 scheduled:true
      - Zweiter call gleich → scheduled:true + dedup-event
    Step 4: JSONL-PII-Checks (analog wie vorherige Plans).
    Step 5: Cleanup: Smoke-Event im Calendar + Discord-Message + Scheduled-Task manuell oder SQL.
  </action>
  <acceptance_criteria>
    - tsc + vitest sauber
    - systemctl nanoclaw active
    - Alle 8 Smoke-Requests 200 mit REQ-konformen shapes
    - Idempotenz-Beweis: dedup-events im JSONL
    - PII-clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

| Threat | Disposition | Mitigation |
|---|---|---|
| Breaking test-refactor regression | mitigate | Alle Test-Files bekommen komplett neue Inhalte — nicht partial. Pre-commit check: tsc + vitest full. |
| Idempotency-Cache-Leak (discord dedup-map) | accept | In-memory Map, TTL 5min, Worst-case ~10MB fuer 100k messages — unrealistisch in Case-6-Privat-Use. |
| Calendar-available-false-negative (Tag vollstaendig aber Luecke in Rumpfzeit) | accept | V0 simple logic, Case-6-UX-Review pro Tag wenn relevant. Bot kann bei Ambiguitaet nachfragen ("wann genau?"). |
| Travel-buffer-Atomic-Failure | accept + document | Caveat in SUMMARY: wenn main-event insert OK aber buffer-insert crash → orphan main. V2 transactional. |

</threat_model>

<success_criteria>
- 6 Handler REQ-TOOLS-01..07-konform.
- Bridge kann ohne Adapter direkt forwarden.
- Idempotency funktional fuer create_calendar + send_discord + schedule_retry.
- Alle Core-Tests gruen.
- Live-Smoke von Hetzner bestaetigt PSTN-ready.
</success_criteria>

<output>
`_planning/phases/03-voice-mcp-endpoint/03-09-SUMMARY.md`:
- Commits
- Smoke-Evidence (alle 6 Tools + idempotency-Beweis via dedup-events)
- Caveats (travel_buffer non-atomic, check_calendar v0-simple-logic)
- Next: STOP — 2. PSTN-Test von Carsten
</output>
