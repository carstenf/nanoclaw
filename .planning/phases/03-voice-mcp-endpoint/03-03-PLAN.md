---
phase: 03-voice-mcp-endpoint
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"  # MCP server + registry
  - "03-02"  # pattern etabliert (per-tool handler + JSONL + smoke)
files_modified:
  - /home/carsten_bot/nanoclaw/src/mcp-tools/calendar-client.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/calendar-client.test.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.test.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.test.ts
  - /home/carsten_bot/nanoclaw/src/mcp-tools/index.ts
  - /home/carsten_bot/nanoclaw/src/config.ts  # GCALENDAR_* env-const additions
autonomous: true  # Authority-Briefing §2 Prio 1
requirements:
  - TOOLS-01   # check_calendar
  - TOOLS-02   # create_calendar_entry
  - C6-02
  - DIR-11     # MCP-Tool-Server-Pattern
tags:
  - mcp-tools
  - google-calendar
  - voice-tools
  - case-6

must_haves:
  truths:
    - "Zwei neue MCP-Tools werden in der bestehenden Registry (`buildDefaultRegistry` aus 03-01) registriert: `voice.check_calendar` (read-only, `mutating:false`) und `voice.create_calendar_entry` (write, `mutating:true`)."
    - "Credentials liegen bereits auf Lenovo1 unter `~/.gcalendar-mcp/gcp-oauth.keys.json` (client_id+secret aus OAuth2-Installed-App-Flow) und `~/.gcalendar-mcp/google-calendar-mcp/tokens.json` (access_token+refresh_token+expiry_date). Die Token-Datei wird von `google-calendar-mcp` Container-MCP geteilt — WIR SCHREIBEN nur zurueck wenn wir neu refreshen, und dann atomic (tmp+rename) damit der Container-MCP nicht mitten im Read mit halber Datei kollidiert."
    - "OneCLI-Proxy wird NICHT verwendet (OneCLI hat heute nur den Anthropic-Secret — Google-OAuth-Tokens bleiben im Filesystem). `googleapis` + `google-auth-library` sind bereits in `package.json` (^146, ^9.15) — keine neuen Dependencies."
    - "`calendar-client.ts` ist rein funktional: `getCalendarClient(): Promise<calendar_v3.Calendar>` — Auto-Refresh via `google.auth.OAuth2`, bei abgelaufenem access_token wird `refreshAccessToken()` gerufen und die Token-Datei atomically zurueckgeschrieben. Kein Class-State."
    - "`voice.check_calendar` Input-Schema (Zod): `{timeMin: string (ISO8601), timeMax: string (ISO8601), calendarId?: string (default 'primary'), maxResults?: number (default 20, clamp 1..50)}`. Output: `{busy: Array<{start, end, summary?, eventId}>}`."
    - "`voice.create_calendar_entry` Input-Schema: `{summary: string (1..200 chars), start: string (ISO8601), end: string (ISO8601), description?: string (max 2000), location?: string (max 200), calendarId?: string (default 'primary'), attendees?: string[] (max 10 emails, optional)}`. Output: `{eventId: string, htmlLink: string}`."
    - "Timezone-Default: `GCALENDAR_DEFAULT_TZ=Europe/Berlin` (Carsten in Muenchen). Wenn ISO-String kein `Z`/`+`-Suffix hat, wird mit Default-TZ augmented."
    - "Pro-Call-Timeout: 10000ms via AbortController (Google-API manchmal langsam, 5s zu knapp; override via `GCALENDAR_TIMEOUT_MS`). Bei Timeout: 504-ish MCP-Error `{ok:false, error:'gcal_timeout'}`. Keine Exception nach aussen."
    - "Input-Validation: `BadRequestError` bei invaliden Dates (RangeError), leerer `summary`, `end <= start`, invaliden Attendee-Emails. Der MCP-Server aus 03-01 mapt `BadRequestError` bereits auf 400."
    - "JSONL-Log: neue Events `calendar_check_done` + `calendar_create_done` in separater Datei `data/voice-calendar.jsonl` (nicht in voice-slow-brain.jsonl mischen). Felder: `tool`, `call_id`, `latency_ms`, `result_count|event_id`, `calendar_id`. KEIN Event-Titel, KEINE Attendee-Emails, KEINE Description — PII-Schutz wie bei slow-brain.jsonl."
    - "Tests: calendar-client mit mock googleapis (vi.mock('googleapis')) + happy-path-list + happy-path-insert + token-refresh-triggered + timeout; beide Handler mit mock-Client + zod-validation-fail + BadRequestError-mapping; mcp-tools/index.test mit Registry-Eintrag-Check."
    - "Smoke-Test (Task 05): cross-host curl von Hetzner (10.0.0.1 im Allowlist) — (a) check_calendar naechste 24h, erwarte 200 + busy-Array, (b) create_calendar_entry mit summary='NanoClaw-Smoke 03-03' start=now+2h end=now+3h, erwarte 200 + eventId, (c) check_calendar wieder, erwarte das neue Event in busy-Array, (d) cleanup: direkt via googleapis in Node-REPL oder via curl DELETE (oder manual in Google-Calendar)."
  artifacts:
    - path: "/home/carsten_bot/nanoclaw/src/mcp-tools/calendar-client.ts"
      provides: "getCalendarClient(opts?) — OAuth2 init + auto-refresh + atomic token persistence; kein Class-State"
      contains: "google.auth.OAuth2"
      min_lines: 60
    - path: "/home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.ts"
      provides: "voice.check_calendar MCP-Handler — zod-validation, Calendar.events.list, JSONL-log, BadRequestError-mapping"
      contains: "calendar_check_done"
      min_lines: 50
    - path: "/home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.ts"
      provides: "voice.create_calendar_entry MCP-Handler — zod-validation, Calendar.events.insert, JSONL-log, mutating:true"
      contains: "calendar_create_done"
      min_lines: 50
  key_links:
    - from: "voice.check_calendar handler"
      to: "getCalendarClient"
      via: "Call googleapis Calendar.events.list mit timeMin/timeMax/singleEvents:true/orderBy:'startTime'"
      pattern: "events\\.list"
    - from: "voice.create_calendar_entry handler"
      to: "getCalendarClient"
      via: "Call googleapis Calendar.events.insert mit requestBody"
      pattern: "events\\.insert"
    - from: "buildDefaultRegistry"
      to: "beide Calendar-Handler"
      via: "registerTool mit mutating-Flag"
      pattern: "voice\\.check_calendar|voice\\.create_calendar_entry"

---

<objective>
Implementiere die ersten zwei Core-MCP-Tools fuer den Voice-Bridge-Director: `voice.check_calendar` (read-only Kalender-Abfrage) und `voice.create_calendar_entry` (Kalender-Eintrag anlegen). Per Briefing §2 Prio 1, gemeinsam in einem Plan weil sie sich Client + Credentials + Test-Setup teilen.

Purpose: Case-6 (Carsten spricht mit NanoClaw, will z.B. "Wann habe ich morgen Zeit?" oder "Trag mir einen Termin ein") braucht diese zwei Tools als erstes. Bridge deklariert sie beim `calls/{id}/accept` in den initial tools (AC-04: immutable mid-call), OpenAI Realtime ruft sie bei Bedarf via `response.function_call_arguments.done`, Bridge routet an MCP-Endpoint `http://10.0.0.2:3200/mcp/voice.<tool>`.

Output: Zwei neue Tool-Handler in `src/mcp-tools/`, gemeinsamer `calendar-client.ts`, Registry-Wiring, vollstaendige Tests, cross-host E2E-smoke von Hetzner mit echter Google-Calendar-API.
</objective>

<execution_context>
@/home/carsten_bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/carsten_bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/carsten_bot/nanoclaw-state/briefing.md  # §2 Prio 1
@/home/carsten_bot/nanoclaw-state/.planning/phases/03-voice-mcp-endpoint/03-01-PLAN.md  # MCP-Server + Registry-Pattern
@/home/carsten_bot/nanoclaw-state/.planning/phases/03-voice-mcp-endpoint/03-01-SUMMARY.md
@/home/carsten_bot/nanoclaw-state/.planning/phases/03-voice-mcp-endpoint/03-02-PLAN.md  # Handler-Pattern (Stub → echte Logik)
@/home/carsten_bot/nanoclaw-state/.planning/phases/03-voice-mcp-endpoint/03-02-SUMMARY.md
@/home/carsten_bot/nanoclaw/src/mcp-server.ts
@/home/carsten_bot/nanoclaw/src/mcp-tools/index.ts
@/home/carsten_bot/nanoclaw/src/mcp-tools/voice-on-transcript-turn.ts
@/home/carsten_bot/nanoclaw/src/config.ts

<interfaces>

<!-- Credential-Files (read-only fuer uns bis refresh notwendig) -->
```
~/.gcalendar-mcp/gcp-oauth.keys.json       # {installed: {client_id, client_secret, redirect_uris, ...}}
~/.gcalendar-mcp/google-calendar-mcp/tokens.json  # {default: {access_token, refresh_token, expiry_date, token_type}}
```

`tokens.json`-Default-Key-Form: `{default: {...}}` — die Container-MCP-Konvention `default` ist der einzige Profile-Name. Wir lesen `.default`, updaten in-place bei refresh.

<!-- googleapis SDK Shape -->
```typescript
import { google, calendar_v3 } from 'googleapis'
import { OAuth2Client } from 'google-auth-library'

const oauth = new google.auth.OAuth2(client_id, client_secret, redirect_uri)
oauth.setCredentials({access_token, refresh_token, expiry_date})
oauth.on('tokens', (updated) => { /* persist back */ })  // Event fires on auto-refresh

const calendar = google.calendar({version: 'v3', auth: oauth})

// LIST
const res = await calendar.events.list({
  calendarId: 'primary',
  timeMin: '2026-04-17T00:00:00Z',
  timeMax: '2026-04-18T00:00:00Z',
  singleEvents: true,
  orderBy: 'startTime',
  maxResults: 20,
})
// res.data.items: calendar_v3.Schema$Event[]

// INSERT
const res = await calendar.events.insert({
  calendarId: 'primary',
  requestBody: {
    summary: 'NanoClaw Test',
    description: '...',
    start: {dateTime: '2026-04-17T14:00:00+02:00', timeZone: 'Europe/Berlin'},
    end: {dateTime: '2026-04-17T15:00:00+02:00', timeZone: 'Europe/Berlin'},
    attendees: [{email: 'foo@bar.com'}],
  },
})
// res.data: calendar_v3.Schema$Event (contains id, htmlLink)
```

<!-- Registry-Pattern aus 03-01 -->
```typescript
// src/mcp-tools/index.ts (existing shape)
export interface ToolHandler {
  name: string
  mutating: boolean
  handler: (args: unknown) => Promise<unknown>
}

export function buildDefaultRegistry(deps: DepsShape): ToolRegistry {
  const r = new ToolRegistry()
  r.register({name: 'voice.on_transcript_turn', mutating: false, handler: makeVoiceOnTranscriptTurn(deps.sessionManager)})
  // NEW:
  r.register({name: 'voice.check_calendar', mutating: false, handler: makeVoiceCheckCalendar({calendarClient: deps.calendarClient})})
  r.register({name: 'voice.create_calendar_entry', mutating: true, handler: makeVoiceCreateCalendarEntry({calendarClient: deps.calendarClient})})
  return r
}
```

<!-- JSONL-Log (separate Datei fuer calendar) -->
```
data/voice-calendar.jsonl
{"ts":"2026-04-17T22:30:00Z","event":"calendar_check_done","call_id":"...","tool":"voice.check_calendar","latency_ms":340,"result_count":3,"calendar_id":"primary"}
{"ts":"2026-04-17T22:31:00Z","event":"calendar_create_done","call_id":"...","tool":"voice.create_calendar_entry","latency_ms":510,"event_id":"abc123","calendar_id":"primary"}
```

<!-- Token-Refresh-Persistenz -->
```typescript
// In getCalendarClient():
oauth.on('tokens', async (tokens) => {
  if (!tokens.refresh_token && !tokens.access_token) return
  const current = JSON.parse(await fs.readFile(tokensPath, 'utf8'))
  current.default = {...current.default, ...tokens}
  const tmp = tokensPath + '.tmp'
  await fs.writeFile(tmp, JSON.stringify(current, null, 2))
  await fs.rename(tmp, tokensPath)  // atomic replace
  // fs.rename ist auf Linux atomic solang same-fs — beide Pfade in ~/
})
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-03-01: calendar-client.ts — OAuth2 + Auto-Refresh + Atomic Persistence + Tests</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/calendar-client.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/calendar-client.test.ts
  </files>
  <read_first>
    - /home/carsten_bot/nanoclaw/package.json (verify `googleapis` + `google-auth-library` present)
    - /home/carsten_bot/nanoclaw/src/config.ts (Pattern fuer env-consts)
    - `~/.gcalendar-mcp/gcp-oauth.keys.json` (nur Struktur verifizieren, nicht committen)
    - `~/.gcalendar-mcp/google-calendar-mcp/tokens.json` (nur Struktur)
  </read_first>
  <behavior>
    - Exports: `getCalendarClient(opts?: {credsPath?: string, tokensPath?: string, now?: () => number, fs?: typeof import('fs/promises')}): Promise<calendar_v3.Calendar>`
    - Liest credsPath (default `process.env.GCALENDAR_CREDS_PATH` oder `~/.gcalendar-mcp/gcp-oauth.keys.json`) + tokensPath (default `process.env.GCALENDAR_TOKENS_PATH` oder `~/.gcalendar-mcp/google-calendar-mcp/tokens.json`) bei jedem Aufruf (keine Cache-Race mit Container-MCP). Bei Dateifehler: wirft `CalendarClientError('creds_missing')`.
    - Baut `OAuth2Client` mit client_id+secret+redirect_uri[0]. `setCredentials(defaultToken)`. Registriert `oauth.on('tokens', persistToDisk)` mit atomic tmp+rename.
    - Returns `google.calendar({version:'v3', auth: oauth})`.
    - KEIN Singleton — fresh client pro Call, weil googleapis client intern state hat (queue) und mehrere concurrent Nutzer (check+create in parallel) dadurch sauber bleiben. Creds-Read ist cheap (wenige KB).
    - DI fuer Tests: `opts.fs` (ersetzt fs/promises), `opts.OAuth2?` (factory).
  </behavior>
  <acceptance_criteria>
    - `grep -c "google.auth.OAuth2\|new OAuth2Client" src/mcp-tools/calendar-client.ts` >= 1
    - `grep -c "on('tokens'" src/mcp-tools/calendar-client.ts` >= 1
    - `grep -c "rename\|atomic" src/mcp-tools/calendar-client.ts` >= 1
    - `grep -c "it\(" src/mcp-tools/calendar-client.test.ts` >= 4
    - `npx vitest run src/mcp-tools/calendar-client.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-02: voice-check-calendar.ts — Handler + Zod + JSONL + Tests</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-check-calendar.test.ts
  </files>
  <read_first>
    - Task 03-03-01 Output
    - /home/carsten_bot/nanoclaw/src/mcp-tools/voice-on-transcript-turn.ts (Handler-Shape + BadRequestError + JSONL pattern)
  </read_first>
  <behavior>
    - Exports `makeVoiceCheckCalendar(deps: {calendarClient: () => Promise<calendar_v3.Calendar>, jsonlPath?: string, now?: () => number}): (args: unknown) => Promise<{ok: true, result: {busy: Array<{start, end, summary, eventId}>}} | {ok: false, error: string}>`
    - Zod-Schema: `{call_id?: string, timeMin: string (ISO), timeMax: string (ISO), calendarId?: string default 'primary', maxResults?: number default 20}`.
    - Validation: `new Date(timeMin) < new Date(timeMax)`, maxResults clamp 1..50. Fehler → `BadRequestError`.
    - Call `calendar.events.list({calendarId, timeMin, timeMax, singleEvents: true, orderBy: 'startTime', maxResults})`.
    - Map result: `res.data.items.map(e => ({eventId: e.id, start: e.start.dateTime ?? e.start.date, end: e.end.dateTime ?? e.end.date, summary: e.summary ?? null}))`.
    - Timeout via AbortController (10s default, override via `GCALENDAR_TIMEOUT_MS`) — googleapis v146 akzeptiert `{signal}` option; falls nicht: Promise.race mit reject-timeout.
    - JSONL-append nach Erfolg ODER Fehler: event=`calendar_check_done`, felder per truths[9].
    - call_id optional — falls fehlt, JSONL-Eintrag setzt `call_id: null`.
  </behavior>
  <acceptance_criteria>
    - `grep -c "calendar_check_done" src/mcp-tools/voice-check-calendar.ts` >= 1
    - `grep -c "events\.list" src/mcp-tools/voice-check-calendar.ts` >= 1
    - `grep -c "zod\|z\.object" src/mcp-tools/voice-check-calendar.ts` >= 1
    - `grep -c "it\(" src/mcp-tools/voice-check-calendar.test.ts` >= 5  # happy + invalid-dates + end<=start + timeout + jsonl
    - `npx vitest run src/mcp-tools/voice-check-calendar.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-03: voice-create-calendar-entry.ts — Handler + Zod + JSONL + Tests</name>
  <files>
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/voice-create-calendar-entry.test.ts
  </files>
  <read_first>
    - Task 03-03-01 + 02 Outputs
  </read_first>
  <behavior>
    - Exports `makeVoiceCreateCalendarEntry(deps): (args: unknown) => Promise<{ok:true, result:{eventId, htmlLink}} | {ok:false, error}>`
    - Zod-Schema: `{call_id?: string, summary: string (1..200), start: string (ISO), end: string (ISO), description?: string (<=2000), location?: string (<=200), calendarId?: string default 'primary', attendees?: string[] (<=10, each .email() regex)}`.
    - Validation: `summary.trim().length >= 1`, `end > start`, attendees als valide Emails. Sonst `BadRequestError`.
    - Baue requestBody: start/end as `{dateTime: isoString, timeZone: GCALENDAR_DEFAULT_TZ}` (default 'Europe/Berlin'). Wenn ISO-String bereits Offset hat, timeZone weglassen waere sauberer — aber googleapis akzeptiert beides, behalte timeZone fuer Konsistenz.
    - Call `calendar.events.insert({calendarId, requestBody})`.
    - Return `{eventId: res.data.id, htmlLink: res.data.htmlLink}`.
    - JSONL-append: event=`calendar_create_done`, felder per truths[9] + `event_id`. KEIN summary, KEINE description, KEINE attendee-Mails in JSONL.
  </behavior>
  <acceptance_criteria>
    - `grep -c "calendar_create_done" src/mcp-tools/voice-create-calendar-entry.ts` >= 1
    - `grep -c "events\.insert" src/mcp-tools/voice-create-calendar-entry.ts` >= 1
    - `grep -c "it\(" src/mcp-tools/voice-create-calendar-entry.test.ts` >= 5  # happy + invalid-summary + end<=start + invalid-attendee + jsonl
    - `npx vitest run src/mcp-tools/voice-create-calendar-entry.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 03-03-04: config.ts Env-Const + mcp-tools/index.ts Wiring + Full-Suite</name>
  <files>
    /home/carsten_bot/nanoclaw/src/config.ts
    /home/carsten_bot/nanoclaw/src/mcp-tools/index.ts
  </files>
  <action>
    config.ts additions (analog SLOW_BRAIN_*):
    - GCALENDAR_CREDS_PATH (default `~/.gcalendar-mcp/gcp-oauth.keys.json` — expandiere ~ via os.homedir())
    - GCALENDAR_TOKENS_PATH (default `~/.gcalendar-mcp/google-calendar-mcp/tokens.json`)
    - GCALENDAR_DEFAULT_TZ (default 'Europe/Berlin')
    - GCALENDAR_DEFAULT_CAL_ID (default 'primary')
    - GCALENDAR_TIMEOUT_MS (default 10000)

    mcp-tools/index.ts: `buildDefaultRegistry` erweitern um beide Calendar-Handler. `calendarClient` als Factory-Function `() => getCalendarClient()` in deps injizieren.

    Full-Suite: npx tsc --noEmit + npx vitest run. Erwartung: +10 neue Tests, 0 neu failing (1 pre-existing gmail).
  </action>
  <acceptance_criteria>
    - `grep -c "GCALENDAR_CREDS_PATH\|GCALENDAR_DEFAULT_TZ\|GCALENDAR_DEFAULT_CAL_ID" src/config.ts` >= 3
    - `grep -c "voice\.check_calendar\|voice\.create_calendar_entry" src/mcp-tools/index.ts` >= 2
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (modulo pre-existing gmail failure)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 03-03-05: Live-Deploy + Cross-Host E2E-Smoke von Hetzner</name>
  <files>(nur Ops)</files>
  <action>
    Step 1: `npm run build` in /home/carsten_bot/nanoclaw
    Step 2: `systemctl --user restart nanoclaw`, sleep 3, status active + log-tail prueft `registered tool voice.check_calendar` + `voice.create_calendar_entry`.
    Step 3: Cross-host curl von Hetzner (`ssh voice_bot@10.0.0.1 curl ...` oder via `mcp__claude_ai_Hetzner__exec`):
      ```bash
      curl -s -X POST http://10.0.0.2:3200/mcp/voice.check_calendar \
        -H 'Content-Type: application/json' \
        -d '{"arguments":{"call_id":"smoke-03-03","timeMin":"<now-iso>","timeMax":"<now+24h-iso>"}}'
      ```
      Erwartung: 200 + `{ok:true, result:{busy:[...]}}` — Array-Groesse beliebig (Carsten's Calendar-State).
    Step 4: Create-Smoke:
      ```bash
      curl -s -X POST http://10.0.0.2:3200/mcp/voice.create_calendar_entry \
        -H 'Content-Type: application/json' \
        -d '{"arguments":{"call_id":"smoke-03-03","summary":"NanoClaw-Smoke 03-03","start":"<now+2h>","end":"<now+3h>"}}'
      ```
      Erwartung: 200 + `{ok:true, result:{eventId:"...", htmlLink:"..."}}`.
    Step 5: Verify mit zweitem check_calendar (selbes Zeitfenster), neues Event erscheint in busy-Array.
    Step 6: JSONL tail: `tail -5 data/voice-calendar.jsonl` zeigt beide events (check_done + create_done) mit latency_ms.
    Step 7: Cleanup: Smoke-Event via `curl DELETE` (oder manuell in Google-Calendar-UI falls MCP-DELETE fehlt). Nicht-blocking — Test-Event ist unschaedlich.
  </action>
  <acceptance_criteria>
    - systemctl status nanoclaw = active (running)
    - check_calendar smoke 200 with `busy` array
    - create_calendar_entry smoke 200 with `eventId` + `htmlLink`
    - Zweites check_calendar nach create zeigt das neue Event
    - JSONL enthaelt >= 2 events pro Tool
    - Keine error-logs im systemd-journal waehrend smoke
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

| Threat | Category | Disposition | Mitigation |
|---|---|---|---|
| T-03-03-01 | Credential-Leak | accept | Token-File liegt schon unter 0600 user-home, kein neuer Angriffsvektor. Atomic-Rename verhindert halb-geschriebene Files. |
| T-03-03-02 | Concurrent-Refresh-Race | mitigate | Mehrere nanoclaw-Tool-Calls parallel koennten gleichzeitig refresh triggern → alle schreiben zuletzt-gewinnt. Googleapis intern debounced, aber worst-case ein redundanter refresh. Akzeptabel — refresh-Token bleibt valide. |
| T-03-03-03 | Token-File-Collision mit Container-MCP | mitigate | google-calendar-mcp Container liest dieselbe Datei. Wenn beide parallel refreshen, atomic-rename garantiert Valid-JSON. Letzter-Gewinner ist semantisch OK weil beide den gleichen refresh_token haben. |
| T-03-03-04 | Invalid-Date-DoS | mitigate | Zod + `new Date().getTime()` Check rejected malformed inputs mit 400. Kein Google-API-Call auf Garbage-Input. |
| T-03-03-05 | Calendar-ID-IDOR | accept | MCP-Server steht hinter peer-allowlist (10.0.0.1/4/5 = nur Carsten-Voice-Bridge). Kein externer Zugriff moeglich, `calendarId`-Param kann nicht von Fremden manipuliert werden. |
| T-03-03-06 | Email-Injection via attendees | mitigate | zod `.email()` validation. Max 10 attendees. Google-API rejected ohnehin invalid mails. |
| T-03-03-07 | Event-Flood durch Voice-Bot-Hallucination | accept | Case-6 = Carsten-only, Bridge cadence-cap D-25. Worst-case 20-30 Events/Call (unrealistisch). Falls in Case-2/4 spaeter ein Problem: Rate-Limit per call_id in buildDefaultRegistry. |
| T-03-03-08 | PII in JSONL | mitigate | Felder-Whitelist (call_id, tool, latency_ms, result_count, event_id, calendar_id). KEIN summary, description, attendees, location. |
| T-03-03-09 | Google-API-Rate-Limit | accept | Free-Tier 1M Requests/Tag per Projekt. Case-6 Privat-Use: < 100 Calls/Tag. Headroom ~10000x. |

</threat_model>

<success_criteria>
- `voice.check_calendar` liefert echte Busy-Liste aus Carstens primary-Calendar.
- `voice.create_calendar_entry` legt Events an, `eventId` + `htmlLink` kommen zurueck.
- Beide Tools in Registry, `mutating`-Flag korrekt (false/true).
- Token-Refresh funktioniert transparent (bei abgelaufenem access_token kein Fehler, kein manueller Eingriff).
- Keine neuen Dependencies (`googleapis` + `google-auth-library` bereits da).
- JSONL-log strikt PII-free (nur IDs + Zahlen).
- Full-Suite + cross-host smoke gruen.
</success_criteria>

<output>
After completion, create `_planning/phases/03-voice-mcp-endpoint/03-03-SUMMARY.md` covering:
- Commit-Hashes (calendar-client, beide handler, config, index, deploy-commit)
- Smoke-Evidence (beide curl-Outputs mit echten eventId/htmlLink + JSONL-Auszug)
- Caveats (Token-Refresh-Szenario noch nicht live bewiesen bis Token wirklich ablaeuft — dokumentieren)
- Next: Plan 03-04 (send_discord_message)
</output>
