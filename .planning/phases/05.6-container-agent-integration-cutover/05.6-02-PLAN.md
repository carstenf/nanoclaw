---
phase: 05.6-container-agent-integration-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md
  - .env
  - voice-bridge/.env
autonomous: false
requirements:
  - REQ-DIR-06
  - REQ-DIR-11
  - REQ-DIR-12
  - REQ-VOICE-13
  - REQ-DIR-14
  - REQ-DIR-16
  - REQ-DIR-17
must_haves:
  truths:
    - "Wave-2 begins ONLY after Carsten approves at the explicit start gate (D-28a)"
    - "REASONING_MODE=container-agent is set as runtime ENV (NOT yet flipped as default in config.ts) so the entire Bridge runs the new path during cutover testing"
    - "Synthetic 2-turn test (Wave-1's live-cutover-synth.test.ts) re-runs with REASONING_MODE=container-agent, exits 0 in CI, and produces a captured trace artifact"
    - "Carsten inbound PSTN PASS REQUIRES demonstrated Case-6b functionality: at least one of Kalender / Fahrzeit / Memory-Lookup interaction completes successfully (D-29). Generic 'Hallo + Auf Wiederhoeren' is NOT a PASS."
    - "Case-2 outbound PSTN PASS REQUIRES demonstrated Case-2 booking-attempt dialog: counterpart-question handling, hold-music tolerance, OR readback (D-29). Generic call-connect alone is NOT a PASS."
    - "Each cutover step produces a trace artifact under ~/nanoclaw/voice-container/runs/turns-{call_id}.jsonl (per voice-channel-spec/tracing-contract.md REQ-INFRA-05)"
    - "Each cutover step records explicit Carsten approval (PASS/FAIL + free-form note) in the per-phase cutover-log.md before the next checkpoint releases"
    - "On any FAIL: cutover halts, traces are archived, Carsten decides — revert to REASONING_MODE=slow-brain via runtime ENV, debug, or proceed with caveats. The DEFAULT FLAG in config.ts is NOT touched in this plan (only Plan 06-03 flips it after all PSTN PASSes recorded)."
    - "AGENT_NOT_WIRED path is verifiably absent from any traced session (carsten can grep traces for that string and find none — secondary regression evidence on top of Plan 06-01 unit grep)"
    - "FAIL-recovery is documented (not just 'STOP cutover'): on synth FAIL or PSTN FAIL, the executor records a fail-then-fix workflow that lands the bug-fix in a separate plan/hot-patch commit (NOT in 05.6 plans), re-runs synthetic smoke, then re-executes the failed cutover step from the beginning. Three-strike rule on the same step escalates to chat as ASK FOR CHAT (architectural review)."
  artifacts:
    - path: ".planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md"
      provides: "Cutover record: per-step PASS/FAIL verdict, trace path, Carsten note, timestamp"
      contains: "synth-test"
    - path: ".env (runtime, NOT committed if gitignored)"
      provides: "REASONING_MODE=container-agent for cutover testing window"
      contains: "REASONING_MODE"
  key_links:
    - from: "Carsten gate (D-28a..d)"
      to: "Each cutover step"
      via: "manual checkpoint task with PASS/FAIL recording"
      pattern: "checkpoint:human-verify"
    - from: "Each cutover step"
      to: "voice-channel-spec/tracing-contract.md"
      via: "trace file under ~/nanoclaw/voice-container/runs/turns-{call_id}.jsonl"
      pattern: "turns-rtc"
---

<objective>
Run the live PSTN cutover sequence per D-28 with explicit Carsten gates. Three live tests under `REASONING_MODE=container-agent` (runtime-ENV only, default flag NOT touched yet):

1. **Synthetic 2-turn re-run** — Wave-1's live-cutover-synth.test.ts exercised under the production-runtime ENV configuration to prove the integration is healthy after deploy / before any real call.
2. **Carsten inbound PSTN PASS** — real call from Carsten's mobile to his Sipgate DID, with at least one demonstrated Case-6b functionality (Kalender, Fahrzeit, OR Memory-Lookup). D-29 strict acceptance: generic "Hallo + Auf Wiederhoeren" is NOT a PASS.
3. **Case-2 outbound PSTN PASS** — Carsten triggers an outbound restaurant-reservation call via Andy. Case-2 booking-attempt dialog must run (counterpart-question handling, hold-music tolerance, OR readback). D-29 strict acceptance: a generic connect-then-disconnect is NOT a PASS.

Each step is gated by a Carsten manual checkpoint. Trace artifacts are archived under `~/nanoclaw/voice-container/runs/turns-{call_id}.jsonl` per voice-channel-spec/tracing-contract.md. The phase's per-step record (PASS/FAIL + Carsten note + trace path + timestamp) lives in `06-02-cutover-log.md` so the cleanup commit in Plan 06-03 can reference verified PASSes.

Purpose: Activates REQ-DIR-06/11/12/16/17 + REQ-VOICE-13 (Phase 6) on real traffic. Until this plan's PSTN PASSes land, the Phase-6 versions of those REQs remain only-scaffolded; this plan is where they become authoritative on a real call. REQ-DIR-14 (stateless per-trigger) is observable in the inbound trace via the per-trigger spawn pattern.

Output: 3 PASS verdicts (synth + inbound + outbound), 3 trace artifacts, an updated cutover-log.md. Plan 06-03 unblocks on all three PASSes recorded.

**This plan ships ZERO source-code changes.** All it does is run the live cutover under a runtime ENV flip and record verdicts. The default-flag flip and hard cleanup happen in Plan 06-03.

**FAIL-recovery policy (documented; applies to every FAIL branch in this plan):**
> On FAIL: stop cutover at the failing step. Diagnose the failure (read the trace via `jq`, consult `journalctl --user -u voice-bridge -n 200`, run `/gsd-debug` if needed). Land the fix as a SEPARATE plan or hot-patch commit (NOT inside Phase 05.6 plans — this phase orchestrates cutover, not bugfix). After fix is deployed and a fresh synthetic 2-turn smoke (Task 2 / Step 1) passes, re-execute the failed cutover step (Task 4 for inbound, or Task 6 for outbound) from the beginning. Do not skip steps; do not partial-credit a previous PASS. If three FAIL cycles on the same step occur without progress, escalate to chat as **ASK FOR CHAT** (architectural review needed before further attempts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/05.6-container-agent-integration-cutover/05.6-CONTEXT.md
@.planning/phases/05.5-slow-brain-removal-container-agent/05.5-CONTEXT.md
@.planning/phases/05.6-container-agent-integration-cutover/05.6-01-PLAN.md
@~/nanoclaw-state/voice-channel-spec/tracing-contract.md
@~/nanoclaw-state/voice-channel-spec/decisions/2026-04-24-slow-brain-removal-phase-6.md
@voice-bridge/src/config.ts
@voice-bridge/src/webhook.ts

<interfaces>
From voice-channel-spec/tracing-contract.md:
- Trace path: `~/nanoclaw/voice-container/runs/turns-{call_id}.jsonl` (REQ-INFRA-05/REQ-VOICE-10).
- Persisted fields include: `session.created`, `session.updated`, `input_audio_buffer.speech_started/.speech_stopped`, `output_audio_buffer.started/.stopped`, `response.created/response.done`, `conversation.item.input_audio_transcription.completed` (counterpart transcript text — NOT redacted, DSGVO-Haushaltsausnahme), `response.audio_transcript.delta/.done`, `response.function_call_arguments.done`.
- Audio bytes (`response.audio.delta`) STRIPPED before write — `delta` field is removed and replaced with a `delta_bytes` integer (decoded byte length). The §201 invariant check therefore inspects `delta_bytes` (presence) AND verifies that `.delta` content is absent or empty; any non-empty `.delta` field on a `response.audio.delta` event is a §201 BREACH.

From CONTEXT.md `<specifics>` (cutover sequence skeleton — D-28):
- Pre-cutover gate (Carsten go-ahead).
- Set REASONING_MODE=container-agent (env-level only, default still slow-brain).
- Synthetic 2-turn test → PASS verdict required.
- Inbound PSTN gate.
- Live Carsten inbound PSTN — D-29 case-specific behaviour required.
- Outbound PSTN gate.
- Live Case-2 outbound PSTN — D-29 case-specific behaviour required.

Bridge → NanoClaw transport host (per `/opt/server-docs/MASTER.md` and `project_nanoclaw_infra` user memory):
- NanoClaw runs on Lenovo1 (carsten_bot user, systemd --user). Voice-bridge ALSO runs on Lenovo1 (carsten_bot user, systemd --user) — same host. Bridge → NanoClaw MCP is therefore a LOCAL connection: `http://127.0.0.1:3201/`.
- Hetzner is OUT OF SCOPE for NanoClaw (per `feedback_no_nanoclaw_hetzner` user memory). 10.0.0.x is the WireGuard subnet (e.g. iPhone WG IP `10.0.0.2`) and is NOT a Bridge → NanoClaw transport address. Earlier draft text using `http://10.0.0.2:3201/` was an error and is corrected throughout this plan to `http://127.0.0.1:3201/`.
- If a future deploy puts NanoClaw inside a Docker container with mapped ports, the host `127.0.0.1` may need to become a different lenovo1-internal address — Task 1 step 2 surfaces this as a Carsten-confirmation blocker rather than guessing.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Pre-cutover gate (D-28a) — Wave-1 PASS + Phase 05.5 merged + Carsten go-ahead</name>
  <what-built>
    Plan 06-01 (Wave 1) shipped the real defaultInvokeAgent + defaultInvokeAgentTurn integration AND the new `voice-mid-call-gateway.ts` REQ-DIR-17 dispatch-path enforcement (revision Task 4). All 06-01 unit + integration tests pass. Phase 05.5 is merged on main (transport scaffold + skill + flag).
  </what-built>
  <how-to-verify>
    1. Confirm 06-01-SUMMARY.md exists at `.planning/phases/05.6-container-agent-integration-cutover/05.6-01-SUMMARY.md`.
    2. From repo root: `cd /home/carsten_bot/nanoclaw && npx vitest run src/voice-agent-invoker.test.ts src/voice-mid-call-gateway.test.ts src/mcp-tools/voice-triggers-init.test.ts src/mcp-tools/voice-triggers-transcript.test.ts src/mcp-tools/voice-finalize-call-cost.test.ts && cd voice-bridge && npx vitest run tests/live-cutover-synth.test.ts && cd ..` — all six test files exit 0.
    3. Run grep regression: `grep -rn 'AGENT_NOT_WIRED' src/ voice-bridge/src/ | grep -v '\.test\.ts'` — expect zero output (production code is clean of the no-op stub).
    4. Run gateway grep: `grep -q 'checkMidCallMutation' src/mcp-tools/index.ts && grep -q 'registerActiveCall' src/mcp-tools/voice-triggers-init.ts && grep -q 'deregisterActiveCall' src/mcp-tools/voice-finalize-call-cost.ts` — all three must succeed (REQ-DIR-17 gateway wired).
    5. Confirm Phase 05.5 SUMMARYs exist: `ls .planning/phases/05.5-slow-brain-removal-container-agent/*-SUMMARY.md` should list 5 files (Plans 01-05). If 05.5 is not merged, STOP.
    6. Confirm `git status` is clean (no uncommitted Wave-1 work pending).
    7. Carsten — review the 06-01-SUMMARY.md and decide: proceed with live cutover NOW, or postpone.
  </how-to-verify>
  <resume-signal>Type "approved — proceed cutover" to release Task 1, or describe any blocker (e.g. "wait — 05.5 not yet merged" / "first re-run live-cutover-synth.test.ts after deploy"). On approval, the executor records the timestamp + Carsten signal verbatim into `06-02-cutover-log.md` under "## Pre-cutover gate".</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Initialise cutover log + flip runtime ENV REASONING_MODE=container-agent (NOT default)</name>
  <files>.planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md, .env, voice-bridge/.env</files>
  <read_first>
    - .planning/phases/05.6-container-agent-integration-cutover/05.6-CONTEXT.md (D-28 sequence)
    - voice-bridge/src/config.ts (verify REASONING_MODE reading + NANOCLAW_VOICE_MCP_URL/TOKEN/TIMEOUT_MS still present after Plan 05.5-03)
    - .env / voice-bridge/.env (read current ENV state — do NOT clobber other values)
    - /opt/server-docs/MASTER.md (Lenovo1 host topology — confirms NanoClaw + voice-bridge co-located on Lenovo1)
  </read_first>
  <action>
    1. **Create `06-02-cutover-log.md`** with the structure:
        ```markdown
        # Phase 05.6 Plan 02 — Live PSTN Cutover Log

        **Phase-split rationale:** D-28 (explicit checkpoints between each cutover step). All tests use REASONING_MODE=container-agent runtime-ENV only — config.ts default flag is NOT touched in this plan.

        ## Pre-cutover gate
        - Timestamp: <FILLED BY TASK 0 CHECKPOINT>
        - Carsten signal: <FILLED BY TASK 0 CHECKPOINT>

        ## Step 1 — Synthetic 2-turn re-run
        - Status: PENDING
        - Trace: N/A (synthetic — local capture under /tmp/cutover-synth-${ts}/ if any)
        - Carsten verdict: <FILLED>

        ## Step 2 — Carsten inbound PSTN
        - Status: PENDING
        - Trace path: ~/nanoclaw/voice-container/runs/turns-${call_id}.jsonl
        - call_id: <FILLED>
        - Demonstrated Case-6b functionality (D-29 strict — at least ONE of these): [ ] Kalender   [ ] Fahrzeit   [ ] Memory-Lookup
        - Carsten verdict: <FILLED>

        ## Step 3 — Case-2 outbound PSTN
        - Status: PENDING
        - Trace path: ~/nanoclaw/voice-container/runs/turns-${call_id}.jsonl
        - call_id: <FILLED>
        - Demonstrated Case-2 dialog (D-29 strict — at least ONE of these): [ ] Counterpart-question handling   [ ] Hold-music tolerance   [ ] Readback
        - Carsten verdict: <FILLED>

        ## Final cutover decision
        - All three PASS? <FILLED>
        - Proceed to Plan 06-03 (default flip + cleanup)? <FILLED>
        ```

    2. **Set runtime ENV REASONING_MODE=container-agent** by appending/updating `.env` and `voice-bridge/.env`. Do NOT modify `voice-bridge/src/config.ts` — only ENV. Verify `NANOCLAW_VOICE_MCP_URL`, `NANOCLAW_VOICE_MCP_TOKEN`, `NANOCLAW_VOICE_MCP_TIMEOUT_MS` are populated:
        ```
        # Phase 05.6 cutover (runtime ENV — flag default flip happens in Plan 06-03)
        REASONING_MODE=container-agent
        # NanoClaw + voice-bridge are co-located on Lenovo1 (same host) — local loopback.
        # If NanoClaw is moved into a container with mapped ports, replace 127.0.0.1 with
        # the lenovo1-internal host (Carsten confirms before edit; do NOT use 10.0.0.x —
        # that's the WireGuard subnet, not the NanoClaw MCP transport address).
        NANOCLAW_VOICE_MCP_URL=http://127.0.0.1:3201/
        NANOCLAW_VOICE_MCP_TOKEN=<existing MCP_STREAM_BEARER value>
        NANOCLAW_VOICE_MCP_TIMEOUT_MS=5000
        ```
       If `NANOCLAW_VOICE_MCP_URL` is unset and the systemd voice-bridge unit cannot be reconfigured non-interactively, surface as a blocker BEFORE starting Step 1 — do not silently proceed with `slow-brain` fallback.

       **Host verification:** Confirm `curl -s http://127.0.0.1:3201/health` from Lenovo1 returns 200. If it fails (e.g. NanoClaw is reachable on a different lenovo1-internal host because of containerization), STOP and ASK Carsten for the correct host before proceeding — do NOT guess; do NOT use a 10.0.0.x address.

    3. **Restart the voice-bridge systemd unit** so it picks up the new ENV:
        ```bash
        systemctl --user restart voice-bridge
        sleep 3
        systemctl --user status voice-bridge --no-pager | head -20
        journalctl --user -u voice-bridge -n 50 --no-pager | grep -E 'REASONING_MODE|NANOCLAW_VOICE_MCP_URL'
        ```
       Verify the bridge reports `REASONING_MODE=container-agent` at startup. If startup logs do not surface the value, grep the bridge process env: `tr '\0' '\n' < /proc/$(pgrep -f voice-bridge | head -1)/environ | grep -E 'REASONING_MODE|NANOCLAW_VOICE_MCP'`.

    4. **Restart the nanoclaw main process** so the new MCP-tool defaults from Plan 06-01 are loaded:
        ```bash
        systemctl --user restart nanoclaw
        sleep 3
        journalctl --user -u nanoclaw -n 50 --no-pager | grep -E 'voice_triggers_init|voice_triggers_transcript|registered'
        ```
       Verify both tools register successfully.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && test -f .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md && grep -q 'REASONING_MODE=container-agent' .env voice-bridge/.env && (systemctl --user is-active voice-bridge >/dev/null 2>&1 || launchctl list 2>/dev/null | grep -q voice-bridge) && (systemctl --user is-active nanoclaw >/dev/null 2>&1 || launchctl list 2>/dev/null | grep -q nanoclaw) && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `06-02-cutover-log.md` exists with all four sections (pre-gate, Step 1, Step 2, Step 3) and the D-29 strict-acceptance checkbox lists.
    - `.env` AND `voice-bridge/.env` contain literal `REASONING_MODE=container-agent`.
    - `.env` AND `voice-bridge/.env` contain literal `NANOCLAW_VOICE_MCP_URL=http://127.0.0.1:3201/` (or the Carsten-confirmed lenovo1-internal alternative — never `10.0.0.x`).
    - `voice-bridge/src/config.ts` is UNCHANGED (default flag still `'slow-brain'` — flip is Plan 06-03 only).
    - voice-bridge process is running and its environment shows REASONING_MODE=container-agent.
    - nanoclaw process is running and registers `voice_triggers_init` + `voice_triggers_transcript` at startup.
    - `curl -s http://127.0.0.1:3201/health` returns 200 (transport reachable).
  </acceptance_criteria>
  <done>
    Cutover-log.md initialised. Runtime ENV flipped on both processes (NOT config.ts default). Both services restarted and confirmed active under the new ENV. Bridge → NanoClaw transport verified reachable on local loopback. Ready for Step 1 synthetic test.
  </done>
</task>

<task type="auto">
  <name>Task 2 (Step 1): Synthetic 2-turn re-run under runtime ENV</name>
  <files>.planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md</files>
  <read_first>
    - voice-bridge/tests/live-cutover-synth.test.ts (Plan 06-01 Task 3 output)
    - .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md (current state)
  </read_first>
  <action>
    1. Re-run the synthetic test under the live-deploy ENV state:
        ```bash
        cd /home/carsten_bot/nanoclaw/voice-bridge && \
          REASONING_MODE=container-agent \
          NANOCLAW_VOICE_MCP_URL=$(grep '^NANOCLAW_VOICE_MCP_URL=' ../.env | cut -d= -f2) \
          npx vitest run tests/live-cutover-synth.test.ts 2>&1 | tee /tmp/synth-cutover-$(date +%s).log
        ```

    2. If exit code 0 AND all 4+ test cases pass: append to `06-02-cutover-log.md` Step 1:
        - Status: `PASS`
        - Trace: path of `/tmp/synth-cutover-${ts}.log`
        - Brief 1-line note (e.g., "All 4 tests green; Du/Sie axis exercised; FALLBACK_PERSONA path proven on simulated agent timeout").
        - Timestamp: `date -Iseconds`.

    3. If exit code != 0 OR any test fails: append to log:
        - Status: `FAIL`
        - Failing test names + first error message (extract from the log).
        - **FAIL-recovery (per plan-level FAIL policy in `<objective>`):** stop cutover. Diagnose. Fix in a separate plan/hot-patch commit (NOT inside Phase 05.6 plans). Re-run synthetic smoke. Re-execute Task 2 from the beginning. Do not advance to Task 3 checkpoint until Step 1 PASSes. Three-strike rule: if three FAIL cycles on Step 1 without progress, escalate as ASK FOR CHAT.

    4. Output a one-paragraph summary to stdout for Carsten review.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && grep -E 'Step 1.*Status: (PASS|FAIL)' .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md</automated>
  </verify>
  <acceptance_criteria>
    - 06-02-cutover-log.md Step 1 section has Status updated to PASS or FAIL (not PENDING).
    - On PASS: trace path recorded, timestamp recorded.
    - On FAIL: failing test names recorded, FAIL-recovery workflow noted (separate fix plan + re-run synth + re-execute Task 2 from start; never partial-credit), plan does NOT advance to Task 3 checkpoint without explicit Carsten override.
  </acceptance_criteria>
  <done>
    Synthetic test verdict recorded in cutover-log.md. PASS unblocks the next checkpoint; FAIL halts the cutover and triggers the documented fix-then-retry workflow.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Inbound PSTN gate (D-28b) — Carsten approves live inbound test</name>
  <what-built>
    Step 1 (synthetic) recorded a verdict in 06-02-cutover-log.md. Live deploy is running under REASONING_MODE=container-agent runtime ENV.
  </what-built>
  <how-to-verify>
    1. Read the Step 1 entry in `06-02-cutover-log.md`. Confirm Status: PASS. If FAIL, decide per the plan-level FAIL-recovery policy: revert to slow-brain (set REASONING_MODE=slow-brain in .env, restart bridge), OR debug + re-run, OR escalate.
    2. Confirm both services are healthy:
       - `systemctl --user status voice-bridge --no-pager | head -10`
       - `curl -s http://127.0.0.1:4402/health | jq` (or wherever voice-bridge /health lives — see voice-bridge/src/index.ts).
    3. Confirm the nanoclaw-voice MCP-server is reachable from the bridge (LOCAL loopback — both services on Lenovo1):
       - `curl -sH "Authorization: Bearer $(grep MCP_STREAM_BEARER ~/nanoclaw/.env | cut -d= -f2)" http://127.0.0.1:3201/health` returns 200.
    4. Carsten — call your Sipgate DID from your mobile. Have at least one Case-6b interaction ready: a calendar entry to dictate, a Fahrzeit-Anfrage to make, OR a memory-lookup question. Hang up cleanly.
    5. After hangup, locate the trace: `ls -lt ~/nanoclaw/voice-container/runs/turns-rtc_*.jsonl | head -3`. Note the call_id (filename suffix).
    6. Type the verdict — and the call_id — in your resume-signal so the next task can record both.
  </how-to-verify>
  <resume-signal>Type "approved — call_id rtc_XXX, demonstrated <Kalender|Fahrzeit|Memory-Lookup>, PASS" (D-29 strict) OR "FAIL — call_id rtc_XXX, reason <text>". The executor records both verbatim into `06-02-cutover-log.md` under "## Step 2".</resume-signal>
</task>

<task type="auto">
  <name>Task 4 (Step 2): Record inbound PSTN verdict + verify trace artifact</name>
  <files>.planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md</files>
  <read_first>
    - 06-02-cutover-log.md (current state — Step 2 PENDING)
    - The trace file path provided by Carsten in Task 3 resume-signal
    - ~/nanoclaw-state/voice-channel-spec/tracing-contract.md (jq one-liner for transcript extraction; `delta_bytes` field name + redaction contract for §201 check)
  </read_first>
  <action>
    1. Parse the call_id and verdict from Task 3's resume-signal output.

    2. Record into Step 2 of 06-02-cutover-log.md:
        - Status: PASS or FAIL (per Carsten verdict).
        - call_id: rtc_XXX.
        - Trace path: `~/nanoclaw/voice-container/runs/turns-${call_id}.jsonl`.
        - Demonstrated Case-6b functionality: tick the matching checkbox(es) — REQUIRES at least one for PASS per D-29.
        - Timestamp.

    3. **Verify trace artifact exists and is non-empty**:
        ```bash
        TRACE=~/nanoclaw/voice-container/runs/turns-${call_id}.jsonl
        test -s "$TRACE" || { echo "FAIL: trace missing or empty"; exit 1; }
        wc -l "$TRACE"
        head -3 "$TRACE" | jq '.type // "no_type"'
        ```

    4. **§201 invariant check** on the trace — per `tracing-contract.md`, `response.audio.delta` events must have their `.delta` content STRIPPED (replaced with a `delta_bytes` integer count). The check verifies:
       (a) every `response.audio.delta` event has a `delta_bytes` integer field present (positive integer = redaction worked correctly, content was stripped after byte count was recorded), AND
       (b) NO `response.audio.delta` event retains a non-empty `.delta` field (any non-empty `.delta` content on these events = §201 BREACH).

        ```bash
        # Check (a): every audio.delta event has delta_bytes integer (redaction structurally complete).
        MISSING_BYTES=$(jq -r 'select(.type == "response.audio.delta") | (if (.delta_bytes // null) == null then "missing" else "ok" end)' "$TRACE" | grep -c '^missing$' || true)
        test "$MISSING_BYTES" -eq 0 || { echo "BREACH: $MISSING_BYTES response.audio.delta events lack delta_bytes (redaction integrity broken)"; exit 1; }

        # Check (b): no audio.delta event retains a non-empty .delta payload — any leak is a §201 BREACH.
        AUDIO_LEAK=$(jq -r 'select(.type == "response.audio.delta") | (.delta // "") | length' "$TRACE" | awk '$1 > 0 { print "AUDIO_LEAK_BYTES:" $1 }')
        test -z "$AUDIO_LEAK" || { echo "BREACH: §201 audio leak detected on $TRACE — $AUDIO_LEAK"; exit 1; }
        ```

       If either check fails: append a §201 BREACH note to Step 2 and HALT — Phase-0 violation outranks Phase-6 cutover; no FAIL-recovery shortcut applies for §201 — this requires audit-level investigation before any further cutover work.

    5. **AGENT_NOT_WIRED regression check**:
        ```bash
        grep -c 'AGENT_NOT_WIRED' "$TRACE" || true
        ```
        Expected: 0. Any non-zero count = BLOCKER, HALT.

    6. **Du-form sanity check + placeholder-leak negative check** (case_6b inbound is Du-axis per D-25; rendered instructions must contain Du-axis tokens AND must contain ZERO `{{...}}` placeholder tokens):
        ```bash
        # Positive: Du-axis tokens present.
        DU_HITS=$(jq -r 'select(.type == "session.updated" or .type == "session.created") | .session.instructions // ""' "$TRACE" | grep -E '\b(Du|Dir|Dich)\b' | head -3)
        echo "$DU_HITS"
        test -n "$DU_HITS" || echo "NOTE: no Du-axis tokens in rendered instructions — possible misrendering (Carsten gets final say)"

        # Negative: NO {{anrede_*}} or any other {{...}} placeholder tokens leaked into rendered instructions.
        PLACEHOLDER_LEAK=$(jq -r 'select(.type == "session.updated" or .type == "session.created") | .session.instructions // ""' "$TRACE" | grep -c '{{[a-z_]\+}}' || true)
        test "$PLACEHOLDER_LEAK" -eq 0 || { echo "BREACH: placeholder tokens leaked into rendered instructions: $PLACEHOLDER_LEAK matches"; exit 1; }
        ```

    7. **Phase-6 reasoning-path positive check**:
        ```bash
        journalctl --user -u voice-bridge --since "1 hour ago" --no-pager | grep -c 'transcript_trigger_failed' || true
        ```
        Expected: 0 (or very low — D-12 tolerates non-fatal warnings).

    8. Append all check results to the log entry as a "## Step 2 — checks" sub-section.

    9. **On FAIL** (Carsten verdict FAIL, OR §201 BREACH, OR AGENT_NOT_WIRED found, OR placeholder-leak BREACH): set Status: FAIL and STOP cutover. Apply the plan-level FAIL-recovery policy from `<objective>`:
        - Diagnose (read trace via `jq '. | select(.type | startswith("error"))'`, check `journalctl --user -u nanoclaw` for the call window, consult `/gsd-debug` if root cause is non-obvious).
        - Land the fix as a separate plan or hot-patch commit (NOT inside Phase 05.6 plans).
        - After fix is deployed, re-run synthetic smoke (Task 2).
        - Then re-execute Task 4 from the beginning (Carsten places a fresh inbound call → fresh trace → fresh verdict).
        - Three FAIL cycles on Step 2 without progress → escalate to chat as **ASK FOR CHAT** (architectural review).
        - Note: §201 BREACH does NOT follow this generic FAIL-recovery — §201 halts the entire cutover plan and requires audit-level investigation; do not attempt a hot-fix-and-retry.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && grep -E 'Step 2.*Status: (PASS|FAIL)' .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md && grep -E 'call_id: rtc_' .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md</automated>
  </verify>
  <acceptance_criteria>
    - Step 2 of 06-02-cutover-log.md has Status: PASS or FAIL (not PENDING).
    - call_id (rtc_XXX) recorded.
    - Trace path recorded and the file exists with non-zero size.
    - §201 audio-leak check ran on the correct fields (`delta_bytes` integrity AND `.delta` content emptiness) per `tracing-contract.md`; no BREACH detected (or, on BREACH, a §201 note is in the log and the plan halts pending audit).
    - AGENT_NOT_WIRED grep returns 0 in the trace.
    - Placeholder-leak negative check ran AND returned 0 (no `{{...}}` tokens in rendered instructions); BREACH on non-zero count.
    - "## Step 2 — checks" sub-section appended with all five check results.
    - On Carsten FAIL: FAIL-recovery workflow noted (diagnose + separate fix plan + re-run synth + re-execute Task 4).
  </acceptance_criteria>
  <done>
    Inbound PSTN verdict recorded with full trace verification (existence, §201 invariant on the correct fields, AGENT_NOT_WIRED regression, Du-form positive sanity, placeholder-leak negative sanity, transcript-trigger health). PASS unblocks the next checkpoint; FAIL stops the cutover and triggers the documented fix-then-retry workflow.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Outbound PSTN gate (D-28c) — Carsten approves live Case-2 outbound test</name>
  <what-built>
    Inbound PSTN PASS recorded in cutover-log.md (Task 4). The container-agent reasoning path is proven on a real call. Now: prove the outbound Case-2 path works with the same architecture.
  </what-built>
  <how-to-verify>
    1. Read Step 2 entry in `06-02-cutover-log.md`. Confirm Status: PASS. If FAIL, decide per FAIL-recovery policy whether to abort cutover (revert to slow-brain) or push through to outbound regardless (Carsten judgement).
    2. Confirm both services still healthy: `systemctl --user is-active voice-bridge nanoclaw`.
    3. Pick a Case-2 target restaurant. Have a clear booking request ready (date, time, party_size). Pre-arrange with a friendly counterpart OR pick a restaurant where you can hang up gracefully if needed.
    4. Carsten — message Andy via WhatsApp/Discord with a Case-2 outbound request. Phrasing: imperative form like "Rufe Restaurant <name> an und reserviere fuer 4 Personen morgen 19 Uhr" — this triggers `voice_start_case_2_call` per `feedback_case2_trigger_phrasing` memory.
    5. Andy spawns the outbound call. The container-agent will be warm by the time Sipgate dials (D-6).
    6. Conduct the booking dialog. The PASS criterion (D-29 strict): demonstrate at least ONE of:
        - Counterpart-question handling (counterpart asks something not in the request — bot answers reasonably from skill context).
        - Hold-music tolerance (counterpart puts you on hold; bot stays passive without spamming response.create).
        - Readback (bot reads back the booking details before commit).
       Generic dial-and-hangup is NOT a PASS.
    7. After hangup, locate trace: `ls -lt ~/nanoclaw/voice-container/runs/turns-rtc_*.jsonl | head -3`. Note the new call_id.
    8. Type the verdict + call_id in your resume-signal.
  </how-to-verify>
  <resume-signal>Type "approved — call_id rtc_XXX, demonstrated <counterpart-question|hold-music|readback>, PASS" OR "FAIL — call_id rtc_XXX, reason <text>". The executor records this verbatim into `06-02-cutover-log.md` under "## Step 3".</resume-signal>
</task>

<task type="auto">
  <name>Task 6 (Step 3): Record outbound PSTN verdict + verify trace + finalize cutover decision</name>
  <files>.planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md</files>
  <read_first>
    - 06-02-cutover-log.md (current state — Step 3 PENDING)
    - The trace file path provided by Carsten in Task 5 resume-signal
    - ~/nanoclaw-state/voice-channel-spec/tracing-contract.md (`delta_bytes` field name + redaction contract — same §201 invariant as Task 4)
  </read_first>
  <action>
    1. Parse call_id + verdict from Task 5.

    2. Record into Step 3:
        - Status: PASS or FAIL.
        - call_id: rtc_XXX.
        - Trace path.
        - Demonstrated Case-2 dialog (tick the matching D-29 checkbox).
        - Timestamp.

    3. Run the SAME 5 checks as Task 4 on this trace, but with the Sie-axis variant for the rendered-instructions sanity (Case-2 is Sie-form per D-25). Append "## Step 3 — checks" sub-section.

       (a) Trace existence: `test -s "$TRACE"`.
       (b) §201 invariant — `delta_bytes` integrity + no non-empty `.delta` payload (same jq as Task 4 step 4):
        ```bash
        MISSING_BYTES=$(jq -r 'select(.type == "response.audio.delta") | (if (.delta_bytes // null) == null then "missing" else "ok" end)' "$TRACE" | grep -c '^missing$' || true)
        test "$MISSING_BYTES" -eq 0 || { echo "BREACH: $MISSING_BYTES response.audio.delta events lack delta_bytes"; exit 1; }
        AUDIO_LEAK=$(jq -r 'select(.type == "response.audio.delta") | (.delta // "") | length' "$TRACE" | awk '$1 > 0 { print "AUDIO_LEAK_BYTES:" $1 }')
        test -z "$AUDIO_LEAK" || { echo "BREACH: §201 audio leak detected on $TRACE — $AUDIO_LEAK"; exit 1; }
        ```

       (c) AGENT_NOT_WIRED regression: `grep -c 'AGENT_NOT_WIRED' "$TRACE"` expects 0.

       (d) Sie-form positive + placeholder-leak negative (Case-2 is Sie-axis per D-25; rendered instructions must contain Sie-axis tokens AND zero `{{...}}` placeholder tokens):
        ```bash
        # Positive: Sie-axis tokens present.
        SIE_HITS=$(jq -r 'select(.type == "session.updated" or .type == "session.created") | .session.instructions // ""' "$TRACE" | grep -E '\b(Sie|Ihnen)\b' | head -3)
        echo "$SIE_HITS"
        test -n "$SIE_HITS" || echo "NOTE: no Sie-axis tokens in rendered instructions — possible Du/Sie misderivation (would also surface as conversation problem the counterpart noticed)"

        # Negative: NO {{...}} placeholder tokens leaked into rendered instructions.
        PLACEHOLDER_LEAK=$(jq -r 'select(.type == "session.updated" or .type == "session.created") | .session.instructions // ""' "$TRACE" | grep -c '{{[a-z_]\+}}' || true)
        test "$PLACEHOLDER_LEAK" -eq 0 || { echo "BREACH: placeholder tokens leaked into rendered instructions: $PLACEHOLDER_LEAK matches"; exit 1; }
        ```

       (e) Transcript-trigger health: `journalctl --user -u voice-bridge --since "1 hour ago" --no-pager | grep -c 'transcript_trigger_failed'` — expect 0 or very low.

    4. **Finalize cutover decision** in `## Final cutover decision` section of the log:
        - Read all three steps' Status fields.
        - If all three are PASS: write `All three PASS? YES` and `Proceed to Plan 06-03 (default flip + cleanup)? YES`.
        - If any is FAIL: write `All three PASS? NO — <which step failed>` and `Proceed to Plan 06-03? NO — Carsten decision: <revert | re-run | accept-with-caveat>`.
        - Add a `## Cutover summary` paragraph: 3-4 sentences distilling outcomes for the Plan 06-03 reviewer.

    5. Confirm the cutover-log is committable. Do NOT commit yet — Plan 06-03 atomically commits all cutover artifacts as part of the cleanup commit IF Carsten approves the cleanup gate. (Optional: commit the log alone with `docs(05.6): cutover-log after live PSTN PASS` if Carsten requests an intermediate snapshot — Carsten decides.)

    6. Output a final paragraph for Carsten:
        - The three step verdicts.
        - Whether Plan 06-03 unblocks.
        - Any operational notes (e.g. observed cold-start latency, agent-render quality, edge cases worth flagging in the briefing).

    7. **On FAIL** (Carsten verdict FAIL, OR §201 BREACH, OR AGENT_NOT_WIRED found, OR placeholder-leak BREACH): set Status: FAIL and apply the plan-level FAIL-recovery policy from `<objective>`:
        - Diagnose (root-cause via trace + journal).
        - Fix in a separate plan or hot-patch commit (NOT inside Phase 05.6 plans).
        - Re-run synthetic smoke (Task 2).
        - Re-execute Task 6 from the beginning (Carsten triggers a fresh outbound call → fresh trace → fresh verdict).
        - Three FAIL cycles on Step 3 without progress → escalate to chat as **ASK FOR CHAT** (architectural review).
        - §201 BREACH does NOT follow this generic recovery — halts the entire cutover plan, audit-level investigation required.
  </action>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw && grep -E 'Step 3.*Status: (PASS|FAIL)' .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md && grep -E 'Final cutover decision' .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md && grep -E 'All three PASS' .planning/phases/05.6-container-agent-integration-cutover/06-02-cutover-log.md</automated>
  </verify>
  <acceptance_criteria>
    - Step 3 has Status: PASS or FAIL (not PENDING) with call_id + trace path + D-29 checkbox.
    - "## Step 3 — checks" sub-section present with 5 check results, including §201 (`delta_bytes` integrity + `.delta` content emptiness), AGENT_NOT_WIRED grep, Sie-axis positive, placeholder-leak negative, transcript-trigger health.
    - "## Final cutover decision" section has explicit yes/no for both `All three PASS?` and `Proceed to Plan 06-03?`.
    - "## Cutover summary" paragraph present for Plan 06-03 reviewer.
    - On any FAIL: the log clearly states which step failed and Carsten's decision (revert / re-run / accept-with-caveat); FAIL-recovery workflow noted.
  </acceptance_criteria>
  <done>
    All three cutover steps recorded with verdicts. Final decision is explicit and unambiguous. Plan 06-03 has the inputs it needs to either proceed (3x PASS) or halt (any FAIL — Carsten override possible).
  </done>
</task>

</tasks>

<verification>
- 06-02-cutover-log.md exists with: Pre-cutover gate, Step 1 (synth), Step 2 (inbound), Step 3 (outbound), Final cutover decision, Cutover summary.
- Each step has Status: PASS or FAIL (none PENDING by end of plan).
- Each PSTN step has a non-empty trace artifact at `~/nanoclaw/voice-container/runs/turns-${call_id}.jsonl`.
- §201 audio-leak check ran on each PSTN trace using the contract-correct fields (`delta_bytes` integrity AND `.delta` emptiness) and found no BREACH.
- AGENT_NOT_WIRED grep on each PSTN trace returns 0.
- Placeholder-leak negative check ran on each PSTN trace and returned 0 `{{...}}` matches.
- Du-axis (Step 2) and Sie-axis (Step 3) positive checks ran on rendered instructions.
- voice-bridge/src/config.ts is UNCHANGED — runtime ENV only flip.
- ENV files reference `http://127.0.0.1:3201/` for NanoClaw MCP transport — no `10.0.0.x` addresses.
- Carsten manual approvals captured verbatim at all three checkpoints.
</verification>

<success_criteria>
- Synthetic test PASS under runtime ENV REASONING_MODE=container-agent.
- Inbound PSTN PASS with demonstrated Case-6b functionality (D-29 strict).
- Outbound Case-2 PSTN PASS with demonstrated booking-attempt dialog (D-29 strict).
- All three trace artifacts archived under voice-channel-spec tracing-contract path.
- §201 zero-audio-leak invariant holds in all traces (verified via contract-correct fields).
- No `{{...}}` placeholder leak in any rendered instructions (positive Du/Sie axis token + negative placeholder token both pass).
- AGENT_NOT_WIRED is verifiably absent from production traffic (regression secondary evidence after Plan 06-01 grep).
- Final cutover decision recorded — Plan 06-03 unblocks (if all PASS) or stays blocked (if any FAIL — Carsten override possible via explicit log entry).
- FAIL-recovery workflow exercised correctly if any step FAILed (separate fix plan + re-run synth + re-execute step + 3-strike escalation).
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-container-agent-integration-cutover/05.6-02-SUMMARY.md` describing:
- The three step verdicts (synth + inbound + outbound) and trace paths
- Cold-start latency observed inbound (D-5: 2s tolerated; record actual)
- Outbound greeting latency (D-6: pre-warm via Andy-trigger; record actual time from Andy-trigger receipt to first bot utterance)
- Any §201 / AGENT_NOT_WIRED / Du-or-Sie / placeholder-leak sanity findings
- The decision input for Plan 06-03 (proceed yes/no)
- Operational notes for the nanoclaw-state briefing (Plan 06-03 commits the briefing update at the end)
- If any FAIL occurred and was recovered: a brief note on the fix commit SHA + retry count + final verdict
</output>
</content>
