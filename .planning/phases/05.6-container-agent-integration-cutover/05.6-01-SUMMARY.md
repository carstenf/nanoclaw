---
phase: 05.6-container-agent-integration-cutover
plan: 01
subsystem: voice
tags: [container-agent, voice-triggers, mcp, mid-call-gateway, REQ-DIR-17]

requires:
  - phase: 05.5-slow-brain-removal-container-agent
    provides: Phase-05.5 voice_triggers_* MCP-tool stubs (DI seam, AGENT_NOT_WIRED no-op default), nanoclaw-mcp-client, REASONING_MODE flag, voice-personas skill, FALLBACK_PERSONA
provides:
  - Real `defaultInvokeAgent` and `defaultInvokeAgentTurn` in `src/voice-agent-invoker.ts` — synchronous container-runner invocation that loads the voice-personas skill, derives Du/Sie per case_type, substitutes every `{{...}}` placeholder, and returns the rendered persona string.
  - Real container-runner integration for `voice_triggers_init` (handler entry → registerActiveCall → invokeAgent → rendered string) and `voice_triggers_transcript` (per-turn FIFO → invokeAgentTurn → instructions_update or null).
  - NanoClaw-side mid-call mutation gateway in `src/voice-mid-call-gateway.ts` — module-level active-call set + `checkMidCallMutation(call_id, tool_name, tool_meta)`. ToolRegistry consults the gateway in its `invoke()` dispatch path before invoking any handler whose tool meta has `mutating: true`. 11 mutating tools marked at registration. 3-tier REQ-DIR-17 defense: agent prompt + dispatch gateway + handler-boundary `__MUTATION_ATTEMPT__` sentinel.
  - Live-cutover synthetic test (`voice-bridge/tests/live-cutover-synth.test.ts`) — code-only Bridge↔NanoClaw-MCP↔container-agent end-to-end harness covering init synth (Du-form), transcript synth (instructions_update), Du/Sie axis, and REQ-DIR-12 timeout fallback.
  - Production fix in `src/mcp-stream-server.ts`: skip Pitfall-8 synthetic-ID injection for `voice_triggers_init` / `voice_triggers_transcript` (would otherwise overwrite the real `rtc_*` call_id and replace the numeric Realtime turn_id with a string, breaking every transcript trigger via Zod).
affects: [05.6-02, 05.6-03, all future container-agent voice-channel work]

tech-stack:
  added: []
  patterns:
    - "DI-with-real-default — the Phase-05.5 invokeAgent/invokeAgentTurn DI seam now resolves to a real production default; tests can still inject mocks. Wrapping pattern preserves backwards compatibility with all 05.5 unit tests."
    - "Active-call set as module-local Set<string> — sufficient for single-process NanoClaw deploy. Marked future-work for multi-process scaling (Redis/SQLite)."
    - "ToolRegistry dispatch-path metadata — `register(name, handler, meta?)` with optional `{ mutating: true }`. Backward-compatible (default `meta: {}` → non-mutating)."

key-files:
  created:
    - src/voice-agent-invoker.ts
    - src/voice-agent-invoker.test.ts
    - src/voice-mid-call-gateway.ts
    - src/voice-mid-call-gateway.test.ts
    - voice-bridge/tests/live-cutover-synth.test.ts
  modified:
    - src/mcp-tools/index.ts
    - src/mcp-tools/voice-triggers-init.ts
    - src/mcp-tools/voice-triggers-init.test.ts
    - src/mcp-tools/voice-triggers-transcript.ts
    - src/mcp-tools/voice-triggers-transcript.test.ts
    - src/mcp-tools/voice-finalize-call-cost.ts
    - src/mcp-tools/voice-finalize-call-cost.test.ts
    - src/mcp-stream-server.ts (deviation — skip synthetic IDs for voice_triggers_*)

key-decisions:
  - "REQ-DIR-17 enforced at 3 layers: (1) agent prompt forbids mutating tools mid-call, (2) dispatch-path gateway in mcp-tools/index.ts ToolRegistry.invoke rejects mutating tools when call_id is in active-call set, (3) handler-boundary __MUTATION_ATTEMPT__ sentinel from Phase 05.5-01 retained as defense-in-depth."
  - "Active-call lifecycle anchored at handler entry of voice_triggers_init (registerActiveCall) and finalize handler (deregisterActiveCall, on success AND failure paths so leak-proof)."
  - "Mutating-tool metadata declared once at registration time in `mcp-tools/index.ts`; non-mutating is the default. 11 tools marked: voice_create/update/delete_calendar_entry, voice_send_discord_message, voice_schedule_retry, voice_request_outbound_call, voice_insert_price_snapshot, voice_reset_monthly_cap, voice_start_case_2_call, voice_case_2_retry, voice_notify_user."
  - "Pitfall-8 synthetic-ID injection in mcp-stream-server.ts must NOT apply to voice_triggers_* (Bridge passes real IDs). Surgical name-based skip — preserves chat/iOS disjoint-key semantics for every other voice.* tool."

patterns-established:
  - "Test 1+3 of live-cutover-synth: full Bridge /accept fixture (mocks openai.webhooks.unwrap + supertest) driving real defaultInvokeAgent through stubbed runContainer. Test 2: NanoclawMcpClient.transcript through real MCP wire. Test 4: stub runContainer hangs past timeout → FALLBACK_PERSONA + warn-log."
  - "Active-call set test discipline: every test calls _resetActiveSet() in beforeEach to prevent cross-test bleed."

deviations:
  - "Added `src/mcp-stream-server.ts` to files_modified (not in original Plan files_modified list). Required because the synth test exposed a real production bug — synthetic-ID injection broke voice_triggers_transcript Zod validation in the real wire path. Fix is surgical (name-based skip for two voice_triggers_* tools) and necessary to make Wave 2 PSTN cutover functional. Without this fix, every transcript trigger in production would fail."
  - "Wave 1 was executed by gsd-executor subagent which hit the per-tier usage cap after committing Tasks 1, 2, 4 (commits a58e3fd, 91d6079, 1f3d105). Task 3 (synth test) was finished by the orchestrator inline, plus the production fix discovered during Task 3 verification. Final 5 commits: 3 by subagent + 2 by orchestrator (fix + test)."

verification:
  automated:
    - "voice-bridge: 466/466 pass + 5 skipped — `cd voice-bridge && npx vitest run`"
    - "nanoclaw: 720/721 pass — `npx vitest run` (1 pre-existing gmail.test.ts failure unrelated to this plan; logged in deferred-items.md)"
    - "synth test: 4/4 pass — `cd voice-bridge && npx vitest run tests/live-cutover-synth.test.ts`"
    - "AGENT_NOT_WIRED grep: zero matches in production code (only in comments + assertion-target test strings)"

requirements_satisfied:
  - REQ-DIR-06 — voice_triggers_init now returns a fully-rendered persona string from container-agent
  - REQ-DIR-11 — instructions-only updates via session.update (no tools field, asserted in synth Test 2)
  - REQ-DIR-12 — agent timeout → FALLBACK_PERSONA + container_agent_init_failed warn-log (asserted in synth Test 4)
  - REQ-DIR-14 — real container spawn semantics via runContainerAgent
  - REQ-DIR-16 — full turn-history forwarded on every transcript trigger (5-turn test in voice-triggers-transcript.test.ts + synth Test 2)
  - REQ-DIR-17 — mid-call mutation enforcement at NanoClaw-side dispatch gateway (10 gateway tests + 2 handler-side register/deregister tests)
  - REQ-VOICE-13 — synthetic 2-turn end-to-end test exists (live-cutover-synth.test.ts) ready for Plan 05.6-02 to re-run under REASONING_MODE=container-agent

next:
  - Plan 05.6-02 (Wave 2): live PSTN cutover under REASONING_MODE=container-agent (runtime ENV, default flag NOT yet flipped) — synth test re-run + 3 PSTN tests with explicit Carsten gates (D-28a..d). Requires manual coordination — Carsten on phone.
  - Plan 05.6-03 (Wave 2): final-cut transition. Flip REASONING_MODE default in voice-bridge/src/config.ts in a SEPARATE commit, then atomic cleanup commit (slow-brain.ts + persona/* delete + REASONING_MODE flag check removal + ENV cleanup). Gated by 05.6-02 PASS + Carsten cleanup-gate approval (D-30).

## Self-Check: PASSED
- All 4 plan tasks executed and committed
- 5 atomic commits on main (3 subagent + 2 orchestrator)
- SUMMARY.md created
- AGENT_NOT_WIRED removed from production code paths
- All test gates green (synth + voice-bridge full + nanoclaw full minus pre-existing gmail flake)
- Production bug discovered + fixed (mcp-stream-server synthetic-ID injection)
