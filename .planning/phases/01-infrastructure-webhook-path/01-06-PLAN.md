---
phase: 01-infrastructure-webhook-path
plan: 06
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/test-results.md
  - ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt
  - ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-1.txt
  - ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-2.txt
  - ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-3.txt
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-08
  - SIP-01
  - SIP-03
  - SIP-04
  - SIP-05
  - SIP-06
  - SIP-07
tags:
  - integration-test
  - pstn
  - phase-gate
must_haves:
  truths:
    - "D-25 synthetic webhook test passes: signed POST to public Caddy URL → bridge JSONL has matching entry within 2s with signature_valid: true"
    - "D-26 live integration: 3 consecutive PSTN calls (Carsten's mobile → Sipgate DID +49 30 8687022345) succeed"
    - "Per call: FreeSWITCH bridges to OpenAI SIP within 500ms (SIP-03)"
    - "Per call: webhook arrives at bridge within 2s of INVITE (INFRA-03)"
    - "Per call: BYE releases both legs within 2s (SIP-06)"
    - "Per call: SDP shows PCMU only on Sipgate leg (SIP-04)"
    - "Test results captured in ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/ per D-27"
    - "Phase 1 exit criteria from ROADMAP.md §Phase 1 Success Criteria all green (5 items)"
  artifacts:
    - path: "~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/test-results.md"
      provides: "Phase 1 integration test result document per D-27"
      contains: "D-26"
    - path: "~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt"
      provides: "D-25 synthetic test transcript"
      contains: "signature_valid"
  key_links:
    - from: "Carsten's mobile dial"
      to: "FreeSWITCH bridges to OpenAI SIP"
      via: "Sipgate INVITE on +49 30 8687022345 → dialplan"
      pattern: "Bridge.*sip\\.api\\.openai\\.com"
    - from: "OpenAI realtime.call.incoming"
      to: "Bridge JSONL log entry"
      via: "Caddy → forwarder → WG → bridge re-verify"
      pattern: "event_type.*realtime\\.call\\.incoming.*signature_valid.*true"
---

<objective>
Run the two integration tests defined in CONTEXT D-25 (synthetic webhook)
and D-26 (3 live PSTN calls) to prove Phase 1's full network spine works
end-to-end. Capture results in `voice-channel-spec/spike/01-webhook-path/`
per D-27. This plan is the Phase-1-exit gate for all 5 success criteria
in ROADMAP.md §Phase 1.

Purpose: Phase 1 is "config done"; this plan verifies "actually works."
Without it, the next phase (Director Bridge v0 + Hot-Path Safety) would
build on unverified infra. Per RESEARCH §Summary build order, integration
test is the last step.

Output: Three live-call result files + one synthetic-webhook result file +
one summary results.md. All five Phase 1 success criteria from ROADMAP.md
green with explicit log evidence per QUAL-01-style discipline.

NOTE: This phase 1 stub does NOT call OpenAI /accept (that is Phase 2 per
D-07). Therefore the live calls will: ring Carsten's mobile → bridge to
OpenAI SIP → OpenAI emits webhook → bridge logs it → OpenAI's session
times out (no /accept) → call drops. The test verifies the SIGNAL PATH,
not a working conversation. That's by design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-webhook-path/01-CONTEXT.md
@.planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md
@.planning/phases/01-infrastructure-webhook-path/01-01-PLAN.md
@.planning/phases/01-infrastructure-webhook-path/01-02-PLAN.md
@.planning/phases/01-infrastructure-webhook-path/01-03-PLAN.md
@.planning/phases/01-infrastructure-webhook-path/01-04-PLAN.md
@.planning/phases/01-infrastructure-webhook-path/01-05-PLAN.md

~/nanoclaw/voice-stack/vs-webhook-forwarder/test_synthetic.py  # D-25 fixture (created in Plan 03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-25 — synthetic webhook end-to-end test (no PSTN required)</name>
  <files>
    ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt
  </files>
  <read_first>
    - .planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md §"Template 9: Synthetic webhook test fixture"
    - .planning/phases/01-infrastructure-webhook-path/01-CONTEXT.md D-25
    - .planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md §"Code Examples" — Synthetic webhook test block
    - ~/nanoclaw/voice-stack/vs-webhook-forwarder/test_synthetic.py (the fixture itself)
  </read_first>
  <action>
    1. Ensure capture directory exists:
       ```
       mkdir -p ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path
       ```

    2. Capture starting state of bridge JSONL (so we can diff after):
       ```
       LOG_FILE="$HOME/nanoclaw/voice-container/runs/bridge-$(date +%F).jsonl"
       BEFORE_LINES=$(wc -l < "$LOG_FILE" 2>/dev/null || echo 0)
       echo "Starting log line count: $BEFORE_LINES"
       ```

    3. Run the synthetic webhook fixture from Plan 03 against the public
       Caddy URL. Hostname comes from Plan 02 Task 1; secret from
       ~/nanoclaw/.env:
       ```
       CHOSEN_HOST=$(read -p "voice-webhook hostname (e.g. voice-webhook.example.com): " H && echo "$H")
       OPENAI_WEBHOOK_SECRET=$(grep '^OPENAI_WEBHOOK_SECRET=' ~/nanoclaw/.env | cut -d= -f2-) \
       VOICE_WEBHOOK_PUBLIC_URL="https://${CHOSEN_HOST}/openai/webhook" \
       python3 ~/nanoclaw/voice-stack/vs-webhook-forwarder/test_synthetic.py \
         > ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt 2>&1
       cat ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt
       ```
       Expected stdout: `status=200 body=...{"ok":true}...` per Template 1's
       relay-route response.

    4. Wait 2s for bridge to flush JSONL, then verify entry appeared:
       ```
       sleep 2
       LOG_FILE="$HOME/nanoclaw/voice-container/runs/bridge-$(date +%F).jsonl"
       AFTER_LINES=$(wc -l < "$LOG_FILE")
       echo "Log line count after: $AFTER_LINES (delta: $((AFTER_LINES - BEFORE_LINES)))"
       tail -3 "$LOG_FILE" | tee -a ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt
       grep -q '"signature_valid":true' "$LOG_FILE" && echo "SIGNATURE_OK" \
         || echo "SIGNATURE_FAIL — investigate"
       ```
       Expected: AFTER_LINES > BEFORE_LINES; tail shows entry with
       `event:"webhook_received"`, `event_type:"realtime.call.incoming"`,
       `signature_valid:true`, `payload_size:` numeric.

    5. Append final pass/fail line to result file:
       ```
       if grep -q '"signature_valid":true' "$LOG_FILE"; then
         printf '\n=== D-25 VERDICT: PASS ===\n' >> ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt
       else
         printf '\n=== D-25 VERDICT: FAIL ===\n' >> ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt
       fi
       ```

    Note on RESEARCH Assumption A2: if the synthetic fixture's HMAC
    reproduction does NOT match OpenAI's exact scheme (and thus signature
    verification fails on the bridge side even though the secret is right),
    do NOT mark D-25 as a PASS. Instead, document the discrepancy in the
    result file and rely on the live PSTN test in Task 2 for INFRA-03
    end-to-end coverage. The SDK round-trip in production is unaffected
    (real OpenAI webhooks use the SDK's own signing path) — only the test
    fixture is at risk.
  </action>
  <verify>
    <automated>test -f ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt && grep -qE 'D-25 VERDICT: (PASS|FAIL)' ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/synthetic-webhook-result.txt && echo SYNTHETIC_RECORDED</automated>
  </verify>
  <done>
    synthetic-webhook-result.txt exists with full fixture stdout, JSONL tail
    snippet, and explicit `D-25 VERDICT: PASS` line (or FAIL with documented
    cause referencing Assumption A2). If FAIL: live PSTN test in Task 2 is
    the fallback for INFRA-03 verification.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: D-26 — 3 live PSTN test calls + capture results</name>
  <files>
    ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-1.txt,
    ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-2.txt,
    ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-3.txt
  </files>
  <read_first>
    - .planning/phases/01-infrastructure-webhook-path/01-CONTEXT.md D-26 (3 consecutive calls, three different times of day for codec drift)
    - .planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md §"Code Examples" verification commands
    - .planning/ROADMAP.md §Phase 1 Success Criteria (the 5 items being verified)
  </read_first>
  <what-built>
    Phase 1 infra stack (Plans 01–05) is fully deployed and ready. This
    checkpoint is the live PSTN gate test — Carsten dials his Sipgate DID
    (+49 30 8687022345) from his mobile, three times, ideally at three
    different times of day per CONTEXT specifics ("catch carrier-codec
    drift").

    Each call should:
    - Ring (Sipgate → FreeSWITCH → bridge attempt to OpenAI SIP)
    - Briefly connect (OpenAI accepts SIP, sends webhook)
    - Drop after a few seconds because Phase 1 stub does NOT /accept
      (Phase 2 owns that). Carsten will hear a brief tone or silence.

    The proof of success is in the LOGS, not the audio. The infra
    receives + verifies the signal end-to-end.
  </what-built>
  <how-to-verify>
    For EACH of the 3 calls, capture two log streams in real time. carsten_bot
    (this agent) prepares the capture commands in two terminals; carsten
    presses dial; carsten_bot captures + appends to result files.

    PER CALL (repeat 3 times, ideally 1×morning, 1×midday, 1×evening per
    CONTEXT specifics):

    1. Pick CALL_NUM ∈ {1, 2, 3}. Note start timestamp:
       ```
       CALL_NUM=1
       OUT=~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-${CALL_NUM}.txt
       START_TS=$(date -u +%FT%TZ)
       echo "=== Live call $CALL_NUM start: $START_TS ===" > "$OUT"
       ```

    2. Tail bridge JSONL in one capture command (run as background):
       ```
       LOG_FILE="$HOME/nanoclaw/voice-container/runs/bridge-$(date +%F).jsonl"
       BEFORE=$(wc -l < "$LOG_FILE")
       ```

    3. Tail FreeSWITCH log on Hetzner in another capture command:
       ```
       ssh hetzner 'docker logs --since 5s --follow vs-freeswitch 2>&1' \
         > /tmp/fs-call-${CALL_NUM}.log &
       FS_TAIL_PID=$!
       ```

    4. Notify carsten: "READY — please dial +49 30 8687022345 from your
       mobile NOW for call $CALL_NUM. Let it ring; let any audio play for
       up to 10 seconds; then hang up."

    5. Wait for carsten to confirm hang-up. Then:
       ```
       sleep 3
       kill $FS_TAIL_PID 2>/dev/null
       END_TS=$(date -u +%FT%TZ)
       echo "=== Live call $CALL_NUM end: $END_TS ===" >> "$OUT"
       ```

    6. Extract evidence per Phase 1 success criterion:
       ```
       LOG_FILE="$HOME/nanoclaw/voice-container/runs/bridge-$(date +%F).jsonl"
       AFTER=$(wc -l < "$LOG_FILE")

       echo -e "\n--- ROADMAP §Phase 1 SC #1: FreeSWITCH REGED ---" >> "$OUT"
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "sofia status gateway sipgate"' >> "$OUT" 2>&1

       echo -e "\n--- ROADMAP §Phase 1 SC #2: bridge to OpenAI SIP within 500ms + PCMU + RTP + BYE within 2s ---" >> "$OUT"
       echo "FreeSWITCH log slice (last 200 lines around call):" >> "$OUT"
       tail -200 /tmp/fs-call-${CALL_NUM}.log >> "$OUT" 2>&1
       echo "" >> "$OUT"
       echo "Search for 'Bridge' lines (timing):" >> "$OUT"
       grep -E "Bridge|Outgoing INVITE|sip\.api\.openai" /tmp/fs-call-${CALL_NUM}.log >> "$OUT" 2>&1
       echo "" >> "$OUT"
       echo "Search for SDP (codec verification):" >> "$OUT"
       grep -E "SDP|PCMU|PCMA|G\.722" /tmp/fs-call-${CALL_NUM}.log | head -20 >> "$OUT" 2>&1

       echo -e "\n--- ROADMAP §Phase 1 SC #3: webhook arrived at bridge within 2s, signature OK ---" >> "$OUT"
       echo "Bridge JSONL entries written during this call (lines $BEFORE..$AFTER):" >> "$OUT"
       sed -n "$((BEFORE+1)),${AFTER}p" "$LOG_FILE" >> "$OUT" 2>&1

       echo -e "\n--- ROADMAP §Phase 1 SC #4: WG MTU 1380 + heartbeat ---" >> "$OUT"
       ip link show wg0 | grep mtu >> "$OUT"
       grep -c '"event":"wg_canary_fail"' "$LOG_FILE" | xargs -I{} echo "wg_canary_fail count today: {}" >> "$OUT"

       echo -e "\n--- ROADMAP §Phase 1 SC #5: bridge systemd unit running + /health ---" >> "$OUT"
       systemctl --user is-active voice-bridge >> "$OUT"
       curl -sS http://10.0.0.2:4401/health | tee -a "$OUT"
       ```

    7. Per-call verdict — write at end of result file. The verdict line
       MUST contain a literal `PASS` or `FAIL` word after the colon (NOT
       the placeholder text `<fill in based on above>` — the auto-verify
       below regexes for `PASS / FAIL: <PASS|FAIL>` and rejects the
       placeholder):
       ```
       echo -e "\n=== CALL $CALL_NUM VERDICT ===" >> "$OUT"
       # Decide verdict based on:
       # - At least one Bridge line referencing sip.api.openai.com appears
       # - At least one bridge JSONL entry with event_type=realtime.call.incoming + signature_valid=true
       # - SDP shows PCMU only
       # - sofia status still REGED post-call
       # - /health still 200
       # Then write ONE of these two lines (literal PASS or FAIL after the colon):
       echo "PASS / FAIL: PASS" >> "$OUT"   # if all five criteria met
       # or
       echo "PASS / FAIL: FAIL — <root cause one-liner>" >> "$OUT"  # if any criterion missed
       ```

    Repeat for CALL_NUM=2 and CALL_NUM=3, ideally separated by hours per
    CONTEXT specifics §"Test calls in D-26 should be done at three different
    times of day to catch carrier-codec drift."
  </how-to-verify>
  <action>
    carsten_bot drives the verification commands; carsten executes the
    actual phone dials. Three live-call-N.txt files produced, each with
    full FreeSWITCH log slice + bridge JSONL slice + per-criterion grep
    evidence + explicit PASS/FAIL verdict.

    CRITICAL: each file's verdict line must read literally
    `PASS / FAIL: PASS` or `PASS / FAIL: FAIL` (with optional reason after).
    Do NOT leave the placeholder `<fill in based on above>` in place —
    the auto-verify regex rejects it (per WARNING #3 closure 2026-04-16).
  </action>
  <verify>
    <automated>for n in 1 2 3; do test -f ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-${n}.txt && grep -qE 'PASS / FAIL:[[:space:]]+(PASS|FAIL)\b' ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/live-call-${n}.txt || { echo "MISSING_OR_PLACEHOLDER call-${n}"; exit 1; }; done; echo ALL_3_CAPTURED</automated>
  </verify>
  <done>
    Three live-call-{1,2,3}.txt files exist; each contains FreeSWITCH log
    slice + bridge JSONL slice + 5 Phase-1-criterion grep evidence blocks
    + explicit per-call verdict line `PASS / FAIL: <PASS|FAIL>` (literal
    word, not placeholder). At least 2 of 3 must be PASS for Phase 1
    to ship (one carrier-side flake is acceptable per spike methodology;
    if 2+ fail, surface to carsten for root-cause before declaring phase
    complete).
  </done>
  <resume-signal>Type "3-calls-complete" with summary of pass/fail counts</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Consolidate test-results.md (D-27) + roll up Phase 1 exit verdict</name>
  <files>
    ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/test-results.md
  </files>
  <read_first>
    - All four artifacts from Tasks 1+2 (synthetic-webhook-result.txt,
      live-call-{1,2,3}.txt)
    - .planning/ROADMAP.md §Phase 1 Success Criteria (the 5 items)
    - .planning/phases/01-infrastructure-webhook-path/01-CONTEXT.md D-27
  </read_first>
  <action>
    Create the consolidated test-results.md per D-27 with this structure:

    ```markdown
    # Phase 1 — Infrastructure & Webhook Path — Test Results

    **Date:** <YYYY-MM-DD>
    **Tester:** carsten + carsten_bot
    **Scope:** Phase 1 integration tests per CONTEXT D-25 (synthetic) + D-26 (live)
    **Per CONTEXT D-27:** results captured at this path

    ## Phase 1 Success Criteria Roll-Up (from ROADMAP.md §Phase 1)

    | # | Criterion | Verdict | Evidence |
    |---|-----------|---------|----------|
    | 1 | FreeSWITCH REGED with Sipgate (≤30s of boot, REGED visible in fs_cli) | PASS / FAIL | live-call-1.txt §SC#1, plus Plan 04 SUMMARY |
    | 2 | Real test call bridges to OpenAI SIP within 500ms, PCMU only, RTP 60000–60100/UDP, BYE releases ≤2s | PASS / FAIL | live-call-{1,2,3}.txt §SC#2 |
    | 3 | OpenAI realtime.call.incoming arrives → relayed by forwarder over WG → bridge stub verifies + 200 OK within 2s (3 consecutive test calls) | PASS / FAIL | live-call-{1,2,3}.txt §SC#3 + synthetic-webhook-result.txt |
    | 4 | WireGuard MTU pinned 1380 both ends; heartbeat detects tunnel drop ≤2s (verified by ip link set wg0 down during monitored window) | PASS / FAIL | Plan 01 SUMMARY (MTU probe) + Plan 05 SUMMARY (heartbeat ALERT test via systemd override) |
    | 5 | Bridge runs as systemctl --user under carsten_bot with Restart=on-failure; survives kill -9; /health returns green over WG | PASS / FAIL | Plan 05 SUMMARY (kill -9 → 5s restart) + live-call-1.txt §SC#5 |

    ## D-25 — Synthetic Webhook Test Result

    See: synthetic-webhook-result.txt
    Verdict: <PASS / FAIL>
    Note: <if Assumption A2 hit, document>

    ## D-26 — 3 Live PSTN Calls

    | Call | Time | Bridge target reached | PCMU only | Webhook arrived | BYE ≤ 2s | Verdict |
    |------|------|----------------------|-----------|-----------------|----------|---------|
    | 1 | <ts> | yes/no | yes/no | yes/no | yes/no | PASS/FAIL |
    | 2 | <ts> | yes/no | yes/no | yes/no | yes/no | PASS/FAIL |
    | 3 | <ts> | yes/no | yes/no | yes/no | yes/no | PASS/FAIL |

    Carrier-codec drift check (CONTEXT specifics): <observation across times of day>

    ## REQ Coverage Map

    | REQ | Status | Verified by |
    |-----|--------|-------------|
    | INFRA-01 | <P/F> | sofia status gateway sipgate REGED |
    | INFRA-02 | <P/F> | curl 401 from public Caddy URL + 200 from synthetic test |
    | INFRA-03 | <P/F> | bridge JSONL signature_valid=true on real webhook |
    | INFRA-04 | <P/F> | wg0 mtu 1380 + heartbeat fail detected within 2s |
    | INFRA-08 | <P/F> | systemctl --user is-active + kill -9 recovery |
    | SIP-01 | <P/F> | inbound INVITE accepted (live call) |
    | SIP-02 | <P/F> | smoke test in Plan 04 (NOTES.md) |
    | SIP-03 | <P/F> | Bridge time delta in FreeSWITCH log <500ms |
    | SIP-04 | <P/F> | SDP shows PCMU exclusive |
    | SIP-05 | <P/F> | Plan 02 Task 4 carsten checkpoint — evidence file `voice-channel-spec/spike/01-webhook-path/firewall-rtp-confirmed.{txt,png}` showing UDP 60000-60100 inbound allow rule; further attested by media flow during live calls |
    | SIP-06 | <P/F> | both legs released within 2s of BYE |
    | SIP-07 | <P/F> | originate_timeout=3 in dialplan; not exercised in test (would require breaking OpenAI target) |

    ## Phase 1 Exit Verdict

    **Overall:** <PASS — proceed to Phase 2 / FAIL — see open issues below>

    ## Open Issues / Follow-ups

    - <list any non-blocking items observed; e.g. one of three live calls had carrier flake — acceptable per spike methodology>
    - <items deferred to Phase 2: /accept call, sideband WS, persona, tools>

    ## References

    - .planning/phases/01-infrastructure-webhook-path/01-{01..05}-SUMMARY.md
    - synthetic-webhook-result.txt
    - live-call-{1,2,3}.txt
    - voice-channel-spec/spike/01-webhook-path/firewall-rtp-confirmed.{txt,png}  (SIP-05 evidence per Plan 02 Task 4)
    ```

    1. Fill in all <placeholders> from the four input artifacts.
    2. Compute Overall verdict: PASS iff ALL 5 ROADMAP criteria PASS AND
       at least 2 of 3 live calls PASS AND synthetic test PASS (or
       documented A2 fallback).
    3. If Overall is FAIL: list specific blockers in Open Issues so a
       gap-closure planning round (per `/gsd-plan-phase --gaps`) has
       material to work with.
    4. Commit-staged: this file + the four input artifacts. Per D-27 the
       location IS `~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/`
       — confirm path exactly.
  </action>
  <verify>
    <automated>test -f ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/test-results.md && grep -qE "Phase 1 Exit Verdict" ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/test-results.md && grep -qE "Overall:.*\\*\\*(PASS|FAIL)" ~/nanoclaw-state/voice-channel-spec/spike/01-webhook-path/test-results.md && echo RESULTS_CONSOLIDATED</automated>
  </verify>
  <done>
    test-results.md exists at the D-27 path with 5-criterion roll-up table,
    D-25 verdict, D-26 3-call table, REQ coverage map, Phase 1 exit verdict
    (PASS or FAIL with explicit reason), and Open Issues list. All 12
    Phase-1 REQs (INFRA-01..04, INFRA-08, SIP-01..07) accounted for.
    SIP-05 row points at the Plan 02 Task 4 evidence file (per BLOCKER #2
    closure 2026-04-16). Per D-27.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Carsten's mobile → Sipgate → FreeSWITCH | PSTN — trusted carrier; Carsten knows he's testing |
| FreeSWITCH → OpenAI SIP TLS | Production OpenAI; first time real SIP test crosses to project proj_4tEBz3X… |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Live test calls produce real OpenAI session metadata (call_id, From-headers in webhook) | mitigate | Phase 1 stub does NOT /accept; OpenAI session hangs no audio; ZDR (project-level, see PITFALL #4) covers retention; Phase 0 already verified ZDR |
| T-06-02 | Repudiation | No record of test call evidence | mitigate | live-call-N.txt + JSONL slice + sofia status output captured per call |
| T-06-03 | DoS | 3 test calls flap REGISTER state | accept | Standard SIP behavior; spike-style test harmless to gateway |
| T-06-04 | Tampering | Test result files manipulated | accept | Files committed via git in nanoclaw-state repo; tampering visible in git log |
</threat_model>

<verification>
- synthetic-webhook-result.txt exists and contains explicit `D-25 VERDICT` line
- live-call-{1,2,3}.txt all exist with explicit `PASS / FAIL: <PASS|FAIL>` line each (literal verdict word, not placeholder)
- test-results.md exists at D-27 path with 5-criterion roll-up + REQ coverage table
- Overall verdict is explicit PASS or FAIL (not blank)
- All 12 Phase-1 REQ IDs appear in REQ coverage map
- ROADMAP §Phase 1 Success Criteria #1-5 all addressed in roll-up table
- SIP-05 row in REQ map references Plan 02 Task 4 evidence file
</verification>

<success_criteria>
Phase 1 entry-to-exit verified. If Overall PASS: Phase 1 ships, ROADMAP
checkbox flips, STATE.md updates to "Phase 2 ready." If Overall FAIL:
test-results.md Open Issues feeds `/gsd-plan-phase --gaps` for Plan 07+
remediation; Phase 2 blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-webhook-path/01-06-SUMMARY.md`
recording:
- Synthetic test verdict
- Three call verdicts + times of day
- Phase 1 exit verdict
- Path to test-results.md
- Any items added to Open Issues for follow-up plans
- If Phase 1 PASS: handoff note for Phase 2 planning (Director Bridge v0
  inherits the bridge stub; only needs to add /accept + sideband WS + tools
  per ROADMAP §Phase 2)
</output>
