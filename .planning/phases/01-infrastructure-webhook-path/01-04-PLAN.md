---
phase: 01-infrastructure-webhook-path
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml
autonomous: true
requirements:
  - INFRA-01
  - SIP-01
  - SIP-02
  - SIP-03
  - SIP-04
  - SIP-06
  - SIP-07
tags:
  - sip
  - freeswitch
  - dialplan
must_haves:
  truths:
    - "FreeSWITCH dialplan public/01_sipgate_inbound.xml bridges Sipgate inbound to sip:proj_4tEBz3XjO4gwM5hyrvsxLM8E@sip.api.openai.com;transport=tls per D-19"
    - "PCMU codec is forced exclusive on both legs (absolute_codec_string=PCMU + codec_string=PCMU)"
    - "originate_timeout=3 enforces SIP-07 (>3s bridge fail → 503)"
    - "fs_cli reloadxml + sofia profile external rescan succeed without restart (no active calls dropped)"
    - "sofia status gateway sipgate shows State=REGED post-reload (INFRA-01)"
    - "Backup of original dialplan exists at 01_sipgate_inbound.xml.bak-<timestamp>"
    - "Outbound smoke test: fs_cli originate via sipgate gateway to a test number rings and connects (SIP-02)"
  artifacts:
    - path: "~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml"
      provides: "Phase 1 dialplan with OpenAI SIP bridge target + 503 fallback"
      contains: "sip.api.openai.com"
      min_lines: 18
    - path: "~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml.bak-{timestamp}"
      provides: "Pre-edit backup for rollback"
      contains: "extension"
  key_links:
    - from: "Sipgate inbound INVITE"
      to: "sofia/external/sip:proj_…@sip.api.openai.com;transport=tls"
      via: "FreeSWITCH bridge action"
      pattern: "bridge.*sip\\.api\\.openai\\.com"
    - from: "OpenAI bridge fail >3s"
      to: "SIP 503 to Sipgate"
      via: "originate_timeout=3 + respond 503"
      pattern: "originate_timeout=3"
    - from: "FreeSWITCH outbound"
      to: "Sipgate gateway"
      via: "existing external/sipgate.xml gateway (REGED)"
      pattern: "sofia/gateway/sipgate"
---

<objective>
Replace the existing FreeSWITCH dialplan bridge target in
`public/01_sipgate_inbound.xml` to point at the OpenAI SIP endpoint
(`sip:proj_4tEBz3XjO4gwM5hyrvsxLM8E@sip.api.openai.com;transport=tls` per
D-19), force PCMU exclusively (D-20, SIP-04), enforce 3s originate timeout
with SIP 503 fallback (SIP-07), then live-reload via `reloadxml` + `sofia
profile external rescan` so no active calls drop. Smoke-test outbound via
`fs_cli originate` for SIP-02 coverage (Pitfall NEW-2).

Purpose: This is the SIP-half of Phase 1. Without this edit, FreeSWITCH still
bridges to the legacy `sip-to-ai@127.0.0.1:5080` and Sipgate inbound never
reaches OpenAI's S2S endpoint — no call business logic can be tested.

Output: Inbound INVITE from Sipgate bridges to OpenAI SIP within ≤500ms
(SIP-03), PCMU-only on both legs, BYE releases both legs ≤2s (SIP-06),
gateway REGED visible (INFRA-01), outbound originate smoke-tested (SIP-02).

Wave/dependency note: depends_on now includes "01-03" per WARNING #5
closure (2026-04-16). The dialplan reload happens AFTER the forwarder is
deployed (Plan 03 Task 3) so the deploy mechanism referenced in Task 1
step 5 below is guaranteed available. Plans 04 and 05 still both run in
Wave 3, in parallel, once Plan 03 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-webhook-path/01-CONTEXT.md
@.planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md
@.planning/phases/01-infrastructure-webhook-path/01-01-PLAN.md  # WG MTU only — no functional dep but cleaner sequence
@.planning/phases/01-infrastructure-webhook-path/01-03-PLAN.md  # forwarder deploy mechanism (rsync) reused in Task 1 step 5

~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml  # CURRENT — model bridge target to replace
~/nanoclaw/voice-stack/conf/overlay/sip_profiles/external/sipgate.xml  # CURRENT — REGISTER + gateway config (DO NOT MODIFY)

<interfaces>
<!-- FreeSWITCH dialplan idioms verified in RESEARCH.md Template 8. -->

Inbound dialplan structure (existing file already follows this shape — replace
the <action application="bridge" .../> line; keep <include>/<extension>/<condition> wrapper):

```xml
<include>
  <extension name="sipgate_inbound">
    <condition field="destination_number" expression="...">
      <action application="..."/>
    </condition>
  </extension>
</include>
```

Bridge target syntax for OpenAI SIP:
  sofia/external/sip:proj_4tEBz3XjO4gwM5hyrvsxLM8E@sip.api.openai.com;transport=tls

Codec lock + originate timeout combined into one bridge action via inline vars:
  bridge data="[absolute_codec_string=PCMU,codec_string=PCMU]sofia/external/sip:..."

Reload commands (NO RESTART — does not drop active calls):
  docker exec vs-freeswitch fs_cli -x "reloadxml"
  docker exec vs-freeswitch fs_cli -x "sofia profile external rescan"

Verification commands:
  docker exec vs-freeswitch fs_cli -x "sofia status gateway sipgate"  # State=REGED
  docker exec vs-freeswitch fs_cli -x "show channels"                 # active calls during/post BYE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit dialplan + reload + verify REGED + verify reload didn't drop calls</name>
  <files>
    ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml,
    ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml.bak-{timestamp}
  </files>
  <read_first>
    - ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml (CURRENT — read in full before edit)
    - .planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md §"Template 8: FreeSWITCH dialplan edit"
    - .planning/phases/01-infrastructure-webhook-path/01-CONTEXT.md D-19, D-20, D-21, D-22
    - .planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md §"Pitfall NEW-2: SIP-02 outbound dialplan"
    - .planning/phases/01-infrastructure-webhook-path/01-03-SUMMARY.md (forwarder deploy mechanism — confirm rsync target on Hetzner)
  </read_first>
  <action>
    On Lenovo1 (the dialplan file lives in this repo and is mounted into the
    Hetzner container via the existing voice-stack docker-compose volume
    mount; the deploy mechanism is the same as Plan 03 Task 3 — Plan 04 now
    explicitly depends_on "01-03" per WARNING #5 closure to guarantee that
    mechanism exists and is documented in 01-03-SUMMARY.md before this
    task runs):

    1. Backup current dialplan:
       ```
       cp ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml \
          ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml.bak-$(date +%Y%m%d-%H%M%S)
       ```

    2. Read current file. Identify the `<extension name="sipgate_inbound">`
       block and its existing bridge action (currently targets
       `sofia/internal/sip-to-ai@127.0.0.1:5080` per RESEARCH §Existing Code).

    3. Replace the entire `<include>...</include>` content with the verbatim
       Template 8 from RESEARCH.md. Exact contents (substituting nothing):
       ```xml
       <include>
         <extension name="sipgate_inbound">
           <condition field="destination_number" expression="^(gw\+sipgate|\+?49308687022345|0308687022345|8702234e5)$">
             <action application="log" data="INFO Sipgate inbound destnum=${destination_number} caller=${caller_id_number}"/>

             <!-- A-leg (Sipgate): force PCMU exclusively (REQ-SIP-04, D-20) -->
             <action application="set" data="absolute_codec_string=PCMU"/>
             <action application="set" data="codec_string=PCMU"/>

             <!-- B-leg fail-fast: SIP 503 if OpenAI bridge unreachable >3s (REQ-SIP-07) -->
             <action application="set" data="originate_timeout=3"/>
             <action application="set" data="hangup_after_bridge=true"/>
             <action application="set" data="continue_on_fail=NORMAL_TEMPORARY_FAILURE,USER_BUSY,NO_ANSWER,ALLOTTED_TIMEOUT,NO_USER_RESPONSE"/>

             <!-- Bridge directly to OpenAI SIP endpoint (D-19; ARCHITECTURE.md §Critical boundary) -->
             <action application="bridge" data="[absolute_codec_string=PCMU,codec_string=PCMU]sofia/external/sip:proj_4tEBz3XjO4gwM5hyrvsxLM8E@sip.api.openai.com;transport=tls"/>

             <!-- Failure path: respond SIP 503 to Sipgate (REQ-SIP-07) -->
             <action application="respond" data="503 Service Unavailable"/>
           </condition>
         </extension>
       </include>
       ```

    4. Validate XML well-formedness locally before deploy:
       ```
       python3 -c "import xml.etree.ElementTree as ET; ET.parse('/home/carsten_bot/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml'); print('XML_VALID')"
       ```
       Expected: `XML_VALID`

    5. Deploy to Hetzner using the same mechanism Plan 03 Task 3 used.
       Plan 04 now depends_on "01-03" (WARNING #5 closure 2026-04-16),
       so by the time this task runs, 01-03-SUMMARY.md already documents
       the rsync target path. Specifically: rsync the conf/overlay/ tree
       to `voice_bot@hetzner:/home/voice_bot/nanoclaw/voice-stack/conf/overlay/`
       which is the bind-mount source for vs-freeswitch — file change
       is visible immediately after rsync. If 01-03-SUMMARY.md documents
       a different deploy path, follow that exactly (single source of
       truth on Hetzner-side voice-stack layout).

       Standalone fallback (in case 01-03-SUMMARY.md is missing or
       changes the target): direct rsync command —
       ```
       rsync -avz --delete \
         ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/ \
         hetzner:/home/voice_bot/nanoclaw/voice-stack/conf/overlay/dialplan/public/
       ```

    6. Capture pre-reload channel snapshot (for the "did reload drop active
       calls" assertion):
       ```
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "show channels count"' > /tmp/fs-channels-before.txt
       cat /tmp/fs-channels-before.txt
       ```

    7. Reload (does NOT restart — preserves active calls):
       ```
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "reloadxml"'
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "sofia profile external rescan"'
       sleep 2
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "show channels count"' > /tmp/fs-channels-after.txt
       diff /tmp/fs-channels-before.txt /tmp/fs-channels-after.txt
       ```
       Expected: channel count unchanged (rescan should preserve in-flight
       calls). Per A6 (RESEARCH Assumptions Log): if `restart` is required
       instead of `rescan`, calls drop — the chosen `rescan` path
       deliberately avoids that.

    8. Verify gateway REGED (INFRA-01):
       ```
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "sofia status gateway sipgate"' | tee /tmp/sofia-gw.txt
       grep -E 'State[[:space:]]+REGED|Status[[:space:]]+UP' /tmp/sofia-gw.txt
       ```
       Expected: at least one line matches (REGED is the success signal).
       Note: existing sipgate.xml gateway config has `register=true`,
       `expire-seconds=300`, `retry-seconds=30` per RESEARCH; if NOT REGED
       within 30s of reload, debug via `fs_cli -x "console loglevel debug"`
       and inspect REGISTER attempts.

    9. Verify dialplan parsed without error:
       ```
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "xml_locate dialplan public sipgate_inbound"'
       ```
       Expected: returns the new XML block (proves the parser accepted it).

    Per D-19, D-20, D-21, D-22, INFRA-01, SIP-01, SIP-03, SIP-04, SIP-06,
    SIP-07 (locked decisions + REQs).
  </action>
  <verify>
    <automated>ssh hetzner 'docker exec vs-freeswitch fs_cli -x "sofia status gateway sipgate"' | grep -qE 'State[[:space:]]+REGED' && grep -q 'sip\.api\.openai\.com' ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml && echo DIALPLAN_DEPLOYED</automated>
  </verify>
  <done>
    01_sipgate_inbound.xml contains exact Template 8 content; backup file
    exists; XML well-formed; reloadxml + rescan applied without dropping
    active calls; gateway State=REGED visible in sofia status; new dialplan
    fetched by xml_locate. Per D-19..D-22.
  </done>
</task>

<task type="auto">
  <name>Task 2: Outbound originate smoke test (SIP-02 coverage per Pitfall NEW-2)</name>
  <files>
    .planning/phases/01-infrastructure-webhook-path/01-04-NOTES.md  (NEW — captures fs_cli output)
  </files>
  <read_first>
    - .planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md §"Pitfall NEW-2: SIP-02 outbound dialplan"
    - .planning/phases/01-infrastructure-webhook-path/01-RESEARCH.md §"Validation Architecture" SIP-02 row
  </read_first>
  <action>
    Per Pitfall NEW-2, SIP-02 ("FreeSWITCH initiates outbound calls via
    Sipgate with Carsten's CLI") has no dedicated dialplan extension yet,
    and Phase 1 only requires a SMOKE TEST that the gateway can place
    outbound. Phase 4 will add a real /outbound endpoint.

    1. Identify a safe outbound test target. Options:
       - A Sipgate echo/test number (preferred — costs ~0)
       - A landline owned by carsten that auto-rejects (e.g. office VOIP that
         declines unknown CLI)
       Do NOT dial counterparts in Phase 1 (legal exposure).

       If unclear which test target is safe, surface to carsten as a brief
       sub-question ("Which Sipgate echo number should I dial for SIP-02
       smoke test?") and capture the answer in 01-04-NOTES.md.

    2. Run originate via fs_cli with `&echo` action so call self-terminates
       on answer:
       ```
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "originate sofia/gateway/sipgate/<test-number> &echo"' | tee -a .planning/phases/01-infrastructure-webhook-path/01-04-NOTES.md
       ```
       Expected: returns a UUID prefixed `+OK` (call placed). If returns
       `-ERR GATEWAY_DOWN` or similar: the gateway side is broken — debug
       sipgate.xml. If returns `-ERR USER_BUSY`/`NO_ANSWER`: that's STILL
       a SIP-02 success (origination worked, just no answer at far end).

    3. Capture and document in 01-04-NOTES.md:
       ```markdown
       # Phase 1 Plan 04 — fs_cli notes

       ## SIP-02 outbound smoke test (Pitfall NEW-2)
       Date: <timestamp>
       Command: originate sofia/gateway/sipgate/<test-number> &echo
       Result: <full fs_cli output>
       Verdict: <PASS — origination accepted by gateway / FAIL — reason>

       ## Sofia gateway state at end of plan
       <output of: fs_cli -x "sofia status gateway sipgate">
       ```

    4. Sanity check: confirm gateway still REGED after outbound attempt
       (some buggy stacks deregister on outbound failure):
       ```
       ssh hetzner 'docker exec vs-freeswitch fs_cli -x "sofia status gateway sipgate"' | grep -qE 'State[[:space:]]+REGED' && echo STILL_REGED
       ```

    Per Pitfall NEW-2 — Phase 1 only needs the smoke test; full /outbound
    implementation deferred to Phase 4.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-infrastructure-webhook-path/01-04-NOTES.md && grep -qE '^Verdict:[[:space:]]*PASS' .planning/phases/01-infrastructure-webhook-path/01-04-NOTES.md && ssh hetzner 'docker exec vs-freeswitch fs_cli -x "sofia status gateway sipgate"' | grep -qE 'State[[:space:]]+REGED' && echo SIP02_SMOKE_OK</automated>
  </verify>
  <done>
    01-04-NOTES.md exists with originate result documented; verdict line
    starts with `PASS`; gateway still REGED after the test; SIP-02 smoke
    coverage achieved per Pitfall NEW-2 (full implementation deferred to
    Phase 4 with explicit doc note).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sipgate carrier → FreeSWITCH | TLS-encrypted SIP signalling; carrier-trusted |
| FreeSWITCH → OpenAI SIP | TLS-encrypted SIP; project-id authenticates |
| Dialplan file → vs-freeswitch container | Filesystem mount; trust = file system perms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Forged INVITE bypassing Sipgate | accept | sipgate.xml gateway only accepts requests from Sipgate's known SIP IPs; existing infra; verified upstream |
| T-04-02 | Tampering | Codec downgrade attack on Sipgate leg | mitigate | absolute_codec_string=PCMU + codec_string=PCMU forces single codec; mid-call reINVITE codec change → reject (Pitfall #7 in cross-cutting PITFALLS.md) |
| T-04-03 | DoS | OpenAI SIP unreachable causes pile-up | mitigate | originate_timeout=3 + respond 503 (SIP-07) — fail fast, return error to Sipgate, free resources |
| T-04-04 | Information Disclosure | sip_trace logs may include credentials | accept | sip_trace not enabled by default in Phase 1; if enabled later, redact in Phase 4 logging discipline |
| T-04-05 | Repudiation | No log of inbound INVITE caller | mitigate | `<action application="log" data="INFO Sipgate inbound..."/>` records dest + caller per Template 8 |
</threat_model>

<verification>
- `grep -q "sip\.api\.openai\.com" ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml` returns 0
- `grep -q "originate_timeout=3" ~/nanoclaw/voice-stack/conf/overlay/dialplan/public/01_sipgate_inbound.xml` returns 0
- Backup file `01_sipgate_inbound.xml.bak-*` exists and contains `<extension`
- `ssh hetzner 'docker exec vs-freeswitch fs_cli -x "sofia status gateway sipgate"'` shows State=REGED
- `ssh hetzner 'docker exec vs-freeswitch fs_cli -x "xml_locate dialplan public sipgate_inbound"'` returns the new bridge target
- Channels count before reloadxml == channels count after (no calls dropped)
- 01-04-NOTES.md verdict line `PASS` for originate smoke test
</verification>

<success_criteria>
INFRA-01 + SIP-01..04, SIP-06, SIP-07 met at the dialplan layer (live PSTN
verification happens in Plan 06; SIP-05 RTP firewall confirmation handled
by Plan 02 Task 4 per BLOCKER #2 closure). Phase-1 SIP path is configured
but not yet end-to-end-tested with a real inbound call — that's intentional
per the build order in RESEARCH §Summary (config first, integration test
last).
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-webhook-path/01-04-SUMMARY.md`
recording:
- Backup file path
- Diff summary (what was the old bridge target vs new)
- Reload outcome (channel-count delta = 0)
- sofia status output (REGED state)
- 01-04-NOTES.md path with originate result
- Reference to Plan 06 for live PSTN integration test
- Note: depends_on now ["01-01", "01-03"] per WARNING #5 closure
  (2026-04-16) — Plan 04 still runs in Wave 3 alongside Plan 05
</output>
