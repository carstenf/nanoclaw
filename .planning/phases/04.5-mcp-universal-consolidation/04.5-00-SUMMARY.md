---
phase: 04.5
plan: 00
subsystem: mcp-universal-consolidation
tags: [mcp, wave-0, schema-reexport, test-scaffold]
requires:
  - 04.5-PATTERNS.md §"src/mcp-tools/voice-*.ts × 18" (exact line mapping)
  - 04.5-VALIDATION.md §"Per-Task Verification Map" (scaffold titles)
  - 04.5-CONTEXT.md D-4, D-8, D-15 (scope + REST-path preservation + scenario list)
provides:
  - named zod exports on 18 voice-*.ts modules (19 schema lines)
  - regression scaffold (7 it.skip) at src/mcp-stream-server.regression.test.ts
  - bridge-client scaffold (6 it.skip) at voice-bridge/src/core-mcp-client.test.ts
affects:
  - src/mcp-stream-server.ts (Wave 1 can now import schemas by name)
  - voice-bridge/src/core-mcp-client.ts (Wave 2 test file already present)
tech-stack:
  added: []
  patterns:
    - "export const <Module>Schema = z.object({...})"
    - "it.skip('<exact title>', () => { /* Wave N Task: ... */ })"
key-files:
  created:
    - src/mcp-stream-server.regression.test.ts
    - voice-bridge/src/core-mcp-client.test.ts
  modified:
    - src/mcp-tools/voice-check-calendar.ts
    - src/mcp-tools/voice-create-calendar-entry.ts
    - src/mcp-tools/voice-delete-calendar-entry.ts
    - src/mcp-tools/voice-update-calendar-entry.ts
    - src/mcp-tools/voice-send-discord-message.ts
    - src/mcp-tools/voice-get-travel-time.ts
    - src/mcp-tools/voice-get-contract.ts
    - src/mcp-tools/voice-get-practice-profile.ts
    - src/mcp-tools/voice-schedule-retry.ts
    - src/mcp-tools/voice-ask-core.ts
    - src/mcp-tools/voice-record-turn-cost.ts
    - src/mcp-tools/voice-finalize-call-cost.ts
    - src/mcp-tools/voice-insert-price-snapshot.ts
    - src/mcp-tools/voice-search-competitors.ts
    - src/mcp-tools/voice-request-outbound-call.ts
    - src/mcp-tools/voice-reset-monthly-cap.ts
    - src/mcp-tools/voice-get-day-month-cost-sum.ts
    - src/mcp-tools/voice-on-transcript-turn.ts
decisions:
  - "Ein zod-Schema pro Modul via named export — kein Barrel-Re-Export aus index.ts (D-4)"
  - "validateVoiceTurnArgs in voice-on-transcript-turn.ts bleibt unberührt — REST-Pfad auf Port 3200 nutzt ihn weiter (D-8)"
  - "Scaffold-Titel enthalten Präfixe wie 'scenario 1:'/'scenario 2:' — vitest -t Substring-Match erfüllt VALIDATION.md-Filter-Kontrakt"
metrics:
  duration_minutes: 6
  completed: 2026-04-20T07:54:15Z
  tasks_completed: 2
  files_touched: 20
---

# Phase 04.5 Plan 00: Wave-0 Foundation — Schema Exports + Test Scaffolds Summary

Wave-0 landet die zwei rein additiven Moves, die jede nachfolgende Welle freischalten: 18 voice-\*.ts Module exportieren jetzt ihr zod-Schema namentlich, und die beiden neuen Test-Dateien (MCP-Regression + Bridge-Client) stehen mit 13 skip-Stubs bereit, deren Titel exakt den Wave-1/2 `-t`-Filtern aus VALIDATION.md entsprechen.

## Modules Touched (18)

| # | File | Export landed |
|---|------|---------------|
| 1 | `src/mcp-tools/voice-check-calendar.ts` | `export const CheckCalendarSchema` |
| 2 | `src/mcp-tools/voice-create-calendar-entry.ts` | `export const CreateEntrySchema` |
| 3 | `src/mcp-tools/voice-delete-calendar-entry.ts` | `export const DeleteEntrySchema` |
| 4 | `src/mcp-tools/voice-update-calendar-entry.ts` | `export const FieldsSchema` + `export const UpdateEntrySchema` (2 consts) |
| 5 | `src/mcp-tools/voice-send-discord-message.ts` | `export const SendDiscordMessageSchema` |
| 6 | `src/mcp-tools/voice-get-travel-time.ts` | `export const TravelTimeSchema` |
| 7 | `src/mcp-tools/voice-get-contract.ts` | `export const GetContractSchema` |
| 8 | `src/mcp-tools/voice-get-practice-profile.ts` | `export const GetPracticeProfileSchema` |
| 9 | `src/mcp-tools/voice-schedule-retry.ts` | `export const ScheduleRetrySchema` |
| 10 | `src/mcp-tools/voice-ask-core.ts` | `export const AskCoreSchema` |
| 11 | `src/mcp-tools/voice-record-turn-cost.ts` | `export const RecordTurnCostSchema` |
| 12 | `src/mcp-tools/voice-finalize-call-cost.ts` | `export const FinalizeCallCostSchema` |
| 13 | `src/mcp-tools/voice-insert-price-snapshot.ts` | `export const InsertPriceSnapshotSchema` |
| 14 | `src/mcp-tools/voice-search-competitors.ts` | `export const SearchCompetitorsSchema` |
| 15 | `src/mcp-tools/voice-request-outbound-call.ts` | renamed `Schema` → `export const RequestOutboundCallSchema` |
| 16 | `src/mcp-tools/voice-reset-monthly-cap.ts` | renamed `Schema` → `export const ResetMonthlyCapSchema` |
| 17 | `src/mcp-tools/voice-get-day-month-cost-sum.ts` | renamed `Schema` → `export const GetDayMonthCostSumSchema` |
| 18 | `src/mcp-tools/voice-on-transcript-turn.ts` | **NEU** `export const OnTranscriptTurnSchema = z.object({...})` (neben dem bestehenden `validateVoiceTurnArgs`, der auf dem REST-Pfad bleibt) |

**Total `export const \w+Schema` lines in `src/mcp-tools/voice-*.ts`:** 19 (18 Module, `voice-update-calendar-entry.ts` trägt 2 bei).

## Rename Mappings Applied (3)

Drei Module nannten ihr zod-Konstrukt generisch `Schema` (Kollisionsrisiko beim künftigen Multi-Import in `mcp-stream-server.ts`). Umbenennung + alle in-Modul-Referenzen in derselben Commit:

| File | Alt | Neu | In-Modul-Refs aktualisiert |
|------|-----|-----|----------------------------|
| `voice-request-outbound-call.ts` | `Schema.safeParse(args)` | `RequestOutboundCallSchema.safeParse(args)` | 1 (Zeile 69) |
| `voice-reset-monthly-cap.ts` | `Schema.safeParse(args)` | `ResetMonthlyCapSchema.safeParse(args)` | 1 (Zeile 36) |
| `voice-get-day-month-cost-sum.ts` | `Schema.parse(args ?? {})` | `GetDayMonthCostSumSchema.parse(args ?? {})` | 1 (Zeile 24) |

Kein Test und kein anderes Modul importierte diese Schemas namentlich — externe Kollisionen null.

## Test Scaffolds Created

### `src/mcp-stream-server.regression.test.ts` (7 skip-Stubs)

Titel exakt VALIDATION.md-konform (Substring-Match für `vitest -t`):

- `scenario 1: initialize + tools/list + tools/call × 3 on session-based transport, each < 500 ms`
- `scenario 2: stateless-fallback path returns spec-compliant error within 500 ms (no indefinite hang)`
- `scenario 3: initialize response has capabilities.tools.listChanged === false`
- `scenario 4: two concurrent sessions, 3 tools/call each, no JSON-RPC id cross-contamination`
- `scenario 5: expired session ID returns 404, not 500`
- `tools_list_schemas: all 18 voice.* tools present on tools/list with non-empty inputSchema`
- `request_outbound: voice.request_outbound_call listable + callable via session-based MCP`

### `voice-bridge/src/core-mcp-client.test.ts` (6 skip-Stubs)

- `connect: lazy-opens MCP session on first ensureConnected()`
- `callTool: returns server result, preserves args shape`
- `close: idempotent, second close() is no-op`
- `timeout: callTool rejects with CoreMcpTimeoutError when opts.timeoutMs elapses`
- `server_error: callTool rejects with CoreMcpError when server returns isError: true`
- `reconnect_after_close: ensureConnected() after close() opens a fresh session`

## Test Counts — Before vs. After

| Suite | Baseline (pre-Wave-0) | Post-Wave-0 | Delta |
|-------|-----------------------|-------------|-------|
| Root (`npm test`) | 52 passed / 1 failed file, 578 passed / 1 failed test, 0 skipped | 52 passed / 1 failed file, 578 passed / 1 failed test, 7 skipped | +7 skipped |
| voice-bridge (`cd voice-bridge && npm test`) | 1 skipped (pre-existing) | 7 skipped (1 pre-existing + 6 new) | +6 skipped |
| **Total delta** | — | — | **+13 skipped** (matches plan) |

Die 1 Testfehler im Root-Suite (`src/channels/gmail.test.ts` — `buildQuery()` Gmail-Filter-Erwartung) und die 22 Fehler im voice-bridge-Suite (`TypeError: __vite_ssr_import_0__.Ajv is not a constructor` über `tests/*.ts`) existierten **vor** diesem Plan und sind unverändert — verifiziert via `git stash` + Re-Run der Bridge-Tests ohne die neue Scaffold-Datei: gleicher Ajv-Fehler. Out-of-scope für Wave-0.

## Deviations from Plan

### Rule 1/2/3 Auto-Fixes: None

Keine Bugs, keine fehlende kritische Funktionalität, keine blockierenden Issues während der Ausführung.

### Plan-Text vs. Ausführung

- Plan AC-Liste erwartete `grep -c "'scenario 1'\|..."` mit strikten Hochkommas um den Kurz-Token; tatsächliche Titel sind länger (`'scenario 1: initialize...'`). Der funktionale Kontrakt (`vitest -t "scenario 1"` matcht via Substring) ist erfüllt — per `npx vitest run src/mcp-stream-server.regression.test.ts -t "scenario 1"` verifiziert. Die strikte grep-Fassung der AC war zu eng formuliert; der tatsächliche Wave-1-Nutzungskontrakt ist eingehalten.

## Build & Test Verification

```
npm run build 2>&1 | tail -3
> nanoclaw@1.2.47 build
> tsc
(exit 0)

grep -hc "^export const \w+Schema" src/mcp-tools/voice-*.ts | paste -sd+ | bc
19

npx vitest run src/mcp-stream-server.regression.test.ts
Test Files  1 skipped (1)
Tests       7 skipped (7)

cd voice-bridge && npx vitest run src/core-mcp-client.test.ts
Test Files  1 skipped (1)
Tests       6 skipped (6)
```

## Commits

- `c4d63e5` — feat(04.5-00): export zod schemas from 18 voice-\*.ts modules
- `1867a9a` — test(04.5-00): scaffold regression + bridge-client test files

## Push Status

Push deferred: worktree branch (`worktree-agent-a009020e`) is per-agent scratch; the orchestrator merges back to `main` and pushes per project "Push early" rule after the wave completes.

## Self-Check: PASSED

- `src/mcp-stream-server.regression.test.ts` exists and contains 7 `it.skip` stubs
- `voice-bridge/src/core-mcp-client.test.ts` exists and contains 6 `it.skip` stubs
- 18 voice-\*.ts modules each export at least one `export const \w+Schema` (19 lines total)
- `OnTranscriptTurnSchema` present alongside preserved `validateVoiceTurnArgs`
- `npm run build` exits 0
- `vitest run` on both scaffolds reports 7 + 6 skipped, 0 failures
- Commits `c4d63e5` and `1867a9a` present in `git log --oneline -3`
- No STATE.md / ROADMAP.md changes (worktree mode — orchestrator owns wave-level writes)
