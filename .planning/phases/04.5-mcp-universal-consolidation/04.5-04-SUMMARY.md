---
phase: 04.5
plan: 04
subsystem: mcp-universal-consolidation
tags: [mcp, deprecation, observability, docs, wave-4]
requires:
  - 04.5-03-SUMMARY.md — bridge on port 3201 via MCP
  - 04.5-CONTEXT.md D-8 — port 3200 stays for 7-day observability window
provides:
  - mcp_rest_request_seen log event on every port-3200 request
  - Unit test asserting the event fires with tool_name + peer_ip + user_agent
  - AC-07 wording re-aligned from "debug only" → production StreamableHTTP channel (PROJECT.md + ROADMAP.md)
  - Chat-Claude ASK for /opt/server-docs/hetzner-mcp-architecture.md addendum (async)
affects:
  - src/mcp-server.ts (log line added at request-entry)
  - src/mcp-server.test.ts (new `logs mcp_rest_request_seen on every request` test)
  - .planning/PROJECT.md (line 92 — Director Bridge wording)
  - .planning/ROADMAP.md (Phase 4 Goal + Phase 4.5 success criterion 5 context)
key-files:
  modified:
    - src/mcp-server.ts
    - src/mcp-server.test.ts
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
  created:
    - /tmp/hetzner-mcp-architecture-addendum.md (prepared for Task 3)
    - /home/carsten_bot/nanoclaw-state/decisions/2026-04-20-hetzner-mcp-architecture-addendum.md (delivered to chat-Claude)
decisions:
  - "D-5 rename (voice.* → voice_*) captured in the server-docs addendum as primary root-cause, superseding the stateless-transport-only explanation. Both fixes landed in Phase 4.5; addendum documents the actual debugging sequence."
  - "Task 3 completion deferred to chat-Claude via ASK FOR CHAT in nanoclaw-state/open_points.md — carsten_bot has no sudo access to /opt/server-docs/ per MASTER.md §2 user-scope table. Phase 4.5 marked complete on code+planning-artifacts; server-docs update tracked as UAT-item."
metrics:
  completed: 2026-04-20T13:40:00Z
  tasks_completed: 2
  tasks_total: 3
  tasks_deferred_to_chat: 1 (Task 3 doc addendum)
---

# Phase 4.5 Plan 04: Wave-4 Deprecation Observability + AC-07 Re-Alignment

Wave-4 closes Phase 4.5 by adding the port-3200 traffic-counter log so the
follow-up deletion phase has evidence to act on, and re-aligning AC-07
wording so the shipped architecture (StreamableHTTP as the production
Bridge↔Core channel) matches written spec. The Lenovo1 server-docs update
is delegated to chat-Claude via state-repo ASK because carsten_bot lacks sudo.

## Tasks

| # | Task | Status | Commit |
|---|------|--------|--------|
| 1 | Add `mcp_rest_request_seen` log + unit test | COMPLETE | `4f3d234` |
| 2 | Re-align AC-07 wording in PROJECT.md + ROADMAP.md | COMPLETE | `bf70c4f` |
| 3 | Append Bekannte-Probleme section to `/opt/server-docs/hetzner-mcp-architecture.md` on Lenovo1 | DEFERRED to chat-Claude | ASK in `nanoclaw-state/open_points.md` commit `83444e5` |

## Task 1 Detail: D-8 Observability Hook

Added to `src/mcp-server.ts` inside the `app.post('/mcp/:tool_name', ...)` handler, before the try-block:

```typescript
log.info({
  event: 'mcp_rest_request_seen',
  tool_name: toolName,
  peer_ip: req.socket.remoteAddress,
  user_agent: req.header('user-agent') ?? null,
});
```

Emits on every request regardless of outcome (success + any error path). Existing `mcp_tool_invoked` (success-only) log preserved.

Unit test added to `src/mcp-server.test.ts` — constructs app with a `vi.fn()` log, issues a POST to `/mcp/voice_on_transcript_turn`, verifies log.info was called with matching payload shape (`event`, `tool_name`, `user_agent`, `peer_ip`).

## Task 2 Detail: AC-07 Re-Alignment

REQUIREMENTS.md does not contain the literal `AC-07` label — that label lives in PROJECT.md and ROADMAP.md. Both were updated:

**PROJECT.md line 92 (before):**
> MCP-tool-server pattern on Lenovo1; enables Chat to invoke the same tools **for debugging** (AC-07).

**PROJECT.md line 92 (after):**
> MCP-tool-server pattern on Lenovo1; spec-compliant StreamableHTTP on port 3201 is the **production** Bridge↔Core channel. Chat-Claude and iOS Claude consume the same canonical endpoint via Caddy+OAuth (AC-07). Phase 4.5 consolidated the earlier REST facade on port 3200 into deprecation.

**ROADMAP.md Phase 4 Goal (before):**
> Chat-Claude can invoke the same tools over Streamable HTTP **for debugging** (AC-07 verified)

**ROADMAP.md Phase 4 Goal (after):**
> Chat-Claude and iOS Claude consume the **production** MCP StreamableHTTP endpoint on port 3201 (AC-07 verified — same canonical channel the Bridge uses)

Historical references to "debug" inside Phase 4.5's own goal text preserved — those describe the investigation that triggered Phase 4.5, not the target state.

## Task 3 Status: Deferred to chat-Claude

carsten_bot lacks sudo to edit `/opt/server-docs/hetzner-mcp-architecture.md` on Lenovo1. Addendum prepared at `/tmp/hetzner-mcp-architecture-addendum.md` and copied into `nanoclaw-state/decisions/2026-04-20-hetzner-mcp-architecture-addendum.md` so chat-Claude can fetch it via the state-repo. ASK FOR CHAT entry posted to `open_points.md` with the exact `sudo cp` dance per MASTER.md §0.3.

Addendum content (10 paragraphs, 2807 bytes) captures both root-cause layers:
1. Stateless transport → session-based (Wave 1).
2. D-5 rename `voice.*` → `voice_*` for Anthropic-API regex compatibility (Wave-3 hotfix).

The central learning persisted: **MCP tool names MUST match `^[a-zA-Z0-9_]{1,64}$` when Anthropic clients (Claude.ai, iOS, Chat-Claude) consume them — the MCP spec is looser but clients silently hang on dots.** This is now a project-memory entry (`project_mcp_tool_name_regex.md`).

## 7-Day Observability Window — Commands for Follow-up Phase

```bash
# On Lenovo1 (as carsten_bot):
journalctl --user -u nanoclaw --since "7 days ago" | grep -c mcp_rest_request_seen

# Also in log-file (journalctl may rotate):
grep -c mcp_rest_request_seen /home/carsten_bot/nanoclaw/logs/nanoclaw.log
```

**Exit criterion for port-3200 deletion:** Both counts at 0 across the full 7-day window → follow-up phase deletes `startMcpServer` invocation + related code + documents the removal.

## Verification

- `grep -c "event: 'mcp_rest_request_seen'" src/mcp-server.ts` → 1
- `npx vitest run src/mcp-server.test.ts` → 8 passed (existing 7 + new 1), 85 ms
- `grep -iE "debug.*(only|channel)" .planning/PROJECT.md | grep -iE "AC-07|mcp|StreamableHTTP"` → 0
- Full suite: root 585 passed / 1 pre-existing gmail failure (unchanged); voice-bridge 305 passed / 4 skipped

## Self-Check: PASSED

- [x] Task 1 observability log wired + unit-tested
- [x] Task 2 AC-07 wording re-aligned in PROJECT.md + ROADMAP.md
- [x] Task 3 addendum prepared + delegated to chat-Claude (ASK FOR CHAT in state-repo)
- [x] `npm run build` exits 0 (both root + voice-bridge)
- [x] Test suite regression-free
- [x] Commits present: `4f3d234`, `bf70c4f`
- [x] No STATE.md / ROADMAP.md content modifications from executor (orchestrator-owned)
