---
phase: 04.5
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/mcp-tools/voice-check-calendar.ts
  - src/mcp-tools/voice-create-calendar-entry.ts
  - src/mcp-tools/voice-delete-calendar-entry.ts
  - src/mcp-tools/voice-update-calendar-entry.ts
  - src/mcp-tools/voice-send-discord-message.ts
  - src/mcp-tools/voice-get-travel-time.ts
  - src/mcp-tools/voice-get-contract.ts
  - src/mcp-tools/voice-get-practice-profile.ts
  - src/mcp-tools/voice-schedule-retry.ts
  - src/mcp-tools/voice-ask-core.ts
  - src/mcp-tools/voice-record-turn-cost.ts
  - src/mcp-tools/voice-finalize-call-cost.ts
  - src/mcp-tools/voice-insert-price-snapshot.ts
  - src/mcp-tools/voice-search-competitors.ts
  - src/mcp-tools/voice-request-outbound-call.ts
  - src/mcp-tools/voice-reset-monthly-cap.ts
  - src/mcp-tools/voice-get-day-month-cost-sum.ts
  - src/mcp-tools/voice-on-transcript-turn.ts
  - src/mcp-stream-server.regression.test.ts
  - voice-bridge/src/core-mcp-client.test.ts
autonomous: true
requirements:
  - REQ-DIR-10
tags: [mcp, wave-0, schema-reexport, test-scaffold]

must_haves:
  truths:
    - "All 18 voice-*.ts modules export their zod Schema (or add one in the OnTranscriptTurn case)"
    - "New regression test file exists with vitest scenarios 1/2/3/4/5/tools_list_schemas/request_outbound stubs"
    - "New bridge client test file exists with connect/callTool/close/error test stubs"
    - "Existing full-suite tests stay green (no regression from schema rename)"
  artifacts:
    - path: "src/mcp-stream-server.regression.test.ts"
      provides: "D-15 regression scenarios scaffolding (initial scenario names present, may use .skip)"
      min_lines: 40
    - path: "voice-bridge/src/core-mcp-client.test.ts"
      provides: "CoreMcpClient test-scaffold — connect/callTool/close/error cases stubbed"
      min_lines: 40
    - path: "src/mcp-tools/voice-on-transcript-turn.ts"
      provides: "OnTranscriptTurnSchema zod export (NEW — previously no zod in this file)"
      contains: "export const OnTranscriptTurnSchema = z.object"
  key_links:
    - from: "src/mcp-tools/voice-*.ts (17 existing)"
      to: "src/mcp-stream-server.ts (Wave 1 import site)"
      via: "named export `export const XxxSchema = z.object(...)`"
      pattern: "^export const \\w+Schema = z"
    - from: "src/mcp-tools/voice-on-transcript-turn.ts"
      to: "src/mcp-stream-server.ts"
      via: "new `export const OnTranscriptTurnSchema`"
      pattern: "export const OnTranscriptTurnSchema"
---

<objective>
Wave-0 foundation for Phase 4.5. Two strictly-additive, low-risk moves that unblock every downstream wave:

1. Re-export every per-tool zod `Schema` constant from 17 `src/mcp-tools/voice-*.ts` modules, and ADD a new `OnTranscriptTurnSchema` to `voice-on-transcript-turn.ts` (which today carries only a hand-rolled `validateVoiceTurnArgs` — the validator stays for the port-3200 REST path, the new zod schema is for `TOOL_META` consumption).
2. Scaffold the two new test files (`src/mcp-stream-server.regression.test.ts` + `voice-bridge/src/core-mcp-client.test.ts`) with scenario/case titles matching VALIDATION.md rows, using `it.skip(...)` or `it.todo(...)` placeholders. Wave 1 and Wave 2 fill them in.

Purpose: make Wave 1 / Wave 2 executor tasks trivial import operations (`import { CheckCalendarSchema } from './mcp-tools/voice-check-calendar.js'`) rather than requiring each downstream agent to rename 17+ files mid-task. Also satisfies Nyquist: every Wave 1+ automated verify command points at a test file that already exists.

Output: 18 schema re-exports + 1 new schema + 2 test scaffolds + full build still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.5-mcp-universal-consolidation/04.5-CONTEXT.md
@.planning/phases/04.5-mcp-universal-consolidation/04.5-RESEARCH.md
@.planning/phases/04.5-mcp-universal-consolidation/04.5-PATTERNS.md
@.planning/phases/04.5-mcp-universal-consolidation/04.5-VALIDATION.md

<interfaces>
<!-- Target patterns for the 18 schema modules — extracted from PATTERNS.md lines 207-230. -->
<!-- Every module uses an inline `const XxxSchema = z.object({...})` today. Only the `const` needs -->
<!-- an `export` prefix. Three modules name their schema generically `Schema` and must be renamed -->
<!-- on export to a module-unique name to avoid collisions in the Wave 1 import block. -->

Re-export mapping (source of truth: PATTERNS.md §"src/mcp-tools/voice-*.ts × 18"):

| File | Current identifier | Target export name |
|------|--------------------|---------------------|
| voice-check-calendar.ts | `CheckCalendarSchema` | `export const CheckCalendarSchema` (add `export`) |
| voice-create-calendar-entry.ts | `CreateEntrySchema` | `export const CreateEntrySchema` |
| voice-delete-calendar-entry.ts | `DeleteEntrySchema` | `export const DeleteEntrySchema` |
| voice-update-calendar-entry.ts | `FieldsSchema` + `UpdateEntrySchema` | `export const FieldsSchema` + `export const UpdateEntrySchema` (both) |
| voice-send-discord-message.ts | `SendDiscordMessageSchema` | `export const SendDiscordMessageSchema` |
| voice-get-travel-time.ts | `TravelTimeSchema` | `export const TravelTimeSchema` |
| voice-get-contract.ts | `GetContractSchema` | `export const GetContractSchema` |
| voice-get-practice-profile.ts | `GetPracticeProfileSchema` | `export const GetPracticeProfileSchema` |
| voice-schedule-retry.ts | `ScheduleRetrySchema` | `export const ScheduleRetrySchema` |
| voice-ask-core.ts | `AskCoreSchema` | `export const AskCoreSchema` |
| voice-record-turn-cost.ts | `RecordTurnCostSchema` | `export const RecordTurnCostSchema` |
| voice-finalize-call-cost.ts | `FinalizeCallCostSchema` | `export const FinalizeCallCostSchema` |
| voice-insert-price-snapshot.ts | `InsertPriceSnapshotSchema` | `export const InsertPriceSnapshotSchema` |
| voice-search-competitors.ts | `SearchCompetitorsSchema` | `export const SearchCompetitorsSchema` |
| voice-request-outbound-call.ts | `Schema` (generic) | `export const RequestOutboundCallSchema` + update any in-module refs |
| voice-reset-monthly-cap.ts | `Schema` (generic) | `export const ResetMonthlyCapSchema` + update any in-module refs |
| voice-get-day-month-cost-sum.ts | `Schema` (generic) | `export const GetDayMonthCostSumSchema` + update any in-module refs |
| voice-on-transcript-turn.ts | (no zod today — only `validateVoiceTurnArgs`) | ADD `export const OnTranscriptTurnSchema = z.object({ call_id: z.string().min(1), turn_id: z.string().min(1), transcript: z.string() })` ALONGSIDE the existing validator (keep both) |

Test-scaffold scenario titles (referenced by Wave 1/2 `-t` filters — MUST match exactly):
- `src/mcp-stream-server.regression.test.ts`: "scenario 1", "scenario 2", "scenario 3", "scenario 4", "scenario 5", "tools_list_schemas", "request_outbound"
- `voice-bridge/src/core-mcp-client.test.ts`: "connect", "callTool", "close", "timeout", "server_error", "reconnect_after_close"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-export zod schemas from 17 voice-*.ts modules + add OnTranscriptTurnSchema</name>
  <files>
    src/mcp-tools/voice-check-calendar.ts
    src/mcp-tools/voice-create-calendar-entry.ts
    src/mcp-tools/voice-delete-calendar-entry.ts
    src/mcp-tools/voice-update-calendar-entry.ts
    src/mcp-tools/voice-send-discord-message.ts
    src/mcp-tools/voice-get-travel-time.ts
    src/mcp-tools/voice-get-contract.ts
    src/mcp-tools/voice-get-practice-profile.ts
    src/mcp-tools/voice-schedule-retry.ts
    src/mcp-tools/voice-ask-core.ts
    src/mcp-tools/voice-record-turn-cost.ts
    src/mcp-tools/voice-finalize-call-cost.ts
    src/mcp-tools/voice-insert-price-snapshot.ts
    src/mcp-tools/voice-search-competitors.ts
    src/mcp-tools/voice-request-outbound-call.ts
    src/mcp-tools/voice-reset-monthly-cap.ts
    src/mcp-tools/voice-get-day-month-cost-sum.ts
    src/mcp-tools/voice-on-transcript-turn.ts
  </files>

  <read_first>
    - .planning/phases/04.5-mcp-universal-consolidation/04.5-PATTERNS.md §"src/mcp-tools/voice-*.ts × 18" (lines 207-243) — full mapping table
    - .planning/phases/04.5-mcp-universal-consolidation/04.5-RESEARCH.md §"Standard Stack" — zod usage already in place in every module
    - Each of the 18 files itself (the `const XxxSchema = z.object({...})` block — DO NOT change the shape, only add `export` keyword)
    - CONTEXT.md D-4 (all 18 tools get zod inputSchema), Claude's Discretion bullet 1 (re-export existing schemas, do NOT hand-roll)
  </read_first>

  <behavior>
    - 17 existing modules gain `export` keyword on their `const XxxSchema` line — zero shape change.
    - 3 of those 17 (`voice-request-outbound-call.ts`, `voice-reset-monthly-cap.ts`, `voice-get-day-month-cost-sum.ts`) rename their generic `Schema` identifier to `RequestOutboundCallSchema`/`ResetMonthlyCapSchema`/`GetDayMonthCostSumSchema` respectively; ALL in-module references to the old name update in the same commit.
    - `voice-on-transcript-turn.ts` gains a brand-new `export const OnTranscriptTurnSchema = z.object({ call_id: z.string().min(1), turn_id: z.string().min(1), transcript: z.string() })` — the existing `validateVoiceTurnArgs` hand-rolled validator stays untouched (still used by the port-3200 REST path; D-8).
    - `voice-update-calendar-entry.ts` has TWO consts to export (`FieldsSchema` + `UpdateEntrySchema`) — both get `export`.
    - `npm run build` stays green; `npm test` stays green (no existing test imports the schemas by those names — verify via grep before starting).
  </behavior>

  <action>
    Step 1 — verify no existing test imports these schema names (would break on rename):
    ```bash
    grep -rn "from.*voice-request-outbound-call" src/ voice-bridge/ --include="*.ts" | grep -v "voice-request-outbound-call.ts:"
    grep -rn "from.*voice-reset-monthly-cap" src/ voice-bridge/ --include="*.ts" | grep -v "voice-reset-monthly-cap.ts:"
    grep -rn "from.*voice-get-day-month-cost-sum" src/ voice-bridge/ --include="*.ts" | grep -v "voice-get-day-month-cost-sum.ts:"
    ```
    Expected: zero hits. Module-internal references are the only ones — rename them in step 3.

    Step 2 — for 14 modules with distinct schema names (rows 1-14 of the Interfaces table), add `export` keyword:
    Example concrete edit (`src/mcp-tools/voice-check-calendar.ts:20`):
    - Current: `const CheckCalendarSchema = z.object({`
    - Target:  `export const CheckCalendarSchema = z.object({`
    
    Apply identically to all 14 files. `voice-update-calendar-entry.ts` gets this treatment on BOTH `FieldsSchema` and `UpdateEntrySchema` lines.

    Step 3 — for 3 modules with generic `Schema` identifier, rename + export (per Interfaces table rows 15-17):
    - `voice-request-outbound-call.ts`: `const Schema = z.object({...})` → `export const RequestOutboundCallSchema = z.object({...})`; update every in-module reference `Schema.parse(...)` / `Schema.safeParse(...)` / `Schema.shape` → `RequestOutboundCallSchema.*`.
    - `voice-reset-monthly-cap.ts`: `const Schema` → `export const ResetMonthlyCapSchema`; update in-module refs.
    - `voice-get-day-month-cost-sum.ts`: `const Schema = z.object({}).passthrough()` → `export const GetDayMonthCostSumSchema = z.object({}).passthrough()`; update in-module refs.

    Step 4 — for `src/mcp-tools/voice-on-transcript-turn.ts` (row 18), ADD (do not modify `validateVoiceTurnArgs`):
    ```typescript
    import { z } from 'zod';
    
    export const OnTranscriptTurnSchema = z.object({
      call_id: z.string().min(1),
      turn_id: z.string().min(1),
      transcript: z.string(),
    });
    ```
    Place the import + export after existing imports, before `validateVoiceTurnArgs`. Leave `validateVoiceTurnArgs` and its callers untouched (per D-8, REST path must keep working).

    Step 5 — verify:
    ```bash
    npm run build 2>&1 | tail -5
    # Exit 0, zero errors.
    
    grep -l "^export const \w\+Schema = z" src/mcp-tools/voice-*.ts | wc -l
    # >= 14 (the 3 renamed ones appear AS `export const RequestOutboundCallSchema = z`, `export const ResetMonthlyCapSchema = z`, `export const GetDayMonthCostSumSchema = z` — pattern still matches)
    
    grep -c "^export const OnTranscriptTurnSchema = z.object" src/mcp-tools/voice-on-transcript-turn.ts
    # 1
    
    grep -c "^export const FieldsSchema\|^export const UpdateEntrySchema" src/mcp-tools/voice-update-calendar-entry.ts
    # 2
    
    npm test 2>&1 | tail -15
    # Existing test suites (mcp-stream-server.test.ts, mcp-server.test.ts, voice-bridge/*) stay green.
    ```
  </action>

  <acceptance_criteria>
    - `grep -c "^export const \w\+Schema = z" src/mcp-tools/voice-check-calendar.ts src/mcp-tools/voice-create-calendar-entry.ts src/mcp-tools/voice-delete-calendar-entry.ts src/mcp-tools/voice-send-discord-message.ts src/mcp-tools/voice-get-travel-time.ts src/mcp-tools/voice-get-contract.ts src/mcp-tools/voice-get-practice-profile.ts src/mcp-tools/voice-schedule-retry.ts src/mcp-tools/voice-ask-core.ts src/mcp-tools/voice-record-turn-cost.ts src/mcp-tools/voice-finalize-call-cost.ts src/mcp-tools/voice-insert-price-snapshot.ts src/mcp-tools/voice-search-competitors.ts` each returns ≥ 1 (13 files).
    - `grep -c "^export const RequestOutboundCallSchema = z" src/mcp-tools/voice-request-outbound-call.ts` returns 1.
    - `grep -c "^export const ResetMonthlyCapSchema = z" src/mcp-tools/voice-reset-monthly-cap.ts` returns 1.
    - `grep -c "^export const GetDayMonthCostSumSchema = z" src/mcp-tools/voice-get-day-month-cost-sum.ts` returns 1.
    - `grep -c "^export const OnTranscriptTurnSchema = z.object" src/mcp-tools/voice-on-transcript-turn.ts` returns 1.
    - `grep -c "^export const FieldsSchema\|^export const UpdateEntrySchema" src/mcp-tools/voice-update-calendar-entry.ts` returns 2.
    - `grep -c "validateVoiceTurnArgs" src/mcp-tools/voice-on-transcript-turn.ts` returns ≥ 1 (validator preserved).
    - `npm run build` exits 0.
    - `npm test` exits 0 (no regression in existing suites).
  </acceptance_criteria>

  <verify>
    <automated>npm run build && npm test -- --run 2>&1 | tail -20</automated>
  </verify>

  <done>All 18 voice-*.ts modules export a uniquely-named zod schema. Build green. Full test suite green. No behavior change for any existing caller (validator on transcript-turn module untouched).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold regression + bridge-client test files with skip-stubs</name>
  <files>
    src/mcp-stream-server.regression.test.ts
    voice-bridge/src/core-mcp-client.test.ts
  </files>

  <read_first>
    - .planning/phases/04.5-mcp-universal-consolidation/04.5-VALIDATION.md §"Per-Task Verification Map" — exact scenario titles Wave 1/2 will `-t`-filter on
    - .planning/phases/04.5-mcp-universal-consolidation/04.5-RESEARCH.md §"Regression test: initialize + tools/list + tools/call (scenario 1)" — SDK-Client pattern (lines 469-497)
    - .planning/phases/04.5-mcp-universal-consolidation/04.5-PATTERNS.md §"src/mcp-stream-server.regression.test.ts (NEW ...)" — fixture boot pattern (lines 40-66 analog)
    - .planning/phases/04.5-mcp-universal-consolidation/04.5-PATTERNS.md §"voice-bridge/src/core-mcp-client.test.ts (NEW)" — DI-mock + env-backup (lines 104-131)
    - src/mcp-stream-server.test.ts (existing) — fixture pattern `startApp`, `makeLog`, ephemeral-port `server.listen(0, '127.0.0.1')`, lines 31-72 to mirror verbatim
    - voice-bridge/src/cost/gate.test.ts (existing) — DI-mock + env-backup pattern, lines 9-78 to mirror
    - CONTEXT.md D-15 (scenarios 1-5 listed with exact expected behavior)
  </read_first>

  <behavior>
    - New file `src/mcp-stream-server.regression.test.ts` exists with 7 `it.skip(...)` (or `it.todo(...)`) stubs whose titles EXACTLY match: "scenario 1", "scenario 2", "scenario 3", "scenario 4", "scenario 5", "tools_list_schemas", "request_outbound". Descriptions short but informative.
    - New file `voice-bridge/src/core-mcp-client.test.ts` exists with 6 `it.skip(...)` stubs: "connect", "callTool", "close", "timeout", "server_error", "reconnect_after_close".
    - Both files compile: imports resolve, vitest picks up the files, `npm test` shows both files as SKIPPED (not failed), 13 skipped tests total, 0 failures.
    - Fixture/setup imports are present so Wave 1/Wave 2 editors only have to swap `.skip` for implementation.
  </behavior>

  <action>
    Step 1 — create `src/mcp-stream-server.regression.test.ts`:
    ```typescript
    // Wave-0 scaffold for Phase 4.5 regression harness.
    // D-15 scenarios + tools_list_schemas + request_outbound.
    // Wave 1 fills these in — do NOT remove the skip until the implementation task lands.
    import { describe, it } from 'vitest';

    describe('Phase 4.5 MCP regression — session-based StreamableHTTP', () => {
      it.skip('scenario 1: initialize + tools/list + tools/call × 3 on session-based transport, each < 500 ms', () => {
        // Wave 1 Task: drive SDK Client against ephemeral-port startApp() fixture
        //   Expected: c.connect(t) < 500ms, c.listTools() < 500ms, 3× c.callTool() each < 500ms.
      });

      it.skip('scenario 2: stateless-fallback path returns spec-compliant error within 500 ms (no indefinite hang)', () => {
        // Wave 1 Task: raw fetch to ephemeral port without Mcp-Session-Id on non-initialize body.
        //   Expected: 400 with JSON-RPC error envelope, total elapsed < 500ms.
      });

      it.skip('scenario 3: initialize response has capabilities.tools.listChanged === false', () => {
        // Wave 1 Task: parse SSE `data: {...}` line from initialize response.
      });

      it.skip('scenario 4: two concurrent sessions, 3 tools/call each, no JSON-RPC id cross-contamination', () => {
        // Wave 1 Task: Promise.all over two Client instances against same baseUrl.
        //   Regression guard for SDK Issue #1405 (per-session McpServer workaround).
      });

      it.skip('scenario 5: expired session ID returns 404, not 500', () => {
        // Wave 1 Task: open session, force-advance TTL via ENV or direct Map manipulation hook,
        //   then resend with old sid. Response must be 404.
      });

      it.skip('tools_list_schemas: all 18 voice.* tools present on tools/list with non-empty inputSchema', () => {
        // Wave 1 Task: listTools() result — each has description + inputSchema with at least 1 property.
      });

      it.skip('request_outbound: voice.request_outbound_call listable + callable via session-based MCP', () => {
        // Wave 1 Task: REQ-C6B-03 validation.
      });
    });
    ```

    Step 2 — create `voice-bridge/src/core-mcp-client.test.ts`:
    ```typescript
    // Wave-0 scaffold for Phase 4.5 bridge v2 client tests.
    // Wave 2 fills these in.
    import { describe, it } from 'vitest';

    describe('CoreMcpClient v2 (MCP StreamableHTTP via SDK)', () => {
      it.skip('connect: lazy-opens MCP session on first ensureConnected()', () => {
        // Wave 2 Task: construct CoreMcpClient, call ensureConnected(), assert internal .client is non-null.
      });

      it.skip('callTool: returns server result, preserves args shape', () => {
        // Wave 2 Task: mock server or run ephemeral buildMcpStreamApp; verify echo tool round-trips args.
      });

      it.skip('close: idempotent, second close() is no-op', () => {
        // Wave 2 Task: await client.close(); await client.close(); no throw. Pitfall-5 guard.
      });

      it.skip('timeout: callTool rejects with CoreMcpTimeoutError when opts.timeoutMs elapses', () => {
        // Wave 2 Task: mock slow server, pass timeoutMs: 50, assert instanceof CoreMcpTimeoutError.
      });

      it.skip('server_error: callTool rejects with CoreMcpError when server returns isError: true', () => {
        // Wave 2 Task: server returns MCP error payload, client wraps → CoreMcpError.
      });

      it.skip('reconnect_after_close: ensureConnected() after close() opens a fresh session', () => {
        // Wave 2 Task: close(); ensureConnected(); assert new client instance different from previous.
      });
    });
    ```

    Step 3 — verify both files parse and vitest discovers them:
    ```bash
    npx vitest run src/mcp-stream-server.regression.test.ts 2>&1 | tail -5
    # Output: "7 skipped" — no failures.
    
    npx vitest run voice-bridge/src/core-mcp-client.test.ts 2>&1 | tail -5
    # Output: "6 skipped" — no failures.
    
    npm test 2>&1 | tail -10
    # Existing 0 failures. Total skipped count += 13.
    ```
  </action>

  <acceptance_criteria>
    - `test -f src/mcp-stream-server.regression.test.ts` is true.
    - `test -f voice-bridge/src/core-mcp-client.test.ts` is true.
    - `grep -c "it.skip\|it.todo" src/mcp-stream-server.regression.test.ts` returns 7.
    - `grep -c "it.skip\|it.todo" voice-bridge/src/core-mcp-client.test.ts` returns 6.
    - `grep -c "'scenario 1'\|'scenario 2'\|'scenario 3'\|'scenario 4'\|'scenario 5'\|'tools_list_schemas'\|'request_outbound'" src/mcp-stream-server.regression.test.ts` returns 7 (scenario titles present for `-t` filtering in Wave 1).
    - `grep -c "'connect'\|'callTool'\|'close'\|'timeout'\|'server_error'\|'reconnect_after_close'" voice-bridge/src/core-mcp-client.test.ts` returns 6.
    - `npx vitest run src/mcp-stream-server.regression.test.ts 2>&1 | grep -E "skipped|passed" | tail -1` shows "7 skipped" (or equivalent vitest summary including a 7 count).
    - `npm test` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run src/mcp-stream-server.regression.test.ts voice-bridge/src/core-mcp-client.test.ts 2>&1 | tail -10</automated>
  </verify>

  <done>Both scaffold files exist with 7 + 6 skip stubs matching exact scenario titles from VALIDATION.md. Wave 1/2 executors can drop implementations in without renaming tests. Vitest sees both files, 0 failures, 13 new skipped entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed in Wave 0. This is test scaffolding + schema-export churn only. No new network surface, no new authz, no new data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.5-D | Tampering (schema-bypass) | src/mcp-tools/voice-*.ts zod schemas | prepare-mitigate | Wave 0 re-exports schemas so Wave 1 can wire them into `TOOL_META` for tools/list. Wave 0 itself introduces no bypass (existing validators stay in place; schemas gain `export` only — no shape change). Full mitigation lands in Wave 1. |

No other T-4.5-X applies to Wave 0 (no session lifecycle, no bridge client, no observability hook yet).
</threat_model>

<verification>
**Full-wave check (run at end of Wave 0 before Wave 1 starts):**

```bash
# 1. Build green
npm run build 2>&1 | tail -5

# 2. All schemas exported + named uniquely
grep -hc "^export const \w\+Schema" src/mcp-tools/voice-*.ts | paste -sd+ | bc
# Expected: 18 (17 re-exports + 1 new + update-calendar-entry contributes 2)
# Actually: 18 = 13 simple renames-to-export + 3 generic→module-specific + 1 new (OnTranscriptTurn) + 1 extra (FieldsSchema in update-calendar-entry, alongside UpdateEntrySchema) = 18.

# 3. Scaffold tests pass as skipped
npx vitest run src/mcp-stream-server.regression.test.ts voice-bridge/src/core-mcp-client.test.ts 2>&1 | tail -5
# Expected: "13 skipped" in summary, 0 failures.

# 4. Full suite regression-free
npm test 2>&1 | tail -10
# Expected: pre-Wave-0 pass count preserved, +13 skipped.
```
</verification>

<success_criteria>
Wave 0 complete when:
- [ ] All 17 existing `const XxxSchema = z.object(...)` lines in `src/mcp-tools/voice-*.ts` gain `export` (with 3 renamed to module-specific names).
- [ ] `src/mcp-tools/voice-on-transcript-turn.ts` has new `export const OnTranscriptTurnSchema` alongside preserved `validateVoiceTurnArgs`.
- [ ] `src/mcp-stream-server.regression.test.ts` scaffolded with 7 it.skip stubs matching exact VALIDATION.md scenario titles.
- [ ] `voice-bridge/src/core-mcp-client.test.ts` scaffolded with 6 it.skip stubs matching PATTERNS.md-derived case titles.
- [ ] `npm run build` exits 0.
- [ ] `npm test` exits 0 with 13 additional skipped tests (vs. pre-Wave-0 baseline).
- [ ] Changes committed as `feat(04.5-00): wave-0 foundation — schema exports + test scaffolds` and pushed per project-wide "Push early" rule.
</success_criteria>

<output>
After completion, create `.planning/phases/04.5-mcp-universal-consolidation/04.5-00-SUMMARY.md` recording:
- The 18 modules touched (list).
- The 3 rename mappings applied (generic `Schema` → module-specific).
- Actual `npm test` pass/skip counts before/after.
- Any unexpected deviation from PATTERNS.md §"src/mcp-tools/voice-*.ts × 18" table (e.g. a module already exported, or a const turned out to be at a different line than PATTERNS said).
- Confirmation that `git push origin main` succeeded.
</output>
