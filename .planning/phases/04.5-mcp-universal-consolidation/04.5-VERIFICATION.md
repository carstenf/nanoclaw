---
phase: 04.5
status: passed
verified_by: inline-orchestrator
verified_at: 2026-04-20T13:45:00Z
plans_total: 5
plans_complete: 5
---

# Phase 4.5 Verification — MCP Universal Consolidation

Goal-backward verification of Phase 4.5 against the 5 success criteria in ROADMAP.md lines 122-127. Verifier run inline (not via gsd-verifier subagent) due to earlier usage-limit exhaustion; all success criteria cross-checked against actual codebase + deployed runtime + Carsten's iOS sign-off.

## Success Criterion Cross-Check

### ✓ 1. Bridge is a true MCP-SDK StreamableHTTP client (Wave 3)

- `voice-bridge/src/core-mcp-client.ts` (post-rename from v2) imports from `@modelcontextprotocol/sdk/client/*` and uses `StreamableHTTPClientTransport`.
- 6 bridge call-sites (webhook.ts, sideband.ts, slow-brain.ts, pre-greet.ts, cost/gate.ts, tools/dispatch.ts) go through the SDK client.
- `CORE_MCP_URL` in `.env` = `http://10.0.0.2:3201/` (MCP, not `:3200/mcp` REST).
- `git log --oneline | grep "04.5-03"` → 3 commits present.

### ✓ 2. Session management (Wave 1)

- `src/mcp-stream-server.ts` contains `sessionIdGenerator: () => randomUUID()` + session Map with TTL sweep.
- Server log on live deploy shows `mcp_session_opened` events — verified at 12:56:31 + 12:57:29 during Carsten's iOS test.
- Session cap: 50 (env `MCP_STREAM_MAX_SESSIONS`), idle sweep: 30 min (env `MCP_STREAM_SESSION_TTL_MS`).

### ✓ 3. Port 3200 REST deprecated with observability (Wave 4)

- Server still runs on 10.0.0.2:3200 (verified: `curl http://10.0.0.2:3200/health` → HTTP 200).
- `src/mcp-server.ts` emits `mcp_rest_request_seen` log event on every request (commit `4f3d234`).
- Unit test asserts the log event fires with tool_name + peer_ip + user_agent.
- 7-day observation window command captured in SUMMARY: `journalctl --user -u nanoclaw --since "7 days ago" | grep -c mcp_rest_request_seen`.

### ✓ 4. iOS Claude-App compatibility resolved (Wave 1 + D-5 hotfix)

- Two overlapping root causes fixed:
  1. Stateless → session-based transport (Wave 1).
  2. Tool-name Anthropic-API regex incompatibility: `voice.xxx` → `voice_xxx` rename across 52 files (D-5 hotfix, commit `2dda535`).
- Carsten verified on iPhone Claude-App 2026-04-20 ~13:25 UTC: "namen sind aber ohne punkt und app funktioniert. und die beiden tool call haben funktioniert"
- Central learning captured in project memory (`project_mcp_tool_name_regex.md`): MCP tool names MUST match `^[a-zA-Z0-9_]{1,64}$` for Anthropic clients.

### ✓ 5. AC-07 wording re-aligned (Wave 4)

- `PROJECT.md` line 92 updated: "for debugging" → "production Bridge↔Core channel" (commit `bf70c4f`).
- `ROADMAP.md` Phase 4 Goal updated: "debugging" → "production MCP StreamableHTTP endpoint".
- `grep -iE "debug.*(only|channel)" .planning/PROJECT.md | grep -iE "AC-07|mcp|StreamableHTTP"` → 0 matches.

## Test Suite State

- Root: 585 passed / 1 failed (pre-existing gmail test, unrelated to Phase 4.5) / 7 skipped.
- Voice-bridge: 305 passed / 4 skipped (pre-existing cost-gate integration tests, deferred).
- Plan 04.5-03 Task 5 Case-6b checkpoint: deferred to next real voice call as implicit verification.
- Plan 04.5-04 Task 3 Lenovo1 doc update: delegated to chat-Claude via ASK FOR CHAT in `nanoclaw-state/open_points.md` commit `83444e5`.

## Deferred Items (tracked, non-blocking)

| Item | Owner | Evidence Trail |
|------|-------|----------------|
| Case-6b voice-call end-to-end verification | Carsten, next real call | Plan 04.5-03 Task 5 — resume signal "Case 6b verified" |
| `/opt/server-docs/hetzner-mcp-architecture.md` append | chat-Claude via state-repo | ASK in `open_points.md`, addendum in `decisions/2026-04-20-hetzner-mcp-architecture-addendum.md` |
| Port 3200 REST server deletion | Follow-up phase | 7-day zero-traffic evidence via `mcp_rest_request_seen` |

## Regression Check

Prior phases' test suites re-run post-merge: pass rate unchanged from baseline. No regression from the bridge migration or tool rename. Pre-existing gmail test failure (unrelated to voice/MCP) unchanged.

## Status: PASSED

All 5 ROADMAP success criteria met. Carsten's iOS unblock verified interactively. Phase 4.5 is done.
