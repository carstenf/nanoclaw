---
phase: 05.1
plan: 05
type: execute
wave: 3
depends_on:
  - 05.1-01
  - 05.1-02
  - 05.1-03
  - 05.1-04
files_modified:
  - .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/
  - .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-LIVE-VERIFICATION.md
autonomous: false
requirements:
  - C2-01
  - C2-03
  - C2-04
  - C2-05
  - C2-06
  - C2-07
  - QUAL-01
tags:
  - live-verification
  - pstn
  - human-verify
  - phase-gate
user_setup:
  - service: sipgate
    why: "Outbound PSTN origination for 3 verification calls"
    env_vars:
      - name: SIPGATE_TOKEN_ID
        source: "existing .env on Lenovo1 (already provisioned)"
      - name: SIPGATE_TOKEN
        source: "existing .env on Lenovo1 (already provisioned)"
  - service: carsten-second-phone
    why: "Second handset to simulate restaurant counterpart per Phase 5 D-3 test matrix"
    account_setup:
      - task: "Carsten keeps his second phone reachable + charged for the 3 test calls"
        location: "physical handset in Munich"
must_haves:
  truths:
    - "3 real PSTN outbound calls placed via voice_start_case_2_call → Bridge /outbound → Sipgate REST → FreeSWITCH → OpenAI Realtime. One call per Plan 05-03 Task 5 matrix scenario: HAPPY human pickup, VOICEMAIL, BUSY/NO-ANSWER."
    - "Scenario HAPPY passes: after amd_result=human, bot opens with 'NanoClaw im Auftrag von Carsten' (Sie-form, CASE2 persona) — NOT restaurant-assistant du-form/helper-mode. This is the direct defect #6 live-verification criterion."
    - "Scenario VOICEMAIL passes: AMD verdict=voicemail fires before any bot utterance (§201 StGB invariant — grep `audio_transcript.delta` count = 0 in trace before amd_result event), silent hangup fires, voice_case_2_schedule_retry is called with zod-valid args (from 05.1-03 fix) and produces a DB row in voice_case_2_attempts OR a daily_cap_reached response if >5 attempts exist."
    - "Scenario BUSY/NO-ANSWER passes: outbound-router reportBack emits voice_notify_user urgency='info' with retry plan; no §201 audio leak; trace preserved."
    - "ASR quality verification (05.1-02): Carsten's German utterances in the HAPPY trace render correctly (no Swedish/English garbage from whisper-1 era). Spot-check 3 transcription.completed events against Carsten's confirmed transcripts — match rate > 50% at word level (not word-perfect, but no wholesale language-drift)."
    - "§201 audit script clean after all 3 calls: bash scripts/audit-audio.sh . returns zero .wav/.mp3/.opus/.flac files. Wave 3 audio-hygiene invariant preserved."
    - "Carsten types 'case2-live-verified' (or equivalent resume signal) in chat with a short pass/fail summary per scenario; failures documented and escalated to a fresh 05.1 revision plan if any."
  artifacts:
    - path: ".planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/"
      provides: "Directory holding 3 JSONL sideband traces (one per scenario), mirrors Plan 05-03 task5-traces/ layout"
    - path: ".planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-LIVE-VERIFICATION.md"
      provides: "Human-readable verification report: scenario matrix, pass/fail per must_have, trace-filename index, ASR quality spot-check, cost-cap (Pitfall 3) outcome, Carsten resume-signal"
  key_links:
    - from: "Task 1 orchestration (Bridge /outbound)"
      to: "FreeSWITCH + Sipgate REST + OpenAI Realtime → Carsten second phone"
      via: "voice_start_case_2_call MCP tool → Bridge POST /outbound → Sipgate originate"
      pattern: "voice_start_case_2_call"
    - from: "trace JSONL files"
      to: "05.1-LIVE-VERIFICATION.md must_haves checklist"
      via: "grep `type`, `audio_transcript.delta`, `amd_result`, `session_update_rejected`, `case_2_amd_human_verdict`"
      pattern: "task5-traces-rerun/v7-.*\\.jsonl"
---

<objective>
Re-run the Plan 05-03 Task 5 live verification matrix now that Defects #3, #4, #5, #6 are fixed (Plans 05.1-01 through 05.1-04). Three real PSTN calls via Carsten's second phone, one per outcome path (HAPPY / VOICEMAIL / BUSY-or-NO-ANSWER). Produce new trace artifacts and a written verification report.

This checkpoint is the arbiter for Phase 05.1 acceptance — all four surgical fixes must hold live, the §201 StGB zero-audio-leak invariant must continue to hold, and the Wave 3 architecture (AMD classifier, persona content, VAD thresholds, pre-greet early-return, outbound-router outcome routing) must remain uncompromised.

Purpose: Close the live-verification gap that was opened by DEFECTS.md — Phase 05 Plan 05-03 Task 5 shipped the Wave 3 code but failed the live-PSTN gate on 4 defects. 05.1 closes those defects in code; this plan re-exercises the Task 5 gate against the fixed system.

Output: 3 new sideband JSONL traces + one verification report. Carsten signs off with resume-signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-RESEARCH.md
@.planning/phases/05-case-2-restaurant-reservation-outbound/05-03-TASK5-DEFECTS.md
@.planning/phases/05-case-2-restaurant-reservation-outbound/05-03-PLAN.md
@.planning/phases/05-case-2-restaurant-reservation-outbound/05-CONTEXT.md
@.planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-01-PLAN.md
@.planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-02-PLAN.md
@.planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-03-PLAN.md
@.planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-04-PLAN.md
@voice-bridge/src/webhook.ts
@voice-bridge/src/sideband.ts
@voice-bridge/src/config.ts
@voice-bridge/src/amd-classifier.ts
@voice-bridge/src/persona.ts
@src/mcp-tools/voice-start-case-2-call.ts
@scripts/audit-audio.sh
</context>

<interfaces>
### Pre-flight checklist (Task 1)

Verify all 05.1 prerequisite plans landed:
- 05.1-01 commits: `test(05.1): RED`, `fix(05.1): GREEN — ... session.type`, `fix(05.1): defect #6 L2`
- 05.1-02 commit: `fix(05.1): defect #3 — upgrade transcription ... gpt-4o-mini-transcribe`
- 05.1-03 commits: `test(05.1): RED — onVoicemail retry args`, `fix(05.1): defect #4`
- 05.1-04 commits: `test(05.1): RED — voice-start-case-2-call attempt_no`, `fix(05.1): defect #5`
- Full voice-bridge + repo-root test suites green on HEAD
- Deployed binary matches HEAD: `systemctl --user status nanoclaw` and `ls -la voice-bridge/dist/` confirm recent build
- Cost-cap sanity (RESEARCH §8 Pitfall 3): read CAP_PER_CALL_EUR from src/cost-ledger.ts (or cost/gate.ts), record current value in the verification report — if Task 6 cost trips this cap on the first call, fallback path is documented in 05.1-02-PLAN.md

### Scenario matrix (identical to Plan 05-03 Task 5 per RESEARCH §2.6 Tertiary + per DEFECTS.md §Recommended Remediation Plan Task 6)

Each scenario triggers via a voice_start_case_2_call invocation with these shared args:
```yaml
restaurant_name: 'Carsten Zweites Handy'
restaurant_phone: <Carsten's second phone E.164>   # asks Carsten at kickoff
requested_date: <tomorrow YYYY-MM-DD>
requested_time: '19:00'
time_tolerance_min: 30
party_size: 4
notes: 'Spike-Szenario <N>'
report_to_jid: <main group JID>
```

#### Scenario HAPPY (v7-happy-human-pickup.jsonl) — defect #6 + #3 primary test
- Carsten picks up on 2nd phone, says "Guten Tag, Restaurant Bellavista"
- **Pass criteria:**
  1. amd_result verdict=human fires in trace
  2. Immediately after: session.update event with session.type='realtime' in trace (NO `invalid_request_error` `session.type` missing)
  3. Immediately after: conversation.item.create role=user with `System-Hinweis: AMD-Verdict war human` in content (defect #6 L2)
  4. Bot's first response (response.audio_transcript.delta aggregate) contains "NanoClaw im Auftrag" (NOT "Hallo, du hast das Restaurant erreicht" or any helper-mode opener)
  5. Bot uses Sie-form (grep `\bSie\b|\bIhnen\b|\bIhr\b` in transcript, assert > 0 matches; no `\bdu\b|\bdir\b|\bdich\b` except in quoted restaurant-name contexts)
  6. ASR quality spot-check: Carsten's 3 utterances render as German text; no Swedish/English drift
  7. Complete ≥3 turns (greeting → reservation request → confirmation)

#### Scenario VOICEMAIL (v7-voicemail.jsonl) — defect #4 live + §201 invariant
- Carsten lets 2nd phone roll to Sipgate default voicemail (mailbox plays in German) OR plays a Spike-C corpus fixture greeting
- **Pass criteria:**
  1. amd_result verdict=voicemail fires in trace (OR cadence_cue/silence_mailbox/transcript_cue from VAD gate — all acceptable)
  2. §201 invariant: grep `response.output_audio_transcript.delta` count in trace BEFORE the amd_result event = 0 (zero bot audio leaked before verdict)
  3. openai.realtime.calls.hangup fires silently; trace ends cleanly
  4. Bridge log shows `case_2_schedule_retry_failed` is NOT present (05.1-03 fix landed — no zod rejection)
  5. voice_case_2_attempts table has new row for this (phone, date) with attempt_no incrementing per 05.1-04 rules (or `daily_cap_reached` if already >5 attempts exist — unlikely on fresh test day)
  6. voice_notify_user urgency='info' arrives in Carsten's active channel with retry-plan text

#### Scenario BUSY or NO-ANSWER (v7-busy-or-no-answer.jsonl) — reportBack outcome routing
- Option A (BUSY): Carsten busy-signal from 2nd phone (press call-reject) OR set 2nd phone to DND
- Option B (NO-ANSWER): Carsten ignores the ring for 30s; Sipgate 30s ring-timeout fires (Wave 2 spike-B busy-parser or outbound-router no-answer path)
- Carsten chooses whichever is easier to stage reliably; document choice in verification report
- **Pass criteria:**
  1. Sipgate REST returns 486 Busy (Option A) or outbound-router 30s timeout fires (Option B)
  2. outbound-router reportBack: voice_notify_user urgency='info' arrives
  3. voice_case_2_schedule_retry call fires (same path as voicemail per Plan 05-03 Task 4 outcome routing)
  4. Trace has zero `response.output_audio_transcript.delta` (bot never spoke because OpenAI Realtime /accept never completed)
  5. NEW attempt_no row in voice_case_2_attempts per 05.1-04 fix

### §201 audit (end of all 3 calls)
```bash
cd /home/carsten_bot/nanoclaw && bash scripts/audit-audio.sh voice-bridge data /tmp
```
Expected: no matching audio files. Zero audio persisted to disk through the three-call window. This re-asserts T-05-03-01 (Plan 05-03) and T-05.1-01-03 (this phase plans).
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight + 3 PSTN verification calls + traces + report</name>
  <what-built>
    All four 05.1 surgical fixes (defects #3, #4, #5, #6 + defense-in-depth synthetic user-item injection + session_update_rejected observability). Current deployed binary on Lenovo1 reflects HEAD of main. This task orchestrates 3 real PSTN calls via Carsten's second phone, captures new trace JSONL files, runs the §201 audio-audit, and produces a written verification report.

    NOT a code change. This is the live verification gate that was blocked on Plan 05-03 Task 5 by the four defects now fixed.
  </what-built>
  <gate>
    Phase 05.1 depends on Phase 0 (legal gate) being GREEN — carried forward from Phase 5 (Plan 05-03 Task 5 gate). Verify Phase 0 status by reading the most recent `.planning/phases/00-*/00-*-SUMMARY.md` (per STATE.md Phase 0 is marked complete 2026-04-16). If Phase 0 is complete, proceed.

    Additionally verify Wave 3 architecture integrity BEFORE placing calls:
    - `cd voice-bridge && npm test` full suite green
    - `npm test` (repo root) full suite green
    - Current deployed build matches HEAD of main (`git log -1 --format=%H` matches the commit referenced in `systemctl --user status nanoclaw` or last build timestamp)
  </gate>
  <read_first>
    .planning/phases/05-case-2-restaurant-reservation-outbound/05-03-PLAN.md Task 5 (the original Task 5 live-verification block — same scenario structure, same trigger args shape)
    .planning/phases/05-case-2-restaurant-reservation-outbound/05-03-TASK5-DEFECTS.md §Observed AMD Classifier Successes (the §201 invariant we are re-asserting)
    .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-RESEARCH.md §2.6 (testing approach — Tertiary: live PSTN) §8 Pitfall 3 (cost-cap sanity)
    scripts/audit-audio.sh (the §201 audit command and its dev-artifact exclusions per STATE.md Phase 4 decision)
    .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-01-PLAN.md (defect #6 L1+L2 grep patterns for trace analysis)
    .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-03-PLAN.md (defect #4 args shape for trace analysis)
  </read_first>
  <how-to-verify>
    **PRE-FLIGHT (carsten_bot automates):**

    1. Verify all 4 prerequisite plans committed:
       ```
       git log --oneline --grep="fix(05.1): GREEN" | head
       git log --oneline --grep="fix(05.1): defect" | head
       ```
       Expect: at least 5 matching commits (L1 session.type + L2 item.create + defect #3 + defect #4 + defect #5). If fewer, STOP and remediate in the responsible plan.

    2. Run the full test gauntlet:
       ```
       cd /home/carsten_bot/nanoclaw/voice-bridge && npm test
       cd /home/carsten_bot/nanoclaw && npm test
       cd /home/carsten_bot/nanoclaw/voice-bridge && npm run build
       cd /home/carsten_bot/nanoclaw && npm run build
       ```
       All must exit 0. If any fail, STOP.

    3. Verify deployed binary matches HEAD: check systemd unit status + voice-bridge/dist/ mtime ≥ latest src/ edit. If stale, trigger `systemctl --user restart nanoclaw` AFTER a fresh `npm run build`. Document the deploy-at-timestamp in the verification report.

    4. Cost-cap sanity (RESEARCH §8 Pitfall 3):
       ```
       grep -E "CAP_PER_CALL_EUR|CAP_DAY_EUR|CAP_MONTH_EUR" src/cost-ledger.ts src/cost/*.ts 2>/dev/null
       ```
       Record current values in the verification report. If any per-call cap is < €0.50, flag as risk before placing calls. If live calls trip the cap, the fallback per 05.1-02-PLAN is to revert to whisper-1 or gpt-4o-transcribe (full) — but we should see the trip first; do not pre-emptively roll back.

    5. Prepare trace-capture: `mkdir -p .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun`; ensure voice-bridge JSONL logging is writing to a location you can copy from after each call (check `voice-bridge/src/config.ts` or the systemd unit logs for JSONL target path).

    **LIVE-CALL ORCHESTRATION (Carsten + carsten_bot together):**

    6. Ask Carsten for his second phone's E.164 number. Ask Carsten to:
       - Charge the phone
       - Keep it reachable for ~15 min
       - Stand by for the 3 scenarios in sequence

    7. Scenario HAPPY — place voice_start_case_2_call with the args shape from <interfaces>. Carsten picks up, role-plays per scenario. After BYE, copy the fresh JSONL trace to `task5-traces-rerun/v7-happy-human-pickup.jsonl`. Pull bridge logs for case_id + ensure nothing was truncated.

    8. Verify HAPPY trace immediately:
       ```
       grep "amd_result" task5-traces-rerun/v7-happy-human-pickup.jsonl
       grep "session.update" task5-traces-rerun/v7-happy-human-pickup.jsonl | head -3
       grep "session_update_rejected" task5-traces-rerun/v7-happy-human-pickup.jsonl  # MUST be empty
       grep "conversation.item.create" task5-traces-rerun/v7-happy-human-pickup.jsonl
       grep "NanoClaw im Auftrag" task5-traces-rerun/v7-happy-human-pickup.jsonl      # bot's first response
       grep -c "response.output_audio_transcript.delta" task5-traces-rerun/v7-happy-human-pickup.jsonl  # > 0 is fine for HAPPY
       ```
       If `NanoClaw im Auftrag` is absent from bot transcript, defect #6 is NOT closed — STOP and revise 05.1-01.

    9. Scenario VOICEMAIL — place a second voice_start_case_2_call (updates D-7 to a distinct idempotency_key via different notes field; or use requested_time='19:30'). Carsten does NOT pick up; Sipgate default voicemail answers OR Carsten answers and plays the pre-recorded Spike-C mailbox fixture from Wave 0. After trace capture → `task5-traces-rerun/v7-voicemail.jsonl`.

    10. Verify VOICEMAIL trace immediately (§201 invariant — CRITICAL):
        ```
        AMD_TS=$(grep -n "amd_result" task5-traces-rerun/v7-voicemail.jsonl | head -1 | cut -d: -f1)
        AUDIO_BEFORE_AMD=$(head -n "$AMD_TS" task5-traces-rerun/v7-voicemail.jsonl | grep -c "response.output_audio_transcript.delta")
        echo "Audio deltas before amd_result: $AUDIO_BEFORE_AMD"   # MUST be 0
        grep "case_2_schedule_retry_failed" task5-traces-rerun/v7-voicemail.jsonl  # MUST be empty
        sqlite3 state.db "SELECT target_phone, calendar_date, attempt_no, idempotency_key FROM voice_case_2_attempts WHERE calendar_date = date('now','+1 day') ORDER BY attempt_no;"
        ```
        If AUDIO_BEFORE_AMD > 0, §201 T-05.1-01-03 violated — STOP and escalate immediately.

    11. Scenario BUSY/NO-ANSWER — Carsten picks choice (busy via DND/reject, OR no-answer via 30s ignore). Place call. Trace → `task5-traces-rerun/v7-busy-or-no-answer.jsonl`. Note which option in the verification report.

    12. Verify BUSY/NO-ANSWER trace:
        ```
        grep -E "(486|busy|no_answer)" task5-traces-rerun/v7-busy-or-no-answer.jsonl
        grep "voice_notify_user" bridge logs or MCP audit file for this call_id
        grep -c "response.output_audio_transcript.delta" task5-traces-rerun/v7-busy-or-no-answer.jsonl  # MUST be 0 (no pickup, no bot utterance)
        ```

    13. End-of-calls §201 audit:
        ```
        bash /home/carsten_bot/nanoclaw/scripts/audit-audio.sh /home/carsten_bot/nanoclaw
        ```
        Expected output: zero matches for .wav/.mp3/.opus/.flac. If any hit, STOP and investigate.

    14. Write verification report to `.planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-LIVE-VERIFICATION.md` with:
        - Pre-flight outcomes (commits verified, tests green, build fresh, cost-cap values recorded)
        - Per-scenario: trace filename, pass/fail per must_have criterion, interesting events, bot's opening utterance verbatim
        - ASR quality spot-check: 3 of Carsten's confirmed utterances vs their transcription.completed renderings
        - Cost outcome: total minutes, estimated cost (RESEARCH Pitfall 3 verification)
        - §201 audit output
        - D-7 verification: voice_case_2_attempts table state (phone, date, attempt_no, idempotency_key)
        - Carsten resume-signal + timestamp

    15. Ask Carsten to type resume-signal `case2-live-verified` with optional short summary if any scenario had issues. If any scenario FAILED, do NOT write resume-signal — instead ask Carsten to confirm the failure summary and escalate to a follow-up plan (05.1-06 or 05.2).

    16. After Carsten's sign-off: git add the 3 trace files + the verification report, commit with `docs(05.1): Task 1 live-verification PASS — 3 PSTN scenarios verified + traces preserved` (or `PARTIAL` / `FAIL` if scenarios failed).

    17. `git push origin main` per project push-early rule.
  </how-to-verify>
  <resume-signal>
    Type `case2-live-verified` with a short pass/fail summary per scenario.

    Example PASS: `case2-live-verified — HAPPY pass (bot said "NanoClaw im Auftrag von Carsten", Sie-form, 3 turns complete, ASR clean); VOICEMAIL pass (no audio before amd_result, retry scheduled, DB row at attempt_no=1); BUSY pass (voice_notify_user fired); §201 audit clean`.

    Example FAIL: `case2-live-verified PARTIAL — HAPPY pass, VOICEMAIL FAIL (defect #4 still rejecting — see bridge log session_update_rejected event — investigate)` → then open 05.1-06 to remediate.
  </resume-signal>
  <verify>
    <automated>cd /home/carsten_bot/nanoclaw &amp;&amp; ls .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-voicemail.jsonl .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-busy-or-no-answer.jsonl .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-LIVE-VERIFICATION.md 2&gt;&amp;1 | grep -vc "No such file" | (read c; [ "$c" -ge 4 ]) &amp;&amp; bash scripts/audit-audio.sh . 2&gt;&amp;1 | (! grep -qE "\.(wav|mp3|opus|flac)$") &amp;&amp; grep -q "NanoClaw im Auftrag" .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl &amp;&amp; (! grep -q "session_update_rejected" .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl)</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl` succeeds
    - `ls .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-voicemail.jsonl` succeeds
    - `ls .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-busy-or-no-answer.jsonl` succeeds
    - `ls .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-LIVE-VERIFICATION.md` succeeds
    - `grep -q "NanoClaw im Auftrag" .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl` succeeds (defect #6 live-proof)
    - `grep -cE "session_update_rejected" .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl` returns 0 (defect #6 L1 live-proof)
    - §201 audit (bash scripts/audit-audio.sh) returns zero audio files across the full tree
    - `grep -q "case2-live-verified" .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-LIVE-VERIFICATION.md` succeeds (Carsten's resume-signal recorded)
    - Verification report contains pass/fail entries for all 3 scenarios + ASR spot-check + §201 audit + cost outcome
    - Final commit landed: `docs(05.1): Task 1 live-verification` (status PASS / PARTIAL / FAIL captured in subject)
    - git push completed (per push-early rule)
  </acceptance_criteria>
  <done>
    3 scenarios verified live by Carsten from his second phone. Traces + report preserved in task5-traces-rerun/ + 05.1-LIVE-VERIFICATION.md. §201 invariant holds across all 3 calls. Wave 3 architecture preserved (amd-classifier, persona content, VAD, pre-greet). Carsten's resume-signal recorded. If any scenario failed, a follow-up 05.1-06 plan is queued; otherwise Phase 05.1 is complete and requirements C2-01/03/04/05/06/07 + QUAL-01 are closed pending Phase 5 Wave 4 formal gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge /outbound → Sipgate REST → FreeSWITCH → OpenAI Realtime → Counterpart | Live outbound path — first actual counterpart contact with the fixed code stack |
| Counterpart (Carsten's 2nd phone) speech → Bot response | Counterpart-controlled audio and text crosses into the model's context — the handoff-security threats are the ones being validated here |
| Trace JSONL → filesystem persistence | Trace files contain redacted delta_bytes only (not raw audio); stored under .planning/ which is git-tracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-05-01 | Information Disclosure | §201 StGB audio leak (half-spoken bot greeting on voicemail) | mitigate (re-assert) | VOICEMAIL scenario explicitly verifies `response.output_audio_transcript.delta` count = 0 before amd_result in the trace. If this fires, it is a blocker-level production defect. |
| T-05.1-05-02 | Information Disclosure | Audio files persisted to disk | mitigate (re-assert) | End-of-calls `bash scripts/audit-audio.sh` run over the full tree; zero hits required. Wave 3 T-05-03-01 inherited. |
| T-05.1-05-03 | Spoofing | Carsten's 2nd phone is not actually a restaurant — ethical context | accept | Per D-3 (Phase 5 CONTEXT): simulated-restaurant via second phone is the agreed test rig. No real counterpart is contacted. No goodwill harm. |
| T-05.1-05-04 | Tampering | Counterpart (Carsten role-playing) attempts prompt-injection during verification | accept | Carsten plays cooperative; any injection attempts are noted in report but not blocking — Wave 3 persona hardening is separate scope and was validated in Phase 2. |
| T-05.1-05-05 | Denial of Service | Cost-cap trips during 3-call run (Pitfall 3) | mitigate | Pre-flight step 4 records current caps. If a trip occurs, the in-call cost-farewell should fire cleanly (Phase 4 shipped). The trace will show it. |
| T-05.1-05-06 | Elevation of Privilege | Traces checked into git contain sensitive content | accept | Traces have redacted delta_bytes only per Wave 3 convention; phone numbers may appear — same policy as existing task5-traces/ in Plan 05-03 (already committed). No new disclosure policy. |
| T-05.1-05-07 | Repudiation | Carsten later disputes whether verification passed | mitigate | Verification report captures verbatim resume-signal + timestamps + trace cross-references; git commit + push preserves provenance |
</threat_model>

<verification>
```bash
cd /home/carsten_bot/nanoclaw && \
  # Trace artifacts exist
  test -s .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl && \
  test -s .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-voicemail.jsonl && \
  test -s .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-busy-or-no-answer.jsonl && \
  test -s .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-LIVE-VERIFICATION.md && \
  # Defect #6 live-proof
  grep -q "NanoClaw im Auftrag" .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl && \
  ! grep -q "session_update_rejected" .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-happy-human-pickup.jsonl && \
  # §201 invariant live-proof (voicemail)
  [ "$(awk '/amd_result/{exit} /response.output_audio_transcript.delta/{c++} END{print c+0}' .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/task5-traces-rerun/v7-voicemail.jsonl)" = "0" ] && \
  # Global §201 audit
  bash scripts/audit-audio.sh . 2>&1 | (! grep -qE "\.(wav|mp3|opus|flac)$") && \
  # Carsten sign-off
  grep -q "case2-live-verified" .planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-LIVE-VERIFICATION.md
```
</verification>

<success_criteria>
- [ ] All 05.1 prerequisite plans (01, 02, 03, 04) committed and tests green
- [ ] Deployed binary matches HEAD of main; systemd restart verified
- [ ] Cost-cap values recorded in verification report
- [ ] 3 PSTN calls placed via Carsten's 2nd phone, one per scenario
- [ ] 3 trace JSONL files preserved in task5-traces-rerun/
- [ ] Scenario HAPPY: bot transcript contains "NanoClaw im Auftrag"; Sie-form used; no session_update_rejected
- [ ] Scenario VOICEMAIL: zero response.output_audio_transcript.delta before amd_result; retry scheduled with zod-valid args; attempt_no allocated correctly
- [ ] Scenario BUSY/NO-ANSWER: outbound-router reportBack → voice_notify_user urgency=info; no audio leak
- [ ] End-of-calls §201 audit clean (zero audio files)
- [ ] ASR spot-check documented (3 utterances compared)
- [ ] Verification report includes pass/fail per must_have + Carsten resume-signal
- [ ] Final commit + git push landed
- [ ] Wave 3 architecture preserved: amd-classifier.ts, persona.ts content, VAD thresholds, pre-greet early-return, outbound-router outcome routing — all unchanged in code and confirmed behavioral-intact in traces
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-amd-persona-handoff-redesign-and-asr-upgrade/05.1-05-SUMMARY.md`:
- provides: 3 live-PSTN trace artifacts + verification report documenting Phase 05.1 live-gate outcome
- decisions: scenario choice for BUSY vs NO-ANSWER (Carsten picks at test time); cost-cap values observed in production
- defects-closed-live: #3 (ASR quality audibly improved), #4 (retry scheduled without zod reject), #5 (attempt_no increments correctly — observable in DB), #6 L1+L2 (bot speaks CASE2 persona, not helper-mode)
- wave-3-reaffirmed: §201 invariant held in VOICEMAIL scenario; AMD classifier behavior unchanged; persona content unchanged
- requirements-closed: C2-01 (multi-day/same-day multi-booking — confirmed via DB state), C2-03, C2-04, C2-05, C2-06, C2-07 live-verified; QUAL-01 live-PSTN gate passed
- risk-carryforward: A2 (cost-cap) — observed outcome documented; A3 (ASR WER on German telephony) — spot-check documented; A8 (Phase 5 CONTEXT D-1..D-7 preserved) — no regression observed
- if any scenario FAILED: 05.1-05-SUMMARY.md flags the failure and opens a follow-up 05.1-06 plan or escalates to chat for scope decision

Final step: `git push origin main` per project push-early rule.
</output>
