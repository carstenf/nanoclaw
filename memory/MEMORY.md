- [/add-discord skill chat dep bug](add-discord-skill-chat-version-bug.md) — pins @chat-adapter/discord@4.27.0 without bumping host's `chat` dep, build fails on duplicate ChatInstance types
- [System-wide secrets storage](secrets-storage-location.md) — Lenovo1 secrets live in /etc/secrets/ (group `secrets`); use `sg secrets -c ...` for access from running session
- [Anthropic Max OAuth via OneCLI](anthropic-oauth-via-onecli.md) — wire ~/.claude/.credentials.json's accessToken as type=anthropic vault entry; generic+Bearer collides with container's x-api-key=placeholder
- [Voice channel (Pattern-B) v2](voice-channel-pattern-b-v2.md) — wiring, senderIdentity=voice:&lt;call_id&gt; gotcha (no caller-CLI), vs-freeswitch thread-leak that mimics "no dial tone", **dual-connection wiring as of 2026-05-10** (host-side long-poll for inbound + per-container stdio MCP shim for outbound tools)
- [Andy Max-OAuth refresh pipeline](andy-token-refresh-pipeline.md) — Andy uses ~/.claude-andy login (separate from operator), pexpect helper drives TUI with bracketed-paste, Discord URL post via embed only, systemd timer 5d, conversational triggers via /tmp/refresh-control/ mount. Resolved 2026-05-10: fragment indirection leaked through default prior; inlining operational content fixed auto-invocation.
- [OneCLI stack scope](onecli-stack-scope.md) — ~/.onecli/docker-compose.yml is carsten_bot's; routine OneCLI updates don't need ASK FOR CHAT. Process uid in `ps aux` is uid-mapped, not authoritative. Backup secret-encryption-key tarball alongside pg_dump.
- [OneCLI app-connections per agent](onecli-app-connections-per-agent.md) — connecting an OAuth app (Gmail/Cal/Drive) is step 1; each agent ALSO needs explicit grant at `https://onecli.carstenfreek.de/agents?manage=<id>` or container gets `access_restricted`. No CLI/API for it as of v1.22.0 — web UI only.
- [Gmail-OAuth-via-OneCLI: NEXTAUTH_URL alone doesn't set OAuth redirect_uri](gmail-oauth-onecli-env-vars.md) — for app-OAuth redirect to use a public domain, set NEXTAUTH_URL + AUTH_URL + NEXT_PUBLIC_APP_URL + API_BASE_URL together. NEVER set GATEWAY_BASE_URL — it corrupts container proxy URL.
- [OneCLI port binding must stay 0.0.0.0](onecli-port-binding-must-be-0000.md) — narrowing ports to a single WG IP breaks container HTTPS_PROXY (host.docker.internal resolves to docker-bridge, not WG). Verify with `ss -tlnp | grep 10254`.
- [OneCLI per-agent app-grant via postgres](onecli-per-agent-app-grant-via-postgres.md) — when web UI is impractical, insert into agent_app_connections; MUST restart onecli-app-1 to flush gateway cache.
- [Andy vault token stale after refresh](andy-vault-token-stale-after-refresh.md) — refresh-andy-token skill rewrites .credentials.json but NOT the OneCLI vault entry → 401s. Manual delete+recreate via `onecli secrets`.
