# Dependencies
node_modules/
.npm-cache/
# Build output
dist/

# Local data & auth
store/
logs/

# data/ subdirectories (runtime state, not tracked)
data/env/
data/ipc/
data/sessions/

# data/ runtime logs (not tracked)
data/*.jsonl

# data/ live config files (contain real personal data — not tracked)
# Use .example.json variants as schema reference instead
data/contracts.json
data/practice-profile.json

# Groups - only track base structure and specific CLAUDE.md files
groups/*
!groups/main/
!groups/global/
groups/main/*
groups/global/*
!groups/main/CLAUDE.md
!groups/global/CLAUDE.md

# Secrets
*.keys.json
.env

# Temp files
.tmp-*

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Skills system (local per-installation state)
.nanoclaw/

agents-sdk-docs

# Claude Code local state
.claude/worktrees/
.claude/settings.local.json.bak-*

# Root-level SQLite runtime files
/nanoclaw.db
/state.db

# Voice-bridge runtime traces (per-call JSONL + bridge logs).
# Local-only; not committed. The /home/carsten_bot/nanoclaw/voice-stack/runs
# path is separately managed (older Hetzner stack).
/voice-container/

# 2026-05-08 architecture-B: voice-channel client extracted into the
# /add-voice-channel skill (source-of-truth at carstenf/nanoclaw-voice-channel).
# The actual files live on disk in this working tree (so the running service
# keeps working), but they're tracked in the voice-channel repo, not here.
# To re-install voice on a fresh fork: run /add-voice-channel skill.
container/agent-runner/src/voice-request.ts
src/channels/active-session-tracker.ts
src/channels/active-session-tracker.test.ts
src/channels/voice-mcp.ts
src/cost-ledger.ts
src/cost-ledger.test.ts
src/cost-providers.ts
src/openai-cost-client.ts
src/voice-balance.ts
src/voice-call-cost-snapshots.ts
src/voice-agent-invoker.ts
src/voice-agent-invoker.test.ts
src/voice-config.ts
src/voice-config.test.ts
src/voice-instructions.ts
src/voice-mid-call-gateway.ts
src/voice-mid-call-gateway.test.ts
src/voice-trigger-queue.ts
src/voice-trigger-queue.test.ts
src/voice-channel/
src/mcp-tools/voice-*.ts
src/mcp-tools/claude-client.ts
src/mcp-tools/claude-client.test.ts
src/mcp-tools/skill-loader.ts
src/mcp-tools/skill-loader.test.ts
src/mcp-tools/slow-brain-session.ts
src/mcp-tools/slow-brain-session.test.ts
systemd/voice-trace-sweep.service
systemd/voice-trace-sweep.timer
